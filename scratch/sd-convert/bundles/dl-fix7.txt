@WORK distributed-locks
#### system-design-fundamentals-distributed-locks-07-challenge-3
%%{init: {'flowchart': {'subGraphTitleMargin': {'top': 4, 'bottom': 18}, 'nodeSpacing': 30, 'rankSpacing': 40, 'padding': 8, 'wrappingWidth': 260}}}%%
flowchart TB
subgraph TE["AUTOMATIC TTL EXTENSION FLOW"]
  direction TB
  subgraph R1[" "]
    direction LR
    la["Acquire lock with TTL=9s<br/><br/>LOCK ACQUIRED<br/>T=0s Acquire lock, start renewal thread"]
    t3["T=3s ──> PEXPIRE lock, key=9000ms<br/>TTL resets to 9s from now"]
    t6["T=6s ──> PEXPIRE lock, key=9000ms<br/>TTL resets to 9s from now"]
    la -->|"Extend TTL (every 3s = TTL/3)"| t3
    t3 --> t6
  end
  subgraph R2[" "]
    direction LR
    t9["T=9s ──> PEXPIRE lock, key=9000ms<br/>TTL resets to 9s from now"]
    t12["T=12s ─> PEXPIRE fails (lock lost)<br/>Notify holder, abort operation"]
    le["LOCK EXPIRED<br/>T=12.5s Lock expires (no renewal after failure)"]
    t9 --> t12
    t12 --> le
  end
  t6 --> t9
  @SRC TC 1839-1847 lead
  R2 ~~~ TC
end
