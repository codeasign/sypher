@WORK logging
#### system-design-fundamentals-logging-01-concepts-1
@H
flowchart TB
subgraph OUT["Observability"]
  direction TB
  subgraph R1[" "]
    direction LR
    M["Metrics<br/>aggregated<br/>counters,<br/>gauges,<br/>histograms"] --- LG["Logs<br/>discrete<br/>events,<br/>structured<br/>searchable"]
    LG ~~~ TR["Traces<br/>request paths<br/>across<br/>services,<br/>span trees"]
  end
  N["The correlation ID connects<br/>logs to traces. Metrics<br/>trigger alerts that lead to<br/>log investigation."]
  R1 ~~~ N
end

#### system-design-fundamentals-logging-02-deep-dive-1
@H
flowchart TB
subgraph OUT["Production Logging Stack"]
  direction TB
  subgraph R1[" "]
    direction LR
    @SRC A 165-168 strip wrapl=46
    @SRC B 172-175 strip wrapl=46
    A -->|"writes to stdout or file"| B
  end
  subgraph R2[" "]
    direction LR
    @SRC C 179-182 strip wrapl=46
    @SRC D 186-189 strip wrapl=46
    C -->|"write to storage"| D
  end
  R1 -->|"batch send over HTTP or gRPC"| R2
end
