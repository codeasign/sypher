@WORK autoscaling
#### system-design-fundamentals-autoscaling-01-concepts-1
@H
flowchart LR
subgraph OUT["Autoscaling System"]
  direction LR
  subgraph MP["Metrics Pipeline"]
    direction TB
    m1["CloudWatch /<br/>Prometheus /<br/>Datadog"]
    m2["Instance<br/>Health Checks"]
    m1 ~~~ m2
  end
  subgraph AC["Autoscaler Controller"]
    direction TB
    sp["Scaling Policy Engine<br/><br/>Target Tracking<br/>Step Scaling<br/>Scheduled Scaling<br/>Predictive Scaling"]
    dc["Desired Capacity<br/>Calculator<br/><br/>min: 3  max: 50<br/>current: 12<br/>target: 18"]
    il["Instance Lifecycle<br/>Manager<br/><br/>Launch → Pending →<br/>InService → Draining<br/>→ Terminating"]
    sp --> dc
    dc --> il
  end
  subgraph PL[" "]
    direction TB
    subgraph IP["Instance Pool"]
      direction TB
      @FLAT i per=4 A | B | C | D | E | F | G | H
      st["4 running, 4 pending"]
      i4 ~~~ st
    end
    lb["Load Balancer<br/>(NLB/ALB)"]
    cl["Clients"]
    IP --> lb
    lb --> cl
  end
  MP -->|"metric values"| sp
  dc --> IP
  @M lg Legend:;;  [ ] = Service component;;  ──▶  = Data flow;;  ──▶  = Control flow
  MP ~~~ lg
end

#### system-design-fundamentals-autoscaling-01-concepts-2
@H
flowchart TB
subgraph OUT["Step 1: Metrics Collection from Each Instance"]
  direction TB
  a["Instance A<br/>(InService)"]
  b["Instance B<br/>(InService)"]
  c["Instance C<br/>(InService)"]
  d["Instance D<br/>(InService)"]
  mp["Metrics Pipeline<br/><br/>CloudWatch /<br/>Prometheus /<br/>Datadog<br/><br/>Aggregation:<br/>AVG CPU = 58%<br/>AVG req/s = 623<br/>AVG lat = 165ms"]
  a -->|"CPU: 72%<br/>mem: 4.2GB<br/>req/s: 850<br/>lat: 210ms"| mp
  b -->|"CPU: 45%<br/>mem: 2.1GB<br/>req/s: 420<br/>lat: 95ms"| mp
  c -->|"CPU: 88%<br/>mem: 5.8GB<br/>req/s: 920<br/>lat: 340ms"| mp
  d -->|"CPU: 31%<br/>mem: 1.8GB<br/>req/s: 300<br/>lat: 78ms<br/>(data flows to<br/>same pipeline)"| mp
end

#### system-design-fundamentals-autoscaling-01-concepts-3
@H
flowchart LR
ac["Autoscaler<br/>Controller<br/><br/>Desired Capacity:<br/>4 → 5"]
subgraph CP["Cloud Provider Compute API"]
  direction TB
  subgraph LT["Launch Template: web-app-v3"]
    direction TB
    @SRC l 106-111 lead2
  end
  ins["Instantiating...<br/>Instance i-abc12345 → Pending"]
  LT ~~~ ins
end
ac -->|"RunInstances API call<br/>(LaunchTemplate: web-app-v3,<br/>InstanceType: t3.large,<br/>Subnet: subnet-abc,<br/>Count: 1)"| CP

#### system-design-fundamentals-autoscaling-01-concepts-4
@H
flowchart TB
subgraph OUT["Instance Lifecycle States"]
  direction TB
  subgraph R1[" "]
    direction LR
    s1["Pending<br/><br/>Booting<br/>User<br/>data<br/>script"] --> s2["InService<br/><br/>Serving<br/>Traffic<br/><br/>LB:<br/>Healthy"] --> s3["Draining<br/><br/>No new<br/>requests<br/>Existing<br/>requests<br/>drain"] --> s4["Terminate<br/><br/>Instance<br/>deleted"]
  end
  subgraph TG["Load Balancer Target Group"]
    direction TB
    @FLAT t per=99 A;;ok | B;;ok | C;;ok | D;;ok | E;;drain
    @SRC n 143-145 lead2
    t0 ~~~ n
  end
  R1 --> TG
end

#### system-design-fundamentals-autoscaling-01-concepts-5
@H
flowchart LR
subgraph OUT["Reactive Autoscaling -- Response Timeline"]
  direction LR
  @SRC C 155-166 lead
  subgraph AN[" "]
    direction TB
    @FLAT a per=99 T-3m: System stable at 20K req/s | T-1m: Traffic starts rising | T+0: Metric collection (60s interval) | T+1m: Metric exceeds threshold (60%) | T+3m to T+5m: Scale-out action (6 instances) | T+7m: New capacity online | ⚠ Total gap from spike to capacity: 5-7 minutes
  end
  C ~~~ AN
end

#### system-design-fundamentals-autoscaling-01-concepts-6
@H
flowchart LR
subgraph OUT["Scheduled Autoscaling -- Capacity Timeline"]
  direction LR
  @SRC C 191-207 lead skip=205
  @M t Schedule:;;Time         │ Action       │ Reason;;─────────────┼──────────────┼─────────────────────;;06:00 daily  │ 10 instances │ Morning ramp-up;;08:00 daily  │ 20 instances │ Business hours start;;10:00 daily  │ 30 instances │ Peak preparation;;11:00 Wed    │ 40 instances │ Product launch event;;14:00 Wed    │ 10 instances │ Event ends;;20:00 daily  │ 5 instances  │ Night-time minimum
  C ~~~ t
end

#### system-design-fundamentals-autoscaling-02-deep-dive-1
@H
flowchart LR
subgraph OUT["Autoscaling Internal Architecture"]
  direction LR
  subgraph MS["Metrics Subsystem"]
    direction TB
    a1["Per-Instance<br/>Metrics Agent<br/><br/>CPU  Memory<br/>Disk Net<br/>Proc Count"]
    a2["Aggregation<br/>Engine<br/><br/>AVG, SUM, MIN,<br/>MAX, p50, p99<br/><br/>Window: 5 min"]
    a3["Alarm<br/>Evaluator<br/><br/>Compare metric<br/>vs threshold<br/>State: OK /<br/>ALARM / INSUF"]
    a1 --> a2
    a2 --> a3
  end
  subgraph DS["Decision Subsystem"]
    direction TB
    @M b1 Policy Evaluator;;;;For each policy in ASG:;;1. Get aggregated metric value;;2. Compare against threshold;;3. Determine breach magnitude;;4. Compute scaling action
    @M b2 Capacity Calculator;;;;new_desired = clamp(;;  policy_function(current_metric),;;  min_size,;;  max_size;;)
    @M b3 Cooldown State Machine;;;;State: idle | scaling | cooldown;;;;cooldown_start = now;;cooldown_end = now + 300s;;Skip if now < cooldown_end
    b1 --> b2
    b2 --> b3
  end
  subgraph EX["Execution Subsystem"]
    direction TB
    c1["Instance Lifecycle<br/>Manager<br/><br/>Pending → InService<br/>InService → Drain<br/>Drain → Terminate<br/><br/>Launch API call<br/>Terminate API call"]
    c2["Load Balancer<br/>Integration<br/><br/>Register targets<br/>Deregister targets<br/>Slow-start mode<br/><br/>Connection drain<br/>timeout config"]
    c3["Health Check<br/>Manager<br/><br/>HTTP check<br/>TCP check<br/>Custom check<br/><br/>3 fail →<br/>replace"]
    c1 ~~~ c2
    c2 ~~~ c3
  end
  MS ~~~ DS
  MS --> EX
  DS --> EX
  @M lg Legend:;;  [ ] = Subsystem component;;  ──▶  = Data flow;;  ──▶  = Control flow
  EX ~~~ lg
end

#### system-design-fundamentals-autoscaling-02-deep-dive-2
@H
flowchart TB
subgraph OUT["Complete Scaling Lifecycle"]
  direction TB
  subgraph R1[" "]
    direction LR
    s1["S1<br/>Traffic<br/>Surge<br/>Instant<br/>(0ms)"] --> s2["S2<br/>Metric<br/>Collected<br/>60s<br/>(60s)"] --> s3["S3<br/>Alarm<br/>Evaluated<br/>60s<br/>(120s)"] --> s4["S4<br/>Policy<br/>Evaluated<br/>60s<br/>(180s)"] --> s5["S5<br/>Capacity<br/>Calculated<br/>10ms<br/>(180s)"] --> s6["S6<br/>Launch<br/>Instances<br/>5-10s<br/>(190s)"] --> s7["S7<br/>Warm-Up<br/>Complete<br/>180s<br/>(370s)"]
  end
  subgraph R2[" "]
    direction LR
    s8["S8: LB Registration (10s)<br/>10s<br/>(380s)"] --> s9["S9: Traffic Ramp-Up (60s)<br/>60s<br/>(440s)"] --> s10["S10: Metric Stabilization + Cooldown (300s)<br/>300s<br/>(740s)"]
  end
  @L F Total time from surge to stable: ~740 seconds (12.3 minutes);;Total time from surge to new capacity serving: ~380 seconds (6.3 minutes)
  R1 ~~~ R2
  R2 ~~~ F
end

#### system-design-fundamentals-autoscaling-02-deep-dive-3
@H
flowchart TB
subgraph OUT["Scaling Decision Flow"]
  direction TB
  subgraph R1[" "]
    direction LR
    st["START"] --> c1["Is cooldown active?<br/>(now &lt; cooldown_end)"]
    c1 -->|"YES"| sk["Skip this cycle"]
    c1 -->|"NO"| g1["Get aggregated metric value<br/>from metrics pipeline"] --> c2["Is metric_data_sufficient?<br/>(enough data points)"]
  end
  subgraph R2[" "]
    direction LR
    c3["Does metric breach threshold?<br/>(metric &gt; target + tolerance)"] -->|"YES"| d1["Determine scaling direction<br/>and magnitude<br/><br/>Scale-out: metric TOO HIGH<br/>Scale-in:  metric TOO LOW"] --> d2["Calculate new desired<br/>capacity<br/><br/>target_tracking: ceil(<br/>  current * metric / target)<br/><br/>step_scaling: add/remove<br/>  N instances per tier"] --> d3["Clamp to [min_size,<br/>max_size] bounds"]
  end
  subgraph R3[" "]
    direction LR
    c4["Does new != old desired?"] -->|"YES"| d4["Execute scaling action<br/><br/>Update desired_capacity<br/>Start cooldown timer<br/>Log scaling event<br/>Emit scaling metric"] --> dn["DONE"]
  end
  R1 -->|"YES"| R2
  R2 --> R3
  R1 -.->|"NO"| dn
  R2 -.->|"NO"| dn
  R3 -.->|"NO"| dn
  @M lg Legend:;;  ┌──┐ = Decision/Process;;  ──▶  = Flow direction
  R3 ~~~ lg
end

#### system-design-fundamentals-autoscaling-03-architecture-1
@H
flowchart TB
subgraph OUT["Naive: 1,000 Users"]
  direction TB
  subgraph R1[" "]
    direction LR
    cl["Clients<br/><br/>1,000 users<br/>50 req/s"]
    subgraph SS["Single Server"]
      direction TB
      sp["CPU: 4 vCPU     RAM: 16 GB<br/>Disk: 500 GB SSD<br/>OS: Ubuntu 22.04<br/>App: Node.js API<br/>DB: PostgreSQL (same server)<br/>Cache: Redis (same server)"]
      sc["Scaling: Manual only<br/>&quot;ssh and fix&quot;<br/><br/>SLA: None<br/>MTTR: 30-120 minutes<br/>Cost: $100/month"]
      sp ~~~ sc
    end
    cl --> SS
  end
  @L w ⚠ Single point of failure at every layer;;⚠ Manual scaling: 30-minute response time;;⚠ No health checks, no auto-recovery
  R1 ~~~ w
end

#### system-design-fundamentals-autoscaling-03-architecture-2
@H
flowchart TB
subgraph OUT["Semi-Manual: 100,000 Users"]
  direction TB
  subgraph R1[" "]
    direction LR
    cl["Clients<br/><br/>100,000 users<br/>5,000 req/s"] --> lb["Load Balancer<br/>(ALB)<br/><br/>Target group<br/>port 443"]
    subgraph AP["Application Pool"]
      direction TB
      @FLAT a per=99 A | B | C | D | E
      mn["(manual)"]
      a0 ~~~ mn
    end
    lb --> AP
    AP --> db["Database (RDS)<br/>PostgreSQL db.r5<br/>Multi-AZ standby"]
  end
  subgraph R2[" "]
    direction LR
    subgraph HO["Human Operator"]
      direction TB
      @M h Monitors: CloudWatch;;Decision: Manual;;Action: Console/CLI;;;;Runbook:;;If CPU > 70% for 5m:;;  Launch 2 instances;;If CPU < 30% for 10m:;;  Terminate 1 instance
    end
    ch["Cache (ElastiCache)<br/>Redis 6.x, 2 shards"]
    HO ~~~ ch
  end
  @L f Manual scaling: 10-minute response time (best case);;Human error: 1 in 3 scaling actions is wrong size or timing;;After-hours incidents: 45-minute average response time
  R1 ~~~ R2
  R2 ~~~ f
end

#### system-design-fundamentals-autoscaling-03-architecture-3
@H
flowchart LR
subgraph OUT["Production-Grade: 10 Million Users"]
  direction LR
  gt["Global Traffic Manager<br/>Route53 Latency-Based<br/>Routing + Failover"]
  subgraph RG[" "]
    direction TB
    subgraph US["us-east-1"]
      direction LR
      u1["ALB"] --> u2["ASG<br/>Web"] --> u3["ASG<br/>Worker"] --> u4["SQS<br/>Queue"] --> u5["Shared Services<br/>((RDS Aurora)) {ElastiCache} S3<br/>Global Database<br/>Global Datastore"]
    end
    subgraph EU["eu-west-1"]
      direction LR
      e1["ALB"] --> e2["ASG<br/>Web"] --> e3["ASG<br/>Worker"] --> e4["SQS<br/>Queue"] --> e5["Shared Services<br/>((RDS)) {Redis}"]
    end
    US ~~~ EU
  end
  subgraph CN[" "]
    direction TB
    cd["CI/CD Pipeline<br/><br/>CodeCommit →<br/>CodeBuild →<br/>CodeDeploy<br/>(Blue/Green)"]
    ag["Autoscaling Group<br/><br/>Min: 10  Max: 200  Desired: 40<br/>[A..N] [ ] [ ] [ ]"]
    sp["Scaling Policies<br/><br/>Target Tracking: CPU @ 50%<br/>Target Tracking: req/target<br/>Scheduled: 8AM → 80, 8PM → 20<br/>Predictive: 24h forecast"]
    cd ~~~ ag
    ag ~~~ sp
  end
  mo["Monitoring & Observability<br/><br/>CloudWatch Dashboards │ Prometheus+Grafana │ PagerDuty │ DataDog<br/>Scaling Activity Logs │ Alarm Notifications │ Runbook Automation"]
  @M lg Legend:;;  [ ] = Service/Component     (( )) = Database;;  { } = Cache                 ──▶ = Traffic flow;;  ──▶ = Control flow
  gt --> u1
  gt --> e1
  u2 --- ag
  RG ~~~ CN
  gt ~~~ mo
  mo ~~~ lg
end

#### system-design-fundamentals-autoscaling-04-tradeoffs-1
@H
flowchart LR
subgraph OUT["Autoscaling Trade-Off Triangle"]
  direction LR
  subgraph TR[" "]
    direction TB
    ce["Cost Efficiency"]
    rs["Responsiveness"]
    sb["Stability"]
    ob["★<br/>Optimal<br/>Balance"]
    ce --- rs
    ce --- sb
    rs --- sb
    ce ~~~ ob
  end
  @SRC P 558-564 lead
  TR ~~~ P
end

#### system-design-fundamentals-autoscaling-04-tradeoffs-2
@H
flowchart LR
subgraph OUT["Slow Scale-Out -- Under-Provisioning Timeline"]
  direction LR
  @SRC C 575-587 lead
  @SRC T 589-601 lead wrapl=48
  C ~~~ T
end

#### system-design-fundamentals-autoscaling-04-tradeoffs-3
@H
flowchart LR
subgraph OUT["Connection Draining: Without vs. With"]
  direction LR
  subgraph W["WITHOUT DRAINING"]
    direction LR
    a1["Load Balancer<br/><br/>Terminate!"] --> a2["Instance E<br/><br/>Active conn<br/>to users:<br/>12 reqs<br/>in-flight<br/><br/>⚠ RST!"]
    an["Users:<br/>Connection reset<br/>Retry needed<br/><br/>⚠ 12 failed reqs"]
    a2 ~~~ an
  end
  subgraph D["WITH DRAINING"]
    direction LR
    b1["Load Balancer<br/><br/>Terminate!"] --> b2["Instance E<br/><br/>State:<br/>DRAINING<br/><br/>No new<br/>connections<br/><br/>Wait for<br/>in-flight:<br/>12 reqs<br/>complete<br/>⌛ 3.2s<br/><br/>✅ Done!"] --> b3["Terminate"]
  end
  W ~~~ D
end

#### system-design-fundamentals-autoscaling-05-real-world-1
@H
flowchart TB
subgraph OUT["Uber Cell-Based Autoscaling"]
  direction TB
  gr["Global Traffic Router<br/>City → Cell mapping, latency-based routing"]
  subgraph CE[" "]
    direction TB
    @FLAT c per=99 Cell: NYC;;;;ASG: nyc;;Min: 5;;Max: 100;;;;20 inst;;;;Metric:;;req/s/cell;;Target:500 | Cell: SF;;;;ASG: sf;;Min: 3;;Max: 60;;;;12 inst;;;;Metric:;;req/s/cell;;Target:500 | Cell: LON;;;;ASG: lon;;Min: 5;;Max: 80;;;;25 inst;;;;Metric:;;req/s/cell;;Target:500 | Cell: RIO;;;;ASG: rio;;Min: 3;;Max: 50;;;;18 inst;;;;Metric:;;req/s/cell;;Target:500
  end
  subgraph R3[" "]
    direction LR
    ev["Event: Concert<br/>in Rio<br/>Traffic: 5x<br/>Duration: 3h"]
    sa["Scaling Activity:<br/>RIO cell: 18 → 35 → 42 instances<br/>All other cells: No change<br/>Total instances added: 24 (only in RIO)"]
    ev ~~~ sa
  end
  gr --> c0
  gr --> c1
  gr --> c2
  gr --> c3
  CE ~~~ R3
end
