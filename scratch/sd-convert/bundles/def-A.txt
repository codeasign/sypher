@WORK vertical-scaling
#### system-design-fundamentals-vertical-scaling-02-deep-dive-4
%%{init: {'flowchart': {'subGraphTitleMargin': {'top': 4, 'bottom': 18}, 'nodeSpacing': 30, 'rankSpacing': 40, 'padding': 8, 'wrappingWidth': 1000}}}%%
flowchart TB
subgraph LC["Request Lifecycle — Steps 7-12: Buffer Cache to Response"]
  direction TB
  subgraph R1[" "]
    direction LR
    @L s7 Step 7: Buffer Cache Lookup;;[PostgreSQL shared_buffers (kernel + PG)];;──> Check shared_buffers for data pages;;Cache HIT:  read from RAM (0.1ms);;Cache MISS: schedule disk I/O (10ms)
    @L s8 Step 8: Data Page Read (on cache miss);;[Block Layer (blk-mq)] ──>;;[NVMe Driver + NVMe Device];;──> 1. Page cache check (kernel);;2. If also miss in page cache:;;request goes to NVMe driver;;3. DMA transfer from NVMe to RAM;;4. PG shared_buffers populated;;Latency: 10-100 microseconds (NVMe);;>1ms (EBS — network RTT)
    @L s9 Step 9: Data Processing;;[Query Executor (PostgreSQL)];;──> 1. Process qualifying rows;;2. Apply WHERE clause filter;;3. Apply ORDER BY / GROUP BY;;4. Join rows (nested loop / hash / merge);;Latency: 10 nanoseconds per row (filter);;1-100 microseconds per page (join)
    s7 --> s8
    s8 --> s9
  end
  subgraph R2[" "]
    direction LR
    @L s10 Step 10: WAL Write (for write queries);;[Write-Ahead Log (PostgreSQL WAL)];;──> 1. Append transaction to WAL buffer;;2. WAL writer flushes to disk;;3. fsync() call (kernel page cache flush);;4. Return success to client;;Latency: 0.5-5ms (WAL write to NVMe);;2-10ms (WAL write to EBS)
    @L s11 Step 11: Response Encoding;;[Response Encoding (PostgreSQL)];;──> 1. Format result rows as protocol buffer;;2. Copy to socket send buffer;;3. Trigger TCP send;;Latency: 1-10 microseconds per row
    @L s12 Step 12: Network Send;;[NIC Transmit];;──> 1. TCP segmentation / GSO;;2. Checksum offload (checksum to NIC);;3. DMA from kernel buffer to NIC;;Latency: 0.5-2 microseconds
    s10 --> s11
    s11 --> s12
  end
  s9 --> s10
  @SRC BK 446-455
  R2 ~~~ BK
end
