**Timeline:**
t=0s: Traffic starts rising from 1,000 RPS. CPU is 40%, so 10 instances can serve about 2,500 RPS at 100% CPU (250 RPS each).
t=30s: Traffic hits 3,000 RPS. CPU is pinned at 100%. The fleet serves about 2,500 RPS, so about 500 RPS (17% of traffic) gets errors or timeouts, and everyone else sees much higher latency.
t=30s to ~3.5min: The alarm needs CPU above 70% for 3 consecutive minutes, so nothing happens yet. Failed requests so far: 500 RPS * 180s = 90,000.
t=~3.5min: The alarm fires and the group launches 4 more instances (a typical step size).
t=~9.5min: The new instances have booted, registered with the ALB, and passed health checks (6 minutes after the launch). 14 instances can serve up to 3,500 RPS, so the shortfall ends. CPU is still about 86%, which is above the 70% threshold, so a further scale-out has already been triggered.
Failed requests over the whole window: 500 RPS * 540s = about 270,000.

**Result:** Roughly 270,000 requests fail over about 9 minutes, and latency is poor for all users during that time. The delay comes from three stacked lags: the evaluation window (3 minutes), instance initialization (6 minutes), and a scale-out step that is too small.

