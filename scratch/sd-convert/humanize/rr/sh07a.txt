**Shard key:** `(channel_id, bucket)`, where `bucket` is a fixed window of time (for example 10 days), with consistent hashing (Cassandra or ScyllaDB).

**Why channel, not guild?** Nearly every query is scoped to one channel. A guild can contain thousands of channels, so partitioning by `guild_id` would place all of that data in a single partition and make big guilds the hot spots. Partitioning by channel matches the access pattern and spreads a large guild's traffic across many partitions.

**Why a time bucket?** A busy channel would otherwise have one partition that grows forever. Bucketing by time bounds each partition's size, and "the last 50 messages" reads the newest bucket (and the previous one if the newest has fewer than 50 messages).

**Why consistent hashing?** Chat workloads grow unpredictably. Consistent hashing lets the cluster add nodes incrementally without a full rebalance.

**Database technology:** Cassandra (or ScyllaDB for better performance). Within one partition, messages are clustered by a time-sortable message ID (a Snowflake-style ID) in descending order, so "last 50 messages" is a short sequential read.

**Schema design:**
```cql
CREATE TABLE messages (
    channel_id  bigint,
    bucket      int,
    message_id  bigint,
    author_id   bigint,
    content     text,
    PRIMARY KEY ((channel_id, bucket), message_id)
) WITH CLUSTERING ORDER BY (message_id DESC);
```

The partition key is `(channel_id, bucket)`. The query "show last 50 messages in channel X" reads `WHERE channel_id = X AND bucket = current_bucket LIMIT 50`, and falls back to the previous bucket if needed.

**Handling hot channels and large guilds:** Bucketing keeps each partition bounded (a common guideline is to keep Cassandra partitions well under about 100 MB). A single very active channel can still concentrate traffic on one partition and its replicas. Mitigations: cache the latest page of messages for hot channels in Redis, and coalesce identical concurrent reads in a data service in front of the database so a burst becomes one query.

**Search (10% of queries):** Cassandra is not built for full-text search. Stream messages through a change pipeline into a search engine such as Elasticsearch or OpenSearch and query that instead.

