@@ pacelc/05-real-world.mdx
<<<<
It was designed at Amazon based on the Dynamo paper, which explicitly chose availability over consistency during partitions and low latency over consistency during normal operation.
====
The DynamoDB service descends from the ideas in Amazon's Dynamo paper, which explicitly chose availability over consistency during partitions and low latency over consistency during normal operation. (The managed service's internals differ from the paper, but its defaults follow the same philosophy: eventually consistent reads are the default.)
>>>>
<<<<
DynamoDB with default settings at 1000 write capacity units (WCU) delivers:
- p50 write latency: 3-5ms
- p99 write latency: 20-50ms
- Replication lag: 50-200ms in the same region, 200-1000ms cross-region
- ConsistentRead (strong): adds 10-15ms to read latency
====
DynamoDB advertises single-digit millisecond latency for reads and writes. In broad terms, and with the caveat that real numbers depend on the workload:
- Typical write latency: a few milliseconds
- Replication between replicas in a region: usually well under a second
- Cross-region (global tables) replication: typically about a second
- ConsistentRead (strong): costs twice the read capacity and slightly more latency, because it goes to the partition's leader
>>>>
<<<<
During the 2013 Prime Day, DynamoDB handled 11.8 million cart operations per minute without degradation. The PA/EL design was essential: if every cart write required quorum, the system would have needed 3x the capacity.
====
Amazon has reported that DynamoDB handles well over 100 million requests per second at peak during Prime Day. That kind of scale is what the low-latency, availability-first design is built for.
>>>>
<<<<
### Production Lesson: Netflix on Cassandra

Netflix runs Cassandra for its user activity tracking, recommendation data, and viewing history. The key PACELC decision: Netflix uses CL=LOCAL_ONE for writes (PA/EL) and CL=LOCAL_QUORUM for reads that power the recommendation engine.

The rationale: write throughput for user activity is massive (billions of events per day). Using LOCAL_QUORUM for writes would require 3x the cluster size. Since activity data is append-mostly and missing a few events does not break the product, PA/EL writes are optimal. Reads for the recommendation engine use LOCAL_QUORUM because stale recommendations would degrade the user experience measurably.

Netflix reported that this hybrid approach saved approximately 40% in infrastructure costs compared to a uniform QUORUM configuration, while maintaining recommendation quality within acceptable bounds.

### Production Lesson: Apple's Cassandra Deployment

Apple runs one of the largest Cassandra deployments globally for iCloud services. Apple uses CL=LOCAL_QUORUM as the default for user-facing data (contacts, calendars, notes) to ensure synced data across devices is consistent. For analytics and logging, CL=ONE is used.
====
### Production Lesson: Netflix and Apple on Cassandra

Netflix and Apple have both described very large Cassandra deployments in public talks and blog posts, and both run across multiple data centers or regions. The general pattern in such deployments, and an illustration of how the PACELC choice is made per workload, looks like this:

- For high-volume, append-mostly data such as activity events and metrics, a low consistency level (`LOCAL_ONE`) keeps write latency and cluster size down, and losing or delaying a few events does not break the product.
- For data whose staleness users would notice, such as account or synced user data, `LOCAL_QUORUM` gives consistent reads and writes within a data center while avoiding cross-data-center latency.

The specific settings each company uses are their own choices, so treat the list above as an example of the reasoning, not as a report of their configurations.
>>>>
<<<<
- **Strong reads:** Linearizable. Go through the leader. 5-15ms.
- **Bounded staleness reads:** Client specifies maximum staleness (e.g., 10 seconds). The system reads from any replica that has caught up to that point. 1-5ms.
====
- **Strong reads:** Linearizable. The serving replica must confirm it is up to date, which may involve the leader, so latency includes possible cross-region coordination.
- **Bounded staleness reads:** Client specifies maximum staleness (e.g., 10 seconds). The system reads from any replica that has caught up to that point, which is typically faster and can be served locally.
>>>>
<<<<
### Production Lesson: Cosmos DB in Microsoft 365

Microsoft 365 uses Cosmos DB with Session consistency for most user data. Session consistency guarantees monotonic reads, monotonic writes, and read-your-writes within a single client session. This maps to PA/EL during normal operation with a session-specific consistency guarantee that prevents the most confusing inconsistency pattern (a user seeing their write disappear).

For global configuration data, Microsoft uses Strong consistency. For telemetry, Eventual consistency is used.
====
### Production Lesson: Session Consistency as the Default

Session consistency is the default consistency level for Cosmos DB accounts. It guarantees monotonic reads, monotonic writes, and read-your-writes within a single client session. This maps to PA/EL during normal operation with a session-specific guarantee that prevents the most confusing inconsistency pattern (a user seeing their own write disappear). Data that must be globally consistent, such as configuration, can use Strong consistency, and telemetry can use Eventual consistency, with the latency and availability costs that each choice carries.
>>>>
<<<<
It provides 10 consistency levels across reads and writes, covering the entire PA/EL to PC/EC spectrum.
====
It provides about a dozen consistency levels across reads and writes, covering the entire PA/EL to PC/EC spectrum.
>>>>
<<<<
but with significantly lower latencies (2-3x faster per node) through a shared-nothing per-core architecture.
====
but with lower latencies and higher throughput per node in many workloads, through a shared-nothing per-core architecture.
>>>>
<<<<
**Lesson 1: The Else regime matters more than the Partition regime.** A system that is fast and eventually consistent for 99% of operating time but slow and strongly consistent for 1% is better than the reverse.
====
**Lesson 1: The Else regime deserves as much attention as the Partition regime.** Partitions are rare, and your system runs in the Else regime nearly all the time, so its latency and consistency choices shape the everyday user experience.
>>>>
