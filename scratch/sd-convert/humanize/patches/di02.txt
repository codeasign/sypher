@@ distributed-ids/02-deep-dive.mdx
<<<<
On containerized platforms (Kubernetes), the clock manager must also account for paused containers whose clock freezes during suspend.
====
On virtual machines and containers, the clock manager must also account for suspend and resume, after which the wall clock can jump forward (or, after a bad sync, backward).
>>>>
<<<<
The core data structure is a **64-bit unsigned integer** partitioned into bit fields.
====
The core data structure is a **64-bit integer** partitioned into bit fields.
>>>>
<<<<
The sequence in the low bits allows the counter to wrap naturally into the timestamp.
====
The sequence in the low bits keeps IDs generated within the same millisecond in order.
>>>>
<<<<
            while current_ms <= last_ms:
                current_ms = clock.now()  // spin-wait
            sequence = 0
====
            while current_ms <= last_ms:
                current_ms = clock.now()  // spin-wait
            sequence = 0
            last_ms = current_ms
>>>>
<<<<
The sequence resets to zero. No duplicates, no gaps in the monotonic sequence from the system's perspective: the gap in sequence numbers (e.g., sequence left off at 2047, restart picks up at 0) is irrelevant because the timestamp component differs.
====
The sequence resets to zero. No duplicates, as long as the clock has not moved backward to or before the crash timestamp: the gap in sequence numbers (e.g., sequence left off at 2047, restart picks up at 0) is irrelevant because the timestamp component differs.
>>>>
<<<<
**Key insight:** Snowflake is slower per-generation than UUID v4 (clock read adds overhead), but the total system cost is often lower because sequential IDs reduce database write amplification dramatically. At scale, the 10x reduction in I/O from fewer B-tree page splits more than compensates for the 5x increase in generation latency.
====
The figures in this table are illustrative, in the range that typical microbenchmarks report. Measure on your own hardware and database.

**Key insight:** Snowflake is slower per-generation than UUID v4 (clock read adds overhead), but the total system cost is often lower because sequential IDs reduce database write amplification. At scale, the reduction in I/O from fewer B-tree page splits can far outweigh the small increase in generation latency, which is measured in microseconds.
>>>>
