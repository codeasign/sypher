@@ cap-theorem/06-interview.mdx
<<<<
State the theorem accurately: during a network partition, you can guarantee at most two of the three properties.
====
State the theorem accurately: a distributed system cannot provide consistency, availability, and partition tolerance all at once. Since partitions happen, the real choice is between consistency and availability while a partition lasts.
>>>>
<<<<
"With 1 billion writes per day and a 0.001 percent conflict rate, we have 10,000 conflicts per day. Each conflict requires manual resolution at 5 minutes each. That is 35 person-hours per day.
====
"With 1 billion writes per day and a 0.0001 percent conflict rate, we have 1,000 conflicts per day. Each conflict requires manual resolution at 5 minutes each. That is about 83 person-hours per day.
>>>>
<<<<
This points to an AP system with eventually consistent reads and strongly consistent writes."
====
This points to an AP-leaning system with eventually consistent reads and quorum writes."
>>>>
<<<<
I will use ZooKeeper with N=5 and W=3."
====
I will use a 5-node ZooKeeper ensemble, where a quorum is 3 nodes."
>>>>
<<<<
Reads go to any node but must check the commit index. During a partition, the minority side will reject writes."
====
Linearizable reads go through the leader (or confirm the commit index first). During a partition, the minority side will reject writes."
>>>>
<<<<
Read repair will run on 10 percent of reads. Anti-entropy will run every 5 minutes using Merkle trees. Conflict resolution uses last-write-wins by timestamp."
====
Read repair will fix disagreements found on quorum reads. Anti-entropy repair using Merkle trees will run on a schedule, at least once within the tombstone grace period (10 days by default in Cassandra). Conflict resolution uses last-write-wins by timestamp."
>>>>
<<<<
"The CAP theorem states that a distributed data store can only guarantee two of three properties (Consistency, Availability, and Partition Tolerance) during a network partition."
====
"The CAP theorem states that a distributed data store cannot provide all of Consistency, Availability, and Partition Tolerance at once. Because partitions are unavoidable, during a partition you must choose between consistency and availability."
>>>>
<<<<
Use ZooKeeper or etcd with N=5, W=QUORUM. Lock implementation:
====
Use a 5-node ZooKeeper or etcd cluster. Lock implementation:
>>>>
<<<<
Handle thundering herd with try-lock pattern.
====
Handle thundering herd with the try-lock pattern (each waiter watches only its predecessor). Add fencing tokens so that a client that paused, lost its lock, and woke up cannot corrupt shared data.
>>>>
