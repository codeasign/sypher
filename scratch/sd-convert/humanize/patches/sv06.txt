@@ stateless-vs-stateful/06-interview.mdx
<<<<
I would use a hybrid approach inspired by Discord's architecture.
====
I would use a hybrid approach inspired by the architectures of large chat systems such as Discord's.
>>>>
<<<<
Map each component to the decision matrix: stateless for shared, durable, strongly-consistent state that lives in a database. Stateful for session-scoped, latency-sensitive, ephemeral state that benefits from local memory.
====
Map each component to the decision matrix. Keep a component stateless when its state can live in a shared, durable store such as a database or Redis. Make it stateful only when session-scoped, latency-sensitive, or ephemeral state benefits from local memory.
>>>>
<<<<
7. "We use a database for session state, so we are stateless.": Correct but trivial. The interviewer wants to know what else you understand.
====
7. "We use a database for session state, so we are stateless.": Correct as far as it goes, but incomplete. The interviewer wants to hear the cost of a relational database for high-rate session lookups and why a key-value store is usually a better fit.
>>>>
<<<<
Can you articulate the trade-off between operational simplicity and upload reliability?
====
Can you articulate the trade-off between operational simplicity and upload reliability? A strong answer also mentions the best stateless option: clients upload directly to S3 through pre-signed URLs, so the servers never buffer the file at all.
>>>>
<<<<
Can you estimate the memory requirements (500,000 sessions x 2 KB = 1 GB per node)?
====
Can you estimate the memory requirements (500,000 sessions x 2 KB = 1 GB in total, so a few hundred MB per node across a small cluster)?
>>>>
