@@ cap-theorem/05-real-world.mdx
<<<<
DynamoDB is a fully managed AP service. It provides 99.999 percent availability SLA. Key features:
====
DynamoDB is a fully managed service that favors availability by default. It provides a 99.99 percent availability SLA (99.999 percent for global tables). Key features:
>>>>
<<<<
DynamoDB's consistency model: eventually consistent reads (default, 1-second staleness window), strongly consistent reads (optional, may fail during partitions), transactional reads (ACID, across multiple items).
====
DynamoDB's consistency model: eventually consistent reads (the default, usually converging within about a second), strongly consistent reads (optional, and they can return an error if the partition's leader is unreachable), and transactional reads and writes (ACID, across multiple items).
>>>>
<<<<
Spanner is a fully managed CP service with TrueTime. It provides 99.999 percent availability SLA. Key features:
====
Spanner is a fully managed CP service built on TrueTime. It offers a 99.99 percent availability SLA for regional configurations and 99.999 percent for multi-region ones. Key features:
>>>>
<<<<
A misconfigured DynamoDB table with strongly consistent reads will fail during a partition just like a self-hosted CP system.
====
Strongly consistent reads in DynamoDB can return errors when a partition's leader is unreachable, just as a self-hosted CP system would refuse requests.
>>>>
