@@ sharding/06-interview.mdx
<<<<
Trip data must be sharded. The city-based sharding approach is a central architectural decision.
====
Trip data must be sharded. Choosing the shard key (rider, driver, trip ID, or geography such as city) is a central architectural decision.
>>>>
<<<<
Can explain why Cassandra chose consistent hashing while Vitess chose hash modulo, and the implications of each choice.
====
Can explain why Cassandra chose consistent hashing on a token ring while Vitess maps a hashed keyspace ID onto ranges owned by shards, and the implications of each choice.
>>>>
<<<<
The rebalancing algorithm would use a variant of the CRUSH algorithm (used by Ceph) to minimize data movement during topology changes, with a theoretical bound of O(log N) data movement per node addition.
====
The rebalancing algorithm would use a deterministic placement scheme in the style of CRUSH (used by Ceph) so that any client can compute where data lives and only a small fraction of data moves when topology changes.
>>>>
<<<<
to reduce p99 latency by 30-40 percent."
====
to reduce tail latency (hedged requests trade a little extra load for a lower p99)."
>>>>
