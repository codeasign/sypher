@@ consensus/06-interview.mdx
<<<<
If one node fails, the remaining one node cannot form a majority. 2-node clusters also risk split-brain. Always use an odd number.
====
If either node fails, the remaining node cannot form a majority, so a 2-node cluster is less fault tolerant than a single node (there are two things that can fail and no failure can be tolerated). Use an odd number of nodes, starting at 3.
>>>>
<<<<
Total failover time: etcd session timeout (1 second) plus key creation time (under 10ms).
====
Total failover time: etcd session timeout (1 second) plus key creation time (under 10ms). Note the tension with the 500ms requirement: a session TTL that short risks spurious failovers during network jitter or GC pauses, so a good answer negotiates the requirement or explains the trade-off. Also, the old primary must be fenced (for example with the etcd revision used as a fencing token, or by shutting it down) so it cannot keep writing after a new primary takes over.
>>>>
<<<<
The system handles the split-brain case because only the side with a majority can elect a leader and commit writes. The minority side rejects all writes."
====
The system handles the split-brain case because only the side with a majority can elect a leader and commit writes. The minority side rejects all writes. I would also return a monotonically increasing fencing token with each lock grant, so that a client whose lease expired during a long pause cannot corrupt the protected resource."
>>>>
<<<<
zero downtime for Kafka.

### Drill 5: Compare Consensus Algorithms (10 minutes)

Practice comparing Raft, Paxos, and Zab across these dimensions: understandability, implementation complexity, performance in steady state, performance under leader changes, ecosystem support, and community adoption. The most important insight: all three achieve the same safety guarantees. The differences are in how they achieve liveness and how easy they are to implement correctly.
====
zero downtime for Kafka.
>>>>
