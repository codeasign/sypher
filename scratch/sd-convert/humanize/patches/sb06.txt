@@ split-brain/06-interview.mdx
<<<<
"Stronger split brain prevention reduces availability because the cluster requires more nodes to reach consensus."
====
"Split brain prevention trades availability for safety: the minority side of a partition must stop accepting writes, so some clients see errors until the partition heals."
>>>>
<<<<
"The optimal cluster size balances fault tolerance against availability probability. Five nodes offers a good trade-off for most production systems."
====
"The right cluster size balances fault tolerance against cost and write latency. Three nodes is the minimum that tolerates a failure, and five nodes is a common choice for production systems that must survive two failures, such as one in maintenance and one unplanned."
>>>>
<<<<
- Availability: P(at least 4 nodes up) = sum of binomial
  P(4) + P(5) + P(6) + P(7) = 99.97%

Try: 5 nodes, 99% uptime
- Quorum: 3, tolerance: 2, availability: 99.9%
====
- Availability: P(at least 4 nodes up) = sum of binomial
  P(4) + P(5) + P(6) + P(7) = about 99.99998%

Try: 5 nodes, 99% uptime
- Quorum: 3, tolerance: 2, availability: about 99.999%
>>>>
<<<<
Good answer: "Leases expire after 10 seconds, but we also
configure a 200-millisecond clock skew tolerance and use NTP
with tight bounds. The effective lease is 10.2 seconds to
account for clock drift between nodes."
====
Good answer: "Leases expire after 10 seconds, but we also
configure a 200-millisecond clock skew tolerance and use NTP
with tight bounds. The leader treats its lease as expiring 200
milliseconds early, and other nodes wait an extra 200
milliseconds before granting the lease to anyone else, so two
leaders never overlap even with clock drift between nodes."
>>>>
<<<<
- Primary: STONITH via IPMI (200-800ms, strongest guarantee)
====
- Primary: STONITH via IPMI (typically a few seconds, strongest guarantee)
>>>>
<<<<
RTO achieved: ~5 seconds (detection) + ~1 second (fencing)
RPO: zero (STONITH ensures no writes after fencing)
====
RTO achieved: ~5 seconds (detection) + ~2-10 seconds (fencing)
RPO: zero if replication is synchronous (STONITH stops further
     writes, but it cannot recover writes that were acknowledged
     and never replicated)
>>>>
<<<<
- 5-node MongoDB replica set
- Network partition: 3 nodes in AZ1, 2 nodes in AZ2
- Both sides elected primaries
- 147 writes on AZ2 side that did not replicate to AZ1
- Partition lasted 4 minutes and 23 seconds
====
- 5-node MongoDB replica set
- Network partition: 3 nodes in AZ1, 2 nodes in AZ2
- The old primary was in AZ2. AZ1 elected a new primary, while the old
  one kept accepting w:1 writes until it noticed it had lost its majority
- 147 writes on the AZ2 side did not replicate to AZ1
- Partition lasted 4 minutes and 23 seconds
>>>>
<<<<
1. Verify the authoritative side (AZ1 has 3 nodes, higher term)
2. Fence the stale primary in AZ2 (STONITH via IPMI)
3. Export the 147 unique writes from AZ2 side
4. Analyze each write for conflicts with AZ1 state
====
1. Verify the authoritative side (AZ1 has 3 nodes, higher term)
2. Confirm the old primary in AZ2 has stepped down (rs.status()), or isolate it
3. When it rejoins, MongoDB rolls back the 147 unreplicated writes and saves them in rollback files
4. Inspect the rollback files (for example with bsondump) and analyze each write for conflicts with AZ1 state
>>>>
<<<<
- Write latency: local region 50ms, cross-region 200ms
====
- Write latency: a quorum of 4 needs one remote node besides the
  three in the leader's region, so about one cross-region round trip
  (roughly 70-100ms); clients in other regions add their own trip to
  the leader
>>>>
<<<<
- Fencing: fencing tokens + IPMI STONITH
====
- Fencing: fencing tokens checked by storage, plus STONITH where
  the platform allows it (in the cloud, an instance stop/terminate API)
>>>>
<<<<
Availability: 99.9996% (with 99.99% per-node uptime)
====
Availability: very high if failures are independent, but in practice limited by correlated failures and operations
>>>>
<<<<
- Which node has the highest fencing token? That node was
  elected most recently.
====
- Which node has the highest term or timeline? That node was
  elected most recently (Patroni shows the PostgreSQL timeline).
>>>>
<<<<
- If stale leader is still running, fence it:
  kubectl delete pod db-n-0 --grace-period=0 --force
====
- If the stale leader is still running, fence it. Note that
  force-deleting a pod object (kubectl delete pod --force) only
  removes it from the API. If its node is unreachable, the process
  may keep running and writing, so fence the node or its storage
  (cordon and power off or delete the node, or block its network
  or volume access) before trusting that it has stopped.
>>>>
<<<<
- Export any unique WAL segments from the fenced node
====
- Preserve any unique WAL from the fenced node before rewinding or re-cloning it (pg_rewind or a fresh base backup will discard it)
>>>>
