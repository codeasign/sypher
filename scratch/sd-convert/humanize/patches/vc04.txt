@@ vector-clocks/04-tradeoffs.mdx
<<<<
The curve shows the inflection point around 50 nodes. Below 50, vector clocks are cheap and precise. Above 50, the cost begins to outweigh the precision gains. Above 200-500 nodes, alternative approaches (interval tree clocks, hierarchical clocks, TrueTime) become more attractive.
====
As a rule of thumb, the cost curve bends somewhere around 50 nodes that write to the same key. Below that, vector clocks are cheap and precise. Above it, the cost begins to outweigh the precision gains, and above a few hundred writers per key, alternative approaches (interval tree clocks, hierarchical clocks, or avoiding per-key clocks entirely) become more attractive. The exact thresholds depend on how many nodes really write to a given key, not on the size of the cluster.
>>>>
<<<<
| Comparison cost | O(N) | O(1) | O(1) | O(log active) |
====
| Comparison cost | O(N) | O(1) | O(1) | Proportional to clock size |
>>>>
<<<<
| Scalability limit | ~200 nodes | Unlimited | Unlimited | ~10,000 nodes |
====
| Scalability limit (rough guide) | Hundreds of writers per key | Unlimited | Unlimited | Higher, but not well established in production |
>>>>
<<<<
**Scenario:** A cluster of 10 nodes grows to 100 nodes over 2 years. Old nodes are decommissioned but their counters remain in every vector clock. The clock size grows from 10 to 100 entries for every key, even though most of the 90 new nodes have never modified older keys.
====
**Scenario:** A cluster of 10 nodes is replaced, generation by generation, over 2 years, and grows to 100 distinct node identities along the way. Old nodes are decommissioned but their counters remain in every vector clock they touched. A key that was written through successive generations of coordinators accumulates an entry for each one, growing from a handful of entries toward 100, even though only a few of those nodes still exist.
>>>>
<<<<
- Aggressive pruning: remove entries where counter = 0 or below threshold
- Periodic clock compaction: merge small counters into a single "epoch" counter
- Node decommissioning: remove decommissioned nodes' counters during compaction
====
- Sparse clocks: never store entries whose counter is zero
- Careful pruning: remove stale entries only with a clear policy, because pruning is lossy (see Failure Mode 3)
- Node decommissioning: collapse the entries of decommissioned nodes only after every replica has converged past them, so that no replica still needs them to order versions
>>>>
<<<<
**Scenario:** Node A and Node B both modify key K. Node A increments its counter to A=5. Node B increments its counter to B=5. Both clocks are pruned to remove the other node's counter (zero). Each clock now has only its own entry: [A=5] and [B=5]. When compared, these clocks are concurrent, which is correct. But if Node A also has Node C's counter and prunes it to reduce from 100 to 20 entries, the lost entries mean that comparisons involving those pruned nodes may produce incorrect results.
====
**Scenario:** Version X has clock [A=5, B=2] and version Y has clock [A=6, C=3]. They are concurrent: X has a B update that Y never saw, and Y has a C update that X never saw. Now suppose pruning drops the B entry from X to save space, leaving [A=5]. Compared with Y, X now looks strictly smaller (5 < 6, and the missing B counts as zero), so the system decides X is an ancestor of Y and discards it. The update recorded in B=2 is silently lost, and no conflict is ever reported.
>>>>
<<<<
A node's clock must never decrease. If a node crashes and restores state from a backup that has an older clock, the new clock may be a descendant of previous writes that the backup does not include. This causes the system to incorrectly treat later writes as concurrent with the restored state.

**Scenario:** Node A has counter A=10 in all its keys. A disk failure causes Node A to restore from an 8-hour-old backup where its counter was A=3. After restore, Node A accepts a write with clock [A=4], but another node has already seen [A=10] from Node A. The system sees [A=10] and [A=4] as concurrent versions when [A=4] is actually stale. The write from the restored node should be rejected, but the system treats it as a legitimate concurrent update.
====
A node's counter must never decrease. If a node crashes and restores state from a backup that has an older counter, it will reuse counter values it has already issued. Two different writes can then carry the same clock, or a genuinely new write can look older than data that peers already hold.

**Scenario:** Node A has issued counters up to A=10, and other nodes have seen [A=10] from it. A disk failure causes Node A to restore from an 8-hour-old backup where its counter was A=3. After the restore, Node A accepts a new write and stamps it [A=4]. Other nodes already hold a different version stamped [A=4] from before the crash, so two different values share an identical clock, and the system cannot tell them apart. In other cases the new write is compared with a version stamped [A=10] and is judged to be an ancestor, so it is dropped as stale. Either way, an acknowledged write is lost or corrupted without any conflict being reported.
>>>>
<<<<
Prune aggressively. Remove zero-count entries on every read. Remove stale entries on a configurable schedule. Use a pruning threshold that balances storage cost against causal precision.
====
Do not store zero-count entries. Remove entries of decommissioned nodes once every replica has converged past them, and treat any other pruning as a deliberate, lossy trade-off with a configured limit (as Dynamo does), balancing storage cost against causal precision.
>>>>
<<<<
| Message passing (no shared state) | Vector clocks designed for shared mutable state | Lamport timestamps, causal broadcast |
| Low-latency constraints (under 1ms per write) | Clock comparison and merge add latency overhead | Last-writer-wins, CRDTs without clocks |
====
| Only a total order is needed | Vector clocks give a partial order and cannot break ties between concurrent events | Lamport timestamps, sequence numbers from a log |
| Hot keys with many concurrent writers | Sibling counts explode and every read must reconcile them | CRDTs, or last-writer-wins where loss is acceptable |
>>>>
<<<<
In a multi-tenant system where each tenant has dedicated nodes, the vector clock must track all nodes across all tenants. If 50 tenants each have 10 nodes (500 total nodes), every vector clock has 500 entries. Most entries are zero because a key only belongs to one tenant, but the clock still allocates space for all 500 nodes.
====
In a multi-tenant system where each tenant has dedicated nodes, a naive vector clock must be able to name all nodes across all tenants. If 50 tenants each have 10 nodes (500 total nodes) and the clock is a fixed-size array indexed by node, every clock has 500 slots. Most are zero because a key only belongs to one tenant, but the array still allocates space for all 500 nodes. A sparse map avoids that waste, but a shared node namespace still makes tooling and pruning harder.
>>>>
<<<<
switch back to LWW by using the hybrid logical clock component of the vector clock (which includes physical time). The timestamp is always available as part of the HLC, so no data is lost during rollback.
====
switch back to LWW using the timestamp that was kept alongside the vector clock during the dual-write phase. Because the timestamp was stored for every write, no data is lost during rollback (though LWW's usual loss of concurrent updates returns).
>>>>
<<<<
- **Fixed size**: An HLC is a single (physical, logical) pair, 16 bytes total, regardless of node count.
====
- **Fixed size**: An HLC is a single (physical, logical) pair, typically 8 to 16 bytes, regardless of node count.
>>>>
<<<<
- **Bounded clock drift**: HLCs assume bounded clock drift between nodes (typically 200-500 microseconds with NTP).
====
- **Bounded clock drift**: HLCs assume bounded clock drift between nodes. They work with ordinary NTP accuracy (typically milliseconds), and systems such as CockroachDB enforce a maximum offset (500 ms by default) and stop nodes whose clocks exceed it.
>>>>
<<<<
- The data is non-critical (session data, cache entries, analytics counters)
====
- The data is non-critical (session data, cache entries, derived or recomputable values)
>>>>
<<<<
A single vector clock bug can cost an organization 10-50 engineering-days to diagnose and fix. This operational cost must be factored into the decision to use vector clocks over simpler alternatives.
====
A single vector clock bug can consume days or weeks of engineering time to diagnose and fix. This operational cost must be factored into the decision to use vector clocks over simpler alternatives.
>>>>
@@ vector-clocks/03-architecture.mdx
<<<<
// RocksDB column family for vector clock metadata
====
// RocksDB column family for vector clock metadata (simplified pseudocode, not the exact RocksDB API)
>>>>
