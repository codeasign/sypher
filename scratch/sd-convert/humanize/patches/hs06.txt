@@ horizontal-scaling/06-interview.mdx
<<<<
- "Read replicas for the database. Sharding if data exceeds 500 GB."
====
- "Read replicas for the database. Sharding once one primary can no longer handle the write rate or the data size."
>>>>
<<<<
- "At 1 million RPS, the load balancer itself needs horizontal scaling; we deploy multiple LBs behind a virtual IP."
====
- "At 1 million RPS, the load balancer tier itself needs horizontal scaling; we run multiple LBs and spread traffic across them with DNS, anycast, or ECMP routing (a virtual IP only gives failover, not extra capacity)."
>>>>
