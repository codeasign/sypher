@@ raft/02-deep-dive.mdx
<<<<
**Leader Completeness (Log Matching):** Once a log entry is committed,
====
**Leader Completeness:** Once a log entry is committed,
>>>>
<<<<
Losing the leader before fsync means losing all uncommitted writes.
====
This can lose writes that were already acknowledged to clients if the machines crash or lose power before the data reaches disk.
>>>>
<<<<
- Read latency (leader-direct): 0.1-1ms (no disk writes, no network round trip to followers)
====
- Read latency (leader-direct, with a valid leader lease): 0.1-1ms (no disk writes, no network round trip to followers)
>>>>
<<<<
**Heartbeat interval:** Typically 50-100ms. Should be significantly shorter than the election timeout (by a factor of 5-10). A 100ms heartbeat with 150-300ms timeout provides a good balance.
====
**Heartbeat interval:** Should be significantly shorter than the election timeout, and comfortably longer than the network round trip. With the paper's 150-300ms election timeout, a heartbeat of roughly 15-50ms fits. etcd's defaults are a 100ms heartbeat with a 1000ms election timeout (a 10x ratio).
>>>>
<<<<
**Max inflight snapshots:** Limits simultaneous InstallSnapshot transfers. Default 1, to avoid saturating the network.
====
**Max inflight snapshots:** Limits simultaneous InstallSnapshot transfers, to avoid saturating the network.
>>>>
