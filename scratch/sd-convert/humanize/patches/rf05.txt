@@ raft/05-real-world.mdx
<<<<
- **The 8 GB database limit.** etcd has a hard limit of 8 GB for the database by default. Beyond this, snapshot creation times become impractical and compaction cannot keep up. Mitigation: use periodic compaction and defragmentation, or increase the limit with care.
====
- **The database size quota.** etcd enforces a storage quota (2 GiB by default) and recommends staying under about 8 GiB. Beyond that, snapshots, defragmentation, and recovery become slow. When the quota is exceeded, etcd goes read-only until space is reclaimed. Mitigation: use periodic compaction and defragmentation, and raise the quota only with care.
>>>>
<<<<
"default" is strongly consistent through the leader, "stale" allows any server to serve reads for lower latency, and "consistent" enforces leader-based linearizable reads.
====
"default" is served by the leader without an extra check (usually current, with a small window of possible staleness), "stale" allows any server to serve reads for lower latency, and "consistent" makes the leader verify its leadership with a quorum first, for fully linearizable reads.
>>>>
<<<<
- **The 5-node bottleneck.** In Consul's early days, 5-node clusters became a bottleneck for large deployments (10,000+ nodes) because all health check information had to pass through the Raft consensus layer. Modern Consul uses a separate streaming subsystem for health information.
====
- **Keep high-volume data off the Raft path.** Health checks run on the client agents, and only changes are written through Raft. Large deployments learned that routing every piece of health data through consensus would overload the servers, so later Consul versions added streaming of updates to agents to cut read load on the servers.
>>>>
<<<<
It is inspired by Google Spanner but uses Raft instead of Paxos and a hybrid logical clock instead of TrueTime.
====
It is inspired by Google Spanner but uses Raft instead of Paxos and a central timestamp oracle (in the Placement Driver) instead of TrueTime.
>>>>
<<<<
The key space is divided into 64 MB regions, each managed by an independent Raft group.
====
The key space is divided into regions (roughly 96 MiB by default in recent versions), each managed by an independent Raft group.
>>>>
<<<<
- **Raft log = write-ahead log.** TiDB made the Raft log the same as the storage engine's write-ahead log. This eliminates redundant writes (write amplification drops from 2x to 1x).
- **Parallel raft append.** Follower nodes can write to temporary storage and acknowledge before the data is fully applied, reducing perceived latency.
====
- **A dedicated Raft log engine.** TiKV stores the Raft log in a separate log-structured engine, apart from the engine that holds the data, so that the frequent small log appends do not compete with data compaction.
- **Batching and pipelining.** TiKV batches Raft messages and writes across many regions to keep the cost of running many groups per node low.
>>>>
<<<<
A region exceeding 64 MB is automatically split into two smaller regions.
====
A region that grows past its size threshold is automatically split into two smaller regions.
>>>>
<<<<
This enables geographic read locality: a replica in the nearest data center holds the range lease while a replica in a different region holds the Raft leadership.
====
The leaseholder serves reads and coordinates writes for the range, and CockroachDB tries to keep the Raft leader on the same node so that writes do not take an extra hop. Placement settings let you steer leaseholders toward the region where most of the traffic comes from.
>>>>
<<<<
Used by TiDB (via TiKV) and many other projects.
====
Its Rust port (raft-rs) is used by TiKV, and the Go library is used by many other projects.
>>>>
<<<<
state machine machine interface
====
state machine interface
>>>>
<<<<
**AWS MemoryDB for Redis:** Uses Raft for cluster metadata consensus and primary election. Each shard has a primary node (writes) and up to 5 replicas. Raft elects a new primary if the current one fails.

**Google Cloud Spanner:** Uses Paxos, not Raft, but the lessons are similar. Spanner's TrueTime API provides clock synchronization that eliminates the need for Raft-style randomized timeouts. This is a key advantage for global deployments.

**Azure Cosmos DB:** Uses a custom consensus protocol based on Paxos, not Raft. The design is optimized for multi-region writes, which Raft's single-leader model cannot support.
====
**AWS MemoryDB for Redis:** A durable Redis-compatible service built on a multi-AZ transaction log. AWS has not published it as a Raft implementation, but it follows the same principle: acknowledge a write only after it is durably replicated.

**Google Cloud Spanner:** Uses Paxos, not Raft, but the lessons are similar. Each Paxos group has a leader that holds a time-limited lease. TrueTime, Google's clock service, is used to assign transaction timestamps, not to elect leaders.

**Azure Cosmos DB:** Uses a proprietary replication protocol rather than Raft. Its design supports configurable consistency levels and multi-region writes.
>>>>
