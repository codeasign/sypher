@@ consistent-hashing/02-deep-dive.mdx
<<<<
For a 1,000-node cluster, this means under 10 network hops per lookup.
====
For a 1,000-node cluster, this means at most about 10 network hops per lookup (about 5 on average).
>>>>
<<<<
The hash value is `0x4A7B3C2D`.
====
The hash value, truncated here to 32 bits for readability, is `0x4A7B3C2D`.
>>>>
<<<<
**Resolution**: Idempotent writes with exactly-once semantics. Each write carries a unique sequence number. The receiving node deduplicates by sequence number, ensuring that retries do not create duplicate entries.
====
**Resolution**: Idempotent writes, which give effectively-once behavior. Each write carries a unique sequence number. The receiving node deduplicates by sequence number, ensuring that retries do not create duplicate entries.
>>>>
<<<<
Both replicas end with Y (last write wins), but the intermediate state differs.
====
Both replicas end with Y if each write carries a timestamp or version and the last write wins, but the intermediate state differs. Without a timestamp or version, arrival order decides and the replicas can diverge permanently.
>>>>
<<<<
A single client process can sustain 280,000 consistent hash lookups per second on a single CPU core. The bottleneck is the hash function, not the binary search. With 10 client processes and 10 server nodes, the system handles 2.5 million lookups per second before CPU saturation.
====
At about 430 ns per full lookup, a single client core can sustain roughly 2 million consistent hash lookups per second. The hash function and the binary search cost about the same. In practice the lookup is never the bottleneck: the network round trip to the chosen node is thousands of times slower.
>>>>
<<<<
For clusters with 50-500 nodes, 200 virtual nodes per physical node is sufficient because the law of large numbers already distributes load evenly.
====
For clusters with 50-500 nodes, 100-200 virtual nodes per physical node is usually enough to keep every node within roughly 10 percent of its fair share, and higher values mostly cost memory and rebuild time because the ring size grows as nodes times virtual nodes.
>>>>
<<<<
Binary search takes 19 comparisons instead of 10. Topology insertion (adding one node) takes 19 milliseconds to rebuild the array instead of 2 milliseconds.
====
Binary search takes 19 comparisons instead of about 16 (at 1,000 virtual nodes per node, the ring has 50,000 entries). Topology insertion (adding one node) takes about 19 milliseconds to rebuild the array instead of 2 milliseconds.
>>>>
<<<<
Factor 5 requires 5x storage for replicated data. Write p99 latency increases by roughly 40 percent per additional replica.
====
Factor 5 requires 5x storage for replicated data. Write cost and tail latency grow with each additional replica, because more nodes must be written and the slowest one can delay the acknowledgement.
>>>>
<<<<
A poor hash function (Java's default `hashCode()`, simple CRC32) produces non-uniform distribution. In one production incident, CRC32 caused 40 percent of virtual nodes to cluster within 5 percent of the ring, creating severe hot spots.
====
A poor hash function (for example the raw `hashCode()` of short, similar strings) can produce a non-uniform distribution, leaving virtual nodes clustered on one part of the ring and creating severe hot spots. Test a candidate hash by measuring the resulting load spread before you rely on it.
>>>>
<<<<
A 50ms network spike triggers rebalancing in a cluster with aggressive settings.
====
A multi-second network stall or a long garbage-collection pause triggers rebalancing in a cluster with aggressive settings.
>>>>
