@@ distributed-counters/04-tradeoffs.mdx
<<<<
This adds 50-200ms of latency but guarantees that no increment is acknowledged until at least N/2+1 replicas have stored it.
====
This adds a network round trip (about 1-5ms within a region and 50-200ms across regions) but guarantees that no increment is acknowledged until at least N/2+1 replicas have stored it.
>>>>
<<<<
The new primary accepts the retry, applies the increment, and the counter recovers within 5 seconds of failover.
====
The new primary accepts the retry, applies the increment, and the counter recovers within 5 seconds of failover. This only recovers requests that never got an acknowledgment. Increments that were acknowledged to the client but had not yet reached the replica are not retried, and they stay lost, which is why counters that back billing need a durable event log behind them.
>>>>
<<<<
A 1% network packet loss at 100,000 requests per second creates 1,000 overcounts per second.
====
If 1% of requests at 100,000 requests per second time out and are retried, that creates up to 1,000 overcounts per second.
>>>>
<<<<
This works correctly for fresh increments. However, during reconciliation with a replica that has not received the delete, the old value persists. In CRDT terms, a delete is a reset to zero, which is NOT commutative with increments: a concurrent increment after a delete can be lost. Use `SET key 0` instead of `DEL key`, or use a PN-Counter with an explicit reset mechanism.
====
This works correctly on a single Redis instance. However, a reset is not commutative with increments: in a replicated or CRDT counter, a reset racing with a concurrent increment can lose the increment or resurrect the old value on a replica that has not yet seen the delete. Avoid resets. Roll over to a new key per time bucket (for example one per day), or use an explicit epoch number that increments values are tagged with.
>>>>
