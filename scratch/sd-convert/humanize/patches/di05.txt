@@ distributed-ids/05-real-world.mdx
<<<<
In 2010, Twitter's database was running out of 32-bit integer IDs for tweets. The MySQL auto-increment primary key was approaching the 2^31 - 1 limit. Worse, the centralized sequence was a bottleneck: the database could generate approximately 10,000 IDs per second, but Twitter was already exceeding that during peak events like the 2010 FIFA World Cup, where tweet volume hit 2,940 tweets per second.
====
In 2010, Twitter was moving its tweet storage from a single MySQL database to a distributed store. A distributed store cannot rely on a single auto-increment counter, so Twitter needed a way to generate IDs on many machines at once. The requirements it described were high rates (tens of thousands of IDs per second), rough time ordering, and IDs that fit in 64 bits. Traffic was growing quickly: the 2010 FIFA World Cup produced what was then a record, about 2,940 tweets per second.
>>>>
<<<<
The 64-bit format gave 2^63 possible IDs, enough for 9.2 quintillion tweets. Each worker generated IDs locally with zero database contention.
====
The 64-bit format gave 2^63 possible IDs, enough for 9.2 quintillion tweets. Each worker generated IDs locally, and only the worker IDs needed any coordination.
>>>>
<<<<
- 10-bit worker ID allowed 1,024 workers.
- 12-bit sequence allowed 4,096 IDs per millisecond per worker.
- Custom epoch of 1288834974657 (November 4, 2010, 01:42:54 UTC), the date Twitter announced the project.
====
- 10-bit worker ID allowed 1,024 workers (Twitter split it into 5 bits for a data center and 5 bits for a worker).
- 12-bit sequence allowed 4,096 IDs per millisecond per worker.
- Custom epoch of 1288834974657 (November 4, 2010, 01:42:54 UTC), a date close to when the code was written, chosen so that the 41-bit timestamp would last as long as possible.
>>>>
<<<<
**Publicly known results:** Snowflake generated IDs at over 100,000 per second across Twitter's fleet, eliminating the database bottleneck. The system ran for over 12 years without a major incident. The Snowflake paper and reference implementation (open-sourced as a Scala project) became the foundation for virtually every distributed ID system that followed.
====
**Publicly known results:** Snowflake removed the central ID bottleneck, and its announcement post and open-source Scala implementation (2010) became the template for many later distributed ID systems, including the ones below.
>>>>
<<<<
- Discord assigns worker IDs via a simple internal registry (not ZooKeeper; they were comfortable with the operational overhead).
- The 5-bit internal worker ID and 5-bit process ID give 32 workers with 32 processes each, for 1,024 generating threads per deployment.
====
- The layout is documented in Discord's public API documentation, including how to extract each field.
- The 5-bit internal worker ID and 5-bit process ID give 32 workers with 32 processes each, for 1,024 distinct generators.
>>>>
<<<<
**Why it mattered:** Discord processes millions of messages per second during peak gaming events. The Snowflake scheme guarantees each message has a unique, time-sortable ID without any cross-server communication. The 64-bit IDs fit in their Cassandra primary keys efficiently.
====
**Why it mattered:** A chat platform creates enormous numbers of messages, and the Snowflake scheme gives each one a unique, time-sortable ID without any cross-server communication. Time-sortable 64-bit IDs also fit naturally into the message store, because "the latest messages in a channel" is a range read on the ID.
>>>>
<<<<
- 41-bit timestamp (milliseconds since custom epoch: January 1, 2011)
====
- 41-bit timestamp (milliseconds since a custom epoch in 2011)
>>>>
<<<<
**Hit rate improvement:** The embedded shard ID eliminated one network round-trip per query (the shard lookup), reducing p99 read latency by 30-50% for their workload.
====
**Benefit:** The embedded shard ID removes a lookup step for queries by ID. Instagram also generated the IDs inside PostgreSQL itself (with a stored function), so it did not have to run a separate ID service.
>>>>
<<<<
**Lesson 1: Do not use UUID v4 as a database primary key at production scale.** The B-tree page split problem is real and severe. Instagram (2012), Twitter (2010), and Discord (2015) all rejected UUID v4 for this reason. If you must use UUIDs, use UUID v7 (time-ordered) instead.
====
**Lesson 1: Prefer time-ordered IDs for database primary keys at scale.** Random UUID v4 keys scatter inserts across the index, which causes page splits and fragmentation. The systems above (Snowflake-style IDs, ObjectId, KSUID, ULID) all put a timestamp first for this reason, among others. If you want UUIDs, use UUID v7 (time-ordered).
>>>>
<<<<
**Lesson 2: Plan for clock skew from day one.** Every production incident at companies using Snowflake-style IDs has a clock skew root cause somewhere in the chain. Implement clock skew monitoring in your first deployment, not as a post-incident addition.
====
**Lesson 2: Plan for clock skew from day one.** Clock problems are the classic failure mode of timestamp-based IDs. Implement clock skew monitoring in your first deployment, not as a post-incident addition.
>>>>
