@@ distributed-ids/06-interview.mdx
<<<<
The system would handle 4 million IDs per second across 1,024 workers."
====
Each worker can generate about 4 million IDs per second, so capacity across up to 1,024 workers is far beyond what most systems need."
>>>>
<<<<
I would allocate 2 bits for the region identifier, 7 bits for worker ID (128 workers per region), and 14 bits for sequence (16,384 IDs per ms per worker). The extra sequence bits handle traffic spikes without spin-wait.
====
I would allocate 2 bits for the region identifier, 7 bits for worker ID (128 workers per region), and 13 bits for sequence (8,192 IDs per ms per worker), which together with the sign bit and a 41-bit timestamp adds up to exactly 64 bits. The extra sequence bits handle traffic spikes without spin-wait.
>>>>
<<<<
Shows understanding of CP positioning and graceful degradation.
====
Shows understanding that generation stays available during a partition, and of graceful degradation.
>>>>
<<<<
6. **"UUID v4 causes B-tree page splits on every insert, increasing write amplification by 10x at scale."**
====
6. **"UUID v4 inserts land at random positions in the B-tree, causing frequent page splits and significant write amplification at scale."**
>>>>
<<<<
12. **"Emb edding shard IDs in the primary key eliminates one network round-trip per query."**
====
12. **"Embedding shard IDs in the primary key removes a lookup step when routing a query."**
>>>>
