@@ pacelc/06-interview.mdx
<<<<
the follow-up is "so do you always choose AP or CP?" The correct answer acknowledges that CAP only covers partitions and that PACELC addresses the other 97% of operating time.
====
the follow-up is "so do you always choose AP or CP?" The correct answer acknowledges that CAP only covers partitions and that PACELC addresses the rest of the time, when the network is healthy.
>>>>
<<<<
- "In this system, 95% of operating time the network is healthy. I want a framework that covers that 95%, not just the 5% of failure cases."
====
- "In this system, the network is healthy nearly all of the time. I want a framework that covers that everyday case, not just the rare failure cases."
>>>>
<<<<
- "Spanner implements PC/EC using TrueTime and two-phase commit. Every write is linearizable across any number of regions."
====
- "Spanner implements PC/EC using Paxos replication and two-phase commit, with TrueTime-based commit timestamps. Transactions are externally consistent, even across regions."
>>>>
<<<<
But partitions affect only about 3% of operating time. PACELC extends CAP to cover the other 97%: when the network is healthy
====
But partitions are rare. PACELC extends CAP to cover the rest of the time: when the network is healthy
>>>>
<<<<
I would use PA/EL: DynamoDB with CL=ONE for reads and writes. During normal operation, each read returns the nearest replica's data in 1-3ms.
====
I would use PA/EL: for example Cassandra with `ONE` (or DynamoDB with its default eventually consistent reads). During normal operation, each read returns the nearest replica's data in a few milliseconds.
>>>>
<<<<
I would use PC/EC with CL=QUORUM, because authentication must be strongly consistent.
====
I would use PC/EC (for example `QUORUM` reads and writes in Cassandra), because authentication must be strongly consistent.
>>>>
