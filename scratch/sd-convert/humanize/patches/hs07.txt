@@ horizontal-scaling/07-challenge.mdx
<<<<
The database is PostgreSQL with `max_connections = 400`. Each connection uses 2 MB of RAM.
====
The database is PostgreSQL with `max_connections = 400`. Each connection uses about 10 MB of RAM.
>>>>
<<<<
350 connections * 2 MB = 700 MB RAM for connection overhead
====
350 connections * 10 MB = 3.5 GB RAM for connection overhead
>>>>
<<<<
max_overflow = 2       # Burst capacity, total 100 extra
pool_timeout = 5       # Fast fail: don't queue for 30 seconds
max_lifetime = 1800    # Recycle every 30 minutes to avoid firewalls
====
max_overflow = 0       # No burst above the budget: overflow would push the total past max_connections
pool_timeout = 5       # Fast fail: don't queue for 30 seconds
max_lifetime = 1800    # Recycle every 30 minutes so idle connections dropped by firewalls are replaced
>>>>
<<<<
Cache can hold 80% of the working set (48/60)

**Result:** 20% of working set data is always evicted. Cache hit rate will be 80% at best. Frequently accessed data is evicted and re-queried from the database, adding 10-50ms per miss.
====
Cache can hold 80% of the working set (48/60)

**Result:** 20% of the working set does not fit. If accesses within the working set were uniform, the hit rate would be about 80%. Real access patterns are skewed, so LRU keeps the hottest keys and the hit rate is usually higher. The misses are re-queried from the database, adding 10-50ms each.
>>>>
<<<<
Hit rate drops to 60% (48/80). Database load increases by 2x.
====
Under the same uniform-access assumption, hit rate drops to 60% (48/80). Database load increases by 2x (misses go from 20% to 40%).
>>>>
<<<<
Existing instances are at 80% CPU at 1,000 RPS.
====
Existing instances are at 40% CPU at 1,000 RPS.
>>>>
<<<<
The shard key is the first byte of the base62-decoded short code.
====
The shard key is a hash of the short code, which spreads keys evenly across the 8 shards.
>>>>
<<<<
Cache configuration: Redis cluster, 12 nodes at 16 GB each (144 GB total, about 108 GB usable). Enough for the working set. LRU eviction handles the rest.
====
Cache configuration: Redis cluster, 12 primary nodes at 16 GB each (192 GB total, about 144 GB usable), with replicas on top. That is enough for the 120 GB working set. LRU eviction handles the rest. (Real access is far more skewed than 10/90, so this is conservative.)
>>>>
<<<<
The database sees zero load for that URL because it is cache-resident.
====
The database sees zero load for that URL because it is cache-resident. One risk remains: a single hot key lives on one Redis node, which can serve on the order of 100,000 simple reads per second, enough for this spike. For a hotter key, add a small in-process cache on each API server so most reads never reach Redis.
>>>>
<<<<
- Use warm pools: keep 50 instances at 0% CPU (they exist but handle no traffic) for instant scale-up
====
- Use a warm pool: keep 50 pre-initialized instances (stopped or idle) so they can join in seconds instead of minutes
- Put a virtual waiting room (a queue) in front of checkout so the backend sees a capped, steady request rate instead of the raw spike
>>>>
<<<<
- Reserve instances for the pre-warmed capacity
====
- Cover the always-on baseline with Savings Plans or reservations, not the event capacity
>>>>
<<<<
**Prayer rule of auto-scaling:**
====
**Golden rule of auto-scaling:**
>>>>
<<<<
2. **Two-phase commit within a cell**: Each cell runs its own database (PostgreSQL with synchronous replication across 3 AZs within the same region). ACID transactions work within a cell.
====
2. **ACID within a cell**: Each cell runs its own database (PostgreSQL with synchronous replication across 3 AZs within the same region). A payment that touches only one cell is a single local database transaction, so no distributed commit protocol is needed.
>>>>
<<<<
Each message is idempotent (same message processed twice produces the same result).
====
Each message is idempotent (same message processed twice produces the same result). Use the transactional outbox pattern so the debit and the "credit US account" message are committed atomically in the EU cell's database.
>>>>
<<<<
Each cell has a warm standby in another region within the same geopolitical boundary. US data has a standby in us-west-2 if primary is us-east-1.
====
Each cell has a warm standby in another region that the data-residency rules allow. US data has a standby in us-west-2 if primary is us-east-1. (Some countries have only one cloud region, in which case the standby may have to be in a different provider or an in-country data center.)
>>>>
<<<<
- Storing all data in a single region and serving globally violates GDPR explicitly
====
- Storing all data in a single region and serving globally may break data localization laws, and under GDPR moving EU personal data outside the EU requires a legal transfer mechanism
>>>>
<<<<
- Synchronous cross-region commits would add 200ms+ latency to every cross-border payment
====
- Synchronous cross-region commits would add 100-200ms or more of latency to every cross-border payment
>>>>
<<<<
Uploads stored in S3 Standard (fast access during first week). After 30 days, move to S3 Infrequent Access. After 1 year, move to S3 Glacier. After 3 years, move to S3 Glacier Deep Archive. This reduces storage cost by approximately 80% compared to keeping everything in Standard.
====
Uploads are stored in S3 Standard while they are new and heavily watched. Then use S3 Intelligent-Tiering or a lifecycle rule to Infrequent Access (or Glacier Instant Retrieval) for videos that are still playable but rarely watched. Deep archive tiers such as Glacier Deep Archive take hours to retrieve, so use them only for the original upload files, not for the transcoded versions that users stream. Tiering the cold data can cut storage cost substantially compared with keeping everything in Standard.
>>>>
<<<<
During the Super Bowl, uploads may spike to 5 million in 10 minutes. The queue grows to several million messages.
====
During the Super Bowl, uploads may spike to 10x the normal rate (thousands of uploads per minute). The queue grows to tens or hundreds of thousands of messages.
>>>>
