@@ stateless-vs-stateful/02-deep-dive.mdx
<<<<
approximately 1 GB of heap for data plus 32 MB of hash table overhead.
====
approximately 1 GB of heap for data plus tens of MB of hash table overhead (the exact figure depends on the language and map implementation).
>>>>
<<<<
Snapshotting compacts the log to O(1) for recovery.
====
Snapshotting bounds this: recovery loads the latest snapshot and replays only the entries written since then.
>>>>
<<<<
A naive hash mod N approach would reshuffle N-1 out of N partitions when a node is added or removed. Consistent hashing moves only K/N partitions, where K is the number of virtual nodes.
====
A naive hash mod N approach would reassign about (N-1)/N of all keys when a node is added or removed. Consistent hashing moves only about 1/N of the keys, the share the new node takes over.
>>>>
<<<<
A node can act autonomously as long as its lease is valid. The trade-off is the TTL:
====
A node can act autonomously as long as its lease is valid. Leases depend on reasonably synchronized clocks, and a node that pauses (for example in a long garbage-collection pause) can wake up believing its lease is still valid after it has expired, so critical writes should also carry a fencing token that the storage layer checks. The trade-off is the TTL:
>>>>
<<<<
6. S7 applies the operation to the in-memory document state. The version increments to 105.
====
6. S7 applies the operation to the in-memory document state. The version increments to 105. (Real editors also transform concurrent operations with operational transformation or a CRDT. This walkthrough shows a single writer for clarity.)
>>>>
<<<<
11. If S7 crashes, a new server loads the latest snapshot from PostgreSQL and replays the WAL.
====
11. If S7 crashes, a new server loads the latest snapshot from PostgreSQL and replays the WAL. (For this to work, the WAL must live on replicated or shared storage, not only on S7's local disk.)
>>>>
<<<<
Too long: memory leaks from stale connections.
====
Too long: idle connections pile up and waste memory and file descriptors.
>>>>
<<<<
Too high (5): write latency increases with each additional replica.
====
Too high (5): more storage and network cost per write, and a higher chance that a slow replica delays the quorum.
>>>>
