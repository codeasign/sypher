@@ raft/03-architecture.mdx
<<<<
The key space is divided into contiguous ranges (default 64 MB each).
====
The key space is divided into contiguous ranges (512 MiB by default in recent versions, and smaller in older ones).
>>>>
<<<<
The store uses a **shared-nothing** approach for CPU: each Raft group runs on its own goroutine or thread, but the storage layer serializes writes to batch fsyncs across groups. This reduces the per-group fsync cost significantly.
====
Each Raft group is driven independently, but the storage layer batches writes from many groups into shared fsyncs. This reduces the per-group fsync cost significantly.
>>>>
<<<<
**TiDB:** A distributed SQL database that uses Raft for consensus within each region (a shard of data). TiDB's Raft implementation includes a significant optimization: the Raft log is the same as the database's write-ahead log, eliminating redundant writes. This reduces write amplification from 2x to nearly 1x.
====
**TiDB:** A distributed SQL database whose storage layer, TiKV, uses Raft for consensus within each region (a shard of data) and Multi-Raft to run many regions per node. TiKV keeps Raft logs in a dedicated log engine, separate from the engine that stores the data, so that log appends stay cheap.
>>>>
<<<<
The Raft log is stored in a separate RocksDB instance per node. CockroachDB implements
====
The Raft log is stored in the node's storage engine alongside the data. CockroachDB implements
>>>>
<<<<
For example, Redis Sentinel uses a Raft-like protocol for leader election, and some Redis Cluster deployments embed Raft for configuration consensus.
====
For example, Redis Sentinel uses a Raft-inspired election among sentinels to choose which one performs a failover, and RedisRaft is a module that explores running Redis on Raft.
>>>>
<<<<
- Each node performs approximately 6,700 fsyncs/sec (leader: 10,000, each follower contributes to majority roughly 3,350)
====
- Every node must persist every entry, but implementations batch entries into group commits. With batches of about 50 entries, each node needs only around 200 fsyncs/sec
>>>>
<<<<
With 3 nodes and 10,000 writes/sec: 20,000 outbound RPCs/sec from the leader, each containing the batch of entries plus metadata.
====
With 3 nodes and 10,000 writes/sec: if entries were sent one per RPC, that is 20,000 outbound RPCs/sec from the leader. Batching reduces the RPC count by the batch factor while the byte volume stays about the same.
>>>>
<<<<
2. Force that node to become leader (skip election safety checks)
====
2. Restart that node as a new single-member cluster (in etcd, with `--force-new-cluster`), which bypasses the normal quorum requirement
>>>>
