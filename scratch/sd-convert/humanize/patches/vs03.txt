@@ vertical-scaling/03-architecture.mdx
<<<<
The database server is a large instance: 64 GB RAM, 16 vCPU. But the 64 GB RAM is only 60% of the working set.
====
The database server is a mid-sized instance: 64 GB RAM, 8 vCPU. But that 64 GB of RAM holds only 60% of the working set.
>>>>
<<<<
Cost: $4,000/month for the instance, $3,000/month for the storage. The working set is 480 GB, 94% of available RAM. Any growth pushes it over the edge. There is no larger memory-optimized instance in the current generation. You must either migrate to the `u-*` high-memory family (up to 24 TB RAM, but at $50,000+/month) or design a horizontal scaling architecture.
====
Cost: roughly $2,500/month for the instance, plus around $10,000/month for that much provisioned IOPS. The working set is 480 GB, 94% of available RAM. Any growth pushes it over the edge. This is the largest size in the `r7g` family, so the next step is a different family: `r7i` or `x2` instances (up to a few TB of RAM) or the `u-*` high-memory family (up to 24 TB RAM, but at tens of thousands of dollars per month). Or you design a horizontal scaling architecture.
>>>>
<<<<
  At eviction > 500 keys/sec: too little RAM: vertical scaling needed
====
  At eviction > 500 keys/sec: too little RAM, so the instance needs to scale up
>>>>
<<<<
  RabbitMQ:    ~50,000 messages/sec (single queue, persistent)
  Redis       ~100,000 messages/sec (Streams, non-persistent)
  Streams:
  Kafka:      ~200,000 messages/sec (single broker, 3 partitions)
====
  RabbitMQ:       ~50,000 messages/sec (single queue, persistent)
  Redis Streams:  ~100,000 messages/sec (single instance, small messages)
  Kafka:          ~200,000 messages/sec (single broker, 3 partitions; often far higher with batching)
>>>>
<<<<
  - Network throughput: 10 Gbps ENA max per instance
  - Disk I/O: 16,000 IOPS gp3 max per volume
====
  - Network throughput: fixed per instance size (10 Gbps on mid-sized instances, more on larger ones)
  - Disk I/O: per-volume IOPS ceiling (gp3 was capped at 16,000 IOPS until 2025)
>>>>
<<<<
    Fix: ulimit -n 65535 in systemd unit file
====
    Fix: LimitNOFILE=65535 in the systemd unit file
>>>>
<<<<
  Instance: r7g.4xlarge (16 vCPU, 128 GB RAM)
  Working set: 80 GB (62% of RAM)
  Cache hit ratio: 97%
  Query throughput: 20,000 QPS
  CPU at peak: 60%
  Storage: 2 TB NVMe (55% used)
====
  Instance: r7g.4xlarge (16 vCPU, 128 GB RAM)
  Working set: 80 GB (62% of RAM)
  Cache hit ratio: 97%
  Query throughput: 20,000 QPS
  CPU at peak: 60%
  Storage: 2 TB SSD volume (55% used)
>>>>
<<<<
   Next instance: r7g.8xlarge (256 GB RAM, $1,612/month)
====
   Next instance: r7g.8xlarge (256 GB RAM, ~$1,250/month)
>>>>
<<<<
   Time to 80%: 20,000 * 1.08^t = 26,667 => t = 3.8 months
   Time to 100%: 20,000 * 1.08^t = 33,333 => t = 6.5 months
====
   Time to 80%: 20,000 * 1.08^t = 26,667 => t = 3.7 months
   Time to 100%: 20,000 * 1.08^t = 33,333 => t = 6.6 months
>>>>
<<<<
  Next instance: c7g.8xlarge (32 vCPU, ~40,000 QPS at 60%)
   Time to 80%: 40,000 * 1.08^t = 53,333 => t = 4.0 months
====
  Next instance: r7g.8xlarge (32 vCPU, so ~40,000 QPS at 60% CPU; it fixes memory too)
   Time to 80% from the new baseline: 40,000 * 1.08^t = 53,333 => t = 3.7 months
>>>>
<<<<
   CPU runs out in 6.5 months
====
   CPU runs out in 6.6 months
>>>>
<<<<
   Current: r7g.4xlarge at $589/month
   Proposed: r7g.8xlarge at $1,177/month
   Cost increase: $588/month ($7,056/year)
====
   Current: r7g.4xlarge at ~$625/month
   Proposed: r7g.8xlarge at ~$1,250/month
   Cost increase: ~$625/month (~$7,500/year)
>>>>
<<<<
   Alternative: Add Redis cache (r7g.xlarge at $147/month)
====
   Alternative: Add Redis cache (r7g.xlarge at ~$155/month)
>>>>
<<<<
   Combined cost: $589 + $147 = $736/month
   Extends memory timeline from 2.7 to ~8 months at lower cost.
====
   Combined cost: $625 + $155 = $780/month
   If the cache absorbs most reads of the hottest data, it can push the memory timeline out
   toward ~8 months at lower cost (measure the hit rate before you rely on this).
>>>>
<<<<
  Current working set:       [GB] (pg_buffercache hit ratio < 95% means exceeded)
====
  Current working set:       [GB] (a buffer cache hit ratio under 95% means RAM is exceeded)
>>>>
<<<<
    2. Configure PostgreSQL from template
    3. Configure WAL streaming from old instance
    4. Start PostgreSQL in recovery mode (replica)
====
    2. Configure PostgreSQL from template (same major version and CPU architecture
       as the old instance, since physical replication requires both)
    3. Take a base backup from the old instance (pg_basebackup)
    4. Start PostgreSQL in recovery mode as a streaming replica
>>>>
<<<<
    3. Run ANALYZE on new instance (warm caches)
====
    3. Warm the caches on the new instance (pg_prewarm on the hot tables and indexes)
>>>>
<<<<
     - Check shared_buffers: cat /proc/sys/kernel/shmmax
====
     - Check shared_buffers: SHOW shared_buffers; (in psql)
>>>>
<<<<
     - Run: numastat -p postgres
     - If "other_node" > 20% of total, NUMA pin the process
====
     - Run: numastat (system-wide) and numastat -p <postgres pid>
     - If other_node is more than ~20% of numa_hit, pin or interleave the process
>>>>
<<<<
Disk (increase size)      Modify volume + grow2fs   0 sec         As needed
Database tuning           Reload config (pg_reload) 0 sec         After upgrades
====
Disk (increase size)      Modify volume + growpart  0 sec         As needed
                          and resize2fs/xfs_growfs
Database tuning           pg_reload_conf()          0 sec         After upgrades
>>>>
<<<<
PostgreSQL major version  pg_upgrade + blue-green   30-60 sec     Annually
====
PostgreSQL major version  logical replication +     30-60 sec     Annually
                          blue-green (or pg_upgrade, minutes)
>>>>
