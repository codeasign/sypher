@@ distributed-ids/01-concepts.mdx
<<<<
The classic horror story comes from a social media platform whose user IDs reached the 32-bit integer limit, forcing an emergency migration to 64-bit IDs while the site was live.
====
A classic horror story, told in many variations, is a fast-growing platform whose IDs reach the 32-bit integer limit, forcing an emergency migration to 64-bit IDs while the site is live.
>>>>
<<<<
64-bit IDs fit in a standard integer column, are time-sortable, and support over 4,000 IDs per second per worker.
====
64-bit IDs fit in a standard integer column, are time-sortable, and support 4,096 IDs per millisecond (over 4 million per second) per worker.
>>>>
<<<<
the sequence counter (typically 12 bits, max 4096) is incremented. If the counter overflows (exceeds 4095), the generator spins-waits until the next millisecond.
====
the sequence counter (typically 12 bits, so 4,096 values from 0 to 4095) is incremented. If the counter overflows (exceeds 4095), the generator spins-waits until the next millisecond.
>>>>
<<<<
systems that want to avoid clock synchronization entirely, and applications
====
systems that want to avoid worker ID assignment and coordination, and applications
>>>>
<<<<
improving write throughput by up to 10x on databases with clustered indexes.
====
often improving write throughput several-fold on databases with clustered indexes.
>>>>
