@@ stateless-vs-stateful/03-architecture.mdx
<<<<
Retries are safe because the external API provides idempotency keys.
====
Retries are safe when the external API supports idempotency keys (Stripe does) and the service sends one with every call.
>>>>
<<<<
- Peak hour handles 20% of daily traffic: 2 million requests per hour = 555 requests per second
- Peak minute within peak hour handles 2x the average: 1,110 requests per second
====
- Total: 100 million requests per day (about 1,157 requests per second on average)
- Peak hour handles 20% of daily traffic: 20 million requests per hour = about 5,555 requests per second
- Peak minute within peak hour handles 2x the average: about 11,110 requests per second
>>>>
<<<<
- Instances needed: 1,110 / 2,000 = 0.55, round up to 2 for redundancy
- With N+2 redundancy (handle one failure, one maintenance): 4 instances
====
- Instances needed: 11,110 / 2,000 = 5.6, round up to 6
- With N+2 redundancy (handle one failure, one maintenance): 8 instances
>>>>
<<<<
- Shard count: 6 shards of 8 GB each (allowing headroom)
====
- Shard count: 4 primary shards of 8 GB each (20 GB of data fits with headroom), each with one replica, for 8 nodes in total
>>>>
<<<<
- Write throughput: 555 writes/second average, 1,110 peak
- A single PostgreSQL primary handles 10,000 writes/second comfortably
- Read throughput: 555 reads/second average (assuming 50/50 read/write)
- Three read replicas each handle 1,000 reads/second
====
- Assume a 50/50 read/write split: about 2,780 writes/second and 2,780 reads/second at peak hour, twice that in the peak minute
- A single PostgreSQL primary handles 10,000 writes/second comfortably
- Peak-minute reads of about 5,555/second are served by replicas and a cache: with an 80% cache hit rate, about 1,100 reads/second reach the database, which two read replicas (about 1,000 reads/second each) handle
>>>>
<<<<
- Each write: 100 bytes (player ID, x, y, z, timestamp)
====
- Each write: 100 bytes (player ID, x, y, z, timestamp)
- (Real game servers usually keep positions only in memory and persist just important events. This is a deliberately worst-case sizing exercise.)
>>>>
