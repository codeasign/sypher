---
id: chatgpt-clone-deploy
title: "ChatGPT Clone — Deploy"
sidebar_label: Deploy
sidebar_position: 3
---

## Production Hardening

### 1. Token Usage Tracking

```python
# app/tokens.py
import tiktoken

def count_tokens(text: str, model: str = "cl100k_base") -> int:
    enc = tiktoken.get_encoding(model)
    return len(enc.encode(text))

def estimate_cost(model: str, input_tokens: int, output_tokens: int) -> float:
    rates = {
        "claude-haiku-3-5": (0.00025, 0.00125),
        "claude-sonnet-4": (0.003, 0.015),
        "gpt-4o-mini": (0.00015, 0.0006),
    }
    input_rate, output_rate = rates.get(model, (0.001, 0.002))
    return (input_tokens * input_rate + output_tokens * output_rate) / 1000
```

## Deploy It

### Docker Compose

```yaml
# docker-compose.yml
version: "3.8"
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DB_PATH=/data/chatclone.db
    volumes:
      - ./data:/data

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
```

### Fly.io Deployment

```bash
fly launch
fly secrets set ANTHROPIC_API_KEY=sk-ant-...
fly secrets set OPENAI_API_KEY=sk-...
fly deploy
```

## Cost to Build This

| Item | Cost |
|---|---|
| Setup (one-time) | ~$0.00 |
| Per chat message | ~$0.001–$0.01 |
| Free tier sufficient | Yes (Ollama is free) |
| Recommended spend cap | $5 |