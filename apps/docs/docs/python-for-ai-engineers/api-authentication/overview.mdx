---
title: API Authentication
sidebar_label: Overview
---

# API Authentication

⏱ 12 minutes · Difficulty: Intermediate

## What You Will Learn

By the end of this lesson you will be able to:

- Authenticate with API keys and bearer tokens
- Implement OAuth2 client credentials and authorization code flows
- Handle token expiration and refresh cycles automatically
- Store and load credentials securely using environment variables
- Build an authenticated API client with automatic retry on 401 errors

## Prerequisites

You should already understand:

- [Requests](../requests/overview) — authentication headers are sent with every HTTP request
- [HTTPX](../httpx/overview) — async authentication follows the same patterns with AsyncClient
- [Environment Variables](../virtual-environments/overview) — credentials should be stored in environment variables, not code

## Why This Matters

Every AI API requires authentication. OpenAI, Anthropic, Groq, Hugging Face, and every other provider uses some form of API key or token to identify you, enforce rate limits, and track usage. Getting authentication wrong means 401 errors, security vulnerabilities, or both.

In AI engineering, you will work with multiple authentication patterns: simple API keys for model inference, OAuth2 for service-to-service communication, and JWT tokens for user-facing applications. Each has its own security considerations and implementation patterns.

## The Problem

You need to call an API that requires authentication. The simplest approach — paste an API key into your code — works in development but creates a security risk in production (keys leaked in version control, shared accidentally, rotated infrequently). You need a systematic approach to storing credentials, attaching them to requests, handling token expiry, and refreshing tokens transparently.

## The Concept

Authentication is proving who you are to the server. The three most common patterns for AI APIs:

**API Key** — a static string sent as a header or query parameter. Simple, but the key is long-lived and must be kept secret.

**Bearer Token** — a token sent in the `Authorization` header. Often short-lived, obtained by exchanging credentials (like an API key) for a temporary token.

**OAuth2** — a standard protocol where you obtain a token from an authorization server. The client credentials flow is common for server-to-server communication.

<AsciiDiagram id="python-for-ai-engineers/api-authentication/overview" alt="Three common API authentication patterns" caption="Figure 34: API Key, Bearer Token, and OAuth2 Client Credentials flows"
  mermaidSrc="/img/diagrams/1d8241307fcd.svg" content={`
    API Key              Bearer Token           OAuth2 Client Credentials
    ┌─────────┐          ┌─────────┐            ┌──────┐    ┌──────────┐
    │ Request │──key──→  │ 1. Get  │──auth──→   │Client│──→ │   Auth   │
    │ + key   │          │  token  │            │      │←── │  Server  │
    │         │          │         │            │      │    │ (token)  │
    │ Static  │          │ 2. Use  │──token──→  │      │──token──→ API │
    │ key per │          │  token  │            └──────┘    └──────────┘
    │ project │          └─────────┘
`} />

## What's Next

This lesson continues in [Build It](./build-it), where you will implement API key authentication, bearer token flow, automatic token refresh, and a secure credential manager.