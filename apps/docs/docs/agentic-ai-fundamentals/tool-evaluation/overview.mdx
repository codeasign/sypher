---
title: Tool Evaluation
sidebar_label: Overview
---

# Tool Evaluation

⏱️ 15 minutes · Difficulty: Advanced

## What You Will Learn

By the end of this lesson you will be able to:

- Measure how often and how correctly your agent invokes each tool
- Evaluate tool description quality and its impact on agent behavior
- Detect tools that are never called, called too often, or called with wrong parameters
- Iterate on tool design based on empirical usage data

## Prerequisites

You should already understand:

- How tools are defined and registered in MCP (see [Agent Tools and Tool Calling](../../agent-tools-and-tool-calling/overview))
- How tool descriptions influence the agent's choice (see [MCP Primitives](../../mcp-primitives/overview))
- The trajectory evaluation framework (see [Trajectory Evaluation](../trajectory-evaluation/overview))
- Basic logging and aggregation techniques

## Why This Matters

Your agent is only as good as its tools. A tool with a vague description will be misused. A tool with a confusing parameter schema will be called with wrong values. A tool that overlaps with another tool will cause confusion and wasted calls. These are not agent problems — they are tool design problems.

In production agent systems, tool quality is the single highest-leverage improvement you can make. Improving one tool description can reduce error rates by 30-50% for any task involving that tool. Adding a missing parameter validation catches failures before they reach downstream systems. Removing a redundant tool eliminates an entire class of agent mistakes.

Tool evaluation gives you the data to make these improvements systematically. Instead of guessing which tools are confusing the agent, you measure invocation rates, error rates, parameter accuracy, and description relevance. The results tell you exactly which tools need better descriptions, which need schema changes, and which should be removed.

## The Problem

You defined a tool called `search_database` with the description "Search the database for records." The agent calls it for everything — searching users, searching orders, searching products, even searching for files. The tool works, but the agent frequently constructs wrong queries because the description does not specify what kind of search it performs.

Meanwhile, a tool called `get_user_profile` exists, but the agent has never called it. The agent does not know `get_user_profile` exists because its description is buried — or the agent finds `search_database` ambiguous enough that it tries to use it for user lookups too.

Without tool evaluation, you never know which tools are working, which are underused, and which are causing errors. You make guesses about tool quality based on overall agent performance, but you cannot separate tool problems from prompt problems or model problems.

## The Concept

Tool evaluation measures the interaction between the agent and each individual tool. For every tool, you track:

- **Invocation rate** — how often is this tool called, relative to other tools?
- **Parameter accuracy** — do the arguments the agent passes match the tool's schema and expectations?
- **Error rate** — how often does this tool return an error or empty result?
- **Description relevance** — when the agent calls a different tool for a task this tool handles, is the description unclear?
- **Tool confusion** — how often does the agent pick a different tool when this one was the correct choice?

These metrics help you diagnose specific tool problems. A tool with a high error rate may have a confusing schema. A tool with a low invocation rate relative to its utility may have a poor description. A tool that is called with consistently wrong parameter values may need better parameter names or descriptions.

## The Big Picture

<AsciiDiagram id="agentic-ai-fundamentals/tool-evaluation/overview" alt="Tool evaluation process showing invocation tracking, parameter validation, and feedback loop" caption="Figure 1: Tool evaluation feedback loop"
  mermaidSrc="/img/diagrams/41d62d187145.svg" content={`
┌─────────────────────────────────────────────────────┐
│              Tool Evaluation Loop                    │
├──────────────┬──────────────┬────────────┬──────────┤
│  Invocation  │  Parameter   │   Error    │  Tool    │
│  Tracking    │  Validation  │  Analysis  │  Design  │
│              │              │            │  Iterate │
│  ┌────────┐  │  ┌────────┐  │  ┌───────┐ │ ┌──────┐ │
│  │ Count  │  │  │ Type   │  │  │ Rate  │ │ │ Fix  │ │
│  │ calls  │  │  │ match  │  │  │ per   │ │ │ desc │ │
│  ├────────┤  │  ├────────┤  │  │ tool  │ │ ├──────┤ │
│  │ Per-   │  │  │ Value  │  │  ├───────┤ │ │ Fix  │ │
│  │ task   │  │  │ range  │  │  │ Error │ │ │ schema││
│  ├────────┤  │  ├────────┤  │  │ types │ │ ├──────┤ │
│  │ Tool   │  │  │ Redun- │  │  ├───────┤ │ │Merge │ │
│  │ distri-│  │  │ dant   │  │  │ Empty │ │ │tools │ │
│  │ bution │  │  │ params │  │  │ result│ │ └──────┘ │
│  └────────┘  │  └────────┘  │  └───────┘ │          │
└──────────────┴──────────────┴────────────┴──────────┘
`} />

## What's Next

This lesson continues in [Build It](./build-it), where you will implement tool usage tracking, build a tool evaluation dashboard, and iterate on tool descriptions based on real invocation data.