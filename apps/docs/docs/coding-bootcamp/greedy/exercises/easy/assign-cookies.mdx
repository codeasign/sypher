---
title: Assign Cookies
sidebar_label: Assign Cookies
hide_table_of_contents: true
---
import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/greedy/easy/assign-cookies', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '3\n1 2 3\n2\n1 1\n', expectedOutput: '1\n', isSample: true },
    { stdin: '2\n1 2\n3\n1 2 3\n', expectedOutput: '2\n', isSample: true },
    { stdin: '4\n10 9 8 7\n4\n5 6 7 8\n', expectedOutput: '2\n' },
    { stdin: '4\n1 2 3 4\n4\n1 1 1 1\n', expectedOutput: '1\n' },
    { stdin: '3\n5 5 5\n1\n5\n', expectedOutput: '1\n' },
    { stdin: '0\n\n3\n1 2 3\n', expectedOutput: '0\n' },
    { stdin: '3\n1 2 3\n0\n\n', expectedOutput: '0\n' },
    { stdin: '3\n3 2 1\n3\n1 2 3\n', expectedOutput: '3\n' },
    { stdin: '4\n7 8 9 10\n4\n5 6 7 8\n', expectedOutput: '2\n' },
    { stdin: '3\n1 1 1\n3\n2 2 2\n', expectedOutput: '3\n' },
    { stdin: '3\n2 3 4\n2\n1 5\n', expectedOutput: '1\n' },
    { stdin: '5\n1 2 3 4 5\n3\n2 3 4\n', expectedOutput: '3\n' },
    { stdin: '4\n1 1 1 1\n2\n1 2\n', expectedOutput: '2\n' },
    { stdin: '6\n3 2 1 1 2 3\n4\n1 1 2 2\n', expectedOutput: '4\n' },
    { stdin: '1\n5\n5\n1 2 3 4 5\n', expectedOutput: '1\n' },
    { stdin: '3\n6 7 8\n5\n1 2 3 4 5\n', expectedOutput: '0\n' },
    { stdin: '5\n1 2 3 4 5\n5\n5 4 3 2 1\n', expectedOutput: '5\n' },
    { stdin: '4\n2 2 2 2\n4\n1 1 3 3\n', expectedOutput: '2\n' },
    { stdin: '5\n4 4 4 4 4\n1\n4\n', expectedOutput: '1\n' },
    { stdin: '6\n1 100 2 99 3 98\n6\n98 99 100 1 2 3\n', expectedOutput: '6\n' },
  ]}
  starterCode={{
    python: `def find_content_children(g, s):
    # Sort both arrays
    # Use two pointers to match children to cookies
    pass`,
    java: `class Solution {
    public int findContentChildren(int[] g, int[] s) {
        // Sort both arrays
        // Use two pointers to match children to cookies
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int findContentChildren(vector<int>& g, vector<int>& s) {
        // Sort both arrays
        // Use two pointers to match children to cookies
        return 0;
    }
};`,
    javascript: `function findContentChildren(g, s) {
    // Sort both arrays
    // Use two pointers to match children to cookies
    return 0;
}`,
    typescript: `function findContentChildren(g: number[], s: number[]): number {
    // Sort both arrays
    // Use two pointers to match children to cookies
    return 0;
}`,
    rust: `pub fn find_content_children(g: Vec<i32>, s: Vec<i32>) -> i32 {
    // Sort both arrays
    // Use two pointers to match children to cookies
    0
}`,
    c: `int findContentChildren(int* g, int gSize, int* s, int sSize) {
    // Sort both arrays
    // Use two pointers to match children to cookies
    return 0;
}`,
    csharp: `public class Solution {
    public int FindContentChildren(int[] g, int[] s) {
        // Sort both arrays
        // Use two pointers to match children to cookies
        return 0;
    }
}`,
    go: `func findContentChildren(g []int, s []int) int {
    // Sort both arrays
    // Use two pointers to match children to cookies
    return 0
}`,
  }}
  harness={{
    python: `import sys
data = sys.stdin.read().split('\\n')
idx = 0
g_size = int(data[idx].strip()); idx += 1
g = list(map(int, data[idx].split())) if g_size > 0 else []
idx += 1
s_size = int(data[idx].strip()); idx += 1
s = list(map(int, data[idx].split())) if s_size > 0 else []
idx += 1
result = find_content_children(g, s)
print("###SYPHER_JUDGE0_RESULT###")
print(result)`,
    java: `import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int gSize = Integer.parseInt(sc.nextLine().trim());
        String gLine = sc.nextLine().trim();
        int[] g = new int[gSize];
        if (gSize > 0) {
            String[] parts = gLine.split("\\\\s+");
            for (int i = 0; i < gSize; i++) g[i] = Integer.parseInt(parts[i]);
        }
        int sSize = Integer.parseInt(sc.nextLine().trim());
        String sLine = sc.nextLine().trim();
        int[] s = new int[sSize];
        if (sSize > 0) {
            String[] parts = sLine.split("\\\\s+");
            for (int i = 0; i < sSize; i++) s[i] = Integer.parseInt(parts[i]);
        }
        Solution sol = new Solution();
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.println(sol.findContentChildren(g, s));
    }
}`,
    cpp: `#include <iostream>
#include <vector>
#include <sstream>
#include <string>
int main() {
    std::string line;
    std::getline(std::cin, line);
    std::getline(std::cin, line);
    std::vector<int> g;
    { std::stringstream ss(line); int v; while (ss >> v) g.push_back(v); }
    std::getline(std::cin, line);
    std::getline(std::cin, line);
    std::vector<int> s;
    { std::stringstream ss(line); int v; while (ss >> v) s.push_back(v); }
    Solution sol;
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    std::cout << sol.findContentChildren(g, s) << std::endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l));
rl.on('close', () => {
    let idx = 0;
    const gSize = parseInt(lines[idx].trim()); idx++;
    const g = gSize > 0 ? lines[idx].trim().split(/\\s+/).map(Number) : [];
    idx++;
    const sSize = parseInt(lines[idx].trim()); idx++;
    const s = sSize > 0 ? lines[idx].trim().split(/\\s+/).map(Number) : [];
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(findContentChildren(g, s));
});`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    let idx = 0;
    const gSize = parseInt(lines[idx].trim()); idx++;
    const g: number[] = gSize > 0 ? lines[idx].trim().split(/\\s+/).map(Number) : [];
    idx++;
    const sSize = parseInt(lines[idx].trim()); idx++;
    const s: number[] = sSize > 0 ? lines[idx].trim().split(/\\s+/).map(Number) : [];
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(findContentChildren(g, s));
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines().map(|l| l.unwrap());
    let g_size: usize = lines.next().unwrap().trim().parse().unwrap();
    let g_line = lines.next().unwrap();
    let g: Vec<i32> = if g_size > 0 {
        g_line.trim().split_whitespace().map(|x| x.parse().unwrap()).collect()
    } else {
        Vec::new()
    };
    let s_size: usize = lines.next().unwrap().trim().parse().unwrap();
    let s_line = lines.next().unwrap();
    let s: Vec<i32> = if s_size > 0 {
        s_line.trim().split_whitespace().map(|x| x.parse().unwrap()).collect()
    } else {
        Vec::new()
    };
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", find_content_children(g, s));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    char line[100000];
    int g[40000], s[40000];
    fgets(line, sizeof(line), stdin);
    int gSize = atoi(line);
    fgets(line, sizeof(line), stdin);
    int gCount = 0;
    char *token = strtok(line, " \\t\\n");
    while (token) { g[gCount++] = atoi(token); token = strtok(NULL, " \\t\\n"); }
    fgets(line, sizeof(line), stdin);
    int sSize = atoi(line);
    fgets(line, sizeof(line), stdin);
    int sCount = 0;
    token = strtok(line, " \\t\\n");
    while (token) { s[sCount++] = atoi(token); token = strtok(NULL, " \\t\\n"); }
    (void)gSize;
    (void)sSize;
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    printf("%d\\n", findContentChildren(g, gCount, s, sCount));
    return 0;
}`,
    csharp: `using System;
using System.Linq;
class Program {
    static void Main() {
        int gSize = int.Parse(Console.ReadLine().Trim());
        string gLine = Console.ReadLine();
        int[] g = gSize > 0 ? gLine.Trim().Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries).Select(int.Parse).ToArray() : new int[0];
        int sSize = int.Parse(Console.ReadLine().Trim());
        string sLine = Console.ReadLine();
        int[] s = sSize > 0 ? sLine.Trim().Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries).Select(int.Parse).ToArray() : new int[0];
        var sol = new Solution();
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(sol.FindContentChildren(g, s));
    }
}`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "sort"
    "strconv"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    gSizeLine, _ := reader.ReadString('\\n')
    gSize, _ := strconv.Atoi(strings.TrimSpace(gSizeLine))
    gLine, _ := reader.ReadString('\\n')
    g := []int{}
    if gSize > 0 {
        fields := strings.Fields(gLine)
        g = make([]int, len(fields))
        for i, f := range fields {
            g[i], _ = strconv.Atoi(f)
        }
    }
    sSizeLine, _ := reader.ReadString('\\n')
    sSize, _ := strconv.Atoi(strings.TrimSpace(sSizeLine))
    sLine, _ := reader.ReadString('\\n')
    s := []int{}
    if sSize > 0 {
        fields := strings.Fields(sLine)
        s = make([]int, len(fields))
        for i, f := range fields {
            s[i], _ = strconv.Atoi(f)
        }
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(findContentChildren(g, s))
}`,
  }}
  defaultLanguage="python"
>

# Assign Cookies

Assume you are an awesome parent and want to give your children some cookies. But you should give each child at most one cookie.

Each child `i` has a greed factor `g[i]`, which is the minimum size of a cookie that the child will be content with. Each cookie `j` has a size `s[j]`. If `s[j] >= g[i]`, we can assign cookie `j` to child `i`, and the child will be content.

Your goal is to maximize the number of content children and output the maximum number.

## Examples

**Example 1:**
```
Input: g = [1,2,3], s = [1,1]
Output: 1
Explanation: You have 3 children and 2 cookies. The greed factors are 1, 2, 3.
Even though you have two cookies, both are size 1. You can only make the child
with greed factor 1 content. You need to give each child at most one cookie.
```

**Example 2:**
```
Input: g = [1,2], s = [1,2,3]
Output: 2
Explanation: You have 2 children and 3 cookies. The greed factors are 1 and 2.
You can make both children content by giving the cookie of size 1 to the child
with greed factor 1 and the cookie of size 2 to the child with greed factor 2.
```

**Example 3:**
```
Input: g = [10,9,8,7], s = [5,6,7,8]
Output: 2
Explanation: You can make the children with greed factors 7 and 8 content.
```

## Constraints

- `1 <= g.length <= 3 * 10^4`
- `0 <= s.length <= 3 * 10^4`
- `1 <= g[i], s[j] <= 2^31 - 1`

## Hints

<details>
<summary>Hint 1</summary>
What if you sort the children by greed factor and the cookies by size?
</details>

<details>
<summary>Hint 2</summary>
Try to assign the smallest cookie that satisfies each child. This is a greedy matching strategy.
</details>

</ProblemEditor>