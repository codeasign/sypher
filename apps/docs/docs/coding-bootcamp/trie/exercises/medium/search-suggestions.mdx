---
title: Search Suggestions System
sidebar_label: Search Suggestions System
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/trie/search-suggestions', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: 'mobile mouse moneypot monitor mousepad\nmouse\n', expectedOutput: 'mobile moneypot monitor\nmobile moneypot monitor\nmouse mousepad\nmouse mousepad\nmouse mousepad\n' },
    { stdin: 'havana\nhavana\n', expectedOutput: 'havana\nhavana\nhavana\nhavana\nhavana\nhavana\n' },
    { stdin: 'bags baggage banana bag\nbag\n', expectedOutput: 'bag baggage bags\nbag baggage bags\nbag baggage bags\n' },
    { stdin: 'cat car card care\ncar\n', expectedOutput: 'car card care\ncar card care\ncar card care\n' },
    { stdin: 'apple app application apply\napp\n', expectedOutput: 'app apple application\napp apple application\napp apple application\n' },
    { stdin: 'abc abcd abcde abd\nabcd\n', expectedOutput: 'abc abcd abcde\nabc abcd abcde\nabc abcd abcde\nabcd abcde\n' },
    { stdin: 'code coder codes coding\ncoding\n', expectedOutput: 'code coder codes\ncode coder codes\ncode coder codes\ncoding\ncoding\ncoding\n' },
    { stdin: 'data database day\ndat\n', expectedOutput: 'data database day\ndata database day\ndata database\n' },
    { stdin: 'zip zone zoom zoo\nzoo\n', expectedOutput: 'zip zone zoom\nzone zoo zoom\nzoo zoom\n' },
    { stdin: 'a ab abc abcd abcde\nabc\n', expectedOutput: 'a ab abc\nab abc abcd\nabc abcd abcde\n' },
    { stdin: 'sun sunshine sunny\nsunny\n', expectedOutput: 'sun sunshine sunny\nsun sunshine sunny\nsun sunshine sunny\nsunny\nsunny\n' },
    { stdin: 'pen pencil penguin pennant\npen\n', expectedOutput: 'pen pencil penguin\npen pencil penguin\npen pencil penguin\n' },
    { stdin: 'red reed reel reef\nreel\n', expectedOutput: 'red reed reel\nred reed reel\nreed reel reef\nreel\n' },
    { stdin: 'fan fancy fantastic fanatic fast\nfan\n', expectedOutput: 'fan fanatic fancy\nfan fanatic fancy\nfan fanatic fancy\n' },
    { stdin: 'tower town tow towel\ntowel\n', expectedOutput: 'tow towel tower\ntow towel tower\ntow towel tower\ntow towel\ntowel\n' },
    { stdin: 'xyz\nabc\n', expectedOutput: '\n\n\n' },
    { stdin: 'alpha alphabet alps alp\nalpha\n', expectedOutput: 'alp alpha alphabet\nalp alpha alphabet\nalp alpha alphabet\nalpha alphabet\nalpha alphabet\n' },
    { stdin: 'boot book booze\nboo\n', expectedOutput: 'book boot booze\nbook boot booze\nbook boot booze\n' },
  ]}
  starterCode={{
    python: `def search_suggestions(products: list, search_word: str) -> list:
    # Return a list of lists: for each prefix of search_word,
    # return up to 3 lexicographically smallest matching products.
    pass`,
    java: `import java.util.*;
class Solution {
    public List<List<String>> suggestedProducts(String[] products, String searchWord) {
        // your code here
        return new ArrayList<>();
    }
}`,
    cpp: `class Solution {
public:
    vector<vector<string>> suggestedProducts(vector<string>& products, string searchWord) {
        // your code here
        return {};
    }
};`,
    javascript: `function suggestedProducts(products, searchWord) {
    // your code here
    return [];
}`,
    typescript: `function suggestedProducts(products: string[], searchWord: string): string[][] {
    // your code here
    return [];
}`,
    rust: `fn suggested_products(products: &mut Vec<String>, search_word: &str) -> Vec<Vec<String>> {
    // your code here
    vec![]
}`,
    c: `#include <string.h>
#include <stdlib.h>
int cmp(const void* a, const void* b) {
    return strcmp(*(char**)a, *(char**)b);
}
char*** suggestedProducts(char** products, int productsSize, char* searchWord,
                          int* returnSize, int** returnColumnSizes) {
    // your code here
    *returnSize = 0;
    *returnColumnSizes = NULL;
    return NULL;
}`,
    csharp: `using System.Collections.Generic;
using System.Linq;
class Solution {
    public IList<IList<string>> SuggestedProducts(string[] products, string searchWord) {
        // your code here
        return new List<IList<string>>();
    }
}`,
    go: `func suggestedProducts(products []string, searchWord string) [][]string {
    // your code here
    return nil
}`,
  }}
  harness={{
    python: `import sys

def main():
    data = sys.stdin.read().strip().splitlines()
    products = data[0].split()
    search_word = data[1].strip()
    result = search_suggestions(products, search_word)
    for group in result:
        print(' '.join(group))

main()`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String[] products = sc.nextLine().trim().split(" ");
        String searchWord = sc.nextLine().trim();
        Solution sol = new Solution();
        java.util.List<java.util.List<String>> result = sol.suggestedProducts(products, searchWord);
        for (java.util.List<String> group : result) {
            System.out.println(String.join(" ", group));
        }
    }
}`,
    cpp: `#include <bits/stdc++.h>
using namespace std;
int main() {
    string line1, searchWord;
    getline(cin, line1);
    getline(cin, searchWord);
    stringstream ss(line1);
    string token;
    vector<string> products;
    while (ss >> token) products.push_back(token);
    Solution sol;
    vector<vector<string>> result = sol.suggestedProducts(products, searchWord);
    for (auto& group : result) {
        for (int i = 0; i < (int)group.size(); i++) {
            if (i > 0) cout << " ";
            cout << group[i];
        }
        cout << "\n";
    }
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l.trim()));
rl.on('close', () => {
    const products = lines[0].split(' ');
    const searchWord = lines[1];
    const result = suggestedProducts(products, searchWord);
    for (const group of result) {
        console.log(group.join(' '));
    }
});`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l.trim()));
rl.on('close', () => {
    const products: string[] = lines[0].split(' ');
    const searchWord: string = lines[1];
    const result: string[][] = suggestedProducts(products, searchWord);
    for (const group of result) {
        console.log(group.join(' '));
    }
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines();
    let line1 = lines.next().unwrap().unwrap();
    let line2 = lines.next().unwrap().unwrap();
    let mut products: Vec<String> = line1.trim().split_whitespace().map(|s| s.to_string()).collect();
    let search_word = line2.trim().to_string();
    let result = suggested_products(&mut products, &search_word);
    for group in &result {
        println!("{}", group.join(" "));
    }
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int cmp(const void* a, const void* b) {
    return strcmp(*(char**)a, *(char**)b);
}
int main() {
    char line1[100000], line2[1000];
    fgets(line1, sizeof(line1), stdin);
    fgets(line2, sizeof(line2), stdin);
    // trim newlines
    line1[strcspn(line1, "\n")] = 0;
    line2[strcspn(line2, "\n")] = 0;
    char* products[10000];
    int productsSize = 0;
    char* tok = strtok(line1, " ");
    while (tok) {
        products[productsSize++] = tok;
        tok = strtok(NULL, " ");
    }
    char* searchWord = line2;
    int returnSize;
    int* returnColumnSizes;
    char*** result = suggestedProducts(products, productsSize, searchWord, &returnSize, &returnColumnSizes);
    for (int i = 0; i < returnSize; i++) {
        for (int j = 0; j < returnColumnSizes[i]; j++) {
            if (j > 0) printf(" ");
            printf("%s", result[i][j]);
        }
        printf("\n");
    }
    return 0;
}`,
    csharp: `using System;
using System.Linq;
using System.Collections.Generic;
class Program {
    static void Main() {
        string[] products = Console.ReadLine().Trim().Split(' ');
        string searchWord = Console.ReadLine().Trim();
        Solution sol = new Solution();
        var result = sol.SuggestedProducts(products, searchWord);
        foreach (var group in result) {
            Console.WriteLine(string.Join(" ", group));
        }
    }
}`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    line1, _ := reader.ReadString('\n')
    line2, _ := reader.ReadString('\n')
    line1 = strings.TrimSpace(line1)
    line2 = strings.TrimSpace(line2)
    products := strings.Fields(line1)
    searchWord := line2
    result := suggestedProducts(products, searchWord)
    for _, group := range result {
        fmt.Println(strings.Join(group, " "))
    }
}`,
  }}
  defaultLanguage="python"
>

# Search Suggestions System

**Difficulty:** Medium

Given an array of strings `products` and a string `searchWord`, design a system that suggests at most **3 product names** from `products` after each character of `searchWord` is typed.

Suggested products should share a common prefix with `searchWord`. If there are more than 3 products with a common prefix, return the 3 **lexicographically smallest** products.

Return a list of lists of the suggested products after each character of `searchWord` is typed.

## Example 1

**Input:**
```
products = ["mobile", "mouse", "moneypot", "monitor", "mousepad"]
searchWord = "mouse"
```

**Output:**
```
["mobile","moneypot","monitor"]
["mobile","moneypot","monitor"]
["mouse","mousepad"]
["mouse","mousepad"]
["mouse","mousepad"]
```

**Explanation:** Products sorted: `["mobile","moneypot","monitor","mouse","mousepad"]`
- After "m": mobile, moneypot, monitor
- After "mo": mobile, moneypot, monitor
- After "mou": mouse, mousepad
- After "mous": mouse, mousepad
- After "mouse": mouse, mousepad

## Example 2

**Input:**
```
products = ["havana"]
searchWord = "havana"
```

**Output:**
```
["havana"]
["havana"]
["havana"]
["havana"]
["havana"]
["havana"]
```

## Example 3

**Input:**
```
products = ["bags","baggage","banana","bag"]
searchWord = "bag"
```

**Output:**
```
["bag","baggage","bags"]
["bag","baggage","bags"]
["bag","baggage","bags"]
```

## Input Specification

The first line contains space-separated product names.  
The second line contains the search word.

## Output Specification

Print one line per character of the search word. Each line contains up to 3 space-separated product names (lexicographically smallest matching products). If no products match, print an empty line.

## Constraints

- `1 <= products.length <= 1000`
- `1 <= products[i].length <= 3000`
- `1 <= sum(products[i].length) <= 2 * 10^4`
- All characters of `products[i]` are lowercase English letters.
- `1 <= searchWord.length <= 1000`
- All characters of `searchWord` are lowercase English letters.

## Hints

<details>
<summary>Hint 1</summary>

Sort the products array lexicographically first. Once sorted, products sharing a common prefix will be **adjacent** to each other. You can use binary search to find where a given prefix starts.

</details>

<details>
<summary>Hint 2</summary>

A **Trie** is a natural fit here. Insert all products into a trie, then for each prefix, traverse the trie and collect up to 3 products using DFS. Stop once you have found 3.

</details>

<details>
<summary>Hint 3</summary>

The binary search approach is often simpler to implement. Sort the products, then for each prefix of length `i`, use `bisect_left` to find the insertion point. Scan forward from there and collect up to 3 products that still start with the prefix. Stop early once you find a product that does not match.

</details>

</ProblemEditor>

---

**View the [detailed solution](../../solutions/medium/search-suggestions)** for a step-by-step walkthrough and code in all languages.
