---
title: Group Anagrams
sidebar_label: Group Anagrams
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/hash-maps/group-anagrams', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: "eat,tea,tan,ate,nat,bat", expectedOutput: "[[\"ate\",\"eat\",\"tea\"],[\"bat\"],[\"nat\",\"tan\"]]\n" , isSample: true },
    { stdin: "", expectedOutput: "[]\n" , isSample: true },
    { stdin: "a", expectedOutput: "[[\"a\"]]\n" },
    { stdin: "abc,bca,cab", expectedOutput: "[[\"abc\",\"bca\",\"cab\"]]\n" },
    { stdin: "ab,bc,cd,de,ef", expectedOutput: "[[\"ab\"],[\"bc\"],[\"cd\"],[\"de\"],[\"ef\"]]\n" },
    { stdin: "aaa,aaa,aaa", expectedOutput: "[[\"aaa\",\"aaa\",\"aaa\"]]\n" },
    { stdin: "dog,god,cat,tac,act", expectedOutput: "[[\"act\",\"cat\",\"tac\"],[\"dog\",\"god\"]]\n" },
    { stdin: "a,b,c,d,e", expectedOutput: "[[\"a\"],[\"b\"],[\"c\"],[\"d\"],[\"e\"]]\n" },
    { stdin: "ab,ba,abc,cba,bca", expectedOutput: "[[\"ab\",\"ba\"],[\"abc\",\"bca\",\"cba\"]]\n" },
    { stdin: "star,arts,rats,star,arts", expectedOutput: "[[\"arts\",\"arts\",\"rats\",\"star\",\"star\"]]\n" },
    { stdin: "arc,car,rac,art,rat,tar", expectedOutput: "[[\"arc\",\"car\",\"rac\"],[\"art\",\"rat\",\"tar\"]]\n" },
    { stdin: "hello,world,olleh,dlrow", expectedOutput: "[[\"dlrow\",\"world\"],[\"hello\",\"olleh\"]]\n" },
    { stdin: "xxy,xyx,yxx,yyx,xyy,yxy", expectedOutput: "[[\"xxy\",\"xyx\",\"yxx\"],[\"xyy\",\"yxy\",\"yyx\"]]\n" },
    { stdin: "abcd,dbca,cdba,dcba,zzzz,zzzz", expectedOutput: "[[\"abcd\",\"cdba\",\"dbca\",\"dcba\"],[\"zzzz\",\"zzzz\"]]\n" },
    { stdin: "one,two,three,four,five", expectedOutput: "[[\"five\"],[\"four\"],[\"one\"],[\"three\"],[\"two\"]]\n" },
    { stdin: "tac,cat,act,atc,tca,cta", expectedOutput: "[[\"act\",\"atc\",\"cat\",\"cta\",\"tac\",\"tca\"]]\n" },
    { stdin: "listen,silent,enlist,hello,world", expectedOutput: "[[\"enlist\",\"listen\",\"silent\"],[\"hello\"],[\"world\"]]\n" },
    { stdin: "aab,aba,baa,abb,bab,bba", expectedOutput: "[[\"aab\",\"aba\",\"baa\"],[\"abb\",\"bab\",\"bba\"]]\n" },
    { stdin: "evil,vile,levi,veil,life,file", expectedOutput: "[[\"evil\",\"levi\",\"veil\",\"vile\"],[\"file\",\"life\"]]\n" },
    { stdin: "loop,pool,polo,look,kool", expectedOutput: "[[\"kool\",\"look\"],[\"loop\",\"polo\",\"pool\"]]\n" },
  ]}
  starterCode={{
    python: `def group_anagrams(strs: list[str]) -> list[list[str]]:
    # Your code here
    pass`,
    java: `import java.util.*;
class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        // Your code here
        return new ArrayList<>();
    }
}`,
    cpp: `#include <vector>
#include <string>
using namespace std;

class Solution {
public:
    vector<vector<string>> groupAnagrams(vector<string>& strs) {
        // Your code here
        return {};
    }
};`,
    javascript: `function groupAnagrams(strs) {
    // Your code here
}`,
    typescript: `function groupAnagrams(strs: string[]): string[][] {
    // Your code here
    return [];
}`,
    rust: `fn group_anagrams(strs: Vec<String>) -> Vec<Vec<String>> {
    // Your code here
    vec![]
}`,
    c: `// Note: In C, returning a dynamic 2D array is complex.
// Focus on the algorithm logic.
#include <stdlib.h>
#include <string.h>

char*** groupAnagrams(char** strs, int strsSize, int* returnSize, int** returnColumnSizes) {
    // Your code here
    return NULL;
}`,
    csharp: `using System;
using System.Collections.Generic;

public class Solution {
    public IList<IList<string>> GroupAnagrams(string[] strs) {
        // Your code here
        return new List<IList<string>>();
    }
}`,
    go: `func groupAnagrams(strs []string) [][]string {
    // Your code here
    return nil
}`,
  }}
  harness={{
    python: `import sys
import json
data = sys.stdin.read().strip()
print("###SYPHER_JUDGE0_RESULT###")
if not data:
    print("[]")
else:
    strs = [x.strip() for x in data.split(',')]
    result = [sorted(g) for g in group_anagrams(strs)]
    result.sort(key=lambda g: g[0])
    print(json.dumps(result, separators=(',', ':')))`,
    java: `import java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String line = sc.hasNextLine() ? sc.nextLine() : "";
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        if (line.isEmpty()) {
            System.out.println("[]");
            return;
        }
        String[] strs = line.split(",");
        for (int i = 0; i < strs.length; i++) strs[i] = strs[i].trim();
        Solution sol = new Solution();
        List<List<String>> result = sol.groupAnagrams(strs);
        for (List<String> g : result) Collections.sort(g);
        result.sort((a, b) -> a.get(0).compareTo(b.get(0)));
        StringBuilder sb = new StringBuilder();
        sb.append("[");
        for (int i = 0; i < result.size(); i++) {
            sb.append("[");
            for (int j = 0; j < result.get(i).size(); j++) {
                sb.append("\\"").append(result.get(i).get(j)).append("\\"");
                if (j + 1 < result.get(i).size()) sb.append(",");
            }
            sb.append("]");
            if (i + 1 < result.size()) sb.append(",");
        }
        sb.append("]");
        System.out.println(sb.toString());
    }
}`,
    cpp: `#include <iostream>
#include <sstream>
#include <vector>
#include <string>
using namespace std;

int main() {
    string line;
    getline(cin, line);
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    if (line.empty()) {
        cout << "[]" << endl;
        return 0;
    }
    vector<string> strs;
    stringstream ss(line);
    string token;
    while (getline(ss, token, ',')) {
        strs.push_back(token);
    }
    Solution sol;
    auto result = sol.groupAnagrams(strs);
    for (auto& g : result) sort(g.begin(), g.end());
    sort(result.begin(), result.end(), [](const vector<string>& a, const vector<string>& b) { return a[0] < b[0]; });
    cout << "[";
    for (size_t i = 0; i < result.size(); i++) {
        cout << "[";
        for (size_t j = 0; j < result[i].size(); j++) {
            cout << "\\"" << result[i][j] << "\\"";
            if (j + 1 < result[i].size()) cout << ",";
        }
        cout << "]";
        if (i + 1 < result.size()) cout << ",";
    }
    cout << "]" << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l));
rl.on('close', () => {
    const line = lines[0] || '';
    const strs = line.trim() ? line.split(',').map(s => s.trim()) : [];
    const result = groupAnagrams(strs).map(g => [...g].sort());
    result.sort((a, b) => a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(JSON.stringify(result));
});`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    const line: string = lines[0] || '';
    const strs: string[] = line.trim() ? line.split(',').map(s => s.trim()) : [];
    const result: string[][] = groupAnagrams(strs).map((g: string[]) => [...g].sort());
    result.sort((a: string[], b: string[]) => a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(JSON.stringify(result));
});`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().transpose().unwrap().unwrap_or_default();
    let strs: Vec<String> = if line.trim().is_empty() {
        Vec::new()
    } else {
        line.split(',').map(|s| s.trim().to_string()).collect()
    };
    let mut result = group_anagrams(strs);
    for g in result.iter_mut() { g.sort(); }
    result.sort_by(|a, b| a[0].cmp(&b[0]));
    let json = format!("[{}]", result.iter()
        .map(|group| format!("[{}]", group.iter()
            .map(|s| format!("\\"{}\\"", s))
            .collect::<Vec<_>>().join(",")))
        .collect::<Vec<_>>().join(","));
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", json);
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int cmpStr(const void* a, const void* b) {
    return strcmp(*(char**)a, *(char**)b);
}

static char*** g_result;

int cmpGroupIdx(const void* a, const void* b) {
    int ia = *(const int*)a, ib = *(const int*)b;
    return strcmp(g_result[ia][0], g_result[ib][0]);
}

int main() {
    char line[10000];
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    if (!fgets(line, 10000, stdin)) {
        printf("[]\\n");
        return 0;
    }
    line[strcspn(line, "\\n")] = 0;
    if (strlen(line) == 0) {
        printf("[]\\n");
        return 0;
    }
    char* strs[1000];
    int n = 0;
    char* token = strtok(line, ",");
    while (token) {
        strs[n++] = token;
        token = strtok(NULL, ",");
    }
    int returnSize;
    int* returnColumnSizes;
    char*** result = groupAnagrams(strs, n, &returnSize, &returnColumnSizes);
    for (int i = 0; i < returnSize; i++) {
        qsort(result[i], returnColumnSizes[i], sizeof(char*), cmpStr);
    }
    g_result = result;
    int* order = malloc(returnSize * sizeof(int));
    for (int i = 0; i < returnSize; i++) order[i] = i;
    qsort(order, returnSize, sizeof(int), cmpGroupIdx);
    printf("[");
    for (int oi = 0; oi < returnSize; oi++) {
        int i = order[oi];
        printf("[");
        for (int j = 0; j < returnColumnSizes[i]; j++) {
            printf("\\"%s\\"", result[i][j]);
            if (j + 1 < returnColumnSizes[i]) printf(",");
        }
        printf("]");
        if (oi + 1 < returnSize) printf(",");
    }
    printf("]\\n");
    free(order);
    return 0;
}`,
    csharp: `using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

class Program {
    static void Main() {
        string line = Console.ReadLine();
        string[] strs = string.IsNullOrEmpty(line) ? new string[0] : line.Split(',').Select(s => s.Trim()).ToArray();
        Solution sol = new Solution();
        var raw = sol.GroupAnagrams(strs);
        var result = raw.Select(g => g.OrderBy(x => x).ToList()).OrderBy(g => g[0]).ToList();
        StringBuilder sb = new StringBuilder();
        sb.Append("[");
        for (int i = 0; i < result.Count; i++) {
            sb.Append("[");
            var group = result[i];
            for (int j = 0; j < group.Count; j++) {
                sb.Append("\\"").Append(group[j]).Append("\\"");
                if (j + 1 < group.Count) sb.Append(",");
            }
            sb.Append("]");
            if (i + 1 < result.Count) sb.Append(",");
        }
        sb.Append("]");
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(sb.ToString());
    }
}`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "sort"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    line = strings.TrimRight(line, "\\r\\n")
    var strs []string
    if line != "" {
        strs = strings.Split(line, ",")
    }
    result := groupAnagrams(strs)
    for _, g := range result {
        sort.Strings(g)
    }
    sort.Slice(result, func(i, j int) bool {
        return result[i][0] < result[j][0]
    })
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Print("[")
    for i, g := range result {
        fmt.Print("[")
        for j, s := range g {
            fmt.Printf("\\"%s\\"", s)
            if j+1 < len(g) {
                fmt.Print(",")
            }
        }
        fmt.Print("]")
        if i+1 < len(result) {
            fmt.Print(",")
        }
    }
    fmt.Println("]")
}`,
  }}
  defaultLanguage="python"
>
# Group Anagrams

Given an array of strings `strs`, group the **anagrams** together. You can return the answer in **any order**.

An **anagram** is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.

## Examples

**Example 1:**
```
Input: strs = ["eat","tea","tan","ate","nat","bat"]
Output: [["bat"],["nat","tan"],["ate","eat","tea"]]
```

**Example 2:**
```
Input: strs = [""]
Output: [[""]]
```

**Example 3:**
```
Input: strs = ["a"]
Output: [["a"]]
```

## Constraints

- `1 <= strs.length <= 10^4`
- `0 <= strs[i].length <= 100`
- `strs[i]` consists of lowercase English letters.

## Hints

<details>
<summary>Hint 1</summary>

Two strings are anagrams if they have the same sorted character sequence. Try sorting each string and using the sorted version as a hash map key.
</details>

<details>
<summary>Hint 2</summary>

Alternatively, use a character frequency count (26-element array for lowercase letters) as the key. This avoids the O(k log k) sorting cost.
</details>

<details>
<summary>Hint 3</summary>

Iterate through the input array once. For each string, compute its key, and append the original string to the corresponding group in the hash map.
</details>

</ProblemEditor>

---
**View the [detailed solution](../solutions/group-anagrams)**