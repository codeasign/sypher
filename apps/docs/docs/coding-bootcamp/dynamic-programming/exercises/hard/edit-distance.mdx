---
title: Edit Distance
sidebar_label: Edit Distance
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'dynamic-programming-edit-distance', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '\n\n', expectedOutput: '0\n' },
    { stdin: 'abc\nabc\n', expectedOutput: '0\n' },
    { stdin: '\nabc\n', expectedOutput: '3\n' },
    { stdin: 'abc\n\n', expectedOutput: '3\n' },
    { stdin: 'a\n\n', expectedOutput: '1\n' },
    { stdin: '\na\n', expectedOutput: '1\n' },
    { stdin: 'a\nb\n', expectedOutput: '1\n' },
    { stdin: 'a\na\n', expectedOutput: '0\n' },
    { stdin: 'ab\nab\n', expectedOutput: '0\n' },
    { stdin: 'ab\nba\n', expectedOutput: '2\n' },
    { stdin: 'cat\ndog\n', expectedOutput: '3\n' },
    { stdin: 'horse\nros\n', expectedOutput: '3\n' },
    { stdin: 'intention\nexecution\n', expectedOutput: '5\n' },
    { stdin: 'Saturday\nSunday\n', expectedOutput: '3\n' },
    { stdin: 'kitten\nsitting\n', expectedOutput: '3\n' },
    { stdin: 'book\nback\n', expectedOutput: '2\n' },
    { stdin: 'hello\nyellow\n', expectedOutput: '2\n' },
    { stdin: 'pneumonoultramicroscopicsilicovolcanoconiosis\npneumonoultramicroscopicsilicovolcanoconiosis\n', expectedOutput: '0\n' },
    { stdin: 'a\nab\n', expectedOutput: '1\n' },
    { stdin: 'ab\na\n', expectedOutput: '1\n' },
  ]}
  starterCode={{
    python: `def min_distance(word1: str, word2: str) -> int:
    # your code here
    pass
`,
    python27: `def min_distance(word1, word2):
    # your code here
    pass
`,
    python35: `def min_distance(word1, word2):
    # your code here
    pass
`,
    python36: `def min_distance(word1: str, word2: str) -> int:
    # your code here
    pass
`,
    javascript: `function minDistance(word1, word2) {
    // your code here
}
`,
    java: `class Solution {
    public int minDistance(String word1, String word2) {
        // your code here
        return 0;
    }
}
`,
    java8: `class Solution {
    public int minDistance(String word1, String word2) {
        // your code here
        return 0;
    }
}
`,
    cpp: `class Solution {
public:
    int minDistance(string word1, string word2) {
        // your code here
        return 0;
    }
};
`,
    cpp14: `class Solution {
public:
    int minDistance(string word1, string word2) {
        // your code here
        return 0;
    }
};
`,
    cpp83: `class Solution {
public:
    int minDistance(string word1, string word2) {
        // your code here
        return 0;
    }
};
`,
    c: `int minDistance(char* word1, char* word2) {
    // your code here
    return 0;
}
`,
    c_gcc7: `int minDistance(char* word1, char* word2) {
    // your code here
    return 0;
}
`,
    c_gcc8: `int minDistance(char* word1, char* word2) {
    // your code here
    return 0;
}
`,
    csharp: `class Solution {
    public int MinDistance(string word1, string word2) {
        // your code here
        return 0;
    }
}
`,
    csharp_mono52: `class Solution {
    public int MinDistance(string word1, string word2) {
        // your code here
        return 0;
    }
}
`,
    csharp_mono54: `class Solution {
    public int MinDistance(string word1, string word2) {
        // your code here
        return 0;
    }
}
`,
    rust: `fn min_distance(word1: &str, word2: &str) -> i32 {
    // your code here
    0
}
`,
    rust120: `fn min_distance(word1: &str, word2: &str) -> i32 {
    // your code here
    0
}
`,
    typescript: `function minDistance(word1: string, word2: string): number {
    // your code here
    return 0;
}
`,
  }}
  harness={{
    python: `import sys
lines = sys.stdin.read().strip().split('\\n')
word1 = lines[0] if len(lines) > 0 else ''
word2 = lines[1] if len(lines) > 1 else ''
print(min_distance(word1, word2))
`,
    python27: `import sys
lines = sys.stdin.read().strip().split('\\n')
word1 = lines[0] if len(lines) > 0 else ''
word2 = lines[1] if len(lines) > 1 else ''
print min_distance(word1, word2)
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const lines = [];
rl.on('line', l => lines.push(l));
rl.on('close', () => {
    console.log(minDistance(lines[0] || '', lines[1] || ''));
});
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String word1 = sc.nextLine();
        String word2 = sc.nextLine();
        System.out.println(new Solution().minDistance(word1, word2));
    }
}
`,
    cpp: `int main() {
    string word1, word2;
    getline(cin, word1);
    getline(cin, word2);
    Solution sol;
    cout << sol.minDistance(word1, word2) << endl;
    return 0;
}
`,
    c: `int main() {
    char word1[1005], word2[1005];
    fgets(word1, sizeof(word1), stdin);
    word1[strcspn(word1, "\\n")] = 0;
    fgets(word2, sizeof(word2), stdin);
    word2[strcspn(word2, "\\n")] = 0;
    printf("%d\\n", minDistance(word1, word2));
    return 0;
}
`,
    csharp: `class Program {
    static void Main() {
        string word1 = System.Console.ReadLine() ?? "";
        string word2 = System.Console.ReadLine() ?? "";
        System.Console.WriteLine(new Solution().MinDistance(word1, word2));
    }
}
`,
    rust: `fn main() {
    let mut word1 = String::new();
    let mut word2 = String::new();
    std::io::stdin().read_line(&mut word1).unwrap();
    std::io::stdin().read_line(&mut word2).unwrap();
    println!("{}", min_distance(word1.trim(), word2.trim()));
}
`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    console.log(minDistance(lines[0] || '', lines[1] || ''));
});
`,
  }}
  defaultLanguage="python"
>

# Edit Distance

**Difficulty:** Hard

Given two strings `word1` and `word2`, return the **minimum number of operations** required to convert `word1` to `word2`.

You have the following three operations permitted on a word:

- **Insert** a character
- **Delete** a character
- **Replace** a character

## Example 1

**Input:** `word1 = "horse"`, `word2 = "ros"`  
**Output:** `3`  
**Explanation:**
```
horse -> rorse (replace 'h' with 'r')
rorse -> rose (delete 'r')
rose -> ros (delete 'e')
```

## Example 2

**Input:** `word1 = "intention"`, `word2 = "execution"`  
**Output:** `5`  
**Explanation:**
```
intention -> inention (delete 't')
inention -> enention (replace 'i' with 'e')
enention -> exention (replace 'n' with 'x')
exention -> exection (replace 'n' with 'c')
exection -> execution (insert 'u')
```

## Input Specification

The first line contains a string `word1`.  
The second line contains a string `word2`.  
Either or both strings may be empty.

## Output Specification

Print a single integer: the minimum edit distance between the two strings.

## Constraints

- `0 <= len(word1), len(word2) <= 500`
- Both strings consist of lowercase English letters.

<AsciiDiagram
  id="dynamic-programming/edit-distance"
  alt="Diagram showing the 2D DP table for converting horse to ros"
  caption="The 2D DP table for edit distance: dp[i][j] = min operations to convert word1[0..i-1] to word2[0..j-1]"
  content={`┌─────────────────────────────────────────────────────┐
│  Edit Distance — DP Table for "horse" → "ros"      │
│                                                     │
│         ""   r   o   s                              │
│      ┌─────────────────────┐                        │
│   "" │  0   1   2   3      │                        │
│      ├─────────────────────┤                        │
│   h  │  1   1   2   3      │                        │
│      ├─────────────────────┤                        │
│   o  │  2   2   1   2      │                        │
│      ├─────────────────────┤                        │
│   r  │  3   2   2   2      │                        │
│      ├─────────────────────┤                        │
│   s  │  4   3   3   2      │                        │
│      ├─────────────────────┤                        │
│   e  │  5   4   4   3      │                        │
│      └─────────────────────┘                        │
│                                                     │
│  Recurrence:                                         │
│  if word1[i]==word2[j]:  dp[i][j]=dp[i-1][j-1]      │
│  else: dp[i][j]=1+min(                               │
│    dp[i-1][j],    // delete                          │
│    dp[i][j-1],    // insert                          │
│    dp[i-1][j-1]   // replace                         │
│  )                                                   │
└─────────────────────────────────────────────────────┘`}
/>

## Hints

<details>
<summary>Hint 1</summary>

This is a classic 2D dynamic programming problem. Define `dp[i][j]` as the minimum edit distance between `word1[0..i-1]` and `word2[0..j-1]`.
</details>

<details>
<summary>Hint 2</summary>

If the characters match, no operation is needed: `dp[i][j] = dp[i-1][j-1]`. If they differ, choose the cheapest of insert, delete, or replace.
</details>

<details>
<summary>Hint 3</summary>

Initialize the first row and first column with incremental values: `dp[i][0] = i` (i deletions) and `dp[0][j] = j` (j insertions). This handles the base cases where one string is empty.
</details>

<details>
<summary>Hint 4</summary>

For space optimization, you only need two rows at a time (previous and current), bringing space from O(m*n) down to O(min(m, n)).
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/hard/edit-distance)** for a step-by-step walkthrough, multiple approaches, and code in all languages.