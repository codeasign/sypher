---
title: Unique Paths
sidebar_label: Unique Paths
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/dynamic-programming/unique-paths', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '3 7\n', expectedOutput: '28\n' },
    { stdin: '3 2\n', expectedOutput: '3\n' },
    { stdin: '7 3\n', expectedOutput: '28\n' },
    { stdin: '3 3\n', expectedOutput: '6\n' },
    { stdin: '1 1\n', expectedOutput: '1\n' },
    { stdin: '2 2\n', expectedOutput: '2\n' },
    { stdin: '1 100\n', expectedOutput: '1\n' },
    { stdin: '100 1\n', expectedOutput: '1\n' },
    { stdin: '2 3\n', expectedOutput: '3\n' },
    { stdin: '3 4\n', expectedOutput: '10\n' },
    { stdin: '4 4\n', expectedOutput: '20\n' },
    { stdin: '5 5\n', expectedOutput: '70\n' },
    { stdin: '6 6\n', expectedOutput: '252\n' },
    { stdin: '7 4\n', expectedOutput: '84\n' },
    { stdin: '4 7\n', expectedOutput: '84\n' },
    { stdin: '8 8\n', expectedOutput: '3432\n' },
    { stdin: '10 10\n', expectedOutput: '48620\n' },
    { stdin: '2 99\n', expectedOutput: '99\n' },
    { stdin: '99 2\n', expectedOutput: '99\n' },
    { stdin: '5 8\n', expectedOutput: '330\n' },
  ]}
  starterCode={{
    python: `def unique_paths(m: int, n: int) -> int:
    # Your code here
    pass`,
    java: `class Solution {
    public int uniquePaths(int m, int n) {
        // Your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int uniquePaths(int m, int n) {
        // Your code here
        return 0;
    }
};`,
    javascript: `function uniquePaths(m, n) {
    // Your code here
}`,
    typescript: `function uniquePaths(m: number, n: number): number {
    // Your code here
    return 0;
}`,
    rust: `pub fn unique_paths(m: i32, n: i32) -> i32 {
    // Your code here
    0
}`,
    c: `int uniquePaths(int m, int n) {
    // Your code here
    return 0;
}`,
    csharp: `public class Solution {
    public int UniquePaths(int m, int n) {
        // Your code here
        return 0;
    }
}`,
    go: `func uniquePaths(m int, n int) int {
    // Your code here
    return 0
}`,
  }}
  harness={{
    python: `\u0069mport sys
data = sys.stdin.read().strip().split()
m, n = int(data[0]), int(data[1])
print(unique_paths(m, n))`,
    java: `\u0069mport java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int m = sc.nextInt(), n = sc.nextInt();
        Solution sol = new Solution();
        System.out.println(sol.uniquePaths(m, n));
    }
}`,
    cpp: `#include <iostream>
using namespace std;
int main() {
    int m, n; cin >> m >> n;
    Solution sol;
    cout << sol.uniquePaths(m, n) << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const [m, n] = line.trim().split(' ').map(Number);
    console.log(uniquePaths(m, n));
    rl.close();
});`,
    typescript: `\u0069mport * as readline from 'readline';
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const [m, n] = line.trim().split(' ').map(Number);
    console.log(uniquePaths(m, n));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let nums: Vec<i32> = line.split(' ').map(|s| s.parse().unwrap()).collect();
    println!("{}", unique_paths(nums[0], nums[1]));
}`,
    c: `#include <stdio.h>
int main() {
    int m, n; scanf("%d %d", &m, &n);
    printf("%d\\n", uniquePaths(m, n));
    return 0;
}`,
    csharp: `using System;
class Program {
    static void Main() {
        string[] parts = Console.ReadLine().Split(' ');
        int m = int.Parse(parts[0]), n = int.Parse(parts[1]);
        Solution sol = new Solution();
        Console.WriteLine(sol.UniquePaths(m, n));
    }
}`,
    go: `package main
\u0069mport "fmt"
func main() {
    var m, n int
    fmt.Scan(&m, &n)
    fmt.Println(uniquePaths(m, n))
}`,
  }}
  defaultLanguage="python"
>

# Unique Paths

**Difficulty:** Medium

There is a robot on an `m x n` grid. The robot is initially located at the **top-left corner** (i.e., `grid[0][0]`). The robot tries to move to the **bottom-right corner** (i.e., `grid[m-1][n-1]`). The robot can only move either **down** or **right** at any point in time.

Given two integers `m` and `n`, return the number of possible unique paths that the robot can take to reach the bottom-right corner.

## Example 1

**Input:** `m = 3, n = 7`  
**Output:** `28`  

## Example 2

**Input:** `m = 3, n = 2`  
**Output:** `3`  
**Explanation:** From the top-left corner, there are a total of 3 ways to reach the bottom-right corner:
1. Right → Down → Down
2. Down → Down → Right
3. Down → Right → Down

## Input Specification

- A single line containing two space-separated integers `m` and `n`.
- `1 <= m, n <= 100`

## Output Specification

Print a single integer: the number of unique paths.

## Constraints

- `1 <= m, n <= 100`
- The answer is guaranteed to fit within a 32-bit integer for the given constraints.

## Hints

<details>
<summary>Hint 1</summary>

Think about the number of ways to reach any cell `(i, j)`. From which cells can you reach `(i, j)` in a single move?
</details>

<details>
<summary>Hint 2</summary>

Define `dp[i][j]` as the number of ways to reach cell `(i, j)`. You can only come from above `(i-1, j)` or from the left `(i, j-1)`. So `dp[i][j] = dp[i-1][j] + dp[i][j-1]`.
</details>

<details>
<summary>Hint 3</summary>

You can optimize space to O(n) by using a single 1D array. Since each row depends only on the current and previous row, you can update in place.
</details>

</ProblemEditor>

---
**📖 View the [detailed solution](../solutions/medium/unique-paths)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Longest Increasing Subsequence](../exercises/medium/longest-increasing-subsequence).