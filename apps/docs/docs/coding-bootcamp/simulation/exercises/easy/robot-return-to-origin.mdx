---
title: Robot Return to Origin
sidebar_label: Robot Return to Origin
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/simulation/robot-return-to-origin', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: 'UD', expectedOutput: 'true\n' , isSample: true },
    { stdin: 'LL', expectedOutput: 'false\n' , isSample: true },
    { stdin: 'UDLR', expectedOutput: 'true\n' },
    { stdin: 'UUUUUDDDDD', expectedOutput: 'true\n' },
    { stdin: 'RLRUDUDUDL', expectedOutput: 'true\n' },
    { stdin: 'U', expectedOutput: 'false\n' },
    { stdin: 'D', expectedOutput: 'false\n' },
    { stdin: 'L', expectedOutput: 'false\n' },
    { stdin: 'R', expectedOutput: 'false\n' },
    { stdin: 'UDLRUDLR', expectedOutput: 'true\n' },
    { stdin: 'UUUDDD', expectedOutput: 'true\n' },
    { stdin: 'LLL', expectedOutput: 'false\n' },
    { stdin: 'RRR', expectedOutput: 'false\n' },
    { stdin: 'UUDDLLRR', expectedOutput: 'true\n' },
    { stdin: 'URDL', expectedOutput: 'true\n' },
    { stdin: 'UURDLLDR', expectedOutput: 'true\n' },
    { stdin: 'UUURRRDDDLLL', expectedOutput: 'true\n' },
    { stdin: 'RDLU', expectedOutput: 'true\n' },
    { stdin: 'UUUU', expectedOutput: 'false\n' },
    { stdin: 'DDDD', expectedOutput: 'false\n' },
  ]}
  starterCode={{
    python: `def judgeCircle(moves: str) -> bool:
    # Your code here
    pass`,
    java: `class Solution {
    public boolean judgeCircle(String moves) {
        // Your code here
        return false;
    }
}`,
    cpp: `class Solution {
public:
    bool judgeCircle(string moves) {
        // Your code here
        return false;
    }
};`,
    javascript: `function judgeCircle(moves) {
    // Your code here
    return false;
}`,
    typescript: `function judgeCircle(moves: string): boolean {
    // Your code here
    return false;
}`,
    rust: `fn judge_circle(moves: String) -> bool {
    // Your code here
    false
}`,
    c: `bool judgeCircle(char* moves) {
    // Your code here
    return false;
}`,
    csharp: `public class Solution {
    public bool JudgeCircle(string moves) {
        // Your code here
        return false;
    }
}`,
    go: `func judgeCircle(moves string) bool {
    // Your code here
    return false
}`,
  }}
  harness={{
    python: `import sys

if __name__ == "__main__":
    moves = sys.stdin.readline().strip()
    result = judgeCircle(moves)
    print("###SYPHER_JUDGE0_RESULT###")
    print(str(result).lower())`,
    java: `import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String moves = sc.nextLine().trim();
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.println(new Solution().judgeCircle(moves));
    }
}`,
    cpp: `#include <iostream>
#include <string>
using namespace std;

int main() {
    string moves;
    getline(cin, moves);
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    Solution sol;
    cout << (sol.judgeCircle(moves) ? "true" : "false") << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line) => {
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(judgeCircle(line.trim()));
    rl.close();
});`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(judgeCircle(line.trim()));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", judge_circle(line.trim().to_string()));
}`,
    c: `#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int main() {
    char moves[100001];
    fgets(moves, sizeof(moves), stdin);
    moves[strcspn(moves, "\\n")] = 0;
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    printf("%s\\n", judgeCircle(moves) ? "true" : "false");
    return 0;
}`,
    csharp: `using System;

public class Program {
    public static void Main() {
        string moves = Console.ReadLine().Trim();
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(new Solution().JudgeCircle(moves).ToString().ToLower());
    }
}`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    moves, _ := reader.ReadString('\\n')
    moves = strings.TrimSpace(moves)
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(judgeCircle(moves))
}`,
  }}
  defaultLanguage="python"
>

# Robot Return to Origin

There is a robot starting at the position (0, 0), the origin, on a 2D plane. Given a sequence of its moves, judge if this robot **ends up at (0, 0)** after it completes its moves.

You are given a string `moves` that represents the move sequence of the robot where `moves[i]` is one of the following:

- `'U'`: moves the robot up one unit (y + 1)
- `'D'`: moves the robot down one unit (y - 1)
- `'L'`: moves the robot left one unit (x - 1)
- `'R'`: moves the robot right one unit (x + 1)

Return `true` if the robot returns to the origin after all of its moves, otherwise return `false`.

## Examples

**Example 1:**
```
Input: moves = "UD"
Output: true
Explanation: The robot moves up once, then down once. Net change is zero.
```

**Example 2:**
```
Input: moves = "LL"
Output: false
Explanation: The robot moves left twice. Net horizontal position is -2.
```

**Example 3:**
```
Input: moves = "UDLR"
Output: true
Explanation: Up, down, left, right — the robot ends at the origin.
```

## Constraints

- `1 <= moves.length <= 10^4`
- `moves[i]` is one of `'U'`, `'D'`, `'L'`, or `'R'`

## Hints

<details>
<summary>Hint 1</summary>

Track the x and y coordinates separately. Start at (0, 0) and update for each move.
</details>

<details>
<summary>Hint 2</summary>

The robot returns to the origin only if the number of U moves equals D moves AND the number of L moves equals R moves.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../../solutions/easy/robot-return-to-origin)** for a full explanation and multiple approaches.

## Next

Continue with [Baseball Game](./baseball-game) for another easy simulation problem.