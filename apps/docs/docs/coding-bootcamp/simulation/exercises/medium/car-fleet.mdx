---
title: Car Fleet
sidebar_label: Car Fleet
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/simulation/car-fleet', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '12\n5\n10 8 0 5 3\n2 4 1 1 3', expectedOutput: '3\n' , isSample: true },
    { stdin: '10\n3\n1 4 0\n3 2 4', expectedOutput: '1\n' , isSample: true },
    { stdin: '100\n1\n0\n1', expectedOutput: '1\n' },
    { stdin: '10\n2\n0 5\n2 1', expectedOutput: '1\n' },
    { stdin: '10\n2\n5 0\n1 2', expectedOutput: '1\n' },
    { stdin: '20\n4\n0 5 10 15\n5 4 3 2', expectedOutput: '4\n' },
    { stdin: '12\n5\n0 2 4 6 8\n4 3 2 1 1', expectedOutput: '2\n' },
    { stdin: '10\n3\n0 3 6\n2 2 2', expectedOutput: '3\n' },
    { stdin: '10\n3\n0 1 2\n1 1 1', expectedOutput: '3\n' },
    { stdin: '15\n4\n0 5 10 14\n5 3 2 1', expectedOutput: '3\n' },
    { stdin: '10\n2\n0 9\n10 1', expectedOutput: '1\n' },
    { stdin: '10\n3\n0 4 8\n1 2 1', expectedOutput: '3\n' },
    { stdin: '10\n4\n0 2 4 6\n3 2 1 2', expectedOutput: '2\n' },
    { stdin: '10\n3\n0 1 9\n2 2 1', expectedOutput: '3\n' },
    { stdin: '10\n5\n0 1 2 3 4\n5 4 3 2 1', expectedOutput: '1\n' },
    { stdin: '10\n1\n5\n1', expectedOutput: '1\n' },
    { stdin: '10\n2\n0 8\n1 1', expectedOutput: '2\n' },
    { stdin: '10\n3\n2 5 8\n2 1 1', expectedOutput: '2\n' },
    { stdin: '10\n2\n1 5\n3 1', expectedOutput: '1\n' },
    { stdin: '10\n2\n3 7\n2 1', expectedOutput: '2\n' },
  ]}
  starterCode={{
    python: `def carFleet(target: int, position: list[int], speed: list[int]) -> int:
    # Your code here
    pass`,
    java: `class Solution {
    public int carFleet(int target, int[] position, int[] speed) {
        // Your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int carFleet(int target, vector<int>& position, vector<int>& speed) {
        // Your code here
        return 0;
    }
};`,
    javascript: `function carFleet(target, position, speed) {
    // Your code here
    return 0;
}`,
    typescript: `function carFleet(target: number, position: number[], speed: number[]): number {
    // Your code here
    return 0;
}`,
    rust: `fn car_fleet(target: i32, position: Vec<i32>, speed: Vec<i32>) -> i32 {
    // Your code here
    0
}`,
    c: `int carFleet(int target, int* position, int positionSize, int* speed, int speedSize) {
    // Your code here
    return 0;
}`,
    csharp: `public class Solution {
    public int CarFleet(int target, int[] position, int[] speed) {
        // Your code here
        return 0;
    }
}`,
    go: `func carFleet(target int, position []int, speed []int) int {
    // Your code here
    return 0
}`,
  }}
  harness={{
    python: `import sys

if __name__ == "__main__":
    data = sys.stdin.read().strip().splitlines()
    target = int(data[0].strip())
    n = int(data[1].strip())
    position = list(map(int, data[2].strip().split()))
    speed = list(map(int, data[3].strip().split()))
    print("###SYPHER_JUDGE0_RESULT###")
    print(carFleet(target, position, speed))`,
    java: `import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int target = Integer.parseInt(sc.nextLine().trim());
        int n = Integer.parseInt(sc.nextLine().trim());
        String[] posStr = sc.nextLine().trim().split(" ");
        String[] spdStr = sc.nextLine().trim().split(" ");
        int[] position = new int[posStr.length];
        int[] speed = new int[spdStr.length];
        for (int i = 0; i < posStr.length; i++) {
            position[i] = Integer.parseInt(posStr[i]);
            speed[i] = Integer.parseInt(spdStr[i]);
        }
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.println(new Solution().carFleet(target, position, speed));
    }
}`,
    cpp: `#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <sstream>
using namespace std;

int main() {
    string line;
    getline(cin, line); int target = stoi(line);
    getline(cin, line); int n = stoi(line);
    getline(cin, line); stringstream ss1(line);
    vector<int> position, speed;
    int val;
    while (ss1 >> val) position.push_back(val);
    getline(cin, line); stringstream ss2(line);
    while (ss2 >> val) speed.push_back(val);
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    Solution sol;
    cout << sol.carFleet(target, position, speed) << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
const lines = [];
rl.on('line', (l) => lines.push(l));
rl.on('close', () => {
    const target = parseInt(lines[0]);
    const n = parseInt(lines[1]);
    const position = lines[2].trim().split(' ').map(Number);
    const speed = lines[3].trim().split(' ').map(Number);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(carFleet(target, position, speed));
});`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
const lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    const target = parseInt(lines[0]);
    const n = parseInt(lines[1]);
    const position = lines[2].trim().split(' ').map(Number);
    const speed = lines[3].trim().split(' ').map(Number);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(carFleet(target, position, speed));
});`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let lines: Vec<String> = stdin.lock().lines().map(|l| l.unwrap()).collect();
    let target: i32 = lines[0].trim().parse().unwrap();
    let n: i32 = lines[1].trim().parse().unwrap();
    let position: Vec<i32> = lines[2].trim().split(' ').map(|x| x.parse().unwrap()).collect();
    let speed: Vec<i32> = lines[3].trim().split(' ').map(|x| x.parse().unwrap()).collect();
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", car_fleet(target, position, speed));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>

int main() {
    int target, n;
    scanf("%d\\n%d\\n", &target, &n);
    int* position = malloc(n * sizeof(int));
    int* speed = malloc(n * sizeof(int));
    for (int i = 0; i < n; i++) scanf("%d", &position[i]);
    for (int i = 0; i < n; i++) scanf("%d", &speed[i]);
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    printf("%d\\n", carFleet(target, position, n, speed, n));
    free(position); free(speed);
    return 0;
}`,
    csharp: `using System;
using System.Linq;

public class Program {
    public static void Main() {
        int target = int.Parse(Console.ReadLine().Trim());
        int n = int.Parse(Console.ReadLine().Trim());
        int[] position = Console.ReadLine().Trim().Split(' ').Select(int.Parse).ToArray();
        int[] speed = Console.ReadLine().Trim().Split(' ').Select(int.Parse).ToArray();
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(new Solution().CarFleet(target, position, speed));
    }
}`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "sort"
    "strconv"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    lines := make([]string, 4)
    for i := 0; i < 4; i++ { lines[i], _ = reader.ReadString('\\n') }
    target, _ := strconv.Atoi(strings.TrimSpace(lines[0]))
    n, _ := strconv.Atoi(strings.TrimSpace(lines[1]))
    posParts := strings.Fields(lines[2])
    spdParts := strings.Fields(lines[3])
    position := make([]int, n)
    speed := make([]int, n)
    for i := 0; i < n; i++ {
        position[i], _ = strconv.Atoi(posParts[i])
        speed[i], _ = strconv.Atoi(spdParts[i])
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(carFleet(target, position, speed))
}`,
  }}
  defaultLanguage="python"
>

# Car Fleet

There are `n` cars going to the same destination along a one-lane road. The destination is `target` miles away.

Each car has a `position[i]` (in miles from the origin) and a `speed[i]` (in miles per hour). A car can never pass another car ahead of it, but it can catch up to it and drive bumper to bumper at the same speed — forming a **car fleet**.

A car fleet is a non-empty set of cars driving at the same position and same speed. A single car is also a car fleet.

Return the number of car fleets that will arrive at the destination.

## Examples

**Example 1:**
```
Input: target = 12, position = [10, 8, 0, 5, 3], speed = [2, 4, 1, 1, 3]
Output: 3
```

**Example 2:**
```
Input: target = 10, position = [1, 4, 0], speed = [3, 2, 4]
Output: 1
Explanation: The car at position 4 (speed 2) and the car at position 1 (speed 3) both reach the target at t = 3, so they merge into one fleet. The car at position 0 (speed 4) reaches at t = 2.5, which is sooner than that fleet's time, so it also merges in. All three arrive together as a single fleet.
```

## Constraints

- `n == position.length == speed.length`
- `1 <= n <= 10^5`
- `0 < target <= 10^6`
- `0 <= position[i] < target`
- `0 < speed[i] <= 10^6`

## Hints

<details>
<summary>Hint 1</summary>

Sort cars by position descending (closest to destination first). Calculate the time each car takes to reach the destination.
</details>

<details>
<summary>Hint 2</summary>

A car farther from the target that reaches it in less time than the car ahead will catch up and form a fleet. Track the maximum time seen so far.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../../solutions/medium/car-fleet)** for a full explanation and multiple approaches.

## Next

Continue with [Text Justification](../hard/text-justification) for a hard simulation problem.