---
title: Asteroid Collision
sidebar_label: Asteroid Collision
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/simulation/asteroid-collision', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '5 10 -5', expectedOutput: '5 10\n' , isSample: true },
    { stdin: '8 -8', expectedOutput: '\n' , isSample: true },
    { stdin: '10 2 -5', expectedOutput: '10\n' },
    { stdin: '-2 -1 1 2', expectedOutput: '-2 -1 1 2\n' },
    { stdin: '1 -1', expectedOutput: '\n' },
    { stdin: '-5 5', expectedOutput: '-5 5\n' },
    { stdin: '3 2 -4 5', expectedOutput: '-4 5\n' },
    { stdin: '1 2 3 -3 -2 -1', expectedOutput: '\n' },
    { stdin: '10 -10 10', expectedOutput: '10\n' },
    { stdin: '1 2 3 4 5 -10', expectedOutput: '-10\n' },
    { stdin: '10 20 -30', expectedOutput: '-30\n' },
    { stdin: '100 50 -200 -150', expectedOutput: '-200 -150\n' },
    { stdin: '5 4 3 2 1 -1 -2 -3 -4 -5', expectedOutput: '\n' },
    { stdin: '10 -1 -2 -3 -4', expectedOutput: '10\n' },
    { stdin: '1 2 -3 4 -5', expectedOutput: '-3 -5\n' },
    { stdin: '-1 -2 -3 4 5 6', expectedOutput: '-1 -2 -3 4 5 6\n' },
    { stdin: '8 3 -6 2 -7', expectedOutput: '8\n' },
    { stdin: '7 6 5 -10', expectedOutput: '-10\n' },
    { stdin: '1 -2 3 -4 5 -6', expectedOutput: '-2 -4 -6\n' },
    { stdin: '2 4 6 -8', expectedOutput: '-8\n' },
  ]}
  starterCode={{
    python: `def asteroidCollision(asteroids: list[int]) -> list[int]:
    # Your code here
    pass`,
    java: `class Solution {
    public int[] asteroidCollision(int[] asteroids) {
        // Your code here
        return new int[]{};
    }
}`,
    cpp: `class Solution {
public:
    vector<int> asteroidCollision(vector<int>& asteroids) {
        // Your code here
        return {};
    }
};`,
    javascript: `function asteroidCollision(asteroids) {
    // Your code here
    return [];
}`,
    typescript: `function asteroidCollision(asteroids: number[]): number[] {
    // Your code here
    return [];
}`,
    rust: `fn asteroid_collision(asteroids: Vec<i32>) -> Vec<i32> {
    // Your code here
    vec![]
}`,
    c: `int* asteroidCollision(int* asteroids, int asteroidsSize, int* returnSize) {
    // Your code here
    *returnSize = 0;
    return NULL;
}`,
    csharp: `public class Solution {
    public int[] AsteroidCollision(int[] asteroids) {
        // Your code here
        return new int[]{};
    }
}`,
    go: `func asteroidCollision(asteroids []int) []int {
    // Your code here
    return nil
}`,
  }}
  harness={{
    python: `import sys

if __name__ == "__main__":
    asteroids = list(map(int, sys.stdin.readline().strip().split()))
    result = asteroidCollision(asteroids)
    print("###SYPHER_JUDGE0_RESULT###")
    if result:
        print(" ".join(map(str, result)))
    else:
        print()`,
    java: `import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String[] parts = sc.nextLine().trim().split(" ");
        int[] asteroids = new int[parts.length];
        for (int i = 0; i < parts.length; i++) asteroids[i] = Integer.parseInt(parts[i]);
        int[] result = new Solution().asteroidCollision(asteroids);
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < result.length; i++) {
            if (i > 0) sb.append(" ");
            sb.append(result[i]);
        }
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.println(sb.toString());
    }
}`,
    cpp: `#include <iostream>
#include <vector>
#include <string>
#include <sstream>
using namespace std;

int main() {
    string line;
    getline(cin, line);
    stringstream ss(line);
    vector<int> asteroids;
    int val;
    while (ss >> val) asteroids.push_back(val);
    Solution sol;
    vector<int> result = sol.asteroidCollision(asteroids);
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    for (int i = 0; i < result.size(); i++) {
        if (i > 0) cout << " ";
        cout << result[i];
    }
    cout << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line) => {
    const asteroids = line.trim().split(' ').filter(s => s).map(Number);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(asteroidCollision(asteroids).join(' '));
    rl.close();
});`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const asteroids = line.trim().split(' ').filter(s => s).map(Number);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(asteroidCollision(asteroids).join(' '));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let asteroids: Vec<i32> = line.trim().split(' ')
        .filter(|s| !s.is_empty())
        .map(|x| x.parse().unwrap()).collect();
    let result = asteroid_collision(asteroids);
    let output: Vec<String> = result.iter().map(|x| x.to_string()).collect();
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", output.join(" "));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>

int main() {
    int asteroids[10000], n = 0;
    while (scanf("%d", &asteroids[n]) == 1) { n++; }
    int returnSize;
    int* result = asteroidCollision(asteroids, n, &returnSize);
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    for (int i = 0; i < returnSize; i++) {
        if (i > 0) printf(" ");
        printf("%d", result[i]);
    }
    printf("\\n");
    free(result);
    return 0;
}`,
    csharp: `using System;
using System.Collections.Generic;
using System.Linq;

public class Program {
    public static void Main() {
        int[] asteroids = Console.ReadLine().Trim().Split(' ')
            .Where(s => s.Length > 0).Select(int.Parse).ToArray();
        int[] result = new Solution().AsteroidCollision(asteroids);
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(string.Join(" ", result));
    }
}`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    parts := strings.Fields(line)
    asteroids := make([]int, len(parts))
    for i, s := range parts {
        asteroids[i], _ = strconv.Atoi(s)
    }
    result := asteroidCollision(asteroids)
    out := make([]string, len(result))
    for i, v := range result {
        out[i] = strconv.Itoa(v)
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(strings.Join(out, " "))
}`,
  }}
  defaultLanguage="python"
>

# Asteroid Collision

We are given an array `asteroids` of integers representing asteroids in a row. For each asteroid, the absolute value represents its size, and the sign represents its direction (positive = right, negative = left). Each asteroid moves at the same speed.

Find out the state of the asteroids after all collisions. If two asteroids meet, the larger one destroys the smaller one. If they are the same size, both are destroyed. Two asteroids moving in the same direction never meet.

## Examples

**Example 1:**
```
Input: asteroids = [5, 10, -5]
Output: [5, 10]
Explanation: 10 and -5 collide, 10 > 5 so -5 is destroyed. 5 and 10 never collide (same direction).
```

**Example 2:**
```
Input: asteroids = [8, -8]
Output: []
Explanation: Both asteroids are the same size and destroy each other.
```

**Example 3:**
```
Input: asteroids = [10, 2, -5]
Output: [10]
Explanation: 2 and -5 collide, -5 > 2 so 2 is destroyed. Then 10 and -5 collide, 10 > 5 so -5 is destroyed.
```

## Constraints

- `2 <= asteroids.length <= 10^4`
- `-1000 <= asteroids[i] <= 1000`
- `asteroids[i] != 0`

## Hints

<details>
<summary>Hint 1</summary>

Use a stack. Compare each asteroid with the top of the stack. They only collide if the top is moving right (+) and the new asteroid is moving left (-).
</details>

<details>
<summary>Hint 2</summary>

When a moving-right asteroid is smaller than the moving-left asteroid, pop it and continue checking. This is a simulation of cascading collisions.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../../solutions/medium/asteroid-collision)** for a full explanation and multiple approaches.

## Next

Continue with [Car Fleet](./car-fleet) for another medium simulation problem.