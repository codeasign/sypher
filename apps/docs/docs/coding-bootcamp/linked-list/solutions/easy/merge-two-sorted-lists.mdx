---
title: Merge Two Sorted Lists — Solution
sidebar_label:   Solution
---

\u0069mport Tabs from '@theme/Tabs';
\u0069mport TabItem from '@theme/TabItem';

# Merge Two Sorted Lists — Solution

## Problem Overview

Merge two sorted linked lists into one sorted list by splicing nodes. This is a classic **two-pointer** linked list problem.

## Approach: Iterative with Dummy Head

**Idea:** Use a dummy head node to simplify edge cases. Compare nodes from both lists, appending the smaller one to the result.

**Algorithm:**
1. Create a dummy head node and a `tail` pointer starting at dummy.
2. While both lists are non-empty:
   - Compare `list1.val` and `list2.val`.
   - Append the smaller node to `tail.next` and advance that list.
   - Advance `tail`.
3. Append any remaining nodes from the non-empty list.
4. Return `dummy.next`.

**Complexity Analysis:**
- **Time:** O(n + m) — each node is visited once.
- **Space:** O(1) — only pointers used.

## Code

<Tabs>
  <TabItem value="python" label="Python">

```python
def merge_two_lists(list1: list[int], list2: list[int]) -> list[int]:
    # Simple array merge for the array-based harness
    result = []
    i = j = 0
    while i < len(list1) and j < len(list2):
        if list1[i] <= list2[j]:
            result.append(list1[i])
            i += 1
        else:
            result.append(list2[j])
            j += 1
    # Append remaining elements
    result.extend(list1[i:])
    result.extend(list2[j:])
    return result
```

  </TabItem>
  <TabItem value="java" label="Java">

```java
class Solution {
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        // Dummy head avoids special case for the first node
        ListNode dummy = new ListNode(0);
        ListNode tail = dummy;

        // Compare and append the smaller node
        while (list1 != null && list2 != null) {
            if (list1.val <= list2.val) {
                tail.next = list1;
                list1 = list1.next;
            } else {
                tail.next = list2;
                list2 = list2.next;
            }
            tail = tail.next;
        }

        // Append remaining nodes from the non-empty list
        tail.next = (list1 != null) ? list1 : list2;

        return dummy.next;  // head of the merged list
    }
}
```

  </TabItem>
  <TabItem value="cpp" label="C++">

```cpp
class Solution {
public:
    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {
        // Dummy head avoids special case for the first node
        ListNode dummy(0);
        ListNode* tail = &dummy;

        // Compare and append the smaller node
        while (list1 && list2) {
            if (list1->val <= list2->val) {
                tail->next = list1;
                list1 = list1->next;
            } else {
                tail->next = list2;
                list2 = list2->next;
            }
            tail = tail->next;
        }

        // Append remaining nodes
        tail->next = list1 ? list1 : list2;

        return dummy.next;  // head of the merged list
    }
};
```

  </TabItem>
  <TabItem value="javascript" label="JavaScript">

```javascript
function mergeTwoLists(list1, list2) {
    // Simple array merge for the array-based harness
    const result = [];
    let i = 0, j = 0;
    while (i < list1.length && j < list2.length) {
        if (list1[i] <= list2[j]) {
            result.push(list1[i++]);
        } else {
            result.push(list2[j++]);
        }
    }
    // Append remaining elements
    while (i < list1.length) result.push(list1[i++]);
    while (j < list2.length) result.push(list2[j++]);
    return result;
}
```

  </TabItem>
  <TabItem value="typescript" label="TypeScript">

```typescript
function mergeTwoLists(list1: number[], list2: number[]): number[] {
    // Simple array merge for the array-based harness
    const result: number[] = [];
    let i = 0, j = 0;
    while (i < list1.length && j < list2.length) {
        if (list1[i] <= list2[j]) {
            result.push(list1[i++]);
        } else {
            result.push(list2[j++]);
        }
    }
    // Append remaining elements
    while (i < list1.length) result.push(list1[i++]);
    while (j < list2.length) result.push(list2[j++]);
    return result;
}
```

  </TabItem>
  <TabItem value="rust" label="Rust">

```rust
pub fn merge_two_lists(list1: Vec<i32>, list2: Vec<i32>) -> Vec<i32> {
    // Simple array merge for the array-based harness
    let mut result = Vec::with_capacity(list1.len() + list2.len());
    let mut i = 0;
    let mut j = 0;
    while i < list1.len() && j < list2.len() {
        if list1[i] <= list2[j] {
            result.push(list1[i]);
            i += 1;
        } else {
            result.push(list2[j]);
            j += 1;
        }
    }
    result.extend_from_slice(&list1[i..]);
    result.extend_from_slice(&list2[j..]);
    result
}
```

  </TabItem>
  <TabItem value="c" label="C">

```c
struct ListNode* mergeTwoLists(struct ListNode* list1, struct ListNode* list2) {
    // Dummy head avoids special case for the first node
    struct ListNode dummy;
    dummy.next = NULL;
    struct ListNode* tail = &dummy;

    // Compare and append the smaller node
    while (list1 && list2) {
        if (list1->val <= list2->val) {
            tail->next = list1;
            list1 = list1->next;
        } else {
            tail->next = list2;
            list2 = list2->next;
        }
        tail = tail->next;
    }

    // Append remaining nodes
    tail->next = list1 ? list1 : list2;

    return dummy.next;  // head of the merged list
}
```

  </TabItem>
  <TabItem value="csharp" label="C#">

```csharp
public class Solution {
    public ListNode MergeTwoLists(ListNode list1, ListNode list2) {
        // Dummy head avoids special case for the first node
        ListNode dummy = new ListNode(0);
        ListNode tail = dummy;

        // Compare and append the smaller node
        while (list1 != null && list2 != null) {
            if (list1.val <= list2.val) {
                tail.next = list1;
                list1 = list1.next;
            } else {
                tail.next = list2;
                list2 = list2.next;
            }
            tail = tail.next;
        }

        // Append remaining nodes from the non-empty list
        tail.next = (list1 != null) ? list1 : list2;

        return dummy.next;  // head of the merged list
    }
}
```

  </TabItem>
  <TabItem value="go" label="Go">

```go
func mergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {
    // Dummy head avoids special case for the first node
    dummy := &ListNode{}
    tail := dummy

    // Compare and append the smaller node
    for list1 != nil && list2 != nil {
        if list1.Val <= list2.Val {
            tail.Next = list1
            list1 = list1.Next
        } else {
            tail.Next = list2
            list2 = list2.Next
        }
        tail = tail.Next
    }

    // Append remaining nodes
    if list1 != nil {
        tail.Next = list1
    } else {
        tail.Next = list2
    }

    return dummy.Next // head of the merged list
}
```

  </TabItem>
</Tabs>

## Key Takeaways

- **Dummy head pattern**: Always use a dummy node when building a linked list to avoid null-checking the head.
- **Two-pointer merge**: The standard merge algorithm from Merge Sort, applied to linked lists.
- **Recursive solution**: This can also be solved recursively: `merge(l1, l2) = l1.next = merge(l1.next, l2)` if `l1.val < l2.val`.

## Back to Problem

[← Back to Problem](../../exercises/easy/merge-two-sorted-lists)