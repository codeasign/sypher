---
title: Maximum Subarray
sidebar_label: Max Subarray
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/arrays/maximum-subarray', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '-2 1 -3 4 -1 2 1 -5 4\n', expectedOutput: '6\n' },
    { stdin: '1\n', expectedOutput: '1\n' },
    { stdin: '5 4 -1 7 8\n', expectedOutput: '23\n' },
    { stdin: '-1\n', expectedOutput: '-1\n' },
    { stdin: '-2 -1\n', expectedOutput: '-1\n' },
    { stdin: '3 -2 5 -1\n', expectedOutput: '6\n' },
    { stdin: '-2 1 -3 4 -1 2 1 -5 4\n', expectedOutput: '6\n' },
    { stdin: '1 2 3 4 5\n', expectedOutput: '15\n' },
    { stdin: '-5 -4 -3 -2 -1\n', expectedOutput: '-1\n' },
    { stdin: '0 0 0 0\n', expectedOutput: '0\n' },
    { stdin: '8 -19 5 -4 20\n', expectedOutput: '21\n' },
    { stdin: '1 -1 1 -1 1\n', expectedOutput: '1\n' },
    { stdin: '-2 -3 4 -1 -2 1 5 -3\n', expectedOutput: '7\n' },
    { stdin: '2 -1 2 -1 2 -1 2\n', expectedOutput: '5\n' },
    { stdin: '10 -5 -5 10 -5 -5 10\n', expectedOutput: '10\n' },
    { stdin: '-100 -50 10 20 30 -100 5\n', expectedOutput: '60\n' },
    { stdin: '1 2 3 -10 1 2 3\n', expectedOutput: '6\n' },
    { stdin: '-1 -2 3 4 -5 6\n', expectedOutput: '8\n' },
    { stdin: '5 -2 3 -1 2 -3 4 -1 1\n', expectedOutput: '8\n' },
    { stdin: '2 -5 3 -2 4 -1 2\n', expectedOutput: '6\n' },
  ]}
  starterCode={{
    python: `def max_subarray(nums: list[int]) -> int:
    # Your code here
    pass`,
    java: `class Solution {
    public int maxSubArray(int[] nums) {
        // Your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        // Your code here
        return 0;
    }
};`,
    javascript: `function maxSubArray(nums) {
    // Your code here
}`,
    typescript: `function maxSubArray(nums: number[]): number {
    // Your code here
    return 0;
}`,
    rust: `pub fn max_sub_array(nums: Vec<i32>) -> i32 {
    // Your code here
    0
}`,
    c: `int maxSubArray(int* nums, int numsSize) {
    // Your code here
    return 0;
}`,
    csharp: `public class Solution {
    public int MaxSubArray(int[] nums) {
        // Your code here
        return 0;
    }
}`,
    go: `func maxSubArray(nums []int) int {
    // Your code here
    return 0
}`,
  }}
  harness={{
    python: `import sys
nums = [int(x) for x in sys.stdin.read().strip().split()]
print(max_subarray(nums))`,
    java: `import java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int[] nums = Arrays.stream(sc.nextLine().split(" ")).mapToInt(Integer::parseInt).toArray();
        Solution sol = new Solution();
        System.out.println(sol.maxSubArray(nums));
    }
}`,
    cpp: `#include <iostream>
#include <sstream>
#include <vector>
using namespace std;
int main() {
    vector<int> nums; int v; while (cin >> v) nums.push_back(v);
    Solution sol; cout << sol.maxSubArray(nums) << endl; return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => { console.log(maxSubArray(line.trim().split(' ').map(Number))); rl.close(); });`,
    typescript: `import * as readline from 'readline';
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => { console.log(maxSubArray(line.trim().split(' ').map(Number))); rl.close(); });`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let nums: Vec<i32> = line.split(' ').map(|s| s.parse().unwrap()).collect();
    println!("{}", max_sub_array(nums));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    char line[100000]; fgets(line, 100000, stdin);
    int nums[10000], n = 0; char* t = strtok(line, " \\n");
    while (t) { nums[n++] = atoi(t); t = strtok(NULL, " \\n"); }
    printf("%d\\n", maxSubArray(nums, n)); return 0;
}`,
    csharp: `using System; using System.Linq;
class Program {
    static void Main() {
        int[] nums = Console.ReadLine().Split(' ').Select(int.Parse).ToArray();
        Solution sol = new Solution(); Console.WriteLine(sol.MaxSubArray(nums));
    }
}`,
    go: `package main
\u0069mport ("fmt")
func main() {
    var nums []int; var v int;
    for { if _, err := fmt.Scan(&v); err != nil { break }; nums = append(nums, v) }
    fmt.Println(maxSubArray(nums))
}`,
  }}
  defaultLanguage="python"
>

# Maximum Subarray

**Difficulty:** Medium

Given an integer array `nums`, find the **subarray** with the largest sum, and return **its sum**.

## Example 1

**Input:** `nums = [-2, 1, -3, 4, -1, 2, 1, -5, 4]`  
**Output:** `6`  
**Explanation:** The subarray `[4, -1, 2, 1]` has the largest sum 6.

## Example 2

**Input:** `nums = [1]`  
**Output:** `1`  

## Example 3

**Input:** `nums = [5, 4, -1, 7, 8]`  
**Output:** `23`  

## Input Specification

- A single line of space-separated integers.
- `1 <= nums.length <= 10^5`

## Output Specification

Print a single integer: the maximum subarray sum.

## Constraints

- `1 <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`

## Hints

<details>
<summary>Hint 1</summary>

Kadane's Algorithm: maintain a running sum and reset it to the current element whenever it becomes negative.
</details>

<details>
<summary>Hint 2</summary>

At each position, decide whether to extend the existing subarray or start a new one from the current element.
</details>

<details>
<summary>Hint 3</summary>

Track both the current subarray sum and the global maximum. The answer is the highest global maximum seen.
</details>

</ProblemEditor>

---
**📖 View the [detailed solution](../solutions/medium/maximum-subarray)** for a step-by-step walkthrough.