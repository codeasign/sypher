---
title: Best Time to Buy and Sell Stock
sidebar_label: Best Time to Buy and Sell Stock
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/arrays/best-time-to-buy-and-sell-stock', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '7 1 5 3 6 4\n', expectedOutput: '5\n' },
    { stdin: '7 6 4 3 1\n', expectedOutput: '0\n' },
    { stdin: '1 2 3 4 5\n', expectedOutput: '4\n' },
    { stdin: '5 4 3 2 1\n', expectedOutput: '0\n' },
    { stdin: '3 3 3 3 3\n', expectedOutput: '0\n' },
    { stdin: '1\n', expectedOutput: '0\n' },
    { stdin: '2 1\n', expectedOutput: '0\n' },
    { stdin: '1 2\n', expectedOutput: '1\n' },
    { stdin: '2 4 1\n', expectedOutput: '2\n' },
    { stdin: '3 2 6 5 0 3\n', expectedOutput: '4\n' },
    { stdin: '10 9 8 7 6 5 4\n', expectedOutput: '0\n' },
    { stdin: '1 100 2 200 3 300\n', expectedOutput: '299\n' },
    { stdin: '5 10 5 10 5 10\n', expectedOutput: '5\n' },
    { stdin: '7 2 5 1 3 6 4\n', expectedOutput: '5\n' },
    { stdin: '100 80 120 60 140 40 160\n', expectedOutput: '120\n' },
    { stdin: '1 1 1 1 1\n', expectedOutput: '0\n' },
    { stdin: '2 2 2 2 2\n', expectedOutput: '0\n' },
    { stdin: '3 5 1 7 4 9 2\n', expectedOutput: '8\n' },
    { stdin: '10 20 30 40 50 25 15\n', expectedOutput: '40\n' },
    { stdin: '5 2 10 1 12 8\n', expectedOutput: '11\n' },
  ]}
  starterCode={{
    python: `def max_profit(prices: list) -> int:
    pass`,
    java: `class Solution {
    public int maxProfit(int[] prices) {
        // your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int maxProfit(vector<int>& prices) {
        // your code here
        return 0;
    }
};`,
    javascript: `function maxProfit(prices) {
    // your code here
}`,
    typescript: `function maxProfit(prices: number[]): number {
    // your code here
    return 0;
}`,
    rust: `fn max_profit(prices: &Vec<i32>) -> i32 {
    // your code here
    0
}`,
    c: `int maxProfit(int* prices, int pricesSize) {
    // your code here
    return 0;
}`,
    csharp: `class Solution {
    public int MaxProfit(int[] prices) {
        // your code here
        return 0;
    }
}`,
    go: `func maxProfit(prices []int) int {
    // your code here
    return 0
}`,
  }}
  harness={{
    python: `import sys
prices = list(map(int, sys.stdin.read().strip().split()))
result = max_profit(prices)
print(result)`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String line = sc.nextLine();
        String[] parts = line.split(" ");
        int[] prices = new int[parts.length];
        for (int i = 0; i < parts.length; i++) prices[i] = Integer.parseInt(parts[i]);
        Solution sol = new Solution();
        int result = sol.maxProfit(prices);
        System.out.println(result);
    }
}`,
    cpp: `int main() {
    vector<int> prices;
    int val;
    while (cin >> val) prices.push_back(val);
    Solution sol;
    int res = sol.maxProfit(prices);
    cout << res << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const prices = line.split(' ').map(Number);
    console.log(maxProfit(prices));
    rl.close();
});`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const prices = line.split(' ').map(Number);
    console.log(maxProfit(prices));
    rl.close();
});`,
    rust: `fn main() {
    let mut input = String::new();
    std::io::stdin().read_line(&mut input).unwrap();
    let prices: Vec<i32> = input.trim().split_whitespace()
        .map(|s| s.parse().unwrap()).collect();
    let result = max_profit(&prices);
    println!("{}", result);
}`,
    c: `#include <stdio.h>
int main() {
    int prices[10000];
    int n = 0;
    char line[100000];
    fgets(line, sizeof(line), stdin);
    char *p = line;
    while (sscanf(p, "%d", &prices[n]) == 1) {
        n++;
        while (*p && *p != ' ') p++;
        if (*p == ' ') p++;
    }
    int res = maxProfit(prices, n);
    printf("%d\n", res);
    return 0;
}`,
    csharp: `class Program {
    static void Main() {
        string[] parts = System.Console.ReadLine().Split();
        int[] prices = new int[parts.Length];
        for (int i = 0; i < parts.Length; i++) prices[i] = int.Parse(parts[i]);
        Solution sol = new Solution();
        int res = sol.MaxProfit(prices);
        System.Console.WriteLine(res);
    }
}`,
    go: `package main
\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    parts := strings.Fields(scanner.Text())
    prices := make([]int, len(parts))
    for i, s := range parts {
        prices[i], _ = strconv.Atoi(s)
    }
    result := maxProfit(prices)
    fmt.Println(result)
}`,
  }}
  defaultLanguage="python"
>

# Best Time to Buy and Sell Stock

**Difficulty:** Easy

You are given an array `prices` where `prices[i]` is the price of a given stock on the `i`-th day.

You want to maximize your profit by choosing a **single day** to buy one stock and choosing a **different day in the future** to sell that stock.

Return the maximum profit you can achieve from this transaction. If you cannot achieve any profit, return `0`.

## Example 1

**Input:** `prices = [7, 1, 5, 3, 6, 4]`  
**Output:** `5`  
**Explanation:** Buy on day 2 (price = 1) and sell on day 5 (price = 6), profit = 6 - 1 = 5. Note that buying on day 2 and selling on day 1 is not allowed because you must buy before you sell.

## Example 2

**Input:** `prices = [7, 6, 4, 3, 1]`  
**Output:** `0`  
**Explanation:** In this case, no transactions are done and the max profit = 0.

## Input Specification

The input line contains space-separated integers representing the array `prices`.

## Output Specification

Print a single integer representing the maximum profit possible.

## Constraints

- `1 <= prices.length <= 10^5`
- `0 <= prices[i] <= 10^4`

## Hints

<details>
<summary>Hint 1</summary>

The brute-force approach checks every pair of buy/sell days — O(n^2) time. Can you do it in one pass?

</details>

<details>
<summary>Hint 2</summary>

Track the minimum price seen so far as you iterate. For each day, compute the profit if you sold at the current price.

</details>

<details>
<summary>Hint 3</summary>

Keep a running maximum of the profit. Update the minimum price whenever you see a lower price.

</details>

</ProblemEditor>

---

**View the [detailed solution](../../solutions/easy/best-time-to-buy-and-sell-stock)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Product of Array Except Self](../medium/product-of-array-except-self).