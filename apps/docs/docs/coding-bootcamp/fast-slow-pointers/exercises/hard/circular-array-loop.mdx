---
title: Circular Array Loop
sidebar_label: Circular Array Loop
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/fast-slow-pointers/circular-array-loop', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '2 -1 1 2 2', expectedOutput: 'true\n' , isSample: true },
    { stdin: '-1 -2 -3 -4 -5 6', expectedOutput: 'false\n' , isSample: true },
    { stdin: '1 -1 2', expectedOutput: 'false\n' },
    { stdin: '1 1 1 1 1', expectedOutput: 'true\n' },
    { stdin: '-1 -2 -3 -4 -5', expectedOutput: 'false\n' },
    { stdin: '2 2 -1 2', expectedOutput: 'true\n' },
    { stdin: '3 1 2', expectedOutput: 'true\n' },
    { stdin: '1 2 3 4 5', expectedOutput: 'true\n' },
    { stdin: '-2 -3 -1', expectedOutput: 'false\n' },
    { stdin: '2 1 -1 -2', expectedOutput: 'false\n' },
    { stdin: '1 1 2', expectedOutput: 'true\n' },
    { stdin: '-1 1', expectedOutput: 'false\n' },
    { stdin: '1 2 3 4 -1 -2 -3 -4', expectedOutput: 'false\n' },
    { stdin: '5 5 5 5 5', expectedOutput: 'false\n' },
    { stdin: '-5 -5 -5 -5 -5', expectedOutput: 'false\n' },
    { stdin: '1 2 -3 4', expectedOutput: 'false\n' },
    { stdin: '2 1 3 2', expectedOutput: 'true\n' },
    { stdin: '1', expectedOutput: 'false\n' },
    { stdin: '2 2 2 2', expectedOutput: 'true\n' },
    { stdin: '-2 -1 -2 -1', expectedOutput: 'true\n' },
  ]}
  starterCode={{
    python: `def circularArrayLoop(nums: list[int]) -> bool:
    # Your code here
    pass`,
    java: `class Solution {
    public boolean circularArrayLoop(int[] nums) {
        // Your code here
        return false;
    }
}`,
    cpp: `class Solution {
public:
    bool circularArrayLoop(vector<int>& nums) {
        // Your code here
        return false;
    }
};`,
    javascript: `function circularArrayLoop(nums) {
    // Your code here
    return false;
}`,
    typescript: `function circularArrayLoop(nums: number[]): boolean {
    // Your code here
    return false;
}`,
    rust: `fn circular_array_loop(nums: Vec<i32>) -> bool {
    // Your code here
    false
}`,
    c: `bool circularArrayLoop(int* nums, int numsSize) {
    // Your code here
    return false;
}`,
    csharp: `public class Solution {
    public bool CircularArrayLoop(int[] nums) {
        // Your code here
        return false;
    }
}`,
    go: `func circularArrayLoop(nums []int) bool {
    // Your code here
    return false
}`,
  }}
  harness={{
    python: `import sys

if __name__ == "__main__":
    nums = list(map(int, sys.stdin.readline().strip().split()))
    print("###SYPHER_JUDGE0_RESULT###")
    print("true" if circularArrayLoop(nums) else "false")`,
    java: `import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String[] parts = sc.nextLine().trim().split(" ");
        int[] nums = new int[parts.length];
        for (int i = 0; i < parts.length; i++) nums[i] = Integer.parseInt(parts[i]);
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.println(new Solution().circularArrayLoop(nums));
    }
}`,
    cpp: `#include <iostream>
#include <vector>
#include <string>
#include <sstream>
using namespace std;

int main() {
    string line;
    getline(cin, line);
    stringstream ss(line);
    vector<int> nums;
    int val;
    while (ss >> val) nums.push_back(val);
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    Solution sol;
    cout << (sol.circularArrayLoop(nums) ? "true" : "false") << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line) => {
    const nums = line.trim().split(' ').map(Number);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(circularArrayLoop(nums));
    rl.close();
});`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');

const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const nums = line.trim().split(' ').map(Number);
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(circularArrayLoop(nums));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let nums: Vec<i32> = line.trim().split(' ').map(|x| x.parse().unwrap()).collect();
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", circular_array_loop(nums));
}`,
    c: `#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int main() {
    int nums[1000], n = 0;
    while (scanf("%d", &nums[n]) == 1) { n++; }
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    printf("%s\\n", circularArrayLoop(nums, n) ? "true" : "false");
    return 0;
}`,
    csharp: `using System;
using System.Linq;

public class Program {
    public static void Main() {
        int[] nums = Console.ReadLine().Trim().Split(' ').Select(int.Parse).ToArray();
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(new Solution().CircularArrayLoop(nums).ToString().ToLower());
    }
}`,
    go: `package main

\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    parts := strings.Fields(line)
    nums := make([]int, len(parts))
    for i, s := range parts {
        nums[i], _ = strconv.Atoi(s)
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(circularArrayLoop(nums))
}`,
  }}
  defaultLanguage="python"
>

# Circular Array Loop

You are playing a game involving a **circular array** of non-zero integers `nums`. Each `nums[i]` denotes the number of indices forward/backward you must move if you are at index `i`:

- If `nums[i]` is positive, move `nums[i]` steps **forward**
- If `nums[i]` is negative, move `nums[i]` steps **backward**

Since the array is circular, you may assume that moving forward from the last element puts you on the first element, and moving backward from the first element puts you on the last element.

A **cycle** in the array consists of a sequence of indices `seq` of length at least 2 such that:

- Following the movement rules starting from `seq[0]` results in the sequence `seq[0] -> seq[1] -> ... -> seq[k-1] -> seq[0]`
- Every `nums[seq[j]]` is either **all positive** or **all negative**
- The cycle length is at least 2

Return `true` if there is a **cycle** in `nums`, otherwise return `false`.

## Examples

**Example 1:**
```
Input: nums = [2, -1, 1, 2, 2]
Output: true
Explanation: 0 -> 2 -> 3 -> 0 is a cycle of length 3, all positive.
```

**Example 2:**
```
Input: nums = [-1, -2, -3, -4, -5, 6]
Output: false
```

## Constraints

- `1 <= nums.length <= 5000`
- `-1000 <= nums[i] <= 1000`
- `nums[i] != 0`

## Hints

<details>
<summary>Hint 1</summary>

Use Floyd's cycle detection (fast and slow pointers), but also enforce that all elements in the cycle have the same direction (all positive or all negative).
</details>

<details>
<summary>Hint 2</summary>

The cycle must have length at least 2. If slow == fast and slow == next(slow), it's a self-loop of length 1, which is invalid.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../../solutions/hard/circular-array-loop)** for a full explanation and multiple approaches.

## Back to Theory

[Back to Fast & Slow Pointers Theory](../../index)