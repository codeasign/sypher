---
title: Minimum Window Substring
sidebar_label: Minimum Window Substring
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'sliding-window-minimum-window-substring', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
    testCases={[
    { stdin: 'ADOBECODEBANC\nABC\n', expectedOutput: 'BANC\n' },
    { stdin: 'a\na\n', expectedOutput: 'a\n' },
    { stdin: 'a\naa\n', expectedOutput: '\n' },
    { stdin: 'abc\nbc\n', expectedOutput: 'bc\n' },
    { stdin: 'ab\na\n', expectedOutput: 'a\n' },
    { stdin: 'ab\nb\n', expectedOutput: 'b\n' },
    { stdin: 'abc\nabc\n', expectedOutput: 'abc\n' },
    { stdin: 'xyz\nabc\n', expectedOutput: '\n' },
    { stdin: 'aaaaaa\naa\n', expectedOutput: 'aa\n' },
    { stdin: 'a\na\n', expectedOutput: 'a\n' },
    { stdin: 'ABCDEF\nDEF\n', expectedOutput: 'DEF\n' },
    { stdin: 'abcde\nxyz\n', expectedOutput: '\n' },
    { stdin: 'aa\naa\n', expectedOutput: 'aa\n' },
    { stdin: 'abcdef\nbdef\n', expectedOutput: 'bcdef\n' },
    { stdin: 'z\nz\n', expectedOutput: 'z\n' },
    { stdin: 'abcabdef\nbd\n', expectedOutput: 'bd\n' },
    { stdin: 'AAAAAA\nAAAAAA\n', expectedOutput: 'AAAAAA\n' },
    { stdin: 'bbbbbbb\na\n', expectedOutput: '\n' },
    { stdin: 'abcdabc\nabc\n', expectedOutput: 'abc\n' },
    { stdin: 'ADOBECODEBANCADOBECODEBANCADOBECODEBANCADOBECODEBANC\nABC\n', expectedOutput: 'BANC\n' },
  ]}
  starterCode={{
    python: `def min_window(s: str, t: str) -> str:
    # your code here
    pass
`,
    python27: `def min_window(s, t):
    # your code here
    pass
`,
    python35: `def min_window(s: str, t: str) -> str:
    # your code here
    pass
`,
    python36: `def min_window(s: str, t: str) -> str:
    # your code here
    pass
`,
    javascript: `function minWindow(s, t) {
    // your code here
}
`,
    java: `class Solution {
    public String minWindow(String s, String t) {
        // your code here
        return "";
    }
}
`,
    java8: `class Solution {
    public String minWindow(String s, String t) {
        // your code here
        return "";
    }
}
`,
    cpp: `class Solution {
public:
    string minWindow(string s, string t) {
        // your code here
        return "";
    }
};
`,
    cpp14: `class Solution {
public:
    string minWindow(string s, string t) {
        // your code here
        return "";
    }
};
`,
    cpp83: `class Solution {
public:
    string minWindow(string s, string t) {
        // your code here
        return "";
    }
};
`,
    c: `char* minWindow(char* s, char* t) {
    // your code here
    return "";
}
`,
    c_gcc7: `char* minWindow(char* s, char* t) {
    // your code here
    return "";
}
`,
    c_gcc8: `char* minWindow(char* s, char* t) {
    // your code here
    return "";
}
`,
    csharp: `class Solution {
    public string MinWindow(string s, string t) {
        // your code here
        return "";
    }
}
`,
    csharp_mono52: `class Solution {
    public string MinWindow(string s, string t) {
        // your code here
        return "";
    }
}
`,
    csharp_mono54: `class Solution {
    public string MinWindow(string s, string t) {
        // your code here
        return "";
    }
}
`,
    rust: `fn min_window(s: &str, t: &str) -> String {
    // your code here
    String::new()
}
`,
    rust120: `fn min_window(s: &str, t: &str) -> String {
    // your code here
    String::new()
}
`,
    typescript: `function minWindow(s: string, t: string): string {
    // your code here
    return "";
}
`,
    go: `func minWindow(s string, t string) string {
    // your code here
    return ""
}
`,
    kotlin: `class Solution {
    fun minWindow(s: String, t: String): String {
        // your code here
        return ""
    }
}
`,
  }}
  harness={{
    python: `import sys
lines = sys.stdin.read().strip().splitlines()
s = lines[0] if len(lines) > 0 else ''
t = lines[1] if len(lines) > 1 else ''
print(min_window(s, t))
`,
    python27: `import sys
lines = sys.stdin.read().strip().splitlines()
s = lines[0] if len(lines) > 0 else ''
t = lines[1] if len(lines) > 1 else ''
print min_window(s, t)
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const lines = [];
rl.on('line', line => lines.push(line));
rl.on('close', () => {
    const s = lines[0] || '';
    const t = lines[1] || '';
    console.log(minWindow(s, t));
});
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String s = sc.nextLine();
        String t = sc.nextLine();
        System.out.println(new Solution().minWindow(s, t));
    }
}
`,
    cpp: `int main() {
    string s, t;
    getline(cin, s);
    getline(cin, t);
    Solution sol;
    cout << sol.minWindow(s, t) << endl;
    return 0;
}
`,
    c: `int main() {
    char s[100005], t[100005];
    fgets(s, sizeof(s), stdin);
    s[strcspn(s, "\\n")] = 0;
    fgets(t, sizeof(t), stdin);
    t[strcspn(t, "\\n")] = 0;
    printf("%s\\n", minWindow(s, t));
    return 0;
}
`,
    csharp: `class Program {
    static void Main() {
        string s = Console.ReadLine();
        string t = Console.ReadLine();
        Console.WriteLine(new Solution().MinWindow(s, t));
    }
}
`,
    rust: `fn main() {
    let mut input = String::new();
    std::io::stdin().read_line(&mut input).unwrap();
    let s = input.trim().to_string();
    let mut input2 = String::new();
    std::io::stdin().read_line(&mut input2).unwrap();
    let t = input2.trim().to_string();
    println!("{}", min_window(&s, &t));
}
`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const lines: string[] = [];
rl.on('line', (line: string) => lines.push(line));
rl.on('close', () => {
    const s = lines[0] || '';
    const t = lines[1] || '';
    console.log(minWindow(s, t));
});
`,
    go: `package main
import (
    "bufio"
    "fmt"
    "os"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Buffer(make([]byte, 1024*1024), 1024*1024)
    scanner.Scan()
    s := scanner.Text()
    scanner.Scan()
    t := scanner.Text()
    fmt.Println(minWindow(s, t))
}
`,
    kotlin: `fun main() {
    val s = readLine()!!
    val t = readLine()!!
    val sol = Solution()
    println(sol.minWindow(s, t))
}
`,
  }}
  defaultLanguage="python"
>

# Minimum Window Substring

**Difficulty:** Hard

Given two strings `s` and `t`, return the **minimum window substring** of `s` such that every character in `t` (including duplicates) is included in the window. If there is no such substring, return the empty string `""`.

## Example 1

**Input:** `s = "ADOBECODEBANC"`, `t = "ABC"`  
**Output:** `"BANC"`  
**Explanation:** The minimum window substring `"BANC"` includes `'A'`, `'B'`, and `'C'` from string `t`.

## Example 2

**Input:** `s = "a"`, `t = "a"`  
**Output:** `"a"`  
**Explanation:** The entire string `"a"` is the minimum window.

## Example 3

**Input:** `s = "a"`, `t = "aa"`  
**Output:** `""`  
**Explanation:** Both `'a'`'s from `t` must be in the window. Since `s` only has one `'a'`, no valid window exists.

## Input Specification

The first line contains the string `s`. The second line contains the string `t`.

## Output Specification

Print the minimum window substring. If no such substring exists, print an empty line.

## Constraints

- `1 <= s.length, t.length <= 10⁵`
- `s` and `t` consist of uppercase and lowercase English letters

## Hints

<details>
<summary>Hint 1</summary>

Use a sliding window with two pointers. First expand the window (move right pointer) to include all characters of `t`, then contract the window (move left pointer) to find the minimum.
</details>

<details>
<summary>Hint 2</summary>

Use two frequency counters: one for the required characters in `t` and one for the current window. Track how many characters from `t` are satisfied in the current window.
</details>

<details>
<summary>Hint 3</summary>

When the window contains all characters of `t`, try to shrink it from the left while still satisfying the condition. Keep track of the minimum window length and its starting position.
</details>

</ProblemEditor>

---

**View the [detailed solution](../solutions/hard/minimum-window-substring)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Return to [Sliding Window Home](../../).