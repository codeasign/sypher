---
title: Implement Queue using Stacks
sidebar_label: Implement Queue using Stacks
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'implement-queue-using-stacks', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: 'push 1\npush 2\npop\npeek\nempty\n', expectedOutput: '1\n2\nfalse\n' , isSample: true },
    { stdin: 'push 1\npop\nempty\n', expectedOutput: '1\ntrue\n' , isSample: true },
    { stdin: 'push 1\npush 2\npush 3\npop\npop\npop\n', expectedOutput: '1\n2\n3\n' },
    { stdin: 'push 5\npush 10\npeek\npop\npeek\n', expectedOutput: '5\n5\n10\n' },
    { stdin: 'push 1\npush 2\npush 3\npush 4\npop\npop\npop\npop\n', expectedOutput: '1\n2\n3\n4\n' },
    { stdin: 'push 100\npush 200\npeek\npop\npush 300\npop\npop\n', expectedOutput: '100\n100\n200\n300\n' },
    { stdin: 'empty\npush 1\nempty\n', expectedOutput: 'true\nfalse\n' },
    { stdin: 'push 10\npush 20\npush 30\npop\npush 40\npop\npop\npop\n', expectedOutput: '10\n20\n30\n40\n' },
    { stdin: 'push 7\npush 8\npeek\npop\npeek\npop\nempty\n', expectedOutput: '7\n7\n8\n8\ntrue\n' },
    { stdin: 'push 1\npush 2\npush 3\npop\npush 4\npop\npush 5\npop\npop\npop\n', expectedOutput: '1\n2\n3\n4\n5\n' },
    { stdin: 'push 42\npop\npush 42\npop\npush 42\npop\n', expectedOutput: '42\n42\n42\n' },
    { stdin: 'push 1\npeek\npop\npush 2\npeek\npop\nempty\n', expectedOutput: '1\n1\n2\n2\ntrue\n' },
    { stdin: 'push 5\npush 6\npush 7\npop\npop\npush 8\npop\npop\n', expectedOutput: '5\n6\n7\n8\n' },
    { stdin: 'push 9\npush 10\npop\npeek\npop\n', expectedOutput: '9\n10\n10\n' },
    { stdin: 'push 1\npush 2\npush 3\npush 4\npush 5\npop\npop\npop\npop\npop\n', expectedOutput: '1\n2\n3\n4\n5\n' },
    { stdin: 'push 50\npush 60\npop\npeek\npop\nempty\n', expectedOutput: '50\n60\n60\ntrue\n' },
    { stdin: 'push 3\npush 3\npush 3\npop\npop\npop\nempty\n', expectedOutput: '3\n3\n3\ntrue\n' },
    { stdin: 'push 1\npush 2\npush 3\npop\npop\npush 4\npop\npush 5\npop\npop\n', expectedOutput: '1\n2\n3\n4\n5\n' },
    { stdin: 'push 0\npush -1\npush -2\npop\npeek\npop\npeek\npop\n', expectedOutput: '0\n-1\n-1\n-2\n-2\n' },
    { stdin: 'push 1000\npush 2000\npush 3000\npop\npop\npop\nempty\n', expectedOutput: '1000\n2000\n3000\ntrue\n' },
  ]}
  starterCode={{
    python: `class MyQueue:
    def __init__(self):
        # your code here
        pass

    def push(self, x: int) -> None:
        # your code here
        pass

    def pop(self) -> int:
        # your code here
        pass

    def peek(self) -> int:
        # your code here
        pass

    def empty(self) -> bool:
        # your code here
        pass
`,
    java: `import java.util.Stack;

class MyQueue {
    public MyQueue() {
        // your code here
    }

    public void push(int x) {
        // your code here
    }

    public int pop() {
        // your code here
        return -1;
    }

    public int peek() {
        // your code here
        return -1;
    }

    public boolean empty() {
        // your code here
        return false;
    }
}
`,
    cpp: `class MyQueue {
public:
    MyQueue() {
        // your code here
    }

    void push(int x) {
        // your code here
    }

    int pop() {
        // your code here
        return -1;
    }

    int peek() {
        // your code here
        return -1;
    }

    bool empty() {
        // your code here
        return false;
    }
};
`,
    javascript: `class MyQueue {
    constructor() {
        // your code here
    }

    push(x) {
        // your code here
    }

    pop() {
        // your code here
        return -1;
    }

    peek() {
        // your code here
        return -1;
    }

    empty() {
        // your code here
        return false;
    }
}
`,
    typescript: `class MyQueue {
    constructor() {
        // your code here
    }

    push(x: number): void {
        // your code here
    }

    pop(): number {
        // your code here
        return -1;
    }

    peek(): number {
        // your code here
        return -1;
    }

    empty(): boolean {
        // your code here
        return false;
    }
}
`,
    rust: `struct MyQueue {
    // your code here
}

impl MyQueue {
    fn new() -> Self {
        // your code here
        Self {}
    }

    fn push(&mut self, x: i32) {
        // your code here
    }

    fn pop(&mut self) -> i32 {
        // your code here
        -1
    }

    fn peek(&mut self) -> i32 {
        // your code here
        -1
    }

    fn empty(&self) -> bool {
        // your code here
        false
    }
}
`,
    c: `typedef struct {
    // your code here
} MyQueue;

MyQueue* myQueueCreate() {
    // your code here
    return NULL;
}

void myQueuePush(MyQueue* obj, int x) {
    // your code here
}

int myQueuePop(MyQueue* obj) {
    // your code here
    return -1;
}

int myQueuePeek(MyQueue* obj) {
    // your code here
    return -1;
}

bool myQueueEmpty(MyQueue* obj) {
    // your code here
    return false;
}

void myQueueFree(MyQueue* obj) {
    // your code here
}
`,
    csharp: `using System.Collections.Generic;

public class MyQueue {
    public MyQueue() {
        // your code here
    }

    public void Push(int x) {
        // your code here
    }

    public int Pop() {
        // your code here
        return -1;
    }

    public int Peek() {
        // your code here
        return -1;
    }

    public bool Empty() {
        // your code here
        return false;
    }
}
`,
    go: `type MyQueue struct {
    // your code here
}

func Constructor() MyQueue {
    // your code here
    return MyQueue{}
}

func (this *MyQueue) Push(x int) {
    // your code here
}

func (this *MyQueue) Pop() int {
    // your code here
    return -1
}

func (this *MyQueue) Peek() int {
    // your code here
    return -1
}

func (this *MyQueue) Empty() bool {
    // your code here
    return false
}
`,
  }}
  harness={{
    python: `import sys

q = MyQueue()
lines = sys.stdin.read().strip().splitlines()
out_lines = []
for line in lines:
    parts = line.strip().split()
    if not parts:
        continue
    cmd = parts[0]
    if cmd == "push":
        q.push(int(parts[1]))
    elif cmd == "pop":
        out_lines.append(str(q.pop()))
    elif cmd == "peek":
        out_lines.append(str(q.peek()))
    elif cmd == "empty":
        out_lines.append("true" if q.empty() else "false")
print("###SYPHER_JUDGE0_RESULT###")
sys.stdout.write("\\n".join(out_lines))
if out_lines:
    sys.stdout.write("\\n")
`,
    java: `import java.util.Stack;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        MyQueue q = new MyQueue();
        StringBuilder sb = new StringBuilder();
        while (sc.hasNextLine()) {
            String line = sc.nextLine().trim();
            if (line.isEmpty()) continue;
            String[] parts = line.split(" ");
            String cmd = parts[0];
            switch (cmd) {
                case "push":
                    q.push(Integer.parseInt(parts[1]));
                    break;
                case "pop":
                    sb.append(q.pop()).append("\\n");
                    break;
                case "peek":
                    sb.append(q.peek()).append("\\n");
                    break;
                case "empty":
                    sb.append(q.empty()).append("\\n");
                    break;
            }
        }
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.print(sb.toString());
    }
}
`,
    cpp: `#include <iostream>
#include <stack>
#include <string>
#include <sstream>
using namespace std;

int main() {
    MyQueue q;
    string line;
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    while (getline(cin, line)) {
        if (line.empty()) continue;
        istringstream iss(line);
        string cmd;
        iss >> cmd;
        if (cmd == "push") {
            int x;
            iss >> x;
            q.push(x);
        } else if (cmd == "pop") {
            cout << q.pop() << endl;
        } else if (cmd == "peek") {
            cout << q.peek() << endl;
        } else if (cmd == "empty") {
            cout << (q.empty() ? "true" : "false") << endl;
        }
    }
    return 0;
}
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const q = new MyQueue();
const out = [];
rl.on('line', line => {
    const parts = line.trim().split(' ');
    const cmd = parts[0];
    if (cmd === 'push') q.push(parseInt(parts[1]));
    else if (cmd === 'pop') out.push(q.pop());
    else if (cmd === 'peek') out.push(q.peek());
    else if (cmd === 'empty') out.push(q.empty() ? 'true' : 'false');
});
rl.on('close', () => {
    console.log("###SYPHER_JUDGE0_RESULT###");
    if (out.length > 0) console.log(out.join('\\n'));
});
`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const q = new MyQueue();
const out: string[] = [];
rl.on('line', (line: string) => {
    const parts = line.trim().split(' ');
    const cmd = parts[0];
    if (cmd === 'push') q.push(parseInt(parts[1]));
    else if (cmd === 'pop') out.push(String(q.pop()));
    else if (cmd === 'peek') out.push(String(q.peek()));
    else if (cmd === 'empty') out.push(q.empty() ? 'true' : 'false');
});
rl.on('close', () => {
    console.log("###SYPHER_JUDGE0_RESULT###");
    if (out.length > 0) console.log(out.join('\\n'));
});
`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let mut q = MyQueue::new();
    let mut out = Vec::new();
    for line in stdin.lock().lines() {
        let line = line.unwrap();
        let parts: Vec<&str> = line.trim().split_whitespace().collect();
        if parts.is_empty() { continue; }
        match parts[0] {
            "push" => q.push(parts[1].parse().unwrap()),
            "pop" => out.push(q.pop().to_string()),
            "peek" => out.push(q.peek().to_string()),
            "empty" => out.push(if q.empty() { "true".to_string() } else { "false".to_string() }),
            _ => {}
        }
    }
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", out.join("\\n"));
}
`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

int main() {
    MyQueue* q = myQueueCreate();
    char line[1000];
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    while (fgets(line, sizeof(line), stdin)) {
        line[strcspn(line, "\\n")] = 0;
        if (strlen(line) == 0) continue;
        char cmd[10];
        int x;
        if (sscanf(line, "%s %d", cmd, &x) == 2) {
            myQueuePush(q, x);
        } else {
            sscanf(line, "%s", cmd);
            if (strcmp(cmd, "pop") == 0) printf("%d\\n", myQueuePop(q));
            else if (strcmp(cmd, "peek") == 0) printf("%d\\n", myQueuePeek(q));
            else if (strcmp(cmd, "empty") == 0) printf("%s\\n", myQueueEmpty(q) ? "true" : "false");
        }
    }
    myQueueFree(q);
    return 0;
}
`,
    csharp: `using System;
using System.Collections.Generic;
using System.Text;

class Program {
    static void Main() {
        MyQueue q = new MyQueue();
        StringBuilder sb = new StringBuilder();
        string line;
        while ((line = Console.ReadLine()) != null) {
            line = line.Trim();
            if (line.Length == 0) continue;
            string[] parts = line.Split(' ');
            string cmd = parts[0];
            if (cmd == "push") {
                q.Push(int.Parse(parts[1]));
            } else if (cmd == "pop") {
                sb.AppendLine(q.Pop().ToString());
            } else if (cmd == "peek") {
                sb.AppendLine(q.Peek().ToString());
            } else if (cmd == "empty") {
                sb.AppendLine(q.Empty() ? "true" : "false");
            }
        }
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.Write(sb.ToString());
    }
}
`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)
    q := Constructor()
    var out []string
    for scanner.Scan() {
        line := strings.TrimSpace(scanner.Text())
        if line == "" {
            continue
        }
        parts := strings.Fields(line)
        cmd := parts[0]
        switch cmd {
        case "push":
            x, _ := strconv.Atoi(parts[1])
            q.Push(x)
        case "pop":
            out = append(out, strconv.Itoa(q.Pop()))
        case "peek":
            out = append(out, strconv.Itoa(q.Peek()))
        case "empty":
            if q.Empty() {
                out = append(out, "true")
            } else {
                out = append(out, "false")
            }
        }
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(strings.Join(out, "\\n"))
}
`,
  }}
  defaultLanguage="python"
>

# Implement Queue using Stacks

**Difficulty:** Easy

Implement a first-in-first-out (FIFO) queue using only two stacks. The implemented queue should support all the functions of a normal queue (`push`, `pop`, `peek`, and `empty`).

## Example

**Input:**
```
push 1
push 2
pop
peek
empty
```

**Output:**
```
1
1
false
```

**Explanation:**
- `push(1)` adds 1 to the queue
- `push(2)` adds 2 to the queue
- `pop()` removes 1 (FIFO order)
- `peek()` returns 1 (the front element)
- `empty()` returns false (queue still has 2)

## Input Specification

Each line contains a command with optional arguments:
- `push x` — adds x to the back of the queue
- `pop` — removes and returns the front element
- `peek` — returns the front element without removing it
- `empty` — returns whether the queue is empty

## Output Specification

For each `pop` and `peek` command, output the result as an integer. For each `empty` command, output `true` or `false`.

## Constraints

- `1 <= x <= 1000`
- At most 1000 operations total
- All `pop` and `peek` calls are valid (queue is never empty)

## Hints

<details>
<summary>Hint 1</summary>

Use two stacks: one for input (push operations) and one for output (pop/peek operations). When the output stack is empty, transfer all elements from the input stack to the output stack.
</details>

<details>
<summary>Hint 2</summary>

The transfer from input to output reverses the order, effectively converting LIFO to FIFO.
</details>

<details>
<summary>Hint 3</summary>

For `peek()`, you can reuse the same logic as `pop()` but without removing the element.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/easy/implement-queue-using-stacks)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Moving Average from Data Stream](./moving-average).