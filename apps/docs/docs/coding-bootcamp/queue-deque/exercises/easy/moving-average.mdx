---
title: Moving Average from Data Stream
sidebar_label: Moving Average
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'moving-average', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '3\n1\n', expectedOutput: '1.00000\n' , isSample: true },
    { stdin: '3\n1\n10\n3\n5\n', expectedOutput: '1.00000\n5.50000\n4.66667\n6.00000\n' , isSample: true },
    { stdin: '1\n5\n', expectedOutput: '5.00000\n' },
    { stdin: '5\n1\n2\n3\n4\n5\n6\n', expectedOutput: '1.00000\n1.50000\n2.00000\n2.50000\n3.00000\n4.00000\n' },
    { stdin: '2\n10\n20\n30\n', expectedOutput: '10.00000\n15.00000\n25.00000\n' },
    { stdin: '4\n5\n5\n5\n5\n5\n', expectedOutput: '5.00000\n5.00000\n5.00000\n5.00000\n5.00000\n' },
    { stdin: '3\n100\n200\n300\n400\n', expectedOutput: '100.00000\n150.00000\n200.00000\n300.00000\n' },
    { stdin: '1\n-1\n-2\n-3\n', expectedOutput: '-1.00000\n-2.00000\n-3.00000\n' },
    { stdin: '10\n1\n2\n3\n4\n5\n', expectedOutput: '1.00000\n1.50000\n2.00000\n2.50000\n3.00000\n' },
    { stdin: '3\n0\n0\n0\n0\n', expectedOutput: '0.00000\n0.00000\n0.00000\n0.00000\n' },
    { stdin: '5\n100\n200\n300\n400\n500\n600\n700\n', expectedOutput: '100.00000\n150.00000\n200.00000\n250.00000\n300.00000\n400.00000\n500.00000\n' },
    { stdin: '2\n7\n7\n7\n', expectedOutput: '7.00000\n7.00000\n7.00000\n' },
    { stdin: '6\n10\n20\n30\n40\n50\n60\n70\n80\n', expectedOutput: '10.00000\n15.00000\n20.00000\n25.00000\n30.00000\n35.00000\n45.00000\n55.00000\n' },
    { stdin: '3\n-5\n0\n5\n10\n', expectedOutput: '-5.00000\n-2.50000\n0.00000\n5.00000\n' },
    { stdin: '8\n1\n1\n1\n1\n1\n1\n1\n1\n1\n', expectedOutput: '1.00000\n1.00000\n1.00000\n1.00000\n1.00000\n1.00000\n1.00000\n1.00000\n1.00000\n' },
    { stdin: '4\n10\n10\n10\n10\n10\n', expectedOutput: '10.00000\n10.00000\n10.00000\n10.00000\n10.00000\n' },
    { stdin: '2\n0\n100\n0\n', expectedOutput: '0.00000\n50.00000\n50.00000\n' },
    { stdin: '3\n1\n2\n3\n4\n5\n6\n7\n', expectedOutput: '1.00000\n1.50000\n2.00000\n3.00000\n4.00000\n5.00000\n6.00000\n' },
    { stdin: '5\n-10\n-20\n-30\n-40\n-50\n-60\n', expectedOutput: '-10.00000\n-15.00000\n-20.00000\n-25.00000\n-30.00000\n-40.00000\n' },
    { stdin: '3\n7\n14\n21\n28\n35\n', expectedOutput: '7.00000\n10.50000\n14.00000\n21.00000\n28.00000\n' },
  ]}
  starterCode={{
    python: `class MovingAverage:
    def __init__(self, size: int):
        # your code here
        pass

    def next(self, val: int) -> float:
        # your code here
        pass
`,
    java: `class MovingAverage {
    public MovingAverage(int size) {
        // your code here
    }

    public double next(int val) {
        // your code here
        return 0.0;
    }
}
`,
    cpp: `class MovingAverage {
public:
    MovingAverage(int size) {
        // your code here
    }

    double next(int val) {
        // your code here
        return 0.0;
    }
};
`,
    javascript: `class MovingAverage {
    constructor(size) {
        // your code here
    }

    next(val) {
        // your code here
        return 0.0;
    }
}
`,
    typescript: `class MovingAverage {
    constructor(size: number) {
        // your code here
    }

    next(val: number): number {
        // your code here
        return 0.0;
    }
}
`,
    rust: `struct MovingAverage {
    // your code here
}

impl MovingAverage {
    fn new(size: i32) -> Self {
        // your code here
        Self {}
    }

    fn next(&mut self, val: i32) -> f64 {
        // your code here
        0.0
    }
}
`,
    c: `typedef struct {
    // your code here
} MovingAverage;

MovingAverage* movingAverageCreate(int size) {
    // your code here
    return NULL;
}

double movingAverageNext(MovingAverage* obj, int val) {
    // your code here
    return 0.0;
}

void movingAverageFree(MovingAverage* obj) {
    // your code here
}
`,
    csharp: `public class MovingAverage {
    public MovingAverage(int size) {
        // your code here
    }

    public double Next(int val) {
        // your code here
        return 0.0;
    }
}
`,
    go: `type MovingAverage struct {
    // your code here
}

func Constructor(size int) MovingAverage {
    // your code here
    return MovingAverage{}
}

func (this *MovingAverage) Next(val int) float64 {
    // your code here
    return 0.0
}
`,
  }}
  harness={{
    python: `import sys

data = sys.stdin.read().strip().split()
if not data:
    sys.exit()
size = int(data[0])
ma = MovingAverage(size)
vals = [int(x) for x in data[1:]]
out = []
for v in vals:
    out.append("{:.5f}".format(ma.next(v)))
print("###SYPHER_JUDGE0_RESULT###")
sys.stdout.write("\\n".join(out))
if out:
    sys.stdout.write("\\n")
`,
    java: `import java.util.LinkedList;
import java.util.Queue;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int size = sc.nextInt();
        MovingAverage ma = new MovingAverage(size);
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        while (sc.hasNextInt()) {
            int val = sc.nextInt();
            System.out.printf("%.5f\\n", ma.next(val));
        }
    }
}
`,
    cpp: `#include <iostream>
#include <queue>
#include <iomanip>
using namespace std;

int main() {
    int size, val;
    cin >> size;
    MovingAverage ma(size);
    std::cout << "###SYPHER_JUDGE0_RESULT###" << std::endl;
    cout << fixed << setprecision(5);
    while (cin >> val) {
        cout << ma.next(val) << endl;
    }
    return 0;
}
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l.trim()));
rl.on('close', () => {
    const nums = lines.filter(l => l.length > 0).map(Number);
    const size = nums[0];
    const ma = new MovingAverage(size);
    const out = [];
    for (let i = 1; i < nums.length; i++) {
        out.push(ma.next(nums[i]).toFixed(5));
    }
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(out.join('\\n'));
});
`,
    typescript: `declare function require(name: string): any;
declare const process: any;
const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l.trim()));
rl.on('close', () => {
    const nums = lines.filter(l => l.length > 0).map(Number);
    const size = nums[0];
    const ma = new MovingAverage(size);
    const out: string[] = [];
    for (let i = 1; i < nums.length; i++) {
        out.push(ma.next(nums[i]).toFixed(5));
    }
    console.log("###SYPHER_JUDGE0_RESULT###");
    console.log(out.join('\\n'));
});
`,
    rust: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines();
    let first = lines.next().unwrap().unwrap();
    let mut nums: Vec<i32> = first.split_whitespace().map(|s| s.parse().unwrap()).collect();
    let size = nums[0];
    let mut ma = MovingAverage::new(size);
    let mut out = Vec::new();
    for i in 1..nums.len() {
        out.push(format!("{:.5}", ma.next(nums[i])));
    }
    for line in lines {
        for num in line.unwrap().split_whitespace().map(|s| s.parse::<i32>().unwrap()) {
            out.push(format!("{:.5}", ma.next(num)));
        }
    }
    println!("###SYPHER_JUDGE0_RESULT###");
    println!("{}", out.join("\\n"));
}
`,
    c: `#include <stdio.h>
#include <stdlib.h>

int main() {
    int size, val;
    scanf("%d", &size);
    MovingAverage* ma = movingAverageCreate(size);
    printf("###SYPHER_JUDGE0_RESULT###\\n");
    while (scanf("%d", &val) == 1) {
        printf("%.5f\\n", movingAverageNext(ma, val));
    }
    movingAverageFree(ma);
    return 0;
}
`,
    csharp: `using System;
using System.Collections.Generic;

class Program {
    static void Main() {
        int size = int.Parse(Console.ReadLine().Trim());
        MovingAverage ma = new MovingAverage(size);
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        string line;
        while ((line = Console.ReadLine()) != null) {
            line = line.Trim();
            if (line.Length == 0) continue;
            int val = int.Parse(line);
            Console.WriteLine("{0:F5}", ma.Next(val));
        }
    }
}
`,
    go: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    parts := strings.Fields(scanner.Text())
    size, _ := strconv.Atoi(parts[0])
    ma := Constructor(size)
    var out []string
    for _, p := range parts[1:] {
        val, _ := strconv.Atoi(p)
        out = append(out, fmt.Sprintf("%.5f", ma.Next(val)))
    }
    for scanner.Scan() {
        for _, p := range strings.Fields(scanner.Text()) {
            val, _ := strconv.Atoi(p)
            out = append(out, fmt.Sprintf("%.5f", ma.Next(val)))
        }
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(strings.Join(out, "\\n"))
}
`,
  }}
  defaultLanguage="python"
>

# Moving Average from Data Stream

**Difficulty:** Easy

Given a stream of integers and a window size, calculate the moving average of all integers in the sliding window.

## Example

**Input:**
```
3
1 10 3 5
```

**Output:**
```
1.00000
5.50000
4.66667
6.00000
```

**Explanation:**
- `next(1)` = 1 / 1 = 1.00000
- `next(10)` = (1 + 10) / 2 = 5.50000
- `next(3)` = (1 + 10 + 3) / 3 = 4.66667
- `next(5)` = (10 + 3 + 5) / 3 = 6.00000

## Input Specification

First line: integer `size` (window size), followed by a space-separated list of integers to process.

## Output Specification

For each integer, output the moving average after processing it, formatted to 5 decimal places.

## Constraints

- `1 <= size <= 1000`
- `-10^5 <= val <= 10^5`
- At most 10^4 operations

## Hints

<details>
<summary>Hint 1</summary>

Use a queue to maintain the sliding window. When the queue exceeds the size, remove the oldest element.
</details>

<details>
<summary>Hint 2</summary>

Maintain a running sum to avoid recalculating the sum from scratch each time.
</details>

<details>
<summary>Hint 3</summary>

The average is the running sum divided by the current queue size (or the window size, whichever is smaller).
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/easy/moving-average)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Sliding Window Maximum](../medium/sliding-window-maximum).