---
title: Sliding Window Maximum
sidebar_label: Sliding Window Maximum
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'sliding-window-maximum', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '3\n1 3 -1 -3 5 3 6 7\n', expectedOutput: '3 3 5 5 6 7\n' },
    { stdin: '1\n1 -1 0\n', expectedOutput: '1 -1 0\n' },
    { stdin: '5\n1 2 3 4 5\n', expectedOutput: '5\n' },
    { stdin: '2\n2 1 3 4 5 6\n', expectedOutput: '2 3 4 5 6\n' },
    { stdin: '4\n4 3 2 1 0 -1 -2\n', expectedOutput: '4 3 2 1\n' },
    { stdin: '1\n5 5 5 5 5\n', expectedOutput: '5 5 5 5 5\n' },
    { stdin: '3\n1 -1 2 -2 3 -3\n', expectedOutput: '2 2 3 3\n' },
    { stdin: '4\n1 3 1 2 0 5\n', expectedOutput: '3 3 3 5\n' },
    { stdin: '2\n-1 -2 -3 -4 -5\n', expectedOutput: '-1 -2 -3 -4 -5\n' },
    { stdin: '3\n10 9 8 7 6 5 4\n', expectedOutput: '10 9 8 7 6\n' },
    { stdin: '4\n1 2 3 4 5 6 7 8\n', expectedOutput: '4 5 6 7 8\n' },
    { stdin: '2\n3 1 4 1 5 9 2 6\n', expectedOutput: '3 4 4 5 9 9 6\n' },
    { stdin: '5\n1 1 1 1 1 1 1 1\n', expectedOutput: '1 1 1 1\n' },
    { stdin: '3\n5 4 3 2 1 0 -1\n', expectedOutput: '5 4 3 2 1\n' },
    { stdin: '6\n10 20 30 40 50 60 70\n', expectedOutput: '60 70\n' },
    { stdin: '3\n-5 -4 -3 -2 -1 0\n', expectedOutput: '-3 -2 -1 0\n' },
    { stdin: '2\n100 90 80 70 60\n', expectedOutput: '100 90 80 70\n' },
    { stdin: '4\n1 0 -1 -2 -3 -4 -5\n', expectedOutput: '1 0 -1 -2 -3\n' },
    { stdin: '3\n8 8 8 8 8 8\n', expectedOutput: '8 8 8 8\n' },
    { stdin: '7\n1 2 3 4 5 6 7 8 9\n', expectedOutput: '7 8 9\n' },
  ]}
  starterCode={{
    python: `def max_sliding_window(nums: list, k: int) -> list:
    # your code here
    pass
`,
    java: `import java.util.*;

class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        // your code here
        return new int[0];
    }
}
`,
    cpp: `class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        // your code here
        return {};
    }
};
`,
    javascript: `function maxSlidingWindow(nums, k) {
    // your code here
    return [];
}
`,
    typescript: `function maxSlidingWindow(nums: number[], k: number): number[] {
    // your code here
    return [];
}
`,
    rust: `fn max_sliding_window(nums: Vec<i32>, k: i32) -> Vec<i32> {
    // your code here
    vec![]
}
`,
    c: `int* maxSlidingWindow(int* nums, int numsSize, int k, int* returnSize) {
    // your code here
    *returnSize = 0;
    return NULL;
}
`,
    csharp: `public class Solution {
    public int[] MaxSlidingWindow(int[] nums, int k) {
        // your code here
        return new int[0];
    }
}
`,
    go: `func maxSlidingWindow(nums []int, k int) []int {
    // your code here
    return nil
}
`,
  }}
  harness={{
    python: `import sys
from collections import deque

def max_sliding_window(nums, k):
    dq = deque()
    result = []
    for i, n in enumerate(nums):
        while dq and nums[dq[-1]] < n:
            dq.pop()
        dq.append(i)
        if dq[0] == i - k:
            dq.popleft()
        if i >= k - 1:
            result.append(str(nums[dq[0]]))
    return result

data = sys.stdin.read().strip().split()
k = int(data[0])
nums = [int(x) for x in data[1:]]
result = max_sliding_window(nums, k)
sys.stdout.write(" ".join(result) + "\\n")
`,
    java: `import java.util.*;

class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums.length == 0) return new int[0];
        int[] result = new int[nums.length - k + 1];
        Deque<Integer> dq = new ArrayDeque<>();
        int ri = 0;
        for (int i = 0; i < nums.length; i++) {
            while (!dq.isEmpty() && nums[dq.peekLast()] < nums[i]) {
                dq.pollLast();
            }
            dq.offerLast(i);
            if (dq.peekFirst() == i - k) {
                dq.pollFirst();
            }
            if (i >= k - 1) {
                result[ri++] = nums[dq.peekFirst()];
            }
        }
        return result;
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int k = sc.nextInt();
        List<Integer> list = new ArrayList<>();
        while (sc.hasNextInt()) {
            list.add(sc.nextInt());
        }
        int[] nums = list.stream().mapToInt(i -> i).toArray();
        int[] result = new Solution().maxSlidingWindow(nums, k);
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < result.length; i++) {
            if (i > 0) sb.append(" ");
            sb.append(result[i]);
        }
        System.out.println(sb.toString());
    }
}
`,
    cpp: `#include <iostream>
#include <vector>
#include <deque>
#include <sstream>
using namespace std;

class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> result;
        deque<int> dq;
        for (int i = 0; i < nums.size(); i++) {
            while (!dq.empty() && nums[dq.back()] < nums[i]) {
                dq.pop_back();
            }
            dq.push_back(i);
            if (dq.front() == i - k) {
                dq.pop_front();
            }
            if (i >= k - 1) {
                result.push_back(nums[dq.front()]);
            }
        }
        return result;
    }
};

int main() {
    int k, x;
    cin >> k;
    vector<int> nums;
    while (cin >> x) nums.push_back(x);
    Solution sol;
    vector<int> result = sol.maxSlidingWindow(nums, k);
    for (int i = 0; i < result.size(); i++) {
        if (i > 0) cout << " ";
        cout << result[i];
    }
    cout << endl;
    return 0;
}
`,
    javascript: `function maxSlidingWindow(nums, k) {
    const dq = [];
    const result = [];
    for (let i = 0; i < nums.length; i++) {
        while (dq.length > 0 && nums[dq[dq.length - 1]] < nums[i]) {
            dq.pop();
        }
        dq.push(i);
        if (dq[0] === i - k) {
            dq.shift();
        }
        if (i >= k - 1) {
            result.push(nums[dq[0]]);
        }
    }
    return result;
}

const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const parts = line.trim().split(/\\s+/).map(Number);
    const k = parts[0];
    const nums = parts.slice(1);
    const result = maxSlidingWindow(nums, k);
    console.log(result.join(' '));
    rl.close();
});
`,
    typescript: `function maxSlidingWindow(nums: number[], k: number): number[] {
    const dq: number[] = [];
    const result: number[] = [];
    for (let i = 0; i < nums.length; i++) {
        while (dq.length > 0 && nums[dq[dq.length - 1]] < nums[i]) {
            dq.pop();
        }
        dq.push(i);
        if (dq[0] === i - k) {
            dq.shift();
        }
        if (i >= k - 1) {
            result.push(nums[dq[0]]);
        }
    }
    return result;
}

const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const parts = line.trim().split(/\\s+/).map(Number);
    const k = parts[0];
    const nums = parts.slice(1);
    console.log(maxSlidingWindow(nums, k).join(' '));
    rl.close();
});
`,
    rust: `use std::io::{self, BufRead};
use std::collections::VecDeque;

fn max_sliding_window(nums: &[i32], k: i32) -> Vec<i32> {
    let k = k as usize;
    let mut dq: VecDeque<usize> = VecDeque::new();
    let mut result = Vec::new();
    for (i, &n) in nums.iter().enumerate() {
        while let Some(&back) = dq.back() {
            if nums[back] < n {
                dq.pop_back();
            } else {
                break;
            }
        }
        dq.push_back(i);
        if let Some(&front) = dq.front() {
            if front == i - k {
                dq.pop_front();
            }
        }
        if i >= k - 1 {
            result.push(nums[dq.front().unwrap()]);
        }
    }
    result
}

fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let parts: Vec<i32> = line.split_whitespace().map(|s| s.parse().unwrap()).collect();
    let k = parts[0];
    let nums: Vec<i32> = parts[1..].to_vec();
    let result = max_sliding_window(&nums, k);
    for (i, v) in result.iter().enumerate() {
        if i > 0 { print!(" "); }
        print!("{}", v);
    }
    println!();
}
`,
    c: `#include <stdio.h>
#include <stdlib.h>

int* maxSlidingWindow(int* nums, int numsSize, int k, int* returnSize) {
    if (numsSize == 0 || k == 0) {
        *returnSize = 0;
        return NULL;
    }
    *returnSize = numsSize - k + 1;
    int* result = (int*)malloc(*returnSize * sizeof(int));
    int* dq = (int*)malloc(numsSize * sizeof(int));
    int front = 0, back = 0;
    int ri = 0;
    for (int i = 0; i < numsSize; i++) {
        while (back > front && nums[dq[back - 1]] < nums[i]) {
            back--;
        }
        dq[back++] = i;
        if (dq[front] == i - k) {
            front++;
        }
        if (i >= k - 1) {
            result[ri++] = nums[dq[front]];
        }
    }
    free(dq);
    return result;
}

int main() {
    int k, n = 0, cap = 1000;
    scanf("%d", &k);
    int* nums = (int*)malloc(cap * sizeof(int));
    while (scanf("%d", &nums[n]) == 1) {
        n++;
        if (n >= cap) {
            cap *= 2;
            nums = (int*)realloc(nums, cap * sizeof(int));
        }
    }
    int returnSize;
    int* result = maxSlidingWindow(nums, n, k, &returnSize);
    for (int i = 0; i < returnSize; i++) {
        if (i > 0) printf(" ");
        printf("%d", result[i]);
    }
    printf("\\n");
    free(nums);
    free(result);
    return 0;
}
`,
    csharp: `using System;
using System.Collections.Generic;
using System.Linq;

public class Solution {
    public int[] MaxSlidingWindow(int[] nums, int k) {
        if (nums.Length == 0) return new int[0];
        int[] result = new int[nums.Length - k + 1];
        LinkedList<int> dq = new LinkedList<int>();
        int ri = 0;
        for (int i = 0; i < nums.Length; i++) {
            while (dq.Count > 0 && nums[dq.Last.Value] < nums[i]) {
                dq.RemoveLast();
            }
            dq.AddLast(i);
            if (dq.First.Value == i - k) {
                dq.RemoveFirst();
            }
            if (i >= k - 1) {
                result[ri++] = nums[dq.First.Value];
            }
        }
        return result;
    }
}

class Program {
    static void Main() {
        string[] parts = Console.ReadLine().Split();
        int k = int.Parse(parts[0]);
        int[] nums = parts.Skip(1).Select(int.Parse).ToArray();
        int[] result = new Solution().MaxSlidingWindow(nums, k);
        Console.WriteLine(string.Join(" ", result));
    }
}
`,
    go: `package main

\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func maxSlidingWindow(nums []int, k int) []int {
    if len(nums) == 0 {
        return []int{}
    }
    result := make([]int, len(nums)-k+1)
    dq := make([]int, 0)
    ri := 0
    for i, n := range nums {
        for len(dq) > 0 && nums[dq[len(dq)-1]] < n {
            dq = dq[:len(dq)-1]
        }
        dq = append(dq, i)
        if dq[0] == i-k {
            dq = dq[1:]
        }
        if i >= k-1 {
            result[ri] = nums[dq[0]]
            ri++
        }
    }
    return result
}

func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    parts := strings.Fields(scanner.Text())
    k, _ := strconv.Atoi(parts[0])
    nums := make([]int, len(parts)-1)
    for i, p := range parts[1:] {
        nums[i], _ = strconv.Atoi(p)
    }
    result := maxSlidingWindow(nums, k)
    out := make([]string, len(result))
    for i, v := range result {
        out[i] = strconv.Itoa(v)
    }
    fmt.Println(strings.Join(out, " "))
}
`,
  }}
  defaultLanguage="python"
>

# Sliding Window Maximum

**Difficulty:** Medium

You are given an array of integers `nums`, and a sliding window of size `k` which moves from the very left to the very right. You can only see the `k` numbers in the window. Each time the sliding window moves right by one position, return the maximum element in the current window.

## Example

**Input:** `nums = [1,3,-1,-3,5,3,6,7]`, `k = 3`

**Output:** `[3,3,5,5,6,7]`

**Explanation:**
```
Window position                Max
---------------               -----
[1  3  -1] -3  5  3  6  7      3
 1 [3  -1  -3] 5  3  6  7      3
 1  3 [-1  -3  5] 3  6  7      5
 1  3  -1 [-3  5  3] 6  7      5
 1  3  -1  -3 [5  3  6] 7      6
 1  3  -1  -3  5 [3  6  7]     7
```

## Input Specification

First line: integer `k` (window size), followed by space-separated integers of `nums`.

## Output Specification

Space-separated list of maximum values for each window position.

## Constraints

- `1 <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`
- `1 <= k <= nums.length`

## Hints

<details>
<summary>Hint 1</summary>

Use a deque to store indices of elements in decreasing order. The front of the deque always holds the index of the maximum element in the current window.
</details>

<details>
<summary>Hint 2</summary>

When adding a new element, remove all elements from the back of the deque that are smaller than the new element — they can never be the maximum.
</details>

<details>
<summary>Hint 3</summary>

Remove the front element if its index is outside the current window (i - k).
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/medium/sliding-window-maximum)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Design Circular Queue](./design-circular-queue).