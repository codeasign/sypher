---
title: Judge0 Import Diagnostic
sidebar_label: Judge0 Import Diagnostic
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'judge0-diagnostic-import-test', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '1 2 2 3 3 3\n', expectedOutput: '3\n', isSample: true },
    { stdin: '5 5 5 5\n', expectedOutput: '1\n', isSample: true },
    { stdin: '1 2 3 4 5\n', expectedOutput: '5\n' },
  ]}
  starterCode={{
    java: `class Solution {
    public int countDistinct(int[] nums) {
        // paste the diagnostic solution here
        return 0;
    }
}
`,
    go: `func countDistinct(nums []int) int {
    // paste the diagnostic solution here
    return 0
}
`,
    csharp: `public class Solution {
    public int CountDistinct(int[] nums) {
        // paste the diagnostic solution here
        return 0;
    }
}
`,
  }}
  harness={{
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        java.util.List<Integer> nums = new java.util.ArrayList<>();
        while (sc.hasNextInt()) {
            nums.add(sc.nextInt());
        }
        int[] arr = new int[nums.size()];
        for (int i = 0; i < arr.length; i++) arr[i] = nums.get(i);
        System.out.println("###SYPHER_JUDGE0_RESULT###");
        System.out.println(new Solution().countDistinct(arr));
    }
}
`,
    go: `package main
import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    parts := strings.Fields(scanner.Text())
    nums := make([]int, len(parts))
    for i, s := range parts {
        nums[i], _ = strconv.Atoi(s)
    }
    fmt.Println("###SYPHER_JUDGE0_RESULT###")
    fmt.Println(countDistinct(nums))
}
`,
    csharp: `using System;
using System.Collections.Generic;
using System.Linq;
class Program {
    static void Main() {
        int[] nums = Console.ReadLine().Trim().Split(' ').Select(int.Parse).ToArray();
        Solution sol = new Solution();
        Console.WriteLine("###SYPHER_JUDGE0_RESULT###");
        Console.WriteLine(sol.CountDistinct(nums));
    }
}
`,
  }}
  defaultLanguage="java"
>

# Judge0 Import Diagnostic

**Throwaway diagnostic page — not a real bootcamp exercise.** Isolates whether
CoreEditor's harness+code composition correctly handles solutions that need their own
`import`/`using` (Java, C#) or a positionally-required preamble (Go's `package main`),
independent of any specific problem's own authoring.

## Task

Count the number of **distinct** integers in a space-separated list on stdin. Print
the count.

<AsciiDiagram
  id="coding-bootcamp/judge0-diagnostic-import-test"
  content={`stdin ──▶ parse ints ──▶ count distinct ──▶ stdout`}
  alt="Linear flow from stdin parsing to distinct-count computation to stdout."
  caption="Diagnostic problem flow"
/>

</ProblemEditor>
