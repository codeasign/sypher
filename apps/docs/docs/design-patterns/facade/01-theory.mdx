---
id: facade-theory
title: "Facade Pattern: Theory"
sidebar_label: Theory
sidebar_position: 1
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';

## Intent

Provide a unified, simplified interface to a set of interfaces in a subsystem. The Facade pattern defines a higher-level interface that makes the subsystem easier to use by hiding its complexity behind a single entry point. In a cars domain, the `CarFacade` wraps dozens of interconnected subsystems (engine control, transmission, cooling, electrical, fuel system, exhaust, braking) and exposes a handful of intuitive methods like `startEngine()`, `drive()`, and `stopEngine()`. Instead of the driver having to orchestrate a dozen sequential subsystem commands in the correct order, the facade handles all the coordination internally.

The intent of the Facade pattern is not to add new functionality but to simplify access to existing functionality. A well-designed facade reduces the learning curve for a complex subsystem, minimizes coupling between clients and subsystem components, and promotes layering by defining a clear boundary between high-level operations and low-level implementation details.

## The Problem Without This Pattern

Consider what happens when a driver turns the ignition key in a modern car. Behind the dashboard, an elaborate sequence must execute before the engine fires:

```text
Step 1:  Verify battery voltage is above 12.0V
Step 2:  Close the starter relay circuit
Step 3:  Engage the starter motor pinion with the flywheel
Step 4:  Prime the fuel pump to build rail pressure to 55 PSI
Step 5:  Signal the Engine Control Unit (ECU) to begin ignition timing
Step 6:  Open the fuel injectors in cylinder-firing order
Step 7:  Monitor crankshaft position sensor for rotation
Step 8:  Verify oil pressure exceeds 20 PSI within 3 seconds
Step 9:  Disable the immobilizer via the transponder chip
Step 10: Disengage the starter motor once RPM exceeds 800
```

Without a facade, the driver (or worse, the calling code in an automotive software system) must know every one of these steps and execute them in precise order. A software system that controls a car without a facade might look like this:

```java
// PROBLEMATIC: Client code must know every subsystem detail
Battery battery = new Battery();
StarterMotor starter = new StarterMotor();
FuelPump fuelPump = new FuelPump();
ECU ecu = new ECU();
OilPump oilPump = new OilPump();
Immobilizer immobilizer = new Immobilizer();
CoolingSystem cooling = new CoolingSystem();

if (battery.getVoltage() >= 12.0) {
    starter.engagePinion();
    fuelPump.prime(55);
    ecu.initializeIgnitionTiming();
    ecu.openInjectors(CylinderOrder.FIRING_SEQUENCE_4_CYL);
    immobilizer.deactivate();
    starter.crank();
    oilPump.monitorPressure();
    if (ecu.getRPM() > 800) {
        starter.disengage();
    }
}
```

This approach creates several real problems. The client is tightly coupled to every subsystem class, so changing any subsystem's API (`fuelPump.prime(55)` becoming `fuelPump.buildPressure(55)`, say) forces changes throughout every piece of code that calls it. The client also carries the ordering responsibility: it must invoke subsystem operations in exactly the right sequence, and getting that order wrong, such as opening injectors before priming the fuel pump, can cause real mechanical damage.

There's duplication too. Every piece of code that starts the car has to repeat this entire orchestration block, so a system with several control modules (dashboard UI, remote start, autonomous driving) ends up with the same logic copy-pasted everywhere. That duplication makes the code hard to test, since testing the client means mocking every subsystem, and any change to the start sequence forces updates to every client's tests. Worst of all, it violates encapsulation: internal details like required fuel pressure, injector firing order, and the RPM threshold for disengagement all leak into the client layer, where they don't belong.

### Concrete Scenario: Three Different Clients

To illustrate the scope of the problem, imagine an automotive software system with three distinct clients that all need to start the car: the dashboard UI, when the driver presses the Start button on the touch screen; the remote start module, when the driver presses the Lock button three times on the key fob; and the autonomous driving module, which needs to restart the engine on its own after an idle stop at a traffic light.

Without a facade, each of these three clients must independently implement the entire ten-step startup sequence. If the sequence changes (and it will, as the ECU firmware is updated) all three clients must be modified and retested. In a large automotive codebase, the startup logic might be duplicated across a dozen modules, each with its own subtle variations and bugs.

### Concrete Scenario: Hybrid Vehicle Transition

Consider a car manufacturer transitioning from a purely internal-combustion vehicle to a hybrid electric vehicle. The hybrid adds new subsystems: a high-voltage battery pack, an electric motor/generator, a DC-DC converter, and a battery management system. The startup sequence now includes an additional precondition: the high-voltage contactor must close before the electric motor can assist the engine.

Without a facade, every client that implements the startup sequence must be modified to include the contactor check. With a facade, only the single `CarFacade.startEngine()` method changes. The three clients remain identical: they call `startEngine()` and the facade handles the rest.

## Structure

The Facade pattern decouples the client from a complex subsystem by providing a single entry point.

<AsciiDiagram
  id="facade/structure"
  alt="Facade Pattern Structure: CarFacade participants and subsystem relationships"
  caption="Participants: CarFacade (Facade), Engine, Transmission, CoolingSystem, ElectricalSystem, FuelSystem, BrakingSystem, ExhaustSystem (Subsystem classes), Driver (Client)"
  mermaidSrc="/img/diagrams/121859786a16.svg"
  content={`
  ┌─────────────────────────────────────────────────┐
  │                   Driver                        │
  │                  (Client)                       │
  └────────┬────────────┬──────────────┬────────────┘
           │            │              │
           │ start()    │ drive()      │ stop()
           ▼            ▼              ▼
  ┌─────────────────────────────────────────────────┐
  │                  CarFacade                      │
  │                (Facade)                         │
  ├─────────────────────────────────────────────────┤
  │ + startEngine(): void                           │
  │ + drive(speed: int): void                       │
  │ + stopEngine(): void                            │
  └────┬────┬────┬────┬────┬────┬────┬────┬────┬────┘
       │    │    │    │    │    │    │    │    │    │
       ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼
  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐
  │Engine│ │Trans│ │Cool │ │Elec │ │Fuel │ │Brake│
  │     │ │     │ │ Sys │ │ Sys │ │ Sys │ │ Sys │
  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘
  ─ CarFacade coordinates all subsystem calls      ─
  ─ Client calls only high-level operations         ─
  ─ Subsystems never leak into client code          ─
  `} />

Three roles make up the pattern. The facade, `CarFacade`, knows which subsystem classes are responsible for a request and delegates to the right subsystem objects, orchestrating them in the correct order. The subsystem classes, `Engine`, `Transmission`, `CoolingSystem`, and the rest, implement the actual functionality; they handle whatever work the facade assigns but have no knowledge that the facade even exists, and they can talk to each other directly when needed. The client, the driver in this case, requests subsystem operations only through the facade and never touches the subsystem objects directly.

The key insight is that the facade is not a layer that merely forwards calls: it is an orchestrator that adds value through coordination, precondition validation, ordering, and error handling. The subsystems remain independently usable by other clients that need fine-grained access.

## Pseudocode: Cars Example

The following pseudocode demonstrates the Facade pattern for a car control system. The `CarFacade` coordinates six subsystems to provide a simple start-drive-stop interface.

```text
// PSEUDOCODE: Facade Pattern in Cars Domain
// Participants labeled with [ROLE]

// ── Subsystem: Engine ──
class Engine
    private rpm: int = 0
    private oilPressure: float = 0.0
    private coolantTemp: float = 20.0

    method crank(): void
        print("Engine: cranking at 250 RPM")
        this.rpm = 250

    method injectFuel(): void
        print("Engine: injecting fuel into cylinders")

    method spark(): void
        print("Engine: firing spark plugs")

    method getIdleRPM(): int
        return 850

    method getRPM(): int
        return this.rpm

    method rev(amount: int): void
        this.rpm = this.rpm + amount
        print("Engine: revving to " + this.rpm + " RPM")

    method stop(): void
        this.rpm = 0
        print("Engine: stopped")

// ── Subsystem: Transmission ──
class Transmission
    private gear: string = "Park"

    method shiftTo(gearName: string): void
        this.gear = gearName
        print("Transmission: shifted to " + gearName)

    method getCurrentGear(): string
        return this.gear

// ── Subsystem: CoolingSystem ──
class CoolingSystem
    private coolantTemp: float = 20.0

    method runWaterPump(): void
        print("Cooling: water pump circulating coolant")

    method runRadiatorFan(): void
        print("Cooling: radiator fan activated")

    method getCoolantTemperature(): float
        return this.coolantTemp

    method setThermostat(temp: float): void
        this.coolantTemp = temp
        print("Cooling: thermostat set to " + temp + "C")

// ── Subsystem: ElectricalSystem ──
class ElectricalSystem
    private voltage: float = 12.6

    method getBatteryVoltage(): float
        return this.voltage

    method powerAccessories(): void
        print("Electrical: powering dashboard and infotainment")

    method powerECU(): void
        print("Electrical: Engine Control Unit powered on")

    method closeStarterRelay(): void
        print("Electrical: starter relay closed")

// ── Subsystem: FuelSystem ──
class FuelSystem
    private railPressure: float = 0.0

    method prime(pressure: float): void
        this.railPressure = pressure
        print("Fuel: rail pressure primed to " + pressure + " PSI")

    method getRailPressure(): float
        return this.railPressure

    method shutoff(): void
        this.railPressure = 0.0
        print("Fuel: fuel pump shut off")

// ── Subsystem: BrakingSystem ──
class BrakingSystem
    private engaged: bool = true

    method release(): void
        this.engaged = false
        print("Brakes: parking brake released")

    method apply(): void
        this.engaged = true
        print("Brakes: parking brake applied")

// ── Facade ──
class CarFacade
    private engine: Engine
    private transmission: Transmission
    private cooling: CoolingSystem
    private electrical: ElectricalSystem
    private fuelSystem: FuelSystem
    private brakes: BrakingSystem

    constructor()
        this.engine = new Engine()
        this.transmission = new Transmission()
        this.cooling = new CoolingSystem()
        this.electrical = new ElectricalSystem()
        this.fuelSystem = new FuelSystem()
        this.brakes = new BrakingSystem()

    method startEngine(): void
        print("--- Starting Engine ---")
        voltage = this.electrical.getBatteryVoltage()
        if voltage < 12.0
            print("ERROR: Battery voltage too low.")
            return
        this.electrical.powerECU()
        this.electrical.powerAccessories()
        this.electrical.closeStarterRelay()
        this.cooling.runWaterPump()
        this.fuelSystem.prime(55)
        this.engine.crank()
        this.engine.injectFuel()
        this.engine.spark()
        print("Engine started. Idle RPM: " + this.engine.getIdleRPM())
        this.cooling.runRadiatorFan()
        this.brakes.release()

    method drive(speed: int): void
        print("--- Driving at " + speed + " RPM ---")
        this.transmission.shiftTo("Drive")
        this.engine.rev(speed)
        targetTemp = 90.0
        this.cooling.setThermostat(targetTemp)
        print("Driving: cruising at " + speed + " RPM")

    method stopEngine(): void
        print("--- Stopping Engine ---")
        this.transmission.shiftTo("Park")
        this.brakes.apply()
        this.engine.stop()
        this.fuelSystem.shutoff()
        print("Engine stopped. All subsystems off.")

// ── Driver ──
function main()
    car = new CarFacade()
    car.startEngine()
    car.drive(2500)
    car.stopEngine()
```

## How It Works: Step by Step

1. The driver, or the calling code, invokes `car.startEngine()` without knowing any subsystem details. The method name is intuitive and matches the user's mental model, so the client never needs to know that starting an engine involves electricity, fuel, cooling, and mechanical rotation all at once.

2. The facade checks the battery voltage before doing anything else. If it's below 12.0V, it prints an error and returns immediately, protecting a weak battery from being drained further. This precondition check happens once, in one place, instead of being duplicated across every client.

3. Once the voltage check passes, the facade brings subsystems online in the order that actually matters: power goes to the ECU and accessories before the starter relay closes, the water pump starts circulating coolant before the engine cranks (so the block isn't dry), and the fuel pump primes the rail to 55 PSI before the injectors open. Every one of these ordering constraints lives in the facade's method body, not scattered across client code.

4. With preconditions met, the facade runs the actual start sequence: the starter motor cranks the engine, fuel injectors spray into the cylinders, and the spark plugs fire, each in the exact order a successful start requires. The facade isn't just forwarding calls here; it's actively coordinating them: `injectFuel()` has to happen after `crank()` gets the engine rotating, and `spark()` has to fire only once the fuel-air mixture is actually present.

5. Once the engine reaches idle RPM, the facade moves into a post-start state: the radiator fan kicks on to begin active cooling, and the parking brake releases so the car is ready to move. These are exactly the kinds of steps that are easy to forget in ad hoc client code, but the facade guarantees them every time.

6. When the driver calls `stopEngine()`, the facade shifts the transmission to Park, applies the parking brake, stops the engine, and shuts off the fuel pump, returning every subsystem to a safe idle state. It's the mirror image of the start sequence, which is exactly the kind of symmetry that's hard to pull off when every client writes its own shutdown code.

### The Facade Method in Detail: `startEngine()`

The facade's `startEngine()` method is the most complex of the three. Let us examine each line for its purpose:

```text
// Step 1: Diagnostic summary header
print("--- Starting Engine ---")

// Step 2: Read battery voltage from the electrical subsystem
voltage = this.electrical.getBatteryVoltage()

// Step 3: Guard clause; abort if battery is too weak
if voltage < 12.0
    print("ERROR: Battery voltage too low.")
    return

// Step 4: Power on the car's brain (ECU) before anything else
this.electrical.powerECU()

// Step 5: Wake up the dashboard, radio, and climate controls
this.electrical.powerAccessories()

// Step 6: Close the high-current starter relay circuit
this.electrical.closeStarterRelay()

// Step 7: Start circulating coolant to prevent hot spots
this.cooling.runWaterPump()

// Step 8: Build fuel rail pressure for immediate injection
this.fuelSystem.prime(55)

// Step 9: Turn the engine over mechanically
this.engine.crank()

// Step 10: Introduce fuel into the combustion chambers
this.engine.injectFuel()

// Step 11: Ignite the compressed fuel-air mixture
this.engine.spark()

// Step 12: Confirm the engine is running at idle speed
print("Engine started. Idle RPM: " + this.engine.getIdleRPM())

// Step 13: Begin active cooling now that the engine is running
this.cooling.runRadiatorFan()

// Step 14: Release the parking brake so the car can move
this.brakes.release()
```

This fourteen-step sequence is a complete, ordered workflow. Every client that starts the car follows the same sequence, guaranteed by the facade.

## Benefits of the Facade Pattern

1. Reduced client complexity. The client interacts with one facade class instead of six or more subsystem classes, so the learning curve drops from understanding an entire subsystem API to understanding three method names.

2. Decoupling. Because the client is decoupled from the subsystem classes, subsystem internals can be refactored, upgraded, or replaced without affecting anyone who uses the facade, which matters a lot in automotive work, where ECU firmware updates happen constantly.

3. A single place for cross-cutting concerns. Logging, performance monitoring, fault tolerance, and security checks can all live in the facade instead of being scattered across every client. The `startEngine()` method could emit telemetry to a cloud dashboard, log to the diagnostic bus, and record timing metrics, all transparently to whoever calls it.

4. Enforced consistency. The facade guarantees every operation runs in the correct order with the correct parameters, so no client can accidentally skip the fuel priming step or call `spark()` before `crank()`.

5. A layered architecture. The facade draws a clear boundary between high-level operations and low-level subsystem details, which lets one team build the orchestration logic while another builds the subsystem implementations, largely independently of each other.

6. Change isolation. When the car moves from a 12V lead-acid battery to a 48V lithium-ion system, the facade's public API doesn't change at all. Only the internal voltage threshold and subsystem interactions update, and every client keeps calling the same `startEngine()` method as before.

## Consequences and Trade-offs

While the Facade pattern provides significant benefits, it also introduces trade-offs:

The facade restricts clients to whatever operations it chooses to expose, so if a client ever needs something the facade doesn't support, like reading individual cylinder pressure, the facade becomes a bottleneck or has to be extended. The usual fix is to design the facade for the common 90% of use cases and leave the remaining 10% reachable through direct subsystem access.

There's also a risk that the facade itself grows into a monolithic "god object" that knows too much and does too much, which becomes a maintenance headache in its own right. Splitting a large facade into several domain-specific ones, `PowertrainFacade`, `ClimateFacade`, `InfotainmentFacade`, and so on, keeps any one of them from getting out of hand.

Every request now passes through an extra layer of indirection, and for performance-critical paths, such as a real-time engine control loop running at 10 kHz, that indirection can introduce latency that's hard to tolerate. High-performance clients can bypass the facade and talk to subsystems directly when this matters.

Finally, the facade hides complexity, but the complexity is still there underneath. If a subsystem fails, say the fuel pump can't build pressure, the facade has to surface meaningful error information without leaking subsystem internals, which means designing a clear error-reporting protocol (typed errors, codes, or structured status objects) up front rather than as an afterthought.

## When to Use

Reach for a facade when you want to shield clients from subsystem complexity: a modern car has dozens of ECUs, sensors, actuators, and mechanical subsystems, and a clean, minimal API keeps that complexity away from the driver and from higher-level software like infotainment, telematics, or the autonomous driving stack. It's also the right call when clients are tightly coupled to subsystem classes: if a fuel pump API change forces rewrites in five different control modules, a facade lets all five call `carFacade.startEngine()` instead of manipulating the fuel pump directly.

A facade is useful too when you want a layered subsystem architecture, since it draws a clear boundary between high-level operations (start, drive, stop) and low-level implementation, letting each layer evolve on its own. That matters most when the subsystem is complex or changes often: swap an internal combustion engine for a hybrid or electric powertrain, and only the facade's internals need to change, while client code that calls `startEngine()` stays identical. And whenever you need one place to hang cross-cutting concerns like logging, monitoring, security checks, or fault tolerance, centralizing them in the facade beats scattering them across every subsystem call.

## When NOT to Use

A facade isn't the right tool when a client needs fine-grained control over subsystems. A professional mechanic or a diagnostic tool that has to test fuel pressure independently of the engine start sequence needs direct subsystem access, so the facade should hide exactly what it's meant to hide without being the *only* way in. Watch out too for a facade that's grown into a God Object: once it accumulates hundreds of methods covering every subsystem operation, it has lost its purpose and become just as complex and tightly coupled as what it was supposed to simplify, at which point splitting it into focused facades like `EngineFacade`, `ClimateFacade`, and `EntertainmentFacade` makes more sense.

Skip the facade, or at least let performance-critical code bypass it, when the overhead genuinely can't be tolerated, such as a racing application that needs to start the car in under 50 milliseconds. That's rare, though; most systems benefit more from the facade's safeguards than they lose to its overhead. And if the subsystem is already simple, say a three-function API with nothing to hide, wrapping it in a facade just adds complexity and indirection for no real benefit.

## Real-World Occurrences

1. Automotive Engine Control Unit (ECU). A modern ECU is the quintessential facade: the driver presses the accelerator, and the ECU translates that single input into dozens of coordinated subsystem commands, fuel injection timing, spark advance, variable valve timing, turbocharger boost pressure, exhaust gas recirculation, adjusting each one based on coolant temperature, air density, fuel octane rating, and engine load. The driver never interacts with any of those subsystems directly.

2. Car dashboard instrument cluster. The cluster receives simple CAN bus signals (vehicle speed, engine RPM, fuel level) and internally drives speedometer stepper motors, tachometer gauges, LCD displays, warning lights, and chime generators. The wiring harness never routes individual signals to each gauge; a single CAN message for vehicle speed becomes the right stepper motor position on the analog speedometer, the right digital readout on the LCD, and the right trip computer calculation, all handled by the cluster facade.

3. Infotainment system APIs. Apple CarPlay and Android Auto sit as a facade over vehicle-specific infotainment hardware: the smartphone app calls abstract methods like `playAudio()` and `showNavigation()`, and the facade translates them into whatever protocol the car's head unit, amplifier, and display actually speak. The app developer never has to know whether the car uses MOST bus, CAN bus, or Ethernet for audio streaming.

4. Remote start modules. An aftermarket remote start system accepts one signal from the key fob and sequences the ignition, fuel pump, starter motor, climate control, and security system disarm behind it, along with safety preconditions like the hood being closed, the transmission sitting in Park, and the parking brake being engaged. The driver just presses one button.

5. The OBD-II diagnostic interface. On-Board Diagnostics II standardizes a facade over dozens of proprietary ECU protocols: a single `PID 0x0D` request for vehicle speed works the same way across every car brand, while the interface translates it internally into whatever CAN bus message format that manufacturer actually uses. A diagnostic tool developer writes one query that works on Ford, Toyota, BMW, and Mercedes alike.

6. Automatic car wash systems. The control panel exposes one `startWash()` button that sequences pre-soak, foam application, brush rotation, high-pressure rinse, wax spray, and blow-dry behind the scenes, along with safety interlocks such as the conveyor belt refusing to move until the brushes are home and the high-pressure rinse refusing to fire until the foam stage has finished. The operator never touches any subsystem individually.

## Comparison with Related Patterns

| Pattern | Relationship with Facade | Key Difference |
|---------|--------------------------|----------------|
| **Adapter** | Both provide an interface to existing classes. | Adapter converts one interface to another that the client expects. Facade provides a simplified interface to a larger subsystem; it does not convert interfaces but simplifies access. |
| **Mediator** | Both coordinate communication between components. | Mediator abstracts communication between colleague objects that are aware of each other. Facade provides a simplified unidirectional interface to a subsystem; it does not manage peer-to-peer communication. |
| **Proxy** | Both act as intermediaries for another object. | Proxy controls access to a single object (lazy loading, access control, logging). Facade provides a simplified interface to a whole subsystem of objects. |
| **Abstract Factory** | Both can work together to create subsystem objects. | Abstract Factory focuses on creating families of related objects. Facade can use an Abstract Factory to obtain subsystem instances, but its role is coordinating those instances, not creating them. |
| **Singleton** | Facade is often implemented as a Singleton. | Singleton ensures a single instance. A Facade is frequently a Singleton (one car facade per vehicle), but the patterns address different concerns: instance control vs. interface simplification. |
| **Command** | Both can be used to encapsulate operations. | Command encapsulates a single operation as an object with an `execute()` method. Facade encapsulates an entire sequence of subsystem operations into a single high-level method. |

### Adapter vs. Facade: Detailed Distinction

The Adapter and Facade patterns are frequently confused because both sit between a client and an existing class or subsystem. The critical distinction is intent:

- **Adapter** is used when you have an existing class with the right functionality but the wrong interface. The adapter rewrites the interface to match what the client expects. In the cars domain, a 12V-to-5V USB adapter converts the car's cigarette lighter socket to a USB port that charges a phone. The functionality (power delivery) is the same; the interface changes.

- **Facade** is used when you have a complex subsystem with a valid interface, but the client should not need to understand the subsystem's complexity. The facade provides a simpler interface that still uses the subsystem's existing interfaces internally. In the cars domain, the `startEngine()` method does not convert interfaces: it coordinates existing interfaces in a specific sequence with specific parameters.

### Mediator vs. Facade: Detailed Distinction

The Mediator and Facade patterns both involve an intermediary that coordinates communication. The key distinction is directionality:

- **Mediator** manages peer-to-peer communication between colleague objects. The colleagues know about the mediator and communicate through it. The mediator reduces coupling between colleagues. In a car's climate control system, the mediator would manage communication between the temperature sensor, the HVAC actuator, the user's temperature dial, and the sunload sensor: all of which need to share data bidirectionally.

- **Facade** provides a unidirectional entry point from the client to the subsystem. The subsystem objects do not know about the facade. The facade reduces coupling between the client and the subsystem. In the same climate control system, the facade would expose `setTemperature(22)` and `setFanSpeed(3)` without the client needing to know about the temperature sensor or HVAC actuator.

## Implementation Considerations

When implementing the Facade pattern in an automotive context, consider the following:

### 1. Interface Design

A well-designed facade should expose operations at the level of the client's mental model. The three methods `startEngine()`, `drive()`, and `stopEngine()` map directly to what a driver thinks about when operating a car. Avoid exposing methods like `verifyBatteryVoltage()` or `primeFuelPump()`: these are implementation details that the facade should hide.

### 2. Error Handling Strategy

The facade is the natural place to implement error handling for the subsystem. Consider the following error scenarios and how the facade should respond:

If the battery voltage is too low, print an error and abort rather than cranking a weak battery, which risks damaging the starter motor. If the fuel pump fails, catch the subsystem's `FuelPumpException` and translate it into a user-friendly message like "Fuel system error: service required" instead of leaking the exception upward. And if the engine hasn't started after three seconds, cut power to the starter motor to avoid overheating the winding.

Each error should be caught, logged, and translated into a meaningful message without leaking subsystem implementation details.

### 3. Subsystem Access

Consider whether the facade should be the only way to access subsystems. One option is restricted access: making subsystem classes package-private or internal so every client is forced through the facade, which enforces consistency but limits flexibility. The other is open access, where subsystem classes stay public, letting clients use the facade for most operations while reaching subsystems directly for specialized needs like diagnostics or testing, at the cost of some risk of inconsistent behavior.

The open access approach is generally preferred in automotive systems because diagnostic tools and test harnesses need direct subsystem access.

### 4. Facade Granularity

A single large facade may become unwieldy. Consider multiple focused facades:

- `EngineFacade`: start, stop, rev, diagnostic readouts
- `ClimateFacade`: set temperature, fan speed, air distribution
- `InfotainmentFacade`: play media, navigate, phone calls
- `SafetyFacade`: airbag status, tire pressure monitoring, collision detection

These facades can be composed by a higher-level `VehicleFacade` that orchestrates multiple domains.

<AsciiDiagram
  id="facade/start-sequence"
  alt="CarFacade startEngine sequence showing the order of subsystem interactions"
  caption="Detailed sequence diagram of CarFacade.startEngine() coordinating six subsystems in precise order"
  mermaidSrc="/img/diagrams/5305ed5a65ac.svg"
  content={`
  Driver      CarFacade        Electrical   Cooling    FuelSystem   Engine     Brakes
    │            │                │            │            │          │          │
    │ start()    │                │            │            │          │          │
    │───────────>│                │            │            │          │          │
    │            │ getVoltage()   │            │            │          │          │
    │            │──────────────>│            │            │          │          │
    │            │<12.6V─────────│            │            │          │          │
    │            │ powerECU()    │            │            │          │          │
    │            │──────────────>│            │            │          │          │
    │            │ powerAccess() │            │            │          │          │
    │            │──────────────>│            │            │          │          │
    │            │ closeRelay()  │            │            │          │          │
    │            │──────────────>│            │            │          │          │
    │            │ runWaterPump()│            │            │          │          │
    │            │──────────────────────────>│            │          │          │
    │            │ prime(55)     │            │            │          │          │
    │            │───────────────────────────────────────>│          │          │
    │            │ crank()       │            │            │          │          │
    │            │──────────────────────────────────────────────────>│          │
    │            │ injectFuel()  │            │            │          │          │
    │            │──────────────────────────────────────────────────>│          │
    │            │ spark()       │            │            │          │          │
    │            │──────────────────────────────────────────────────>│          │
    │            │ release()     │            │            │          │          │
    │            │─────────────────────────────────────────────────────────────>│
    │  started   │                │            │            │          │          │
    │<───────────│                │            │            │          │          │
  ── The facade orchestrates every step          ──
  ── The driver only calls startEngine()          ──
  ── Subsystem details are completely hidden      ──
  `} />

## Complete Example: End-to-End Driving Scenario

The following scenario traces a complete driving session through the facade's three methods, showing the full subsystem interaction for each stage.

### Stage 1: Morning Start

The driver approaches the car, opens the door, and presses the start button. The facade executes `startEngine()`:

```text
1. electrical.getBatteryVoltage()       → 12.6V (passes check)
2. electrical.powerECU()                → ECU boots, loads calibration data
3. electrical.powerAccessories()         → Dashboard lights up, infotainment boots
4. electrical.closeStarterRelay()        → 12V delivered to starter solenoid
5. cooling.runWaterPump()               → Coolant begins circulating
6. fuelSystem.prime(55)                 → Fuel rail pressurized to 55 PSI
7. engine.crank()                       → Starter motor turns crankshaft at 250 RPM
8. engine.injectFuel()                  → Injectors spray fuel into cylinders 1-3-4-2
9. engine.spark()                       → Spark plugs fire, combustion begins
10. Engine reaches 850 RPM              → Idle speed achieved
11. cooling.runRadiatorFan()            → Fan activates to manage heat
12. brakes.release()                    → Parking brake disengaged
```

### Stage 2: Driving to Work

The driver shifts into Drive and accelerates. The facade executes `drive(2500)`:

```text
1. transmission.shiftTo("Drive")        → Torque converter locks, gear engaged
2. engine.rev(2500)                     → Throttle opens, RPM climbs to 2500
3. cooling.setThermostat(90.0)          → Thermostat opens at 90C for optimal operating temp
4. Engine produces adequate torque       → Car moves forward smoothly
```

### Stage 3: Park and Shut Down

The driver arrives at work, parks the car, and presses the stop button. The facade executes `stopEngine()`:

```text
1. transmission.shiftTo("Park")         → Parking pawl engages
2. brakes.apply()                        → Parking brake holds the car stationary
3. engine.stop()                         → Fuel injection stops, RPM drops to 0
4. fuelSystem.shutoff()                  → Fuel pump power cut, rail pressure drops
5. All subsystems in safe idle state     → Car is secure and powered down
```

This end-to-end scenario demonstrates the facade's value: the driver and the calling code think in terms of three high-level operations, while the facade translates each operation into the correct sequence of subsystem commands. Subsystem details are completely hidden, ordering is guaranteed, and preconditions are validated in one central place.