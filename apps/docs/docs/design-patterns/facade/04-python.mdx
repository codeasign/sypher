---
id: facade-python
title: "Facade Pattern: Python"
sidebar_label: Python
sidebar_position: 4
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';

## Implementation

Below is a complete Python implementation of the Facade pattern using a cars domain. The `CarFacade` class provides a clean, high-level API for starting, driving, and stopping a modern car. Six subsystem classes (`Engine`, `Transmission`, `CoolingSystem`, `ElectricalSystem`, `FuelSystem`, and `BrakingSystem`) each handle a specific mechanical domain. The facade composes these subsystems and orchestrates the correct startup sequence, freeing the caller from any knowledge of the car's internal wiring.

<AsciiDiagram
  id="facade/python"
  alt="Facade structure for CarFacade in Python with six subsystem classes"
  caption="CarFacade in Python composes six subsystems, providing a simplified start-drive-stop interface"
  mermaidSrc="/img/diagrams/1f67241a4e72.svg"
  content={`
  ┌─────────────────────────────────────────────┐
  │                CarFacade                    │
  │              (Facade)                       │
  ├─────────────────────────────────────────────┤
  │ - engine: Engine                            │
  │ - transmission: Transmission                │
  │ - cooling: CoolingSystem                    │
  │ - electrical: ElectricalSystem              │
  │ - fuel_system: FuelSystem                   │
  │ - brakes: BrakingSystem                     │
  ├─────────────────────────────────────────────┤
  │ + start_engine()                            │
  │ + drive(speed)                              │
  │ + stop_engine()                             │
  └────┬────┬────┬────┬────┬────┬────┬────┬────┘
       │    │    │    │    │    │    │    │    │
       ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼
  Engine  Trans   Cool    Elec   Fuel   Brakes
  ─ Python duck typing means any object         ─
  ─ with the right methods can be a subsystem    ─
  ─ Private attributes use _ naming convention   ─
  `} />

### How the Implementation Works

#### Class Diagram (Pseudocode)

```
// ── Participant Diagram ──
//
//   ┌───────────────────────────────────────────┐
//   │   CarFacade (Facade)                      │  ← OOP: Composition, Encapsulation
//   ├───────────────────────────────────────────┤
//   │   - _engine: Engine                       │
//   │   - _transmission: Transmission           │
//   │   - _cooling: CoolingSystem               │
//   │   - _electrical: ElectricalSystem         │
//   │   - _fuel_system: FuelSystem              │
//   │   - _brakes: BrakingSystem                │
//   ├───────────────────────────────────────────┤
//   │   + start_engine()                        │
//   │   + drive(speed: int)                     │
//   │   + stop_engine()                         │
//   └───────────────────────────────────────────┘
```

#### OOP Concepts Applied

Subsystem attributes use the `_` prefix convention to signal privacy, and the facade hides the whole orchestration sequence behind just three public methods, which is encapsulation at both the subsystem and facade level. `CarFacade` composes six subsystem objects in `__init__`, a plain "has-a" relationship: a car facade has an engine, a transmission, and so on. And because Python doesn't require formal interfaces, any object with `crank()`, `injectFuel()`, and `spark()` methods can stand in as the engine, an implicit form of polymorphism that duck typing gives you for free.

#### SOLID Principles Applied

Each subsystem class sticks to exactly one domain: `Engine` handles combustion and RPM, `FuelSystem` manages fuel pressure, and `CarFacade` handles orchestration only, which is SRP in action. Adding a `Turbocharger` subsystem means adding a new class and wiring it into the facade, leaving existing subsystems and client code untouched (OCP). And each subsystem's public methods stay cohesive for ISP's sake: `BrakingSystem` exposes only `release()`, `apply()`, and `is_engaged()`, so no class ever depends on a method it doesn't actually use.

### Code

#### Subsystem: Engine

```python
class Engine:
    """Subsystem: manages engine cranking, fuel injection, ignition, and RPM."""

    def __init__(self) -> None:
        self._rpm: int = 0

    def crank(self) -> None:
        self._rpm = 250
        print("Engine: cranking at 250 RPM")

    def inject_fuel(self) -> None:
        print("Engine: injecting fuel into cylinders")

    def spark(self) -> None:
        print("Engine: firing spark plugs")

    def get_idle_rpm(self) -> int:
        return 850

    def get_rpm(self) -> int:
        return self._rpm

    def rev(self, amount: int) -> None:
        self._rpm += amount
        print(f"Engine: revving to {self._rpm} RPM")

    def stop(self) -> None:
        self._rpm = 0
        print("Engine: stopped")
```

#### Subsystem: Transmission

```python
class Transmission:
    """Subsystem: manages gear selection."""

    def __init__(self) -> None:
        self._gear: str = "Park"

    def shift_to(self, gear_name: str) -> None:
        self._gear = gear_name
        print(f"Transmission: shifted to {gear_name}")

    def get_current_gear(self) -> str:
        return self._gear
```

#### Subsystem: CoolingSystem

```python
class CoolingSystem:
    """Subsystem: manages coolant circulation and temperature."""

    def __init__(self) -> None:
        self._coolant_temp: float = 20.0

    def run_water_pump(self) -> None:
        print("Cooling: water pump circulating coolant")

    def run_radiator_fan(self) -> None:
        print("Cooling: radiator fan activated")

    def get_coolant_temperature(self) -> float:
        return self._coolant_temp

    def set_thermostat(self, temp: float) -> None:
        self._coolant_temp = temp
        print(f"Cooling: thermostat set to {temp} degrees C")
```

#### Subsystem: ElectricalSystem

```python
class ElectricalSystem:
    """Subsystem: manages battery, ECU, and starter relay."""

    def __init__(self) -> None:
        self._voltage: float = 12.6

    def get_battery_voltage(self) -> float:
        return self._voltage

    def power_accessories(self) -> None:
        print("Electrical: powering dashboard and infotainment")

    def power_ecu(self) -> None:
        print("Electrical: Engine Control Unit powered on")

    def close_starter_relay(self) -> None:
        print("Electrical: starter relay closed")
```

#### Subsystem: FuelSystem

```python
class FuelSystem:
    """Subsystem: manages fuel rail pressure."""

    def __init__(self) -> None:
        self._rail_pressure: float = 0.0

    def prime(self, pressure: float) -> None:
        self._rail_pressure = pressure
        print(f"Fuel: rail pressure primed to {pressure} PSI")

    def get_rail_pressure(self) -> float:
        return self._rail_pressure

    def shutoff(self) -> None:
        self._rail_pressure = 0.0
        print("Fuel: fuel pump shut off")
```

#### Subsystem: BrakingSystem

```python
class BrakingSystem:
    """Subsystem: manages parking brake."""

    def __init__(self) -> None:
        self._engaged: bool = True

    def release(self) -> None:
        self._engaged = False
        print("Brakes: parking brake released")

    def apply(self) -> None:
        self._engaged = True
        print("Brakes: parking brake applied")

    def is_engaged(self) -> bool:
        return self._engaged
```

#### Facade

```python
class CarFacade:
    """Facade: provides a simplified interface to the car's subsystems."""

    def __init__(self) -> None:
        self._engine = Engine()
        self._transmission = Transmission()
        self._cooling = CoolingSystem()
        self._electrical = ElectricalSystem()
        self._fuel_system = FuelSystem()
        self._brakes = BrakingSystem()

    def start_engine(self) -> None:
        """Orchestrate the full engine start sequence."""
        print("--- Starting Engine ---")
        voltage = self._electrical.get_battery_voltage()
        if voltage < 12.0:
            print("ERROR: Battery voltage too low.")
            return
        self._electrical.power_ecu()
        self._electrical.power_accessories()
        self._electrical.close_starter_relay()
        self._cooling.run_water_pump()
        self._fuel_system.prime(55)
        self._engine.crank()
        self._engine.inject_fuel()
        self._engine.spark()
        print(f"Engine started. Idle RPM: {self._engine.get_idle_rpm()}")
        self._cooling.run_radiator_fan()
        self._brakes.release()

    def drive(self, speed: int) -> None:
        """Engage the transmission and rev the engine to the target speed."""
        print(f"--- Driving at {speed} RPM ---")
        self._transmission.shift_to("Drive")
        self._engine.rev(speed)
        target_temp = 90.0
        self._cooling.set_thermostat(target_temp)
        print(f"Driving: cruising at {speed} RPM")

    def stop_engine(self) -> None:
        """Safely shut down all subsystems."""
        print("--- Stopping Engine ---")
        self._transmission.shift_to("Park")
        self._brakes.apply()
        self._engine.stop()
        self._fuel_system.shutoff()
        print("Engine stopped. All subsystems off.")
```

#### Main / Driver

```python
def main() -> None:
    """Driver: demonstrate the Facade pattern with a full start-drive-stop cycle."""
    car = CarFacade()
    car.start_engine()
    print()
    car.drive(2500)
    print()
    car.stop_engine()


if __name__ == "__main__":
    main()
```

### Expected Output

```bash
--- Starting Engine ---
Electrical: Engine Control Unit powered on
Electrical: powering dashboard and infotainment
Electrical: starter relay closed
Cooling: water pump circulating coolant
Fuel: rail pressure primed to 55 PSI
Engine: cranking at 250 RPM
Engine: injecting fuel into cylinders
Engine: firing spark plugs
Engine started. Idle RPM: 850
Cooling: radiator fan activated
Brakes: parking brake released

--- Driving at 2500 RPM ---
Transmission: shifted to Drive
Engine: revving to 2500 RPM
Cooling: thermostat set to 90 degrees C
Driving: cruising at 2500 RPM

--- Stopping Engine ---
Transmission: shifted to Park
Brakes: parking brake applied
Engine: stopped
Fuel: fuel pump shut off
Engine stopped. All subsystems off.
```

### Language-Specific Notes

Python relies on the `_` prefix convention for private attributes rather than actually enforcing privacy; nothing stops external code from reaching into `_engine`, but the naming signals that it's an implementation detail. Because the facade doesn't require formal interfaces, any object with `crank()`, `inject_fuel()`, `spark()`, `rev()`, `stop()`, and `get_idle_rpm()` methods can be injected as the engine, trading compile-time safety for flexibility. The type hints throughout (`int`, `float`, `str`, `None`) are optional at runtime, but they document the code and make IDE support far more useful.

The `if __name__ == "__main__"` guard protects the driver code with the standard Python idiom, so the module can be imported without triggering the demo. A `@property` decorator could replace `get_rpm()` and `get_idle_rpm()` with attribute-style access like `engine.rpm`, but the explicit method style is kept here for consistency with the other language versions. And uncommenting a `time.sleep(0.5)` between subsystem calls would simulate real mechanical delays; the code runs instantly as written, but the sequential order is preserved regardless.