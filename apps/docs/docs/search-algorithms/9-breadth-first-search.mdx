---
id: breadth-first-search
title: "Breadth-First Search (Graph/Tree)"
sidebar_label: "9. Breadth-First Search"
sidebar_position: 9
hide_table_of_contents: true
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

## Summary

Breadth-First Search explores a graph or tree one whole "ring" at a time, visiting everything close by before moving further out. Imagine dropping a pebble in a pond and watching the ripples spread outward evenly in all directions — that's BFS: it visits all of a starting point's direct neighbors first, then all of their neighbors, and so on. Because it spreads out evenly, it's the algorithm to use when you want the shortest route measured in number of steps. It gets slower the more places and connections there are, but it never wastes time backtracking.

## Pseudocode

```text
1. If the starting point isn't in the graph, report an empty result.
2. Keep a checklist of visited places, and a waiting line (a queue) starting with just the starting point. Mark the starting point visited right away.
3. While the waiting line isn't empty, take the place that's been waiting longest off the front of the line.
4. Add it to the result list.
5. Look at each of its neighbors. For any neighbor not already checked off, mark it visited and add it to the back of the waiting line.
6. Repeat until the waiting line is empty, then report the result list.
```

## Worked Example

We'll trace BFS over one fixed tree of 7 nodes, starting at `A`:

```text
        A
      /   \
     B     C
    / \   / \
   D   E F   G
```

Below, `[X]` means "X is sitting in the queue, waiting its turn." `(X)` means "X has already been dequeued and visited." A plain box means "not discovered yet."

### Step 1 — Start at A

<AsciiDiagram
  id="search-algorithms/breadth-first-search/step-1"
  mermaidSrc="/img/diagrams/906f0db7e343.svg"
  alt="Tree with node A marked as waiting in the queue, all other nodes undiscovered"
  caption="Step 1: queue = [A], visited = {A}"
  content={`[X] = waiting in queue    (X) = already visited and processed
               ┌───┐               
               │[A]│               
               └─┬─┘               
       ┌─────────┴─────────┐       
       ▼                   ▼       
     ┌───┐               ┌───┐     
     │ B │               │ C │     
     └─┬─┘               └─┬─┘     
  ┌────┴────┐         ┌────┴────┐  
  ▼         ▼         ▼         ▼  
┌───┐     ┌───┐     ┌───┐     ┌───┐
│ D │     │ E │     │ F │     │ G │
└───┘     └───┘     └───┘     └───┘
queue:   [A]
visited: {A}`}
 />

We put `A` in the queue and mark it visited before we've even looked at it — that's how BFS always starts. Nothing else has been discovered yet.

### Step 2 — Dequeue A, discover B and C

<AsciiDiagram
  id="search-algorithms/breadth-first-search/step-2"
  mermaidSrc="/img/diagrams/8710e38110cc.svg"
  alt="Tree with node A marked as visited, and its children B and C now waiting in the queue"
  caption="Step 2: dequeue A, enqueue B and C"
  content={`[X] = waiting in queue    (X) = already visited and processed
               ┌───┐               
               │(A)│               
               └─┬─┘               
       ┌─────────┴─────────┐       
       ▼                   ▼       
     ┌───┐               ┌───┐     
     │[B]│               │[C]│     
     └─┬─┘               └─┬─┘     
  ┌────┴────┐         ┌────┴────┐  
  ▼         ▼         ▼         ▼  
┌───┐     ┌───┐     ┌───┐     ┌───┐
│ D │     │ E │     │ F │     │ G │
└───┘     └───┘     └───┘     └───┘
queue:   [B, C]
visited: {A, B, C}`}
 />

We take `A` off the front of the queue and add it to the result. `A`'s neighbors, `B` and `C`, haven't been seen before, so we mark them visited and push them onto the back of the queue.

### Step 3 — Dequeue B, discover D and E

<AsciiDiagram
  id="search-algorithms/breadth-first-search/step-3"
  mermaidSrc="/img/diagrams/c7869728dbf4.svg"
  alt="Tree with A and B marked as visited, C still waiting, and B's children D and E newly waiting in the queue"
  caption="Step 3: dequeue B, enqueue D and E"
  content={`[X] = waiting in queue    (X) = already visited and processed
               ┌───┐               
               │(A)│               
               └─┬─┘               
       ┌─────────┴─────────┐       
       ▼                   ▼       
     ┌───┐               ┌───┐     
     │(B)│               │[C]│     
     └─┬─┘               └─┬─┘     
  ┌────┴────┐         ┌────┴────┐  
  ▼         ▼         ▼         ▼  
┌───┐     ┌───┐     ┌───┐     ┌───┐
│[D]│     │[E]│     │ F │     │ G │
└───┘     └───┘     └───┘     └───┘
queue:   [C, D, E]
visited: {A, B, C, D, E}`}
 />

`B` was next in line, so we dequeue it and visit it. Its children `D` and `E` are new, so they get marked visited and joined onto the back of the queue, right behind `C`.

### Step 4 — Dequeue C, discover F and G

<AsciiDiagram
  id="search-algorithms/breadth-first-search/step-4"
  mermaidSrc="/img/diagrams/fdf5d9251c31.svg"
  alt="Tree with A, B, and C marked as visited, and C's children F and G newly waiting in the queue alongside D and E"
  caption="Step 4: dequeue C, enqueue F and G"
  content={`[X] = waiting in queue    (X) = already visited and processed
               ┌───┐               
               │(A)│               
               └─┬─┘               
       ┌─────────┴─────────┐       
       ▼                   ▼       
     ┌───┐               ┌───┐     
     │(B)│               │(C)│     
     └─┬─┘               └─┬─┘     
  ┌────┴────┐         ┌────┴────┐  
  ▼         ▼         ▼         ▼  
┌───┐     ┌───┐     ┌───┐     ┌───┐
│[D]│     │[E]│     │[F]│     │[G]│
└───┘     └───┘     └───┘     └───┘
queue:   [D, E, F, G]
visited: {A, B, C, D, E, F, G}`}
 />

Now `C` comes off the front of the queue. Its children `F` and `G` are new too, so they get marked visited and added to the back — behind `D` and `E`, which were already waiting. Notice every node is now discovered, but four of them are still just waiting in line.

### Step 5 — Dequeue D, E, F, G in turn

<AsciiDiagram
  id="search-algorithms/breadth-first-search/step-5"
  mermaidSrc="/img/diagrams/48d3ff5a5c8c.svg"
  alt="Tree with all seven nodes marked as visited and the queue empty, showing BFS is complete"
  caption="Step 5: dequeue D, E, F, G — all leaves, queue empties, BFS complete"
  content={`[X] = waiting in queue    (X) = already visited and processed
               ┌───┐               
               │(A)│               
               └─┬─┘               
       ┌─────────┴─────────┐       
       ▼                   ▼       
     ┌───┐               ┌───┐     
     │(B)│               │(C)│     
     └─┬─┘               └─┬─┘     
  ┌────┴────┐         ┌────┴────┐  
  ▼         ▼         ▼         ▼  
┌───┐     ┌───┐     ┌───┐     ┌───┐
│(D)│     │(E)│     │(F)│     │(G)│
└───┘     └───┘     └───┘     └───┘
queue:   [ ] (empty)
visited: {A, B, C, D, E, F, G}`}
 />

One by one we dequeue `D`, `E`, `F`, and `G`. None of them have any neighbors we haven't already seen (they're leaves), so nothing new gets added to the queue. Once the queue is empty, BFS stops. Final visit order: `A, B, C, D, E, F, G` — notice that's exactly ring by ring: level 0, then all of level 1, then all of level 2.

## Complexity

- **Best case** — Even if the target you're looking for happens to be right next to the start, BFS still has to enqueue and check every node and connection to know it's found everything at that distance, so there's no faster shortcut (`O(V + E)`).
- **Average case** — Same story: every place (`V`) and every connection (`E`) gets looked at once, so the work scales with the size of the whole graph (`O(V + E)`).
- **Worst case** — Even when the target is the very last node discovered, or doesn't exist at all, BFS still only ever visits each place and each connection once, so it stays at (`O(V + E)`).
- **Space** — BFS can end up holding an entire "ring" of places in its waiting line at once. For a wide graph, that ring can be almost as big as the whole graph, so memory use scales with the number of places (`O(V)`).

## Applications

- **Shortest path in unweighted graphs** — because BFS finishes an entire ring before starting the next one, the first time it reaches any node is guaranteed to be by the fewest possible hops.
- **"Degrees of separation" on social networks** — finding how many mutual-friend hops separate two people (the idea behind LinkedIn's "2nd/3rd degree connection" labels) is BFS from one person outward.
- **Web crawlers** — search engines like Google's crawler visit a starting page, then all pages it links to, then all of those, spreading outward ring by ring to index the web.
- **Network broadcast and routing protocols** — protocols that flood a message outward to every reachable device one hop at a time (like OSPF's link-state flooding) follow the same ring-by-ring spread as BFS.
- **Puzzle solvers that need the fewest moves** — solving a sliding puzzle or a Rubik's Cube in the minimum number of moves is BFS over every possible board state, since it guarantees the first solution found used the fewest steps.

## Solutions

<Tabs>
  <TabItem value="python311" label="Python">

<CodeBlock language="python">{`from collections import deque


def breadth_first_search(graph: dict[str, list[str]], start: str) -> list[str]:
    result: list[str] = []

    if start not in graph:
        return result

    visited: set[str] = {start}
    queue: deque[str] = deque([start])

    while queue:
        node = queue.popleft()
        result.append(node)

        for neighbor in graph.get(node, []):
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append(neighbor)

    return result`}</CodeBlock>

  </TabItem>
  <TabItem value="c_gcc13" label="C">

<CodeBlock language="c">{`#include <stdio.h>
#include <string.h>

#define MAX_NODES 50
#define MAX_NEIGHBORS 20
#define MAX_NAME_LEN 32

/*
 * C has no built-in hash map, so the graph is represented with parallel
 * fixed-size arrays instead: names[i] is the i-th node's name, neighbors[i]
 * lists its neighbor names in adjacency-list order, neighborCount[i] is how
 * many neighbors it has. This is sized generously for the small graphs used
 * in the test cases (a handful of short, single-letter node names). The
 * queue is a simple fixed-size array used as a FIFO with head/tail indices.
 */
int breadthFirstSearch(char names[][MAX_NAME_LEN], int neighborCount[],
                        char neighbors[][MAX_NEIGHBORS][MAX_NAME_LEN], int nodeCount,
                        const char *start, char result[][MAX_NAME_LEN]) {
    int resultCount = 0;

    int startIndex = -1;
    for (int i = 0; i < nodeCount; i++) {
        if (strcmp(names[i], start) == 0) { startIndex = i; break; }
    }
    if (startIndex == -1) {
        return 0;
    }

    char visited[MAX_NODES][MAX_NAME_LEN];
    int visitedCount = 0;

    char queue[MAX_NODES * MAX_NEIGHBORS][MAX_NAME_LEN];
    int queueHead = 0;
    int queueTail = 0;

    strcpy(visited[visitedCount++], start);
    strcpy(queue[queueTail++], start);

    while (queueHead < queueTail) {
        char node[MAX_NAME_LEN];
        strcpy(node, queue[queueHead++]);
        strcpy(result[resultCount++], node);

        int nodeIndex = -1;
        for (int i = 0; i < nodeCount; i++) {
            if (strcmp(names[i], node) == 0) { nodeIndex = i; break; }
        }
        if (nodeIndex != -1) {
            for (int i = 0; i < neighborCount[nodeIndex]; i++) {
                const char *neighbor = neighbors[nodeIndex][i];
                int neighborVisited = 0;
                for (int j = 0; j < visitedCount; j++) {
                    if (strcmp(visited[j], neighbor) == 0) { neighborVisited = 1; break; }
                }
                if (!neighborVisited) {
                    strcpy(visited[visitedCount++], neighbor);
                    strcpy(queue[queueTail++], neighbor);
                }
            }
        }
    }

    return resultCount;
}`}</CodeBlock>

  </TabItem>
  <TabItem value="cpp_clang17" label="C++">

<CodeBlock language="cpp">{`#include <map>
#include <queue>
#include <set>
#include <string>
#include <vector>

std::vector<std::string> breadthFirstSearch(const std::map<std::string, std::vector<std::string>>& graph,
                                             const std::string& start) {
    std::vector<std::string> result;
    if (graph.find(start) == graph.end()) {
        return result;
    }

    std::set<std::string> visited;
    std::queue<std::string> q;
    visited.insert(start);
    q.push(start);

    while (!q.empty()) {
        std::string node = q.front();
        q.pop();
        result.push_back(node);

        auto it = graph.find(node);
        if (it != graph.end()) {
            for (const std::string& neighbor : it->second) {
                if (visited.find(neighbor) == visited.end()) {
                    visited.insert(neighbor);
                    q.push(neighbor);
                }
            }
        }
    }

    return result;
}`}</CodeBlock>

  </TabItem>
  <TabItem value="java21" label="Java">

<CodeBlock language="java">{`import java.util.ArrayList;
import java.util.HashSet;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;
import java.util.Queue;
import java.util.Set;

public class BreadthFirstSearch {
    public static List<String> breadthFirstSearch(Map<String, List<String>> graph, String start) {
        List<String> result = new ArrayList<>();

        if (!graph.containsKey(start)) {
            return result;
        }

        Set<String> visited = new HashSet<>();
        Queue<String> queue = new LinkedList<>();

        visited.add(start);
        queue.add(start);

        while (!queue.isEmpty()) {
            String node = queue.poll();
            result.add(node);

            for (String neighbor : graph.getOrDefault(node, new ArrayList<>())) {
                if (!visited.contains(neighbor)) {
                    visited.add(neighbor);
                    queue.add(neighbor);
                }
            }
        }

        return result;
    }
}`}</CodeBlock>

  </TabItem>
  <TabItem value="csharp_dotnet8" label="C#">

<CodeBlock language="csharp">{`using System.Collections.Generic;

public class BreadthFirstSearch
{
    public static List<string> BreadthFirstSearch(Dictionary<string, List<string>> graph, string start)
    {
        var result = new List<string>();

        if (!graph.ContainsKey(start))
        {
            return result;
        }

        var visited = new HashSet<string> { start };
        var queue = new Queue<string>();
        queue.Enqueue(start);

        while (queue.Count > 0)
        {
            string node = queue.Dequeue();
            result.Add(node);

            if (graph.TryGetValue(node, out var neighbors))
            {
                foreach (var neighbor in neighbors)
                {
                    if (!visited.Contains(neighbor))
                    {
                        visited.Add(neighbor);
                        queue.Enqueue(neighbor);
                    }
                }
            }
        }

        return result;
    }
}`}</CodeBlock>

  </TabItem>
  <TabItem value="javascript_node20" label="JavaScript">

<CodeBlock language="javascript">{`function breadthFirstSearch(graph, start) {
  const result = [];

  if (!(start in graph)) {
    return result;
  }

  const visited = new Set([start]);
  const queue = [start];

  while (queue.length > 0) {
    const node = queue.shift();
    result.push(node);

    for (const neighbor of graph[node] ?? []) {
      if (!visited.has(neighbor)) {
        visited.add(neighbor);
        queue.push(neighbor);
      }
    }
  }

  return result;
}`}</CodeBlock>

  </TabItem>
  <TabItem value="typescript516" label="TypeScript">

<CodeBlock language="typescript">{`function breadthFirstSearch(graph: Record<string, string[]>, start: string): string[] {
  const result: string[] = [];

  if (!(start in graph)) {
    return result;
  }

  const visited = new Set<string>([start]);
  const queue: string[] = [start];

  while (queue.length > 0) {
    const node = queue.shift() as string;
    result.push(node);

    for (const neighbor of graph[node] ?? []) {
      if (!visited.has(neighbor)) {
        visited.add(neighbor);
        queue.push(neighbor);
      }
    }
  }

  return result;
}`}</CodeBlock>

  </TabItem>
  <TabItem value="rust179" label="Rust">

<CodeBlock language="rust">{`use std::collections::{HashMap, HashSet, VecDeque};

fn breadth_first_search(graph: &HashMap<String, Vec<String>>, start: &str) -> Vec<String> {
    let mut result = Vec::new();
    if !graph.contains_key(start) {
        return result;
    }

    let mut visited: HashSet<String> = HashSet::new();
    let mut queue: VecDeque<String> = VecDeque::new();
    visited.insert(start.to_string());
    queue.push_back(start.to_string());

    while let Some(node) = queue.pop_front() {
        result.push(node.clone());
        if let Some(neighbors) = graph.get(&node) {
            for neighbor in neighbors.iter() {
                if !visited.contains(neighbor) {
                    visited.insert(neighbor.clone());
                    queue.push_back(neighbor.clone());
                }
            }
        }
    }

    result
}`}</CodeBlock>

  </TabItem>
  <TabItem value="go122" label="Go">

<CodeBlock language="go">{`func breadthFirstSearch(graph map[string][]string, start string) []string {
	result := []string{}
	if _, ok := graph[start]; !ok {
		return result
	}

	visited := make(map[string]bool)
	queue := []string{start}
	visited[start] = true

	for len(queue) > 0 {
		node := queue[0]
		queue = queue[1:]
		result = append(result, node)

		for _, neighbor := range graph[node] {
			if !visited[neighbor] {
				visited[neighbor] = true
				queue = append(queue, neighbor)
			}
		}
	}

	return result
}`}</CodeBlock>

  </TabItem>
</Tabs>
