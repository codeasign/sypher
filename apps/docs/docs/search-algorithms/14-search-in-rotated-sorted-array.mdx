---
id: search-in-rotated-sorted-array
title: "Search in Rotated Sorted Array"
sidebar_label: "14. Search in Rotated Sorted Array"
sidebar_position: 14
hide_table_of_contents: true
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

## Summary

A rotated sorted array is a list that was originally in order from smallest to biggest, but then got "cut" at some point and the front piece was moved to the back — so it looks like two sorted chunks stuck together (like `[4,5,6,7,0,1,2]`, which was `[0,1,2,4,5,6,7]` before the cut). This algorithm finds a target number in that kind of list without checking every item. The trick: at every step, at least one of the two halves you're looking at is still perfectly in order, so you can figure out which half that is and use ordinary binary search reasoning on it. It's nearly as fast as searching a normal sorted list — it only slows down a little the more items you add, instead of slowing down in proportion to every single item.

## Pseudocode

```text
1. Set low to the first index and high to the last index.
2. While low is at or before high:
3.     Look at the middle item between low and high.
4.     If it matches the target, stop — you found it. Report its position.
5.     Figure out which half, left or right, is still in normal sorted order.
6.     If the left half is in order:
7.         If the target could fit inside that left half's range of values, search the left half.
8.         Otherwise, search the right half.
9.     If the right half is in order instead:
10.        If the target could fit inside that right half's range of values, search the right half.
11.        Otherwise, search the left half.
12. If low passes high without a match, report "not found."
```

### Recursive Version

```text
1. Define a helper step that takes a low and a high boundary.
2. If low is past high, report "not found" — there's nothing left to check.
3. Look at the middle item between low and high.
4. If it matches the target, report its position and stop.
5. Figure out which half, left or right, is still in normal sorted order.
6. If the left half is in order:
7.     If the target could fit inside that left half's range of values, repeat this whole step on the left half.
8.     Otherwise, repeat this whole step on the right half.
9. If the right half is in order instead:
10.    If the target could fit inside that right half's range of values, repeat this whole step on the right half.
11.    Otherwise, repeat this whole step on the left half.
```

## Worked Example

This only works if the array is sorted apart from a single rotation — like a deck of cards that was cut once and the bottom half placed on top, rather than fully shuffled. Take the array `[15, 18, 2, 4, 7, 10, 13]`, which is the sorted list `[2, 4, 7, 10, 13, 15, 18]` rotated so its last two items moved to the front. Search it for the target `13`.

### Step 1

<AsciiDiagram
  id="search-algorithms/search-in-rotated-sorted-array/step-1"
  alt="The rotated array with low at index 0, mid at index 3, and high at index 6, showing the left half is unsorted and the right half is sorted and contains the target range"
  caption="Step 1: the right half [3..6] is sorted and 13 fits in its range, so search there"
  content={`
Array (target = 13):
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│ 15  │ 18  │  2  │  4  │  7  │ 10  │ 13  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘
   0     1     2     3     4     5     6
   ▲                 ▲                 ▲
  low               mid               high
Left half [0..3] = 15,18,2,4 ── NOT sorted, rotation hides here
Right half [3..6] = 4,7,10,13 ── sorted, and 13 fits in [4..13]
Decision: move low past mid ──▶ search the right half
`}
 />

The middle item is `4` at index 3. Comparing the low and mid values shows the left chunk (`15,18,2,4`) is not in order, so the right chunk (`4,7,10,13`) must be the sorted one. The target `13` falls inside that chunk's range, so the left half — including the rotation point — gets thrown away.

### Step 2

<AsciiDiagram
  id="search-algorithms/search-in-rotated-sorted-array/step-2"
  alt="The narrowed range from index 4 to 6, with low at index 4, mid at index 5, and high at index 6, showing the sorted left half does not contain the target"
  caption="Step 2: the left half [4..5] is sorted but 13 doesn't fit there, so search the right half instead"
  content={`
Array (target = 13), now only looking at indices 4..6:
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│ 15  │ 18  │  2  │  4  │  7  │ 10  │ 13  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘
   0     1     2     3     4     5     6
                           ▲     ▲     ▲
                          low   mid   high
Left half [4..5] = 7,10 ── sorted, but 13 does not fit in [7..10)
So the target must be past mid instead
Decision: move low past mid ──▶ search the right half
`}
 />

Now the middle item is `10` at index 5. This time the left chunk (`7,10`) is the sorted one, but `13` is bigger than everything in it, so it can't be hiding there. The search moves to the remaining single item on the right.

### Step 3

<AsciiDiagram
  id="search-algorithms/search-in-rotated-sorted-array/step-3"
  alt="The range narrowed to a single index 6 where low, mid, and high all meet and the value matches the target"
  caption="Step 3: low, mid, and high all land on index 6, whose value 13 matches the target"
  content={`
Array (target = 13), down to a single index:
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│ 15  │ 18  │  2  │  4  │  7  │ 10  │ 13  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘
   0     1     2     3     4     5     6
                                       ▲
                                  low=mid=high
Middle value 13 equals the target ──▶ MATCH, return index 6
`}
 />

Only index `6` is left, and its value is `13` — a match. The search stops and reports index `6`.

## Complexity

- **Best case:** if the very first middle item you check is the target, you're done immediately — instant (`O(1)`).
- **Average case:** like ordinary binary search, each step throws away about half of whatever's left to check, so the number of steps grows very slowly as the list grows (`O(log n)`).
- **Worst case:** even when the target is missing entirely, you still only ever cut the remaining range in half each time, so it still finishes in about the same small number of steps (`O(log n)`).
- **Space:** the iterative version only keeps a couple of pointers, no matter how big the list is (`O(1)`). The recursive version keeps one extra "layer" on the call stack per step, so it uses a small, slowly growing amount of extra memory (`O(log n)`).

## Applications

- **Ring buffers and circular queues** in embedded systems and audio/network streaming, where the write position wraps back to the start once it reaches the end, leaving a "rotated" order in memory.
- **Consistent-hashing rings** in distributed databases like Apache Cassandra and Amazon DynamoDB, where keys are looked up on a circular identifier space that has effectively been rotated relative to where you start scanning.
- **Log rotation buffers** in systems like syslog and journald, where the oldest entries get overwritten in place and the "start" of the readable log is wherever the last write landed, not at index 0.
- **Round-robin task scheduling** in operating systems, where the next task to run wraps around a fixed-size list of processes once it reaches the end.
- **Version-control and backup rotation schemes** that reuse a fixed number of slots in a cycle, where finding a specific backup by date means searching a rotated sequence of slot numbers.

## Solutions

<Tabs>
      <TabItem value="python311" label="Python">

<CodeBlock language="python">{`def search(nums: list[int], target: int) -> int:
    low = 0
    high = len(nums) - 1

    while low <= high:
        mid = (low + high) // 2

        if nums[mid] == target:
            return mid

        if nums[low] <= nums[mid]:
            if nums[low] <= target < nums[mid]:
                high = mid - 1
            else:
                low = mid + 1
        else:
            if nums[mid] < target <= nums[high]:
                low = mid + 1
            else:
                high = mid - 1

    return -1`}</CodeBlock>

      </TabItem>
      <TabItem value="c_gcc13" label="C">

<CodeBlock language="c">{`int search(const int *nums, int size, int target) {
    int low = 0;
    int high = size - 1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] == target) {
            return mid;
        }

        if (nums[low] <= nums[mid]) {
            if (nums[low] <= target && target < nums[mid]) {
                high = mid - 1;
            } else {
                low = mid + 1;
            }
        } else {
            if (nums[mid] < target && target <= nums[high]) {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="cpp_clang17" label="C++">

<CodeBlock language="cpp">{`#include <vector>

int search(const std::vector<int>& nums, int target) {
    int low = 0;
    int high = static_cast<int>(nums.size()) - 1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] == target) {
            return mid;
        }

        if (nums[low] <= nums[mid]) {
            if (nums[low] <= target && target < nums[mid]) {
                high = mid - 1;
            } else {
                low = mid + 1;
            }
        } else {
            if (nums[mid] < target && target <= nums[high]) {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="java21" label="Java">

<CodeBlock language="java">{`public class SearchInRotatedSortedArray {
    public static int search(int[] nums, int target) {
        int low = 0;
        int high = nums.length - 1;

        while (low <= high) {
            int mid = low + (high - low) / 2;

            if (nums[mid] == target) {
                return mid;
            }

            if (nums[low] <= nums[mid]) {
                if (nums[low] <= target && target < nums[mid]) {
                    high = mid - 1;
                } else {
                    low = mid + 1;
                }
            } else {
                if (nums[mid] < target && target <= nums[high]) {
                    low = mid + 1;
                } else {
                    high = mid - 1;
                }
            }
        }

        return -1;
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="csharp_dotnet8" label="C#">

<CodeBlock language="csharp">{`public class SearchInRotatedSortedArray
{
    public static int Search(int[] nums, int target)
    {
        int low = 0;
        int high = nums.Length - 1;

        while (low <= high)
        {
            int mid = low + (high - low) / 2;

            if (nums[mid] == target)
            {
                return mid;
            }

            if (nums[low] <= nums[mid])
            {
                if (nums[low] <= target && target < nums[mid])
                {
                    high = mid - 1;
                }
                else
                {
                    low = mid + 1;
                }
            }
            else
            {
                if (nums[mid] < target && target <= nums[high])
                {
                    low = mid + 1;
                }
                else
                {
                    high = mid - 1;
                }
            }
        }

        return -1;
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="javascript_node20" label="JavaScript">

<CodeBlock language="javascript">{`function search(nums, target) {
    let low = 0;
    let high = nums.length - 1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (nums[mid] === target) {
            return mid;
        }

        if (nums[low] <= nums[mid]) {
            if (nums[low] <= target && target < nums[mid]) {
                high = mid - 1;
            } else {
                low = mid + 1;
            }
        } else {
            if (nums[mid] < target && target <= nums[high]) {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="typescript516" label="TypeScript">

<CodeBlock language="typescript">{`function search(nums: number[], target: number): number {
    let low = 0;
    let high = nums.length - 1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (nums[mid] === target) {
            return mid;
        }

        if (nums[low] <= nums[mid]) {
            if (nums[low] <= target && target < nums[mid]) {
                high = mid - 1;
            } else {
                low = mid + 1;
            }
        } else {
            if (nums[mid] < target && target <= nums[high]) {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="rust179" label="Rust">

<CodeBlock language="rust">{`pub fn search(nums: &[i32], target: i32) -> i32 {
    let mut low: i32 = 0;
    let mut high: i32 = nums.len() as i32 - 1;

    while low <= high {
        let mid = low + (high - low) / 2;
        let mid_val = nums[mid as usize];

        if mid_val == target {
            return mid;
        }

        let low_val = nums[low as usize];
        let high_val = nums[high as usize];

        if low_val <= mid_val {
            if low_val <= target && target < mid_val {
                high = mid - 1;
            } else {
                low = mid + 1;
            }
        } else {
            if mid_val < target && target <= high_val {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
    }

    -1
}`}</CodeBlock>

      </TabItem>
      <TabItem value="go122" label="Go">

<CodeBlock language="go">{`func search(nums []int, target int) int {
    low := 0
    high := len(nums) - 1

    for low <= high {
        mid := low + (high-low)/2

        if nums[mid] == target {
            return mid
        }

        if nums[low] <= nums[mid] {
            if nums[low] <= target && target < nums[mid] {
                high = mid - 1
            } else {
                low = mid + 1
            }
        } else {
            if nums[mid] < target && target <= nums[high] {
                low = mid + 1
            } else {
                high = mid - 1
            }
        }
    }

    return -1
}`}</CodeBlock>

      </TabItem>
    </Tabs>
