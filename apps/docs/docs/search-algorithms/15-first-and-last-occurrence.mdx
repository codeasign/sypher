---
id: first-and-last-occurrence
title: "First & Last Occurrence (Lower Bound & Upper Bound)"
sidebar_label: "15. First & Last Occurrence"
sidebar_position: 15
hide_table_of_contents: true
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';
import DetailsPageWithIDE from '@site/src/components/DetailsPageWithIDE/Index';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

<DetailsPageWithIDE
  meta={{ id: 'search-algorithms/first-and-last-occurrence', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '5 7 7 8 8 10\n8\n', expectedOutput: '3 4\n' },
    { stdin: '5 7 7 8 8 10\n6\n', expectedOutput: '-1 -1\n' },
    { stdin: '\n0\n', expectedOutput: '-1 -1\n' },
    { stdin: '2 2 2 2 2\n2\n', expectedOutput: '0 4\n' },
    { stdin: '1\n1\n', expectedOutput: '0 0\n' },
  ]}
  starterCode={{
    python311: `def search_range(nums: list[int], target: int) -> tuple[int, int]:
    # TODO: Implement search algorithm
    return -1`,
    c_gcc13: `void search_range(const int *nums, int n, int target, int *first_out, int *last_out) {
    // TODO: Implement search algorithm
    return -1;
}`,
    cpp_clang17: `#include <utility>
#include <vector>

std::pair<int, int> searchRange(const std::vector<int>& nums, int target) {
    // TODO: Implement search algorithm
    return -1;
}`,
    java21: `public class SearchRangeAlgorithm {
    public static int[] searchRange(int[] nums, int target) {
        // TODO: Implement sorting algorithm
        return new int[0];
    }`,
    csharp_dotnet8: `public class SearchRangeAlgorithm
{
        // TODO: Implement search algorithm
        return -1;
    }`,
    javascript_node20: `function searchRange(nums, target) {
  // TODO: Implement search algorithm
  return -1;`,
    typescript516: `function searchRange(nums: number[], target: number): [number, number] {
  // TODO: Implement search algorithm
  return -1;`,
    rust179: `pub fn search_range(nums: &[i32], target: i32) -> (i32, i32) {
    // TODO: Implement search algorithm
    -1
}`,
    go122: `func searchRange(nums []int, target int) (int, int) {
	// TODO: Implement search algorithm
	return -1
}`,
  }}
  harness={{
    python311: `import sys
data = sys.stdin.read().split('\\n')
nums = list(map(int, data[0].split())) if data[0].strip() else []
target = int(data[1].strip())
first, last = search_range(nums, target)
print(f"{first} {last}")`,
    c_gcc13: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void search_range(const int *nums, int n, int target, int *first_out, int *last_out);

int main(void) {
    char line1[10000];
    char line2[10000];

    if (!fgets(line1, sizeof(line1), stdin)) {
        line1[0] = '\\0';
    }
    if (!fgets(line2, sizeof(line2), stdin)) {
        line2[0] = '\\0';
    }

    int nums[10000];
    int n = 0;
    char *token = strtok(line1, " \\t\\r\\n");
    while (token != NULL) {
        nums[n++] = atoi(token);
        token = strtok(NULL, " \\t\\r\\n");
    }

    int target = atoi(line2);
    int first, last;
    search_range(nums, n, target, &first, &last);
    printf("%d %d\\n", first, last);
    return 0;
}`,
    cpp_clang17: `#include <iostream>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

std::pair<int, int> searchRange(const std::vector<int>& nums, int target);

int main() {
    std::string line1;
    std::string line2;
    std::getline(std::cin, line1);
    std::getline(std::cin, line2);

    std::vector<int> nums;
    std::istringstream iss(line1);
    int value;
    while (iss >> value) {
        nums.push_back(value);
    }

    int target = line2.empty() ? 0 : std::stoi(line2);
    std::pair<int, int> result = searchRange(nums, target);
    std::cout << result.first << " " << result.second << std::endl;
    return 0;
}`,
    java21: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String line = sc.nextLine();
        int[] nums = line.trim().isEmpty() ? new int[0] : java.util.Arrays.stream(line.trim().split("\\\\s+")).mapToInt(Integer::parseInt).toArray();
        int target = Integer.parseInt(sc.nextLine().trim());
        int[] result = SearchRangeAlgorithm.searchRange(nums, target);
        System.out.println(result[0] + " " + result[1]);
    }
}`,
    csharp_dotnet8: `class Program {
    static void Main() {
        string line = System.Console.ReadLine();
        int[] nums = string.IsNullOrWhiteSpace(line) ? new int[0] : System.Array.ConvertAll(line.Trim().Split(' '), int.Parse);
        int target = int.Parse(System.Console.ReadLine());
        int[] result = SearchRangeAlgorithm.SearchRange(nums, target);
        System.Console.WriteLine(result[0] + " " + result[1]);
    }
}`,
    javascript_node20: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', (l) => lines.push(l));
rl.on('close', () => {
    const nums = lines[0].trim() === '' ? [] : lines[0].trim().split(/\\s+/).map(Number);
    const target = parseInt(lines[1]);
    const result = searchRange(nums, target);
    console.log(result[0] + ' ' + result[1]);
});`,
    typescript516: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    const nums = lines[0].trim() === '' ? [] : lines[0].trim().split(/\\s+/).map(Number);
    const target = parseInt(lines[1]);
    const result = searchRange(nums, target);
    console.log(result[0] + ' ' + result[1]);
});`,
    rust179: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines();

    let line1 = lines.next().unwrap_or(Ok(String::new())).unwrap_or_default();
    let line2 = lines.next().unwrap_or(Ok(String::new())).unwrap_or_default();

    let nums: Vec<i32> = line1
        .trim()
        .split_whitespace()
        .map(|s| s.parse().unwrap())
        .collect();

    let target: i32 = line2.trim().parse().unwrap_or(0);
    let (first, last) = search_range(&nums, target);
    println!("{} {}", first, last);
}`,
    go122: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    line1, _ := reader.ReadString('\\n')
    line2, _ := reader.ReadString('\\n')

    line1 = strings.TrimRight(line1, "\\r\\n")
    line2 = strings.TrimSpace(line2)

    var nums []int
    fields := strings.Fields(line1)
    for _, f := range fields {
        n, _ := strconv.Atoi(f)
        nums = append(nums, n)
    }

    target, _ := strconv.Atoi(line2)
    first, last := searchRange(nums, target)
    fmt.Printf("%d %d\\n", first, last)
}`,
  }}
  defaultLanguage="python311"

  solutions={
    <Tabs>
      <TabItem value="python311" label="Python 3.11">

<CodeBlock language="python">{`def search_range(nums: list[int], target: int) -> tuple[int, int]:
    n = len(nums)

    # Lower bound: leftmost index where nums[index] == target
    low, high = 0, n - 1
    first = -1
    while low <= high:
        mid = (low + high) // 2
        if nums[mid] < target:
            low = mid + 1
        else:
            if nums[mid] == target:
                first = mid
            high = mid - 1

    if first == -1:
        return (-1, -1)

    # Upper bound: rightmost index where nums[index] == target
    low, high = 0, n - 1
    last = -1
    while low <= high:
        mid = (low + high) // 2
        if nums[mid] > target:
            high = mid - 1
        else:
            if nums[mid] == target:
                last = mid
            low = mid + 1

    return (first, last)`}</CodeBlock>

      </TabItem>
      <TabItem value="c_gcc13" label="C (GCC 13)">

<CodeBlock language="c">{`void search_range(const int *nums, int n, int target, int *first_out, int *last_out) {
    int low = 0;
    int high = n - 1;
    int first = -1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] < target) {
            low = mid + 1;
        } else {
            if (nums[mid] == target) {
                first = mid;
            }
            high = mid - 1;
        }
    }

    if (first == -1) {
        *first_out = -1;
        *last_out = -1;
        return;
    }

    low = 0;
    high = n - 1;
    int last = -1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] > target) {
            high = mid - 1;
        } else {
            if (nums[mid] == target) {
                last = mid;
            }
            low = mid + 1;
        }
    }

    *first_out = first;
    *last_out = last;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="cpp_clang17" label="C++ (Clang 17)">

<CodeBlock language="cpp">{`#include <utility>
#include <vector>

std::pair<int, int> searchRange(const std::vector<int>& nums, int target) {
    int n = static_cast<int>(nums.size());
    int low = 0;
    int high = n - 1;
    int first = -1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] < target) {
            low = mid + 1;
        } else {
            if (nums[mid] == target) {
                first = mid;
            }
            high = mid - 1;
        }
    }

    if (first == -1) {
        return {-1, -1};
    }

    low = 0;
    high = n - 1;
    int last = -1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] > target) {
            high = mid - 1;
        } else {
            if (nums[mid] == target) {
                last = mid;
            }
            low = mid + 1;
        }
    }

    return {first, last};
}`}</CodeBlock>

      </TabItem>
      <TabItem value="java21" label="Java 21">

<CodeBlock language="java">{`public class SearchRangeAlgorithm {
    public static int[] searchRange(int[] nums, int target) {
        int n = nums.length;
        int low = 0;
        int high = n - 1;
        int first = -1;

        while (low <= high) {
            int mid = low + (high - low) / 2;

            if (nums[mid] < target) {
                low = mid + 1;
            } else {
                if (nums[mid] == target) {
                    first = mid;
                }
                high = mid - 1;
            }
        }

        if (first == -1) {
            return new int[] { -1, -1 };
        }

        low = 0;
        high = n - 1;
        int last = -1;

        while (low <= high) {
            int mid = low + (high - low) / 2;

            if (nums[mid] > target) {
                high = mid - 1;
            } else {
                if (nums[mid] == target) {
                    last = mid;
                }
                low = mid + 1;
            }
        }

        return new int[] { first, last };
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="csharp_dotnet8" label="C# / .NET 8">

<CodeBlock language="csharp">{`public class SearchRangeAlgorithm
{
    public static int[] SearchRange(int[] nums, int target)
    {
        int n = nums.Length;
        int low = 0;
        int high = n - 1;
        int first = -1;

        while (low <= high)
        {
            int mid = low + (high - low) / 2;

            if (nums[mid] < target)
            {
                low = mid + 1;
            }
            else
            {
                if (nums[mid] == target)
                {
                    first = mid;
                }
                high = mid - 1;
            }
        }

        if (first == -1)
        {
            return new int[] { -1, -1 };
        }

        low = 0;
        high = n - 1;
        int last = -1;

        while (low <= high)
        {
            int mid = low + (high - low) / 2;

            if (nums[mid] > target)
            {
                high = mid - 1;
            }
            else
            {
                if (nums[mid] == target)
                {
                    last = mid;
                }
                low = mid + 1;
            }
        }

        return new int[] { first, last };
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="javascript_node20" label="JavaScript (Node 20.10.0)">

<CodeBlock language="javascript">{`function searchRange(nums, target) {
    const n = nums.length;
    let low = 0;
    let high = n - 1;
    let first = -1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (nums[mid] < target) {
            low = mid + 1;
        } else {
            if (nums[mid] === target) {
                first = mid;
            }
            high = mid - 1;
        }
    }

    if (first === -1) {
        return [-1, -1];
    }

    low = 0;
    high = n - 1;
    let last = -1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (nums[mid] > target) {
            high = mid - 1;
        } else {
            if (nums[mid] === target) {
                last = mid;
            }
            low = mid + 1;
        }
    }

    return [first, last];
}`}</CodeBlock>

      </TabItem>
      <TabItem value="typescript516" label="TypeScript 5.1.6">

<CodeBlock language="typescript">{`function searchRange(nums: number[], target: number): [number, number] {
    const n = nums.length;
    let low = 0;
    let high = n - 1;
    let first = -1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (nums[mid] < target) {
            low = mid + 1;
        } else {
            if (nums[mid] === target) {
                first = mid;
            }
            high = mid - 1;
        }
    }

    if (first === -1) {
        return [-1, -1];
    }

    low = 0;
    high = n - 1;
    let last = -1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (nums[mid] > target) {
            high = mid - 1;
        } else {
            if (nums[mid] === target) {
                last = mid;
            }
            low = mid + 1;
        }
    }

    return [first, last];
}`}</CodeBlock>

      </TabItem>
      <TabItem value="rust179" label="Rust 1.79.0">

<CodeBlock language="rust">{`pub fn search_range(nums: &[i32], target: i32) -> (i32, i32) {
    let n = nums.len() as i32;
    let mut low: i32 = 0;
    let mut high: i32 = n - 1;
    let mut first: i32 = -1;

    while low <= high {
        let mid = low + (high - low) / 2;

        if nums[mid as usize] < target {
            low = mid + 1;
        } else {
            if nums[mid as usize] == target {
                first = mid;
            }
            high = mid - 1;
        }
    }

    if first == -1 {
        return (-1, -1);
    }

    low = 0;
    high = n - 1;
    let mut last: i32 = -1;

    while low <= high {
        let mid = low + (high - low) / 2;

        if nums[mid as usize] > target {
            high = mid - 1;
        } else {
            if nums[mid as usize] == target {
                last = mid;
            }
            low = mid + 1;
        }
    }

    (first, last)
}`}</CodeBlock>

      </TabItem>
      <TabItem value="go122" label="Go 1.22">

<CodeBlock language="go">{`func searchRange(nums []int, target int) (int, int) {
    n := len(nums)
    low, high := 0, n-1
    first := -1

    for low <= high {
        mid := low + (high-low)/2

        if nums[mid] < target {
            low = mid + 1
        } else {
            if nums[mid] == target {
                first = mid
            }
            high = mid - 1
        }
    }

    if first == -1 {
        return -1, -1
    }

    low, high = 0, n-1
    last := -1

    for low <= high {
        mid := low + (high-low)/2

        if nums[mid] > target {
            high = mid - 1
        } else {
            if nums[mid] == target {
                last = mid
            }
            low = mid + 1
        }
    }

    return first, last
}`}</CodeBlock>

      </TabItem>
    </Tabs>
  }
>

## Summary

This algorithm finds the very first and very last position of a value in a sorted list, even when that value shows up many times in a row. It does this by running binary search twice: once hunting for the leftmost matching spot, and once hunting for the rightmost. Picture a sorted shelf of numbered folders where several folders in a row all say "8" — this finds exactly where that run of "8"s starts and where it ends. It stays fast even with lots of duplicates, because both searches jump straight to the edges instead of checking every matching folder one by one.

## Pseudocode

```text
Finding the FIRST occurrence:
1. Look at the whole list as your search window.
2. Check the middle item of the window.
3. If it's smaller than the target, the target must be further right — shrink the window to the right half.
4. Otherwise (it's equal to or bigger than the target): if it equals the target, remember this spot as your best guess so far. Then shrink the window to the left half, in case there's an even earlier match.
5. Repeat steps 2-4 until the window is empty.
6. Your last remembered guess is the first occurrence (or "not found" if you never got a match).

Finding the LAST occurrence:
1. Look at the whole list as your search window again.
2. Check the middle item of the window.
3. If it's bigger than the target, the target must be further left — shrink the window to the left half.
4. Otherwise (it's equal to or smaller than the target): if it equals the target, remember this spot as your best guess so far. Then shrink the window to the right half, in case there's an even later match.
5. Repeat steps 2-4 until the window is empty.
6. Your last remembered guess is the last occurrence.

Putting it together:
1. Run the "find first" search.
2. If nothing was found, report "not found" for both and stop.
3. Otherwise, also run the "find last" search.
4. Report both positions.
```

## ASCII Diagram

<AsciiDiagram
  id="search-algorithms/first-and-last-occurrence"
  alt="Sorted array with a run of the value 8 spanning indices 3 through 6, with the first search landing on index 3 and the second search landing on index 6"
  caption="Two binary searches: one finds where the run of 8s starts, the other finds where it ends"
  content={`
Sorted array (target = 8, duplicates form a run):
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│  2  │  4  │  6  │  8  │  8  │  8  │  8  │ 10  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
   0     1     2     3     4     5     6     7
                     ▲                 ▲
                   first              last

Search #1 (leftmost)  ──▶ keeps checking left of each match, settles at index 3
Search #2 (rightmost) ──▶ keeps checking right of each match, settles at index 6
Result: first=3, last=6`}
/>

## How It Works

- Look at the diagram: the target is `8`, and it appears four times in a row, at indices 3, 4, 5, and 6.
- The first search starts in the middle of the array. Every time it lands on an `8`, it writes down "maybe this is the first one" but keeps checking further left, in case there's an earlier `8` it hasn't seen yet. It only stops once it runs out of window to check on the left. Whatever it wrote down last is the true first occurrence — index 3.
- The second search does the mirror-image thing: every time it lands on an `8`, it writes down "maybe this is the last one" but keeps checking further right. It settles on index 6.
- This only works because the list is already sorted, the same way looking up a word in a dictionary only works because the words are in order. Sorted order guarantees all the `8`s sit next to each other in one unbroken run, so "keep checking left" or "keep checking right" never skips over a hidden match.
- If the target isn't in the list at all, the first search never finds anything to write down, so both results come back as "not found" and the second search doesn't even bother running.
- How fast is it? Each of the two searches only looks at a handful of items, since it keeps cutting the window in half — even a list of a million items takes about 20 checks per search (best and worst case are both around `O(log n)` time). That holds whether the target appears once or a hundred thousand times, which is what makes this so much faster than scanning through the list from the start.

## Practice

| # | Input | Expected Output | What It Tests |
|---|---|---|---|
| 1 | nums=[5,7,7,8,8,10], target=8 | [3, 4] | target appears twice, not at the array boundaries |
| 2 | nums=[5,7,7,8,8,10], target=6 | [-1, -1] | target not present, falls between existing values |
| 3 | nums=[], target=0 | [-1, -1] | empty array |
| 4 | nums=[2,2,2,2,2], target=2 | [0, 4] | every element equals the target (full-array range) |
| 5 | nums=[1], target=1 | [0, 0] | single-element array, target present |

Run the test cases above directly in the editor to verify your solution.

</DetailsPageWithIDE>
