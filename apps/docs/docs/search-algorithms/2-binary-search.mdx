---
id: binary-search
title: "Binary Search"
sidebar_label: "2. Binary Search"
sidebar_position: 2
hide_table_of_contents: true
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';
import DetailsPageWithIDE from '@site/src/components/DetailsPageWithIDE/Index';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

<DetailsPageWithIDE
  meta={{ id: 'search-algorithms/binary-search', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '1 3 5 7 9 11\n7\n', expectedOutput: '3\n' },
    { stdin: '1 3 5 7 9 11\n2\n', expectedOutput: '-1\n' },
    { stdin: '\n5\n', expectedOutput: '-1\n' },
    { stdin: '10\n10\n', expectedOutput: '0\n' },
    { stdin: '1 3 5 7 9 11\n1\n', expectedOutput: '0\n' },
  ]}
  starterCode={{
    python311: `def binary_search(array: list[int], target: int) -> int:
    # TODO: Implement search algorithm
    return -1`,
    c_gcc13: `int binary_search(const int *array, int size, int target) {
    // TODO: Implement search algorithm
    return -1;
}`,
    cpp_clang17: `#include <vector>

int binarySearch(const std::vector<int>& array, int target) {
    // TODO: Implement search algorithm
    return -1;
}`,
    java21: `public class BinarySearchAlgorithm {
    public static int binarySearch(int[] array, int target) {
        // TODO: Implement sorting algorithm
        return new int[0];
    }`,
    csharp_dotnet8: `public class BinarySearchAlgorithm
{
        // TODO: Implement search algorithm
        return -1;
    }`,
    javascript_node20: `function binarySearch(array, target) {
  // TODO: Implement search algorithm
  return -1;`,
    typescript516: `function binarySearch(array: number[], target: number): number {
  // TODO: Implement search algorithm
  return -1;`,
    rust179: `pub fn binary_search(array: &[i32], target: i32) -> i32 {
    // TODO: Implement search algorithm
    -1
}`,
    go122: `func binarySearch(array []int, target int) int {
	// TODO: Implement search algorithm
	return -1
}`,
  }}
  harness={{
    python311: `import sys
data = sys.stdin.read().split('\\n')
array = list(map(int, data[0].split())) if data[0].strip() else []
target = int(data[1].strip())
result = binary_search(array, target)
print(result)`,
    c_gcc13: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int binary_search(const int *array, int size, int target);

int main(void) {
    char line1[10000];
    char line2[10000];

    if (!fgets(line1, sizeof(line1), stdin)) {
        line1[0] = '\\0';
    }
    if (!fgets(line2, sizeof(line2), stdin)) {
        line2[0] = '\\0';
    }

    int array[10000];
    int size = 0;
    char *token = strtok(line1, " \\t\\r\\n");
    while (token != NULL) {
        array[size++] = atoi(token);
        token = strtok(NULL, " \\t\\r\\n");
    }

    int target = atoi(line2);
    int result = binary_search(array, size, target);
    printf("%d\\n", result);
    return 0;
}`,
    cpp_clang17: `#include <iostream>
#include <sstream>
#include <string>
#include <vector>

int binarySearch(const std::vector<int>& array, int target);

int main() {
    std::string line1;
    std::string line2;
    std::getline(std::cin, line1);
    std::getline(std::cin, line2);

    std::vector<int> array;
    std::istringstream iss(line1);
    int value;
    while (iss >> value) {
        array.push_back(value);
    }

    int target = line2.empty() ? 0 : std::stoi(line2);
    int result = binarySearch(array, target);
    std::cout << result << std::endl;
    return 0;
}`,
    java21: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String line = sc.nextLine();
        int[] array = line.trim().isEmpty() ? new int[0] : java.util.Arrays.stream(line.trim().split("\\\\s+")).mapToInt(Integer::parseInt).toArray();
        int target = Integer.parseInt(sc.nextLine().trim());
        int result = BinarySearchAlgorithm.binarySearch(array, target);
        System.out.println(result);
    }
}`,
    csharp_dotnet8: `class Program {
    static void Main() {
        string line = System.Console.ReadLine();
        int[] array = string.IsNullOrWhiteSpace(line) ? new int[0] : System.Array.ConvertAll(line.Trim().Split(' '), int.Parse);
        int target = int.Parse(System.Console.ReadLine());
        int result = BinarySearchAlgorithm.BinarySearch(array, target);
        System.Console.WriteLine(result);
    }
}`,
    javascript_node20: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', (l) => lines.push(l));
rl.on('close', () => {
    const array = lines[0].trim() === '' ? [] : lines[0].trim().split(/\\s+/).map(Number);
    const target = parseInt(lines[1]);
    const result = binarySearch(array, target);
    console.log(result);
});`,
    typescript516: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    const array = lines[0].trim() === '' ? [] : lines[0].trim().split(/\\s+/).map(Number);
    const target = parseInt(lines[1]);
    const result = binarySearch(array, target);
    console.log(result);
});`,
    rust179: `use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines();

    let line1 = lines.next().unwrap_or(Ok(String::new())).unwrap_or_default();
    let line2 = lines.next().unwrap_or(Ok(String::new())).unwrap_or_default();

    let array: Vec<i32> = line1
        .trim()
        .split_whitespace()
        .map(|s| s.parse().unwrap())
        .collect();

    let target: i32 = line2.trim().parse().unwrap_or(0);
    let result = binary_search(&array, target);
    println!("{}", result);
}`,
    go122: `package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    line1, _ := reader.ReadString('\\n')
    line2, _ := reader.ReadString('\\n')

    line1 = strings.TrimRight(line1, "\\r\\n")
    line2 = strings.TrimSpace(line2)

    var array []int
    fields := strings.Fields(line1)
    for _, f := range fields {
        n, _ := strconv.Atoi(f)
        array = append(array, n)
    }

    target, _ := strconv.Atoi(line2)
    result := binarySearch(array, target)
    fmt.Println(result)
}`,
  }}
  defaultLanguage="python311"

  solutions={
    <Tabs>
      <TabItem value="python311" label="Python 3.11">

<CodeBlock language="python">{`def binary_search(array: list[int], target: int) -> int:
    low = 0
    high = len(array) - 1

    while low <= high:
        mid = (low + high) // 2

        if array[mid] == target:
            return mid
        elif array[mid] < target:
            low = mid + 1
        else:
            high = mid - 1

    return -1`}</CodeBlock>

      </TabItem>
      <TabItem value="c_gcc13" label="C (GCC 13)">

<CodeBlock language="c">{`int binary_search(const int *array, int size, int target) {
    int low = 0;
    int high = size - 1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (array[mid] == target) {
            return mid;
        } else if (array[mid] < target) {
            low = mid + 1;
        } else {
            high = mid - 1;
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="cpp_clang17" label="C++ (Clang 17)">

<CodeBlock language="cpp">{`#include <vector>

int binarySearch(const std::vector<int>& array, int target) {
    int low = 0;
    int high = static_cast<int>(array.size()) - 1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (array[mid] == target) {
            return mid;
        } else if (array[mid] < target) {
            low = mid + 1;
        } else {
            high = mid - 1;
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="java21" label="Java 21">

<CodeBlock language="java">{`public class BinarySearchAlgorithm {
    public static int binarySearch(int[] array, int target) {
        int low = 0;
        int high = array.length - 1;

        while (low <= high) {
            int mid = low + (high - low) / 2;

            if (array[mid] == target) {
                return mid;
            } else if (array[mid] < target) {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }

        return -1;
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="csharp_dotnet8" label="C# / .NET 8">

<CodeBlock language="csharp">{`public class BinarySearchAlgorithm
{
    public static int BinarySearch(int[] array, int target)
    {
        int low = 0;
        int high = array.Length - 1;

        while (low <= high)
        {
            int mid = low + (high - low) / 2;

            if (array[mid] == target)
            {
                return mid;
            }
            else if (array[mid] < target)
            {
                low = mid + 1;
            }
            else
            {
                high = mid - 1;
            }
        }

        return -1;
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="javascript_node20" label="JavaScript (Node 20.10.0)">

<CodeBlock language="javascript">{`function binarySearch(array, target) {
    let low = 0;
    let high = array.length - 1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (array[mid] === target) {
            return mid;
        } else if (array[mid] < target) {
            low = mid + 1;
        } else {
            high = mid - 1;
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="typescript516" label="TypeScript 5.1.6">

<CodeBlock language="typescript">{`function binarySearch(array: number[], target: number): number {
    let low = 0;
    let high = array.length - 1;

    while (low <= high) {
        const mid = low + Math.floor((high - low) / 2);

        if (array[mid] === target) {
            return mid;
        } else if (array[mid] < target) {
            low = mid + 1;
        } else {
            high = mid - 1;
        }
    }

    return -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="rust179" label="Rust 1.79.0">

<CodeBlock language="rust">{`pub fn binary_search(array: &[i32], target: i32) -> i32 {
    let mut low: i32 = 0;
    let mut high: i32 = array.len() as i32 - 1;

    while low <= high {
        let mid = low + (high - low) / 2;

        if array[mid as usize] == target {
            return mid;
        } else if array[mid as usize] < target {
            low = mid + 1;
        } else {
            high = mid - 1;
        }
    }

    -1
}`}</CodeBlock>

      </TabItem>
      <TabItem value="go122" label="Go 1.22">

<CodeBlock language="go">{`func binarySearch(array []int, target int) int {
    low := 0
    high := len(array) - 1

    for low <= high {
        mid := low + (high-low)/2

        if array[mid] == target {
            return mid
        } else if array[mid] < target {
            low = mid + 1
        } else {
            high = mid - 1
        }
    }

    return -1
}`}</CodeBlock>

      </TabItem>
    </Tabs>
  }
>

## Summary

Binary Search finds something in a sorted list by always checking the middle item first, then throwing away the half of the list that can't have the answer. Imagine guessing a number between 1 and 100: instead of counting up one by one, you'd guess 50, hear "higher" or "lower," and jump straight to the middle of whichever half is left. It repeats that "check the middle, discard a half" move until it finds the target or runs out of list. Because it throws away half the remaining items every single step, it's dramatically faster than checking one item at a time — but it only works if the list is already sorted.

## Pseudocode

```text
1. Set a "low" marker at the start of the list and a "high" marker at the end.
2. While the low marker hasn't passed the high marker:
3. Find the item exactly in the middle of low and high.
4. If that middle item is the target, stop and report its position.
5. If the middle item is smaller than the target, the target must be further right — move the low marker to just past the middle.
6. If the middle item is bigger than the target, the target must be further left — move the high marker to just before the middle.
7. If the low marker ever passes the high marker without a match, report "not found."
```

## ASCII Diagram

<AsciiDiagram
  id="search-algorithms/binary-search"
  alt="Sorted array of eight elements with low, mid, and high pointers narrowing over three iterations toward the target value 13 at index 6"
  caption="Binary Search: low/mid/high pointers narrow in on the target, one half at a time"
  content={`
Sorted array (target = 13):
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│  1  │  3  │  5  │  7  │  9  │ 11  │ 13  │ 15  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
   0     1     2     3     4     5     6     7
Step 1: low=0, mid=3 (val=7), high=7   ──▶ 13 > 7, keep the right half
Step 2: low=4, mid=5 (val=11), high=7  ──▶ 13 > 11, keep the right half
Step 3: low=6, mid=6 (val=13), high=7  ──▶ match! return index 6
`}
/>

## How It Works

- We're looking for `13` in the sorted row of boxes above. The pointers `low` and `high` start at the two ends: `low = 0`, `high = 7`.
- Step 1: the middle spot between `0` and `7` is index `3`, which holds `7`. Since `13` is bigger than `7`, the target has to be somewhere to the right — so we move `low` to just past index 3, and forget the left half ever existed.
- Step 2: now the middle of `4` and `7` is index `5`, which holds `11`. Still smaller than `13`, so we move `low` again, past index 5.
- Step 3: now `low` and `high` have squeezed down to index `6`, which holds `13` — a match. We stop and report position 6.
- This only works because the list is sorted: if it weren't, "13 is bigger than 7, so skip the left half" would be a bad guess, and the trick falls apart.
- How fast is it? If you're lucky and the target is smack in the middle right away, it's found almost instantly. If you're unlucky — or the target isn't in the list at all — you keep halving the list until only one spot is left, which still finishes very quickly since halving shrinks things fast (this is called `O(log n)` time, compared to checking every item one by one at `O(n)`).

## Practice

| # | Input | Expected Output | What It Tests |
|---|---|---|---|
| 1 | array=[1,3,5,7,9,11], target=7 | 3 | the target is somewhere in the middle |
| 2 | array=[1,3,5,7,9,11], target=2 | -1 | the target would fall between two numbers, but isn't there |
| 3 | array=[], target=5 | -1 | an empty list |
| 4 | array=[10], target=10 | 0 | a list with just one item |
| 5 | array=[1,3,5,7,9,11], target=1 | 0 | the target is the very first item |

Run the test cases above directly in the editor to verify your solution.

</DetailsPageWithIDE>
