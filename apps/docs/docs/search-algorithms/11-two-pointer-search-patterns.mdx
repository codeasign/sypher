---
id: two-pointer-search-patterns
title: "Two-Pointer Search Patterns"
sidebar_label: "11. Two-Pointer Search Patterns"
sidebar_position: 11
hide_table_of_contents: true
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

## Summary

Two-Pointer Search finds a pair of numbers in a sorted list that add up to a target number. The trick: put one pointer at the start of the list and one at the end, then move them toward each other based on whether the current pair's sum is too big or too small. Because the list is already sorted, this lets you skip huge chunks of possibilities instead of checking every pair one by one; it only looks at each number roughly once, so it stays fast even on long lists.

## Pseudocode

```text
1. Put a "left" pointer on the first number and a "right" pointer on the last number.
2. While left is still before right:
3.     Add the numbers at left and right together.
4.     If the sum equals the target, stop — you found the pair.
5.     If the sum is too small, move left one step forward (toward bigger numbers).
6.     If the sum is too big, move right one step backward (toward smaller numbers).
7. If left and right meet without a match, report "no pair found."
```

## Worked Example

This trick only works because the list is already sorted from smallest to biggest, like flipping toward the front or back of a sorted phone book instead of checking every page. That order is what makes "too big" or "too small" a useful signal for which pointer to move.

The example below tracks the sorted list `[1, 2, 4, 7, 11, 15]` while searching for a pair that adds up to `9`.

### Step 1: start at both ends

<AsciiDiagram
  id="search-algorithms/two-pointer-search-patterns/step-1"
  alt="A row of six boxes holding 1, 2, 4, 7, 11, 15 with the left pointer on index 0 and the right pointer on index 5, and their sum 16 marked as too big"
  caption="Step 1: left on 1, right on 15 — sum is too big"
  mermaidSrc="/img/diagrams/b44c44b3b0e0.svg"
  content={`target = 9
        ┌─────┬─────┬─────┬─────┬─────┬─────┐
index:  │  0  │  1  │  2  │  3  │  4  │  5  │
        ├─────┼─────┼─────┼─────┼─────┼─────┤
value:  │  1  │  2  │  4  │  7  │ 11  │ 15  │
        └─────┴─────┴─────┴─────┴─────┴─────┘
           ▲                            ▲
           L                            R
sum = 1 + 15 = 16   16 > 9 ──▶ move R left`}
 />

The left pointer starts on the first number, `1`, and the right pointer starts on the last number, `15`. Their sum is `16`, bigger than the target `9`, so the right pointer steps one place to the left.

### Step 2: right pointer moves again

<AsciiDiagram
  id="search-algorithms/two-pointer-search-patterns/step-2"
  mermaidSrc="/img/diagrams/bfed5d1da137.svg"
  alt="The same row of six boxes with the left pointer still on index 0 and the right pointer now on index 4, and their sum 12 marked as too big"
  caption="Step 2: left on 1, right on 11 — sum is still too big"
  content={`target = 9
        ┌─────┬─────┬─────┬─────┬─────┬─────┐
index:  │  0  │  1  │  2  │  3  │  4  │  5  │
        ├─────┼─────┼─────┼─────┼─────┼─────┤
value:  │  1  │  2  │  4  │  7  │ 11  │ 15  │
        └─────┴─────┴─────┴─────┴─────┴─────┘
           ▲                      ▲
           L                      R
sum = 1 + 11 = 12   12 > 9 ──▶ move R left`}
 />

Now the right pointer sits on `11`. The sum `1 + 11 = 12` is still bigger than `9`, so the right pointer moves left again, landing on `7`.

### Step 3: left pointer moves for the first time

<AsciiDiagram
  id="search-algorithms/two-pointer-search-patterns/step-3"
  mermaidSrc="/img/diagrams/2b310c76e83b.svg"
  alt="The same row of six boxes with the left pointer still on index 0 and the right pointer now on index 3, and their sum 8 marked as too small"
  caption="Step 3: left on 1, right on 7 — sum is too small"
  content={`target = 9
        ┌─────┬─────┬─────┬─────┬─────┬─────┐
index:  │  0  │  1  │  2  │  3  │  4  │  5  │
        ├─────┼─────┼─────┼─────┼─────┼─────┤
value:  │  1  │  2  │  4  │  7  │ 11  │ 15  │
        └─────┴─────┴─────┴─────┴─────┴─────┘
           ▲                 ▲
           L                 R
sum = 1 + 7 = 8   8 < 9 ──▶ move L right`}
 />

This time the sum `1 + 7 = 8` is smaller than `9`. Instead of moving the right pointer again, the left pointer moves one step forward, from `1` to `2`.

### Step 4: match found

<AsciiDiagram
  id="search-algorithms/two-pointer-search-patterns/step-4"
  mermaidSrc="/img/diagrams/fb9bb67439f8.svg"
  alt="The same row of six boxes with the left pointer now on index 1 and the right pointer on index 3, and their sum 9 marked as an exact match"
  caption="Step 4: left on 2, right on 7 — sum matches the target"
  content={`target = 9
        ┌─────┬─────┬─────┬─────┬─────┬─────┐
index:  │  0  │  1  │  2  │  3  │  4  │  5  │
        ├─────┼─────┼─────┼─────┼─────┼─────┤
value:  │  1  │  2  │  4  │  7  │ 11  │ 15  │
        └─────┴─────┴─────┴─────┴─────┴─────┘
                 ▲           ▲
                 L           R
sum = 2 + 7 = 9   9 == 9 ──▶ match! return [1, 3]`}
 />

The new sum, `2 + 7 = 9`, is exactly the target. The pointers stop here; the matching pair sits at indexes `1` and `3`.

## Complexity

- **Best:** if the matching pair sits right at the two ends of the list, it's found on the very first check (`O(1)`).
- **Average:** the pointers typically step toward each other for a while before meeting, checking a good chunk of the list once (`O(n)`).
- **Worst:** if the pair is buried in the middle, or there's no matching pair at all, the pointers march all the way toward each other, checking close to every number once (`O(n)`).
- **Space:** only two pointers are ever needed, no matter how long the list is, so extra memory stays constant (`O(1)`).

## Applications

- **Two-sum style checks over sorted data**: fraud-detection and finance tools scanning a sorted list of transaction amounts for two entries that add up to a flagged total.
- **Merging two sorted lists**: the merge step of merge sort (and Timsort's merge phase, used inside Python's `list.sort` and Java's `Arrays.sort` for objects) walks two sorted runs with a pointer in each.
- **Palindrome checking**: string-validation libraries compare characters from both ends inward until the pointers meet.
- **Partitioning during quicksort**: the Lomuto and Hoare partition schemes scan from opposite ends of a range with two pointers to split it around a pivot.
- **Container-with-most-water style problems**: geometric and resource-scheduling algorithms that look for the best pair of boundaries by narrowing in from both ends.

## Solutions

<Tabs>
      <TabItem value="python311" label="Python">

<CodeBlock language="python">{`def two_sum_sorted(array: list[int], target: int) -> list[int]:
    left = 0
    right = len(array) - 1
    while left < right:
        total = array[left] + array[right]
        if total == target:
            return [left, right]
        elif total < target:
            left += 1
        else:
            right -= 1
    return [-1, -1]`}</CodeBlock>

      </TabItem>
      <TabItem value="c_gcc13" label="C">

<CodeBlock language="c">{`void two_sum_sorted(const int *array, int size, int target, int *outLeft, int *outRight) {
    int left = 0;
    int right = size - 1;

    while (left < right) {
        int sum = array[left] + array[right];

        if (sum == target) {
            *outLeft = left;
            *outRight = right;
            return;
        } else if (sum < target) {
            left++;
        } else {
            right--;
        }
    }

    *outLeft = -1;
    *outRight = -1;
}`}</CodeBlock>

      </TabItem>
      <TabItem value="cpp_clang17" label="C++">

<CodeBlock language="cpp">{`#include <utility>
#include <vector>

std::pair<int, int> twoSumSorted(const std::vector<int>& array, int target) {
    int left = 0;
    int right = static_cast<int>(array.size()) - 1;

    while (left < right) {
        int sum = array[left] + array[right];

        if (sum == target) {
            return {left, right};
        } else if (sum < target) {
            left++;
        } else {
            right--;
        }
    }

    return {-1, -1};
}`}</CodeBlock>

      </TabItem>
      <TabItem value="java21" label="Java">

<CodeBlock language="java">{`public class TwoPointerSearch {
    public static int[] twoSumSorted(int[] array, int target) {
        int left = 0;
        int right = array.length - 1;
        while (left < right) {
            int sum = array[left] + array[right];
            if (sum == target) {
                return new int[]{left, right};
            } else if (sum < target) {
                left++;
            } else {
                right--;
            }
        }
        return new int[]{-1, -1};
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="csharp_dotnet8" label="C#">

<CodeBlock language="csharp">{`public class TwoPointerSearch
{
    public static int[] TwoSumSorted(int[] array, int target)
    {
        int left = 0;
        int right = array.Length - 1;
        while (left < right)
        {
            int sum = array[left] + array[right];
            if (sum == target)
            {
                return new int[] { left, right };
            }
            else if (sum < target)
            {
                left++;
            }
            else
            {
                right--;
            }
        }
        return new int[] { -1, -1 };
    }
}`}</CodeBlock>

      </TabItem>
      <TabItem value="javascript_node20" label="JavaScript">

<CodeBlock language="javascript">{`function twoSumSorted(array, target) {
  let left = 0;
  let right = array.length - 1;
  while (left < right) {
    const sum = array[left] + array[right];
    if (sum === target) {
      return [left, right];
    } else if (sum < target) {
      left++;
    } else {
      right--;
    }
  }
  return [-1, -1];
}`}</CodeBlock>

      </TabItem>
      <TabItem value="typescript516" label="TypeScript">

<CodeBlock language="typescript">{`function twoSumSorted(array: number[], target: number): number[] {
  let left = 0;
  let right = array.length - 1;
  while (left < right) {
    const sum = array[left] + array[right];
    if (sum === target) {
      return [left, right];
    } else if (sum < target) {
      left++;
    } else {
      right--;
    }
  }
  return [-1, -1];
}`}</CodeBlock>

      </TabItem>
      <TabItem value="rust179" label="Rust">

<CodeBlock language="rust">{`pub fn two_sum_sorted(array: &[i32], target: i32) -> (i32, i32) {
    let mut left: i32 = 0;
    let mut right: i32 = array.len() as i32 - 1;

    while left < right {
        let sum = array[left as usize] + array[right as usize];

        if sum == target {
            return (left, right);
        } else if sum < target {
            left += 1;
        } else {
            right -= 1;
        }
    }

    (-1, -1)
}`}</CodeBlock>

      </TabItem>
      <TabItem value="go122" label="Go">

<CodeBlock language="go">{`func twoSumSorted(array []int, target int) (int, int) {
    left := 0
    right := len(array) - 1

    for left < right {
        sum := array[left] + array[right]

        if sum == target {
            return left, right
        } else if sum < target {
            left++
        } else {
            right--
        }
    }

    return -1, -1
}`}</CodeBlock>

      </TabItem>
    </Tabs>
