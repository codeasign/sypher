---
id: raft-real-world
title: Raft in Production
sidebar_label: Real World
sidebar_position: 5
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';

## Case Studies

### etcd: The Kubernetes Backbone

etcd is a distributed key-value store that uses Raft for consensus. It is the primary datastore for Kubernetes, storing all cluster state including pods, services, config maps, and secrets.

**Deployment pattern:** etcd runs as a 3-node or 5-node Raft cluster. Kubernetes writes every state change through etcd, typically at rates of 100-5,000 writes per second and 1,000-50,000 reads per second in production clusters.

**Lessons learned:**
- **Defragmentation is mandatory.** etcd's MVCC backend creates version bloat that can consume gigabytes of storage. Without periodic defragmentation, the database grows linearly with the number of key versions, degrading performance.
- **Read optimization matters.** Kubernetes reads config maps frequently. etcd serves linearizable reads with read-index optimization, bypassing disk writes for read-only operations.
- **Heartbeat tuning for large clusters.** With 5,000+ Kubernetes nodes all watching etcd for changes, the watch stream can overwhelm the leader's network stack. etcd uses a streaming gRPC API and compacted event history to mitigate this.
- **The database size quota.** etcd enforces a storage quota (2 GiB by default) and recommends staying under about 8 GiB. Beyond that, snapshots, defragmentation, and recovery become slow. When the quota is exceeded, etcd goes read-only until space is reclaimed. Mitigation: use periodic compaction and defragmentation, and raise the quota only with care.

<AsciiDiagram
  id="raft/real-world-etcd-k8s"
  mermaidSrc="/img/diagrams/6fffa55e5205.svg"
  alt="Architecture diagram showing Kubernetes control plane components (API server, scheduler, controller manager) all reading and writing to an etcd cluster that uses Raft"
  caption="etcd as the consensus backbone for Kubernetes — all control plane state flows through Raft"
  content={`
┌────────────────────────────────────────────────────────────┐
│               Kubernetes Control Plane                        │
│                                                               │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐       │
│  │ API Server │  │ Scheduler  │  │ Controller Mgr    │       │
│  └──────┬─────┘  └─────┬──────┘  └────────┬─────────┘       │
│         │              │                  │                   │
│         └──────────────┼──────────────────┘                   │
│                        │                                      │
│                        ▼ (all writes & linearizable reads)    │
│  ┌──────────────────────────────────────────────┐            │
│  │           etcd Cluster (Raft)                 │            │
│  │                                              │            │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐   │            │
│  │  │  Leader   │  │ Follower │  │ Follower │   │            │
│  │  │  (Raft)   │  │  (Raft)  │  │  (Raft)  │   │            │
│  │  └──────────┘  └──────────┘  └──────────┘   │            │
│  │                                              │            │
│  │  Data stored:                                │            │
│  │  - Pod specs and status                      │            │
│  │  - Service definitions                       │            │
│  │  - ConfigMaps and Secrets                    │            │
│  │  - Deployment and ReplicaSet state           │            │
│  │  - RBAC policies                             │            │
│  └──────────────────────────────────────────────┘            │
│                                                               │
│  Key metrics: 100-5K writes/s, 1K-50K reads/s                 │
│  Key lessons: defragmentation, compaction tuning              │
└──────────────────────────────────────────────────────────────┘
`}
/>

### Consul: Service Discovery at Scale

HashiCorp Consul uses Raft for its consensus layer, providing service discovery, health checking, and key-value storage. Consul's Raft implementation is derived from the Hashicorp Raft library.

**Deployment pattern:** Consul runs a 3-node or 5-node Raft cluster as the server tier. Client agents on each node perform health checks and forward requests to the server cluster. Service discovery reads are served locally by client agents using cached data refreshed from the Raft cluster (eventual consistency for read performance).

**Lessons learned:**
- **Gossip protocol for failure detection.** Consul uses a separate gossip protocol (SWIM) for failure detection, not Raft heartbeats. This decouples membership detection from consensus, allowing Raft to focus on log replication.
- **Stale reads for scale.** Consul allows configuring read consistency: "default" is served by the leader without an extra check (usually current, with a small window of possible staleness), "stale" allows any server to serve reads for lower latency, and "consistent" makes the leader verify its leadership with a quorum first, for fully linearizable reads.
- **Keep high-volume data off the Raft path.** Health checks run on the client agents, and only changes are written through Raft. Large deployments learned that routing every piece of health data through consensus would overload the servers, so later Consul versions added streaming of updates to agents to cut read load on the servers.

### TiDB: Raft-Based Distributed SQL

TiDB is a distributed NewSQL database that uses Raft for consensus within each region (shard). It is inspired by Google Spanner but uses Raft instead of Paxos and a central timestamp oracle (in the Placement Driver) instead of TrueTime.

**Architecture:** TiDB uses a multi-Raft approach. The key space is divided into regions (roughly 96 MiB by default in recent versions), each managed by an independent Raft group. The Placement Driver (PD) manages region distribution and load balancing.

**Lessons learned:**
- **A dedicated Raft log engine.** TiKV stores the Raft log in a separate log-structured engine, apart from the engine that holds the data, so that the frequent small log appends do not compete with data compaction.
- **Batching and pipelining.** TiKV batches Raft messages and writes across many regions to keep the cost of running many groups per node low.
- **Learner nodes for read scaling.** TiDB introduced learner nodes (non-voting Raft members) that receive the log asynchronously and can serve stale reads without affecting write latency.
- **Dynamic region splitting.** A region that grows past its size threshold is automatically split into two smaller regions. Each new region starts its own Raft group with the same replica set.

<AsciiDiagram
  id="raft/real-world-tidb-multi-raft"
  mermaidSrc="/img/diagrams/e7350db469ab.svg"
  alt="TiDB architecture showing multiple Raft groups (regions) spread across three storage nodes, with PD managing placement"
  caption="TiDB's multi-Raft architecture — each region is an independent Raft group"
  content={`
┌─────────────────────────────────────────────────────────────┐
│  Placement Driver (PD)                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ Region      │  │ Region      │  │ Region      │          │
│  │ Assignment  │  │ Splitting   │  │ Load        │          │
│  │ Table       │  │ Logic       │  │ Balancer    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└──────────────────────────┬──────────────────────────────────┘
                           │
    ┌──────────────────────┼──────────────────────┐
    │                      │                      │
    ▼                      ▼                      ▼
┌──────────┐          ┌──────────┐          ┌──────────┐
│ TiKV 1   │          │ TiKV 2   │          │ TiKV 3   │
│          │          │          │          │          │
│ Region A │          │ Region A │          │ Region B │
│ (Leader) │          │(Follower)│          │ (Leader) │
│ Region C │          │ Region B │          │ Region C │
│(Follower)│          │(Follower)│          │ (Leader) │
│          │          │ Region D │          │ Region D │
│          │          │ (Leader) │          │(Follower)│
└──────────┘          └──────────┘          └──────────┘

Key facts:
- Each region is 64 MB by default
- Each region has 3 replicas (Leader + 2 Followers)
- Each TiKV node hosts ~100-10,000 regions
- Writes go to region leader, which runs Raft to replicate
`}
/>

### CockroachDB: Global Multi-Raft

CockroachDB is a distributed SQL database designed for global deployments. It uses Raft for per-range consensus, with ranges automatically rebalanced across nodes and regions.

**Key innovations in CockroachDB's Raft usage:**
- **Parallel commits:** Eliminates one network round trip from the write path for non-conflicting transactions. Instead of waiting for Raft commit then applying, the transaction is optimistically applied and committed via Raft simultaneously.
- **Follower reads:** CockroachDB allows reading from any replica as long as the read timestamp is within a safe interval (defined by the closed timestamp mechanism, which tracks the maximum timestamp at which data can be safely served from followers).
- **Range lease:** Separates the range lease (which replica serves reads and coordinates writes) from the Raft leader. The leaseholder serves reads and coordinates writes for the range, and CockroachDB tries to keep the Raft leader on the same node so that writes do not take an extra hop. Placement settings let you steer leaseholders toward the region where most of the traffic comes from.

## Open Source Raft Implementations

### Hashicorp Raft (Go)

The most widely used standalone Raft library. Powers Consul, Nomad, Vault, and many other HashiCorp products. Features include: automatic leader election, log replication and snapshotting, cluster membership changes, and a pluggable transport and storage layer.

### etcd raft library (Go)

A highly optimized Raft library extracted from etcd. Its Rust port (raft-rs) is used by TiKV, and the Go library is used by many other projects. Features include: Raft groups in a shared core, unlimited log entry retention with async snapshotting, and checkpoint-based log compaction.

### Apache Ratis (Java)

A Raft implementation in Java used by Apache Ozone and Apache Hadoop. Features include: gRPC and Netty transport, state machine interface, and multi-Raft support.

## Cloud Providers and Raft

**AWS MemoryDB for Redis:** A durable Redis-compatible service built on a multi-AZ transaction log. AWS has not published it as a Raft implementation, but it follows the same principle: acknowledge a write only after it is durably replicated.

**Google Cloud Spanner:** Uses Paxos, not Raft, but the lessons are similar. Each Paxos group has a leader that holds a time-limited lease. TrueTime, Google's clock service, is used to assign transaction timestamps, not to elect leaders.

**Azure Cosmos DB:** Uses a proprietary replication protocol rather than Raft. Its design supports configurable consistency levels and multi-region writes.

## Lessons Learned from Production Deployments

**Lesson 1: Tune timeouts to your network, not the defaults.** The Raft paper's 150-300ms defaults work for low-latency datacenters. Cross-region deployments need 500-1000ms, and edge deployments may need 2-5 seconds. Measure actual network latency jitter before setting timeouts.

**Lesson 2: Monitor term changes.** A healthy Raft cluster has extremely rare term changes (effectively zero after initial leader election). Unexpected term increments indicate leader flapping, network issues, or GC pauses. Alert on any term change not caused by planned maintenance.

**Lesson 3: Plan for quorum loss.** A 5-node cluster tolerates 2 node failures. When you lose 2 nodes, replace them immediately. Running with 3 of 5 nodes means any additional failure causes complete unavailability. Have automated node replacement procedures.

**Lesson 4: Log compaction is not optional.** Without snapshots, the Raft log grows unbounded. A 100,000-entry log at 200 bytes each is trivial, but a cluster running for months at 10,000 writes/sec accumulates billions of entries. Compaction (snapshotting) must be configured from day one.

**Lesson 5: Benchmark with production conditions.** A Raft cluster that handles 10,000 ops/sec in benchmarks may handle only 2,000 ops/sec under real workloads with concurrent reads, watches, and client disconnections. Benchmark with your actual workload pattern, including network latency, disk write amplification, and concurrent access patterns.

<AsciiDiagram
  id="raft/real-world-raft-landscape"
  mermaidSrc="/img/diagrams/602236005a69.svg"
  alt="Ecosystem map of Raft-based systems: etcd, Consul, TiDB, CockroachDB, Hashicorp Raft, and cloud services"
  caption="The Raft ecosystem — major systems built on Raft consensus"
  content={`
                      ┌─────────────────────────┐
                      │     Raft Consensus       │
                      │    Algorithm (2013)      │
                      └────────────┬────────────┘
                                   │
               ┌───────────────────┼───────────────────┐
               │                   │                   │
               ▼                   ▼                   ▼
┌──────────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  Coordination Stores │ │ Distributed DBs   │ │  Libraries        │
│                      │ │                   │ │                   │
│  etcd (Kubernetes)   │ │ TiDB/TiKV         │ │ Hashicorp Raft   │
│  Consul (HashiCorp)  │ │ CockroachDB       │ │ etcd raft lib    │
│  ZooKeeper (Zab var) │ │ YugaByte DB       │ │ Apache Ratis     │
│  Kudu                 │ │ Milvus (metadata) │ │ OpenRaft (Rust)  │
│                      │ │ Neo4j (raft)      │ │ braft (C++)      │
└──────────────────────┘ └──────────────────┘ └──────────────────┘
                                   │
                                   ▼
                      ┌─────────────────────────┐
                      │  Cloud Services           │
                      │                           │
                      │ AWS MemoryDB Redis        │
                      │ Azure Cache for Redis     │
                      │ Huawei Cloud GaussDB      │
                      └─────────────────────────┘
`}
/>