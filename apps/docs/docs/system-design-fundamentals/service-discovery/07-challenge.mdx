---
id: service-discovery-challenge
title: Service Discovery — Prove It
sidebar_label: Challenges
sidebar_position: 7
---

import AsciiDiagram from '@site/src/components/AsciiDiagram';

## Foundation Challenges

These challenges test your understanding of core service discovery concepts. You should be able to complete each one in 10-15 minutes.

### Challenge 1: Design a Heartbeat Protocol

**Problem**: You are designing a heartbeat protocol for a service registry. The protocol must handle 5,000 service instances, each sending heartbeats every 15 seconds. The registry runs on a 3-node cluster.

**Tasks**:
1. Design the heartbeat message format (include at minimum: instance ID, service name, timestamp, signature).
2. Describe how the registry processes an incoming heartbeat.
3. Explain what happens when a heartbeat is missed.
4. Calculate the peak heartbeat rate the registry must handle.

**Model Answer**:

**1. Heartbeat message format**:

```
{
  "instance_id": "transcoder-us-east-1a-003",
  "service_name": "video-transcoder",
  "timestamp": "2026-07-02T14:30:00.123Z",
  "lease_duration_seconds": 45,
  "status": "healthy",
  "metadata": {
    "version": "2.1.0",
    "cpu_utilization": 0.45
  },
  "signature": "hmac-sha256(instance_id + timestamp, shared_secret)"
}
```

The signature prevents unauthorized heartbeat sending. An attacker cannot forge a heartbeat for an instance they do not control.

**2. Heartbeat processing**:

```
On receive heartbeat(instance_id, service_name, timestamp, ...):
  1. Validate signature using the instance's registration secret
  2. Look up the instance in the in-memory map
  3. If found:
     a. Update lease_expiry = now + lease_duration_seconds
     b. Set status = healthy
     c. Return 200 OK
  4. If not found:
     a. Return 404 Not Found (instance is not registered)
     b. The sender should re-register before heartbeating
```

**3. Missed heartbeat handling**:

When a heartbeat is missed, the registry does not immediately evict the instance. It waits for the full TTL (45 seconds) from the last successful heartbeat. If no heartbeat arrives within that window, the instance is marked as unhealthy and eventually removed from query results. The eviction is not reversed even if a heartbeat arrives after the TTL expires — the instance must re-register.

**4. Peak heartbeat rate**:

```
Total instances: 5,000
Heartbeat interval: 15 seconds
Normal rate: 5,000 / 15 = 333 heartbeats/second

Peak burst (all instances reconnect after outage):
5,000 instances × 1 heartbeat each = 5,000 heartbeats in 1 second
(5,000 heartbeats/s is manageable for a 3-node cluster if each node
 processes ~1,700 heartbeats/s)
```

### Challenge 2: Compare Discovery Patterns

**Problem**: You have a system with 200 microservices written in 5 different languages (Go, Java, Python, Node.js, Ruby). Each service runs 10 instances. Your team is small (8 engineers). You need to choose a discovery pattern.

**Tasks**:
1. Compare client-side, server-side, and DNS-based discovery for this system.
2. Recommend one pattern and justify your choice.
3. Identify the single biggest risk of your recommendation.

**Model Answer**:

**1. Comparison**:

| Factor | Client-Side | Server-Side | DNS-Based |
|---|---|---|---|
| Consumer complexity | High (each language needs a library) | Low (just point at LB) | Low (standard DNS resolution) |
| Latency | Lowest (no extra hop) | Adds LB hop (1-5ms) | Low (direct connection) |
| Language support | Need 5 client libraries | LB handles all languages | Works with all languages |
| Operational overhead | Library maintenance | LB cluster management | None (uses existing DNS) |
| Health awareness | Built into client | LB handles health checks | None (DNS has no health) |
| Change propagation | Fast (registry query) | Fast (LB updates) | Slow (DNS cache TTL) |

**2. Recommendation**:

For a polyglot system with a small team, **server-side discovery** is the best choice. The primary reason is that the team cannot maintain 5 different client libraries for client-side discovery. With server-side discovery, each service only needs to know the load balancer's address. The load balancer handles discovery, health checking, and routing for all services regardless of language.

Use Envoy or NGINX as the load balancer, with a Consul cluster as the registry. Envoy's xDS API integrates directly with Consul, so the load balancer always has an up-to-date instance list.

**3. Biggest risk**:

The load balancer becomes a single point of failure and a capacity bottleneck. If the load balancer goes down, all inter-service communication stops. Mitigation: Run the load balancer as a cluster (3 Envoy instances) and use a virtual IP with keepalived for failover. Also implement client-side retry with a fallback to direct connection if the load balancer is unreachable.

### Challenge 3: Handle a Registry Outage

**Problem**: Your Consul cluster of 3 nodes experiences a full outage (all nodes crash simultaneously). The application continues running (50 services, 200 instances each). Design a recovery plan.

**Tasks**:
1. Describe what happens to the application during the outage.
2. Design the recovery steps.
3. Propose a mitigation strategy to prevent this from happening again.

**Model Answer**:

**1. Application behavior during outage**:

Existing connections between services continue to work because each service has a cached instance list in its client library. The cache TTL is typically 5-15 minutes with a fallback to the last known good list. During the outage:

- New deployments cannot register new instances, so rolling updates fail.
- Instance failures are not detected by the registry, so consumers may try to connect to dead instances and get connection errors.
- Auto-scaling events produce instances that are invisible to the rest of the system.
- The application continues running but gradually degrades as instances fail and are not replaced.

**2. Recovery steps**:

```
Step 1: Restore the Consul cluster from the latest snapshot.
  consul snapshot restore backup.snap

Step 2: Verify the cluster has quorum and all nodes are healthy.
  consul members

Step 3: Trigger all services to re-register.
  # Option A: Restart all service instances (clean but slow)
  # Option B: Send a signal to each service's Consul agent to
  #           force re-registration (fast but requires agent support)
  # Option C: Use a script to iterate through all known instances
  #           and re-register them from the snapshot

Step 4: Verify all services are visible in the registry.
  consul catalog services

Step 5: Monitor the heartbeat success rate for 5 minutes.
  # If any instances fail to heartbeat, investigate
```

**3. Mitigation**:

- Run 5 Consul nodes instead of 3. This tolerates 2 failures instead of 1.
- Deploy nodes across 3 availability zones. A single AZ failure cannot take down the cluster.
- Automate snapshot backups to S3 every hour.
- Schedule quarterly disaster recovery drills where the cluster is killed and the team practices recovery.
- Implement a fallback DNS-based discovery mechanism that can be activated manually if the registry is down for more than 5 minutes.

## Design Challenges

These challenges test your ability to design service discovery systems. Expect to spend 15-25 minutes on each.

### Challenge 4: Design Discovery for a Video Streaming Platform

**Problem**: You are designing a service discovery system for a video streaming platform (like Netflix). The platform has 500 microservices. Each service runs 20-100 instances. The platform supports 50 million daily active users. Peak traffic is 3x the average.

**Requirements**:
- Sub-10 millisecond discovery latency
- 99.99% availability for the discovery system
- Support for multi-region deployment (us-east-1, eu-west-1, ap-southeast-1)
- Instances are added and removed frequently (auto-scaling, rolling deployments)
- The system must continue working during a registry outage

**Model Answer**:

**Architecture**:

```
1. Discovery Pattern: Client-side (inspired by Netflix Eureka)
   - Every service instance embeds a discovery client library
   - Client caches instance list locally (15-second cache TTL)
   - Client uses a fallback cache (5-minute max) if registry is unreachable

2. Registry: Eureka cluster (AP model, eventual consistency)
   - 3 Eureka servers per region, deployed across 3 AZs
   - Peer-to-peer replication between Eureka servers
   - No quorum required (AP system — always available)
   - Cross-region: Each Eureka server peers with servers in other regions

3. Registration and Health:
   - Self-registration with heartbeats every 30 seconds
   - Eviction after 90 seconds of missed heartbeats
   - Instance-level health check endpoint (HTTP /health)
   - Eureka also performs a TCP check on the service port

4. Routing:
   - Zone-aware: prefer instances in same AZ
   - Region-aware: prefer instances in same region
   - Fall back to cross-region only if no local instances available
   - Round-robin load balancing within the selected zone

5. Failure Handling:
   - If Eureka is unreachable, use cached instance list for up to 5 minutes
   - If a cached instance returns connection refused, remove it from the
     local cache and try the next instance
   - Circuit breaker: 5 consecutive failures to an instance triggers
     a 30-second cooldown before retrying
```

**Justification**:

Client-side discovery is chosen because sub-10ms latency rules out the extra hop of a load balancer. The AP consistency model is chosen because the system prefers availability over strict correctness — a stale instance list is better than no list at all.

### Challenge 5: Design Discovery for a Financial Trading Platform

**Problem**: You are designing service discovery for a high-frequency trading platform. The system has 50 microservices, all running in a single data center. Latency must be under 1 millisecond. Consistency is critical — stale data could cause incorrect trades.

**Requirements**:
- Sub-1 millisecond discovery latency
- Strong consistency (no stale reads)
- 99.999% correctness
- Single data center
- 10 services are stateful (order books, positions)
- 40 services are stateless (market data, analytics, reporting)

**Model Answer**:

**Architecture**:

```
1. Discovery Pattern: DNS-based with extremely low TTL (1 second)
   - DNS records are updated instantly via a custom DNS server
   - TTL of 1 second prevents long-lived stale entries
   - SRV records include port information

2. Stateful Services (10 services):
   - Static VIP assignment instead of dynamic discovery
   - Each stateful service has a fixed virtual IP that is manually
     updated when instances change
   - Changes are propagated through a control plane that directly
     updates iptables NAT rules on the consumer hosts
   - This avoids any DNS or registry lookup for the most critical services

3. Stateless Services (40 services):
   - DNS-based discovery with 1-second TTL
   - A local DNS cache resolver (dnsmasq) on each host
   - Cache TTL honored strictly (no optimistic caching)
   - Health checked by a sidecar process that updates DNS records

4. Health Monitoring:
   - Out-of-band health checker runs on each host
   - Detects failures within 100ms (not 10-30 seconds like heartbeats)
   - Uses shared memory (kernel-level) for health check results
   - Health state is replicated to the DNS server via RDMA
```

**Justification**:

For a trading platform, sub-1ms latency and strong consistency justify the operational complexity of static VIPs and kernel-level health checking. The 10 stateful services are the most critical and receive the most aggressive treatment. The 40 stateless services use DNS because they are less critical and can tolerate a 1-second propagation delay.

### Challenge 6: Design Discovery for an IoT Platform

**Problem**: Design a discovery system for an IoT platform with 1 million devices and 20 backend services. Devices are resource-constrained (128 KB RAM, single-core CPU) and communicate over unreliable cellular networks. Devices go offline frequently and unpredictably.

**Requirements**:
- Devices must discover the nearest available gateway service
- Backend services must discover which devices are connected to which gateway
- The system must handle devices going offline for hours and returning
- Communication bandwidth is extremely limited (devices pay per KB)

**Model Answer**:

**Architecture**:

```
1. Device-Side Discovery:
   - Devices do NOT use a service registry (too resource-intensive)
   - Devices are configured with a static DNS name for the gateway
     service (e.g., gateway.iot.platform.com)
   - DNS is resolved via a lightweight DNS client (mbed TLS)
   - DNS TTL is set to 24 hours (stable, devices rarely reconnect)
   - On connection failure, device falls back to a secondary DNS name

2. Gateway Discovery (back-end side):
   - Each gateway registers itself with Consul when it starts
   - Registration includes: gateway ID, IP, region, device capacity
   - Heartbeats every 60 seconds (long interval to save bandwidth)
   - TTL of 180 seconds (3 missed heartbeats)

3. Device-to-Gateway Mapping:
   - When a device connects, the gateway stores the device ID in
     a local SQLite database
   - The gateway periodically syncs the device-to-gateway mapping
     to a central PostgreSQL database (every 5 minutes)
   - Backend services query the PostgreSQL database to find which
     gateway a device is connected to
   - This is not real-time discovery; it is eventually consistent
     with a 5-minute window

4. Offline Handling:
   - When a device goes offline, the gateway detects the TCP
     disconnection and marks the device as offline locally
   - The gateway reports the offline status on the next sync cycle
   - If the device does not reconnect within 24 hours, the mapping
     is deleted from the database
   - On reconnect, the device authenticates and the gateway creates
     a new mapping entry
```

**Justification**:

The IoT platform prioritizes bandwidth efficiency and simplicity over real-time accuracy. Devices do not need sub-second discovery — they connect to a well-known gateway DNS name and stay connected for hours. The 5-minute sync delay is acceptable because device state changes slowly.

## Expert Challenges

These challenges test your ability to design at the highest level. Expect to spend 30-45 minutes on each.

### Challenge 7: Design a Global Service Discovery System for 100,000 Services

**Problem**: Design a global service discovery system for a company with 100,000 microservices deployed across 10 cloud regions worldwide. Each service runs an average of 5 instances. The system must support:

- 99.999% availability for the registry
- Cross-region discovery with sub-50ms latency for local queries
- Automatic failover when a region is completely unavailable
- Zero-downtime registry upgrades
- 10,000 deployments per day (peak: 500 simultaneous deployments)
- Strong consistency within a region, eventual consistency across regions

**Model Answer**:

**Architecture**:

```
1. Regional Registry Clusters (10 regions):
   - Each region has a 5-node Consul cluster (Raft-based, strong consistency)
   - Nodes are spread across 3 availability zones within the region
   - Each cluster handles registration and discovery for services in its region
   - Regional cluster capacity: 10,000 services × 5 instances = 50,000 instances

2. Global Federation Layer:
   - Consul's WAN gossip pool connects all 10 regional clusters
   - Each region's Consul servers gossip with servers in other regions
   - Gossip carries service health summaries (not full instance lists)
   - Convergence time: 5-10 seconds for cross-region state
   - WAN gossip is encrypted and compressed for bandwidth efficiency

3. Consumer-Side Cache Hierarchy:
   - L1 cache: In-process memory (1-second TTL)
   - L2 cache: Local file-based cache (10-second TTL)
   - L3 cache: Fallback snapshot (5-minute TTL, used only if registry
     is unreachable)

4. Cross-Region Discovery:
   - By default, consumers only see instances in their own region
   - Cross-region discovery is opt-in: services explicitly marked as
     "global" are visible to all regions
   - Global services maintain a local cache of remote instances with
     a 30-second TTL

5. Deployment and Upgrades:
   - Blue-green deployment for registry clusters
   - During an upgrade, half the nodes are replaced, then the other half
   - No downtime: the cluster maintains quorum throughout
   - gRPC load balancing on the client side handles node replacement

6. Disaster Recovery:
   - If a region's registry cluster is completely lost:
     a. Services in that region continue operating (cached instance lists)
     b. New deployments to that region are paused
     c. Traffic is diverted to the nearest healthy region via DNS
     d. A new registry cluster is provisioned in the failed region
     e. Services re-register with the new cluster
   - Full recovery time: under 15 minutes
```

**Capacity Planning**:

```
Total instances: 100,000 services × 5 instances = 500,000 instances
Heartbeat rate: 500,000 / 15s = 33,333 heartbeats/s
Peak heartbeat rate (after outage): 500,000 / 1s = 500,000 heartbeats/s
Query rate: 500,000 instances × 10 queries/s = 5,000,000 queries/s
Watch subscriptions: 500,000 × 5 watched services = 2,500,000 watches

Per region (assuming 10 regions):
  50,000 instances per region
  3,333 heartbeats/s per region (steady state)
  50,000 heartbeats/s per region (peak)
  500,000 queries/s per region
  250,000 watches per region

Each 5-node Consul cluster must handle:
  3,333 writes/s (heartbeats)
  500,000 reads/s (queries)
  Consul handles ~10,000 writes/s and ~100,000 reads/s per node
  Total cluster capacity: 50,000 writes/s, 500,000 reads/s
  (Sufficient with headroom)
```

### Challenge 8: Design a Discovery System for a Multi-Cloud, Multi-Region SaaS Platform

**Problem**: A SaaS platform runs on AWS, GCP, and Azure across 6 regions. Each cloud provider has its own networking model, DNS system, and load balancing services. The platform has 10,000 services. The discovery system must be cloud-agnostic.

**Requirements**:
- The discovery system must work identically across all three cloud providers
- No dependency on cloud-specific DNS or networking features
- Services in one cloud must be able to discover services in another cloud
- The system must support zero-trust security (all traffic is authenticated and encrypted)
- Failure of one cloud provider must not affect discovery in other clouds

**Model Answer**:

**Architecture**:

```
1. Abstraction Layer:
   - Use a cloud-agnostic registry (Consul running on VMs, not cloud-managed services)
   - Consul servers run on dedicated VM instances in each cloud
   - All Consul traffic is encrypted with mutual TLS
   - Consul WAN gossip connects clusters across clouds and regions

2. Network Connectivity:
   - Establish VPN tunnels between all cloud environments
   - Each cloud has a dedicated Consul datacenter name
   - Consul servers in each datacenter form a Raft cluster
   - WAN gossip pool connects all datacenters

3. Service Mesh Layer:
   - Deploy Consul Connect (or Istio) as a service mesh
   - Each service instance gets a sidecar proxy (Envoy)
   - The sidecar handles all discovery, routing, and mTLS
   - Application code is completely unaware of cross-cloud routing

4. Cross-Cloud Routing:
   - Sidecar proxies discover services via the Consul control plane
   - Traffic policies define routing rules:
     a. Prefer same-cloud, same-region
     b. Fall back to same-cloud, other region
     c. Fall back to other cloud, nearest region
   - Latency budget: 100ms for cross-cloud calls

5. Security:
   - All service-to-service traffic uses mTLS
   - Certificates are issued by Consul Connect's built-in CA
   - Certificate rotation every 24 hours
   - Access control lists (ACLs) restrict which services can discover
     which other services

6. Cloud Provider Failure Handling:
   - If a full cloud provider goes down (e.g., AWS us-east-1):
     a. Consul detects the failure via WAN gossip
     b. Services in the failed cloud stop receiving traffic
     c. Traffic is rerouted to the nearest healthy cloud
     d. When AWS recovers, services re-register and traffic resumes
   - No manual intervention required
```

**Key Design Decisions**:

- Consul is chosen over cloud-managed services because it works identically on all three clouds. AWS Cloud Map, GCP Service Directory, and Azure Service Fabric have different APIs and capabilities.
- The service mesh approach abstracts away the complexity of cross-cloud networking. Application developers do not need to know whether a service is running on AWS, GCP, or Azure.
- The 100ms latency budget for cross-cloud calls is realistic for the SaaS use case. For lower latency, teams co-locate services in the same cloud and region.

**Tradeoffs**:

- Running Consul on VMs is more expensive than using cloud-managed registries. The cost is approximately 3 VMs per cloud per region, totaling 54 VMs (6 regions × 3 clouds × 3 VMs).
- The service mesh adds 5-10% CPU overhead per instance for the sidecar proxy. At 50,000 instances, this is significant infrastructure cost.
- Cross-cloud traffic incurs egress charges from the sending cloud. AWS charges $0.09/GB for cross-region and cross-cloud traffic. At 10 TB/day, this is $900/day in egress costs.