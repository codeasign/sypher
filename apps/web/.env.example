NEXT_PUBLIC_API_BASE_URL=https://api-next.sypher.local
# Server-only — see comment in serverApi.ts for why SSR bypasses Caddy.
API_INTERNAL_URL=http://localhost:4000

# Bunny CDN (image/PDF uploads from the MDX editors — Cohort, Blog, and
# course-module editors all read the same four vars, ported unchanged from
# apps/app's naming convention)
NEXT_PUBLIC_BUNNY_STORAGE_ZONE=
NEXT_PUBLIC_BUNNY_STORAGE_ACCESS_KEY=
NEXT_PUBLIC_BUNNY_STORAGE_HOSTNAME=
NEXT_PUBLIC_BUNNY_PULL_ZONE_URL=
