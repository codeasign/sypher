---
title: Insert Interval — Solution
sidebar_label:   Solution
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Insert Interval — Solution

## Approach: Linear Scan

**Algorithm:** Add all intervals before the new one, merge overlapping ones, add the rest.

**Complexity:** O(n) time, O(n) space.

## Code

<Tabs>
  <TabItem value="python" label="Python">

```python
def insert(intervals: list[list[int]], new_interval: list[int]) -> list[list[int]]:
    result = []
    i, n = 0, len(intervals)
    # Add intervals before new_interval
    while i < n and intervals[i][1] < new_interval[0]:
        result.append(intervals[i])
        i += 1
    # Merge overlapping intervals
    while i < n and intervals[i][0] <= new_interval[1]:
        new_interval[0] = min(new_interval[0], intervals[i][0])
        new_interval[1] = max(new_interval[1], intervals[i][1])
        i += 1
    result.append(new_interval)
    # Add remaining intervals
    while i < n:
        result.append(intervals[i])
        i += 1
    return result
```

  </TabItem>
</Tabs>

## Back to Problem

[← Back to Problem](../../exercises/medium/insert-interval)