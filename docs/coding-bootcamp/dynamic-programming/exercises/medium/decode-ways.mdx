---
title: Decode Ways
sidebar_label: Decode Ways
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/dynamic-programming/decode-ways', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '12\n', expectedOutput: '2\n' },
    { stdin: '226\n', expectedOutput: '3\n' },
    { stdin: '06\n', expectedOutput: '0\n' },
    { stdin: '0\n', expectedOutput: '0\n' },
    { stdin: '1\n', expectedOutput: '1\n' },
    { stdin: '10\n', expectedOutput: '1\n' },
    { stdin: '27\n', expectedOutput: '1\n' },
    { stdin: '99\n', expectedOutput: '1\n' },
    { stdin: '111\n', expectedOutput: '3\n' },
    { stdin: '1111\n', expectedOutput: '5\n' },
    { stdin: '11111\n', expectedOutput: '8\n' },
    { stdin: '2101\n', expectedOutput: '1\n' },
    { stdin: '1123\n', expectedOutput: '5\n' },
    { stdin: '12345\n', expectedOutput: '3\n' },
    { stdin: '1010\n', expectedOutput: '1\n' },
    { stdin: '100\n', expectedOutput: '0\n' },
    { stdin: '2626\n', expectedOutput: '4\n' },
    { stdin: '301\n', expectedOutput: '0\n' },
    { stdin: '11106\n', expectedOutput: '2\n' },
  ]}
  starterCode={{
    python: `def num_decodings(s: str) -> int:
    # Your code here
    pass`,
    java: `class Solution {
    public int numDecodings(String s) {
        // your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int numDecodings(string s) {
        // your code here
        return 0;
    }
};`,
    javascript: `function numDecodings(s) {
    // Your code here
}`,
    typescript: `function numDecodings(s: string): number {
    // Your code here
    return 0;
}`,
    rust: `fn num_decodings(s: &str) -> i32 {
    // your code here
    0
}`,
    c: `int numDecodings(char* s) {
    // your code here
    return 0;
}`,
    csharp: `class Solution {
    public int NumDecodings(string s) {
        // your code here
        return 0;
    }
}`,
    go: `func numDecodings(s string) int {
    // your code here
    return 0
}`,
  }}
  harness={{
    python: `import sys
s = sys.stdin.read().strip()
print(num_decodings(s))`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String s = sc.nextLine().trim();
        Solution sol = new Solution();
        System.out.println(sol.numDecodings(s));
    }
}`,
    cpp: `#include <bits/stdc++.h>
using namespace std;
int main() {
    string s;
    cin >> s;
    Solution sol;
    cout << sol.numDecodings(s) << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l.trim()));
rl.on('close', () => {
    const s = lines[0];
    console.log(numDecodings(s));
});`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l.trim()));
rl.on('close', () => {
    const s = lines[0];
    console.log(numDecodings(s));
});`,
    rust: `fn main() {
    let mut s = String::new();
    std::io::stdin().read_line(&mut s).unwrap();
    let s = s.trim();
    println!("{}", num_decodings(s));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    char s[200];
    scanf("%s", s);
    printf("%d\n", numDecodings(s));
    return 0;
}`,
    csharp: `class Program {
    static void Main() {
        string s = System.Console.ReadLine().Trim();
        Solution sol = new Solution();
        System.Console.WriteLine(sol.NumDecodings(s));
    }
}`,
    go: `package main
import (
    "bufio"
    "fmt"
    "os"
    "strings"
)
func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    s := strings.TrimSpace(line)
    fmt.Println(numDecodings(s))
}`,
  }}
  defaultLanguage="python"
>

# Decode Ways

**Difficulty:** Medium

A message containing letters from A–Z can be encoded into numbers using the mapping:

```
A -> "1", B -> "2", ..., Z -> "26"
```

Given a string `s` containing only digits, return the **number of ways** to decode it.

If there is no valid decoding, return `0`.

## Example 1

**Input:** `s = "12"`  
**Output:** `2`  
**Explanation:** "12" can be decoded as "AB" (1 + 2) or "L" (12).

## Example 2

**Input:** `s = "226"`  
**Output:** `3`  
**Explanation:** "226" can be decoded as "BBF" (2 + 2 + 6), "BZ" (2 + 26), or "VF" (22 + 6).

## Example 3

**Input:** `s = "06"`  
**Output:** `0`  
**Explanation:** "06" cannot be decoded because "06" is not a valid mapping (leading zero is not allowed).

## Input Specification

A single line containing a non-empty string of digits `s`.

- `1 <= s.length <= 100`
- `s` contains only digits `'0'`–`'9'`.

## Output Specification

Print a single integer — the number of ways to decode `s`.

## Constraints

- `1 <= s.length <= 100`
- `s` contains only digits.
- The answer fits in a 32-bit integer.

## Hints

<details>
<summary>Hint 1</summary>

Think about this as a dynamic programming problem. Let `dp[i]` be the number of ways to decode the first `i` characters of `s`. The base case is `dp[0] = 1` (empty string has one decoding).

</details>

<details>
<summary>Hint 2</summary>

At each position `i`, you have two choices:
- Decode `s[i-1]` as a single digit — valid only if `s[i-1] != '0'`.
- Decode `s[i-2..i-1]` as a two-digit number — valid only if the two-digit number is between `10` and `26` inclusive.

</details>

<details>
<summary>Hint 3</summary>

The recurrence is: `dp[i] = dp[i-1]` (if single-digit valid) `+ dp[i-2]` (if two-digit valid). You only ever need the previous two values, so you can use O(1) space with two variables `prev` and `curr`.

</details>

</ProblemEditor>

---
**View the [detailed solution](../../solutions/medium/decode-ways)** for a step-by-step walkthrough.
