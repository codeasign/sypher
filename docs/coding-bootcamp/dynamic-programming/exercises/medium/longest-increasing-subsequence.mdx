---
title: Longest Increasing Subsequence
sidebar_label: Longest Inc. Subseq.
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/dynamic-programming/longest-increasing-subsequence', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '10 9 2 5 3 7 101 18\n', expectedOutput: '4\n' },
    { stdin: '0 1 0 3 2 3\n', expectedOutput: '4\n' },
    { stdin: '7 7 7 7 7 7 7\n', expectedOutput: '1\n' },
    { stdin: '1 2 3 4 5\n', expectedOutput: '5\n' },
    { stdin: '5 4 3 2 1\n', expectedOutput: '1\n' },
    { stdin: '5\n', expectedOutput: '1\n' },
    { stdin: '2 2\n', expectedOutput: '1\n' },
    { stdin: '1 3 6 7 9 4 10 5 6\n', expectedOutput: '6\n' },
    { stdin: '3 5 6 2 5 4 19 5 6 7 12\n', expectedOutput: '6\n' },
    { stdin: '10 22 9 33 21 50 41 60 80\n', expectedOutput: '6\n' },
    { stdin: '1 2 3 2 3 4 5\n', expectedOutput: '5\n' },
    { stdin: '4 10 4 3 8 9\n', expectedOutput: '3\n' },
    { stdin: '2 1 2 1 2 1 2\n', expectedOutput: '2\n' },
    { stdin: '1 1 1 2 2 2 3 3\n', expectedOutput: '3\n' },
    { stdin: '100 90 80 70 60 50\n', expectedOutput: '1\n' },
    { stdin: '10 20 30 5 15 25 35\n', expectedOutput: '4\n' },
    { stdin: '5 6 7 8 1 2 3 4\n', expectedOutput: '4\n' },
    { stdin: '3 1 2 4 6 5 8 7 9\n', expectedOutput: '5\n' },
    { stdin: '1 2 5 3 4\n', expectedOutput: '4\n' },
    { stdin: '0 8 4 12 2 10 6 14 1 9 5 13 3 11 7 15\n', expectedOutput: '6\n' },
  ]}
  starterCode={{
    python: `def length_of_lis(nums: list[int]) -> int:
    # Your code here
    pass`,
    java: `class Solution {
    public int lengthOfLIS(int[] nums) {
        // Your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int lengthOfLIS(vector<int>& nums) {
        // Your code here
        return 0;
    }
};`,
    javascript: `function lengthOfLIS(nums) {
    // Your code here
}`,
    typescript: `function lengthOfLIS(nums: number[]): number {
    // Your code here
    return 0;
}`,
    rust: `pub fn length_of_lis(nums: Vec<i32>) -> i32 {
    // Your code here
    0
}`,
    c: `int lengthOfLIS(int* nums, int numsSize) {
    // Your code here
    return 0;
}`,
    csharp: `public class Solution {
    public int LengthOfLIS(int[] nums) {
        // Your code here
        return 0;
    }
}`,
    go: `func lengthOfLIS(nums []int) int {
    // Your code here
    return 0
}`,
  }}
  harness={{
    python: `\u0069mport sys
data = sys.stdin.read().strip().split()
if not data:
    print(0)
else:
    nums = [int(x) for x in data]
    print(length_of_lis(nums))`,
    java: `\u0069mport java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String line = sc.nextLine();
        if (line.isEmpty()) { System.out.println(0); return; }
        String[] parts = line.split(" ");
        int[] nums = new int[parts.length];
        for (int i = 0; i < parts.length; i++) nums[i] = Integer.parseInt(parts[i]);
        Solution sol = new Solution();
        System.out.println(sol.lengthOfLIS(nums));
    }
}`,
    cpp: `#include <iostream>
#include <sstream>
#include <vector>
#include <string>
using namespace std;
int main() {
    string line; getline(cin, line);
    if (line.empty()) { cout << 0 << endl; return 0; }
    vector<int> nums; stringstream ss(line); int val;
    while (ss >> val) nums.push_back(val);
    Solution sol; cout << sol.lengthOfLIS(nums) << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const nums = line.trim() ? line.split(' ').map(Number) : [];
    console.log(lengthOfLIS(nums));
    rl.close();
});`,
    typescript: `\u0069mport * as readline from 'readline';
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const nums: number[] = line.trim() ? line.split(' ').map(Number) : [];
    console.log(lengthOfLIS(nums));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let nums: Vec<i32> = if line.trim().is_empty() { Vec::new() } else { line.split(' ').map(|s| s.parse().unwrap()).collect() };
    println!("{}", length_of_lis(nums));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    char line[100000];
    if (!fgets(line, 100000, stdin)) { printf("0\\n"); return 0; }
    line[strcspn(line, "\\n")] = 0;
    if (strlen(line) == 0) { printf("0\\n"); return 0; }
    int nums[10000], n = 0;
    char* token = strtok(line, " ");
    while (token) { nums[n++] = atoi(token); token = strtok(NULL, " "); }
    printf("%d\\n", lengthOfLIS(nums, n));
    return 0;
}`,
    csharp: `using System; using System.Linq;
class Program {
    static void Main() {
        string line = Console.ReadLine();
        int[] nums = string.IsNullOrEmpty(line) ? new int[0] : line.Split(' ').Select(int.Parse).ToArray();
        Solution sol = new Solution();
        Console.WriteLine(sol.LengthOfLIS(nums));
    }
}`,
    go: `package main
\u0069mport ("bufio";"fmt";"os";"strconv";"strings")
func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    line = strings.TrimSpace(line)
    if line == "" { fmt.Println(0); return }
    parts := strings.Split(line, " ")
    nums := make([]int, len(parts))
    for i, s := range parts { nums[i], _ = strconv.Atoi(strings.TrimSpace(s)) }
    fmt.Println(lengthOfLIS(nums))
}`,
  }}
  defaultLanguage="python"
>

# Longest Increasing Subsequence

**Difficulty:** Medium

Given an integer array `nums`, return the length of the **longest strictly increasing subsequence**.

A **subsequence** is a sequence that can be derived from an array by deleting some or no elements without changing the order of the remaining elements.

## Example 1

**Input:** `nums = [10, 9, 2, 5, 3, 7, 101, 18]`  
**Output:** `4`  
**Explanation:** The longest increasing subsequence is [2, 3, 7, 101], therefore the length is 4.

## Example 2

**Input:** `nums = [0, 1, 0, 3, 2, 3]`  
**Output:** `4`  

## Example 3

**Input:** `nums = [7, 7, 7, 7, 7, 7, 7]`  
**Output:** `1`  

## Input Specification

- A single line containing space-separated integers representing `nums`.
- `1 <= nums.length <= 2500`

## Output Specification

Print a single integer: the length of the longest strictly increasing subsequence.

## Constraints

- `1 <= nums.length <= 2500`
- `-10^4 <= nums[i] <= 10^4`

## Hints

<details>
<summary>Hint 1</summary>

Define `dp[i]` as the length of the longest increasing subsequence ending at index `i`. How can you compute `dp[i]` from earlier values?
</details>

<details>
<summary>Hint 2</summary>

For each `j < i`, if `nums[j] < nums[i]`, then `dp[i]` can be `dp[j] + 1`. Take the maximum over all such `j`.
</details>

<details>
<summary>Hint 3</summary>

Can you optimize beyond O(n^2)? Try maintaining an array `tails` where `tails[k]` is the smallest possible tail value for an increasing subsequence of length `k+1`. Use binary search to update this array.
</details>

</ProblemEditor>

---
**📖 View the [detailed solution](../solutions/medium/longest-increasing-subsequence)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Word Break](../exercises/medium/word-break).