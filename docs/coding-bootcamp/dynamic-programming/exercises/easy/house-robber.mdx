---
title: House Robber
sidebar_label: House Robber
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/dynamic-programming/house-robber', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '1 2 3 1\n', expectedOutput: '4\n' },
    { stdin: '2 7 9 3 1\n', expectedOutput: '12\n' },
    { stdin: '2 1 1 2\n', expectedOutput: '4\n' },
    { stdin: '5\n', expectedOutput: '5\n' },
    { stdin: '1 2\n', expectedOutput: '2\n' },
    { stdin: '2 1\n', expectedOutput: '2\n' },
    { stdin: '1 1 1\n', expectedOutput: '2\n' },
    { stdin: '3 2 1\n', expectedOutput: '4\n' },
    { stdin: '1 3 1 3 1\n', expectedOutput: '6\n' },
    { stdin: '5 1 1 5\n', expectedOutput: '10\n' },
    { stdin: '4 1 2 4\n', expectedOutput: '8\n' },
    { stdin: '1 2 3 4 5 6\n', expectedOutput: '12\n' },
    { stdin: '6 5 4 3 2 1\n', expectedOutput: '12\n' },
    { stdin: '1 1 1 1 1 1 1\n', expectedOutput: '4\n' },
    { stdin: '0 0 0 0\n', expectedOutput: '0\n' },
    { stdin: '100 1 1 100\n', expectedOutput: '200\n' },
    { stdin: '1 2 3 4 5 6 7\n', expectedOutput: '16\n' },
    { stdin: '7 6 5 4 3 2 1\n', expectedOutput: '16\n' },
    { stdin: '2 4 6 2 5\n', expectedOutput: '13\n' },
    { stdin: '5 3 4 11 2\n', expectedOutput: '16\n' },
  ]}
  starterCode={{
    python: `def rob(nums: list[int]) -> int:
    # Your code here
    pass`,
    java: `class Solution {
    public int rob(int[] nums) {
        // Your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int rob(vector<int>& nums) {
        // Your code here
        return 0;
    }
};`,
    javascript: `function rob(nums) {
    // Your code here
}`,
    typescript: `function rob(nums: number[]): number {
    // Your code here
    return 0;
}`,
    rust: `pub fn rob(nums: Vec<i32>) -> i32 {
    // Your code here
    0
}`,
    c: `int rob(int* nums, int numsSize) {
    // Your code here
    return 0;
}`,
    csharp: `public class Solution {
    public int Rob(int[] nums) {
        // Your code here
        return 0;
    }
}`,
    go: `func rob(nums []int) int {
    // Your code here
    return 0
}`,
  }}
  harness={{
    python: `\u0069mport sys
data = sys.stdin.read().strip().split()
if not data:
    print(0)
else:
    nums = [int(x) for x in data]
    print(rob(nums))`,
    java: `\u0069mport java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String line = sc.nextLine();
        if (line.isEmpty()) {
            System.out.println(0);
            return;
        }
        String[] parts = line.split(" ");
        int[] nums = new int[parts.length];
        for (int i = 0; i < parts.length; i++) nums[i] = Integer.parseInt(parts[i]);
        Solution sol = new Solution();
        System.out.println(sol.rob(nums));
    }
}`,
    cpp: `#include <iostream>
#include <sstream>
#include <vector>
#include <string>
using namespace std;
int main() {
    string line;
    getline(cin, line);
    if (line.empty()) { cout << 0 << endl; return 0; }
    vector<int> nums;
    stringstream ss(line);
    int val;
    while (ss >> val) nums.push_back(val);
    Solution sol;
    cout << sol.rob(nums) << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const nums = line.trim() ? line.split(' ').map(Number) : [];
    console.log(rob(nums));
    rl.close();
});`,
    typescript: `\u0069mport * as readline from 'readline';
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const nums: number[] = line.trim() ? line.split(' ').map(Number) : [];
    console.log(rob(nums));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let nums: Vec<i32> = if line.trim().is_empty() { Vec::new() } else { line.split(' ').map(|s| s.parse().unwrap()).collect() };
    println!("{}", rob(nums));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    char line[100000];
    if (!fgets(line, 100000, stdin)) { printf("0\\n"); return 0; }
    line[strcspn(line, "\\n")] = 0;
    if (strlen(line) == 0) { printf("0\\n"); return 0; }
    int nums[10000], n = 0;
    char* token = strtok(line, " ");
    while (token) { nums[n++] = atoi(token); token = strtok(NULL, " "); }
    printf("%d\\n", rob(nums, n));
    return 0;
}`,
    csharp: `using System;
using System.Linq;
class Program {
    static void Main() {
        string line = Console.ReadLine();
        int[] nums = string.IsNullOrEmpty(line) ? new int[0] : line.Split(' ').Select(int.Parse).ToArray();
        Solution sol = new Solution();
        Console.WriteLine(sol.Rob(nums));
    }
}`,
    go: `package main
\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)
func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    line = strings.TrimSpace(line)
    if line == "" { fmt.Println(0); return }
    parts := strings.Split(line, " ")
    nums := make([]int, len(parts))
    for i, s := range parts { nums[i], _ = strconv.Atoi(strings.TrimSpace(s)) }
    fmt.Println(rob(nums))
}`,
  }}
  defaultLanguage="python"
>

# House Robber

**Difficulty:** Easy

You are a professional robber planning to rob houses along a street. Each house has a certain amount of money stashed, the only constraint stopping you from robbing each of them is that **adjacent houses** have security systems connected and **it will automatically contact the police if two adjacent houses were broken into on the same night**.

Given an integer array `nums` representing the amount of money of each house, return the **maximum amount of money you can rob tonight** without alerting the police.

## Example 1

**Input:** `nums = [1, 2, 3, 1]`  
**Output:** `4`  
**Explanation:** Rob house 1 (money = 1) and then rob house 3 (money = 3). Total = 1 + 3 = 4.

## Example 2

**Input:** `nums = [2, 7, 9, 3, 1]`  
**Output:** `12`  
**Explanation:** Rob house 1 (money = 2), rob house 3 (money = 9), and rob house 5 (money = 1). Total = 2 + 9 + 1 = 12.

## Input Specification

- The input consists of a single line containing space-separated integers representing `nums[i]` for each house.
- `0 <= nums.length <= 100`
- `0 <= nums[i] <= 400`

## Output Specification

Print a single integer: the maximum amount of money you can rob without alerting the police.

## Constraints

- `0 <= nums.length <= 100`
- `0 <= nums[i] <= 400`

## Hints

<details>
<summary>Hint 1</summary>

Try to think recursively. At each house, you have two choices: rob it or skip it. If you rob it, you must skip the next house.
</details>

<details>
<summary>Hint 2</summary>

Define `dp[i]` as the maximum amount you can rob from the first `i` houses. How does `dp[i]` relate to `dp[i-1]` and `dp[i-2]`?
</details>

<details>
<summary>Hint 3</summary>

The recurrence is: `dp[i] = max(dp[i-1], dp[i-2] + nums[i-1])`. You can optimize space to O(1) by keeping only the last two values.
</details>

</ProblemEditor>

---
**📖 View the [detailed solution](../solutions/easy/house-robber)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Unique Paths](../exercises/medium/unique-paths).