---
title: LRU Cache
sidebar_label: LRU Cache
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'lru-cache', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: 'LRUCache 2\nput 1 1\nput 2 2\nget 1\nput 3 3\nget 2\nput 4 4\nget 1\nget 3\nget 4\n', expectedOutput: '1\n-1\n-1\n3\n4\n' },
    { stdin: 'LRUCache 1\nput 2 1\nget 2\nput 3 2\nget 2\nget 3\n', expectedOutput: '1\n-1\n2\n' },
    { stdin: 'LRUCache 3\nput 1 1\nput 2 2\nput 3 3\nput 4 4\nget 4\nget 3\nget 2\nget 1\nput 5 5\nget 1\nget 2\nget 3\nget 4\nget 5\n', expectedOutput: '4\n3\n2\n-1\n-1\n2\n3\n-1\n5\n' },
    { stdin: 'LRUCache 2\nput 2 1\nput 2 2\nget 2\nput 1 1\nput 4 1\nget 2\n', expectedOutput: '2\n-1\n' },
    { stdin: 'LRUCache 2\nget 2\nput 2 6\nget 1\nput 1 5\nput 1 2\nget 1\nget 2\n', expectedOutput: '-1\n-1\n2\n6\n' },
    { stdin: 'LRUCache 10\nput 10 13\nput 3 17\nput 6 11\nput 10 5\nput 9 10\nget 13\nput 2 19\nget 2\nget 3\n', expectedOutput: '-1\n19\n17\n' },
    { stdin: 'LRUCache 5\nput 1 10\nput 2 20\nget 1\nput 3 30\nget 2\nput 4 40\nget 1\nget 3\nget 4\n', expectedOutput: '10\n20\n10\n30\n40\n' },
    { stdin: 'LRUCache 2\nput 1 0\nput 2 0\nget 1\nget 2\nput 3 0\nget 1\nget 2\nget 3\n', expectedOutput: '0\n0\n-1\n-1\n0\n' },
    { stdin: 'LRUCache 3\nput 1 1\nput 2 2\nput 3 3\nget 1\nget 2\nget 3\nput 4 4\nget 1\nget 2\nget 3\nget 4\n', expectedOutput: '1\n2\n3\n-1\n2\n3\n4\n' },
    { stdin: 'LRUCache 4\nput 1 1\nput 2 2\nput 3 3\nput 4 4\nget 1\nget 2\nput 5 5\nget 3\nget 4\nget 5\n', expectedOutput: '1\n2\n-1\n4\n5\n' },
    { stdin: 'LRUCache 2\nput 1 1\nput 2 2\nget 1\nput 3 3\nget 2\nput 4 4\nget 1\nget 3\nget 4\n', expectedOutput: '1\n-1\n-1\n3\n4\n' },
    { stdin: 'LRUCache 1\nput 1 1\nget 1\nput 2 2\nget 1\nget 2\n', expectedOutput: '1\n-1\n2\n' },
    { stdin: 'LRUCache 3\nput 1 1\nput 2 2\nput 3 3\nget 1\nput 4 4\nget 2\nget 3\nget 4\n', expectedOutput: '1\n-1\n3\n4\n' },
    { stdin: 'LRUCache 10\nput 1 1\nput 2 2\nput 3 3\nput 4 4\nput 5 5\nput 6 6\nput 7 7\nput 8 8\nput 9 9\nput 10 10\nget 1\nget 10\nput 11 11\nget 1\nget 10\nget 11\n', expectedOutput: '1\n10\n-1\n10\n11\n' },
    { stdin: 'LRUCache 2\nput 1 1\nput 2 2\nput 1 3\nget 1\nput 3 3\nget 2\n', expectedOutput: '3\n-1\n' },
    { stdin: 'LRUCache 2\nput 1 1\nput 2 2\nget 1\nput 3 3\nget 2\nput 4 4\nget 1\nget 3\nget 4\n', expectedOutput: '1\n-1\n-1\n3\n4\n' },
    { stdin: 'LRUCache 5\nput 5 5\nput 4 4\nput 3 3\nput 2 2\nput 1 1\nget 5\nget 4\nget 3\nget 2\nget 1\nput 6 6\nget 5\nget 4\nget 3\nget 2\nget 1\nget 6\n', expectedOutput: '5\n4\n3\n2\n1\n-1\n4\n3\n2\n1\n6\n' },
    { stdin: 'LRUCache 3\nput 1 1\nput 2 2\nput 3 3\nget 1\nget 2\nget 3\nput 4 4\nget 1\nget 2\nget 3\nget 4\n', expectedOutput: '1\n2\n3\n-1\n2\n3\n4\n' },
    { stdin: 'LRUCache 2\nput 1 1\nput 2 2\nget 1\nput 1 3\nget 1\nput 3 3\nget 2\n', expectedOutput: '1\n3\n-1\n' },
    { stdin: 'LRUCache 2\nput 1 1\nput 2 2\nget 2\nget 1\nput 3 3\nget 1\nget 2\nget 3\n', expectedOutput: '2\n1\n1\n-1\n3\n' },
    { stdin: 'LRUCache 10\nput 10 10\nput 9 9\nput 8 8\nput 7 7\nput 6 6\nget 10\nget 9\nput 5 5\nput 4 4\nput 3 3\nget 8\nget 7\n', expectedOutput: '10\n9\n8\n7\n' },
  ]}
  starterCode={{
    python: `class LRUCache:
    def __init__(self, capacity: int):
        # your code here
        pass

    def get(self, key: int) -> int:
        # your code here
        return -1

    def put(self, key: int, value: int) -> None:
        # your code here
        pass
`,
    java: `class LRUCache {
    public LRUCache(int capacity) {
        // your code here
    }

    public int get(int key) {
        // your code here
        return -1;
    }

    public void put(int key, int value) {
        // your code here
    }
}
`,
    cpp: `class LRUCache {
public:
    LRUCache(int capacity) {
        // your code here
    }

    int get(int key) {
        // your code here
        return -1;
    }

    void put(int key, int value) {
        // your code here
    }
};
`,
    javascript: `class LRUCache {
    constructor(capacity) {
        // your code here
    }

    get(key) {
        // your code here
        return -1;
    }

    put(key, value) {
        // your code here
    }
}
`,
    typescript: `class LRUCache {
    constructor(capacity: number) {
        // your code here
    }

    get(key: number): number {
        // your code here
        return -1;
    }

    put(key: number, value: number): void {
        // your code here
    }
}
`,
    rust: `struct LRUCache {
    // your code here
}

impl LRUCache {
    fn new(capacity: i32) -> Self {
        // your code here
        LRUCache { }
    }

    fn get(&mut self, key: i32) -> i32 {
        // your code here
        -1
    }

    fn put(&mut self, key: i32, value: i32) {
        // your code here
    }
}
`,
    c: `typedef struct {
    // your code here
} LRUCache;

LRUCache* lRUCacheCreate(int capacity) {
    // your code here
    return NULL;
}

int lRUCacheGet(LRUCache* obj, int key) {
    // your code here
    return -1;
}

void lRUCachePut(LRUCache* obj, int key, int value) {
    // your code here
}

void lRUCacheFree(LRUCache* obj) {
    // your code here
}
`,
    csharp: `public class LRUCache {
    public LRUCache(int capacity) {
        // your code here
    }

    public int Get(int key) {
        // your code here
        return -1;
    }

    public void Put(int key, int value) {
        // your code here
    }
}
`,
    go: `type LRUCache struct {
    // your code here
}

func Constructor(capacity int) LRUCache {
    // your code here
    return LRUCache{}
}

func (this *LRUCache) Get(key int) int {
    // your code here
    return -1
}

func (this *LRUCache) Put(key int, value int) {
    // your code here
}
`,
  }}
  harness={{
    python: `import sys

class LRUCache:
    def __init__(self, capacity):
        pass
    def get(self, key):
        return -1
    def put(self, key, value):
        pass

lines = sys.stdin.read().strip().splitlines()
if not lines:
    exit()
first = lines[0].strip()
capacity = int(first.split()[1])
obj = LRUCache(capacity)
output = []
for line in lines[1:]:
    line = line.strip()
    if not line:
        continue
    parts = line.split()
    cmd = parts[0]
    if cmd == 'put':
        obj.put(int(parts[1]), int(parts[2]))
    elif cmd == 'get':
        output.append(str(obj.get(int(parts[1]))))
sys.stdout.write('\\n'.join(output) + ('\\n' if output else ''))
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String first = sc.nextLine().trim();
        int cap = Integer.parseInt(first.split(" ")[1]);
        LRUCache obj = new LRUCache(cap);
        StringBuilder sb = new StringBuilder();
        while (sc.hasNext()) {
            String cmd = sc.next();
            if (cmd.equals("put")) {
                obj.put(sc.nextInt(), sc.nextInt());
            } else if (cmd.equals("get")) {
                sb.append(obj.get(sc.nextInt())).append("\\n");
            }
        }
        System.out.print(sb.toString());
    }
}
`,
    cpp: `#include <iostream>
#include <string>
using namespace std;

int main() {
    string first; cin >> first;
    int cap; cin >> cap;
    LRUCache obj(cap);
    string cmd;
    while (cin >> cmd) {
        if (cmd == "put") {
            int k, v; cin >> k >> v;
            obj.put(k, v);
        } else if (cmd == "get") {
            int k; cin >> k;
            cout << obj.get(k) << endl;
        }
    }
    return 0;
}
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', line => lines.push(line.trim()));
rl.on('close', () => {
    const first = lines[0].split(' ');
    const cap = parseInt(first[1]);
    const obj = new LRUCache(cap);
    const output = [];
    for (let i = 1; i < lines.length; i++) {
        if (!lines[i]) continue;
        const parts = lines[i].split(' ');
        const cmd = parts[0];
        if (cmd === 'put') obj.put(parseInt(parts[1]), parseInt(parts[2]));
        else if (cmd === 'get') output.push(obj.get(parseInt(parts[1])).toString());
    }
    console.log(output.join('\\n'));
});
`,
    csharp: `using System;
using System.Collections.Generic;

class Program {
    static void Main() {
        string first = Console.ReadLine().Trim();
        int cap = int.Parse(first.Split(' ')[1]);
        LRUCache obj = new LRUCache(cap);
        List<string> output = new List<string>();
        string line;
        while ((line = Console.ReadLine()) != null) {
            line = line.Trim();
            if (line == "") continue;
            string[] parts = line.Split(' ');
            string cmd = parts[0];
            if (cmd == "put") obj.Put(int.Parse(parts[1]), int.Parse(parts[2]));
            else if (cmd == "get") output.Add(obj.Get(int.Parse(parts[1])).ToString());
        }
        Console.WriteLine(string.Join("\\n", output));
    }
}
`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines();
    let first = lines.next().unwrap().unwrap();
    let cap: i32 = first.split_whitespace().nth(1).unwrap().parse().unwrap();
    let mut obj = LRUCache::new(cap);
    let mut output = Vec::new();
    for line in lines {
        let line = line.unwrap();
        let parts: Vec<&str> = line.trim().split_whitespace().collect();
        if parts.is_empty() { continue; }
        match parts[0] {
            "put" => obj.put(parts[1].parse().unwrap(), parts[2].parse().unwrap()),
            "get" => output.push(obj.get(parts[1].parse().unwrap()).to_string()),
            _ => {}
        }
    }
    println!("{}", output.join("\\n"));
}
`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (line: string) => lines.push(line.trim()));
rl.on('close', () => {
    const first = lines[0].split(' ');
    const cap = parseInt(first[1]);
    const obj = new LRUCache(cap);
    const output: string[] = [];
    for (let i = 1; i < lines.length; i++) {
        if (!lines[i]) continue;
        const parts = lines[i].split(' ');
        const cmd = parts[0];
        if (cmd === 'put') obj.put(parseInt(parts[1]), parseInt(parts[2]));
        else if (cmd === 'get') output.push(obj.get(parseInt(parts[1])).toString());
    }
    console.log(output.join('\\n'));
});
`,
    c: `#include <stdio.h>
#include <string.h>
int main() {
    char cmd[10];
    int cap;
    scanf("LRUCache %d", &cap);
    LRUCache* obj = lRUCacheCreate(cap);
    while (scanf("%s", cmd) != EOF) {
        if (strcmp(cmd, "put") == 0) {
            int k, v; scanf("%d %d", &k, &v);
            lRUCachePut(obj, k, v);
        } else if (strcmp(cmd, "get") == 0) {
            int k; scanf("%d", &k);
            printf("%d\\n", lRUCacheGet(obj, k));
        }
    }
    lRUCacheFree(obj);
    return 0;
}
`,
    go: `package main

\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    first := strings.Fields(scanner.Text())
    cap, _ := strconv.Atoi(first[1])
    obj := Constructor(cap)
    var output []string
    for scanner.Scan() {
        parts := strings.Fields(scanner.Text())
        if len(parts) == 0 {
            continue
        }
        switch parts[0] {
        case "put":
            k, _ := strconv.Atoi(parts[1])
            v, _ := strconv.Atoi(parts[2])
            obj.Put(k, v)
        case "get":
            k, _ := strconv.Atoi(parts[1])
            output = append(output, strconv.Itoa(obj.Get(k)))
        }
    }
    fmt.Println(strings.Join(output, "\\n"))
}
`,
  }}
  defaultLanguage="python"
>

# LRU Cache

**Difficulty:** Medium

Design a data structure that follows the constraints of a **Least Recently Used (LRU) cache**.

Implement the `LRUCache` class:

- `LRUCache(int capacity)` initialize the LRU cache with **positive** size `capacity`.
- `int get(int key)` return the value of the `key` if the key exists, otherwise return `-1`.
- `void put(int key, int value)` update the value of the `key` if the key exists. Otherwise, add the key-value pair to the cache. If the number of keys exceeds the `capacity` from this operation, **evict** the least recently used key.

The functions `get` and `put` must each run in O(1) average time complexity.

## Example 1

**Input:**
```
["LRUCache","put","put","get","put","get","put","get","get","get"]
[[2],[1,1],[2,2],[1],[3,3],[2],[4,4],[1],[3],[4]]
```

**Output:**
```
[null,null,null,1,null,-1,null,-1,3,4]
```

**Explanation:**
```
LRUCache lRUCache = new LRUCache(2);
lRUCache.put(1, 1); // cache is {1=1}
lRUCache.put(2, 2); // cache is {1=1, 2=2}
lRUCache.get(1);    // return 1
lRUCache.put(3, 3); // LRU key was 2, evicts key 2, cache is {1=1, 3=3}
lRUCache.get(2);    // returns -1 (not found)
lRUCache.put(4, 4); // LRU key was 1, evicts key 1, cache is {4=4, 3=3}
lRUCache.get(1);    // return -1 (not found)
lRUCache.get(3);    // return 3
lRUCache.get(4);    // return 4
```

## Input Specification

The first line contains `LRUCache N` where N is the capacity. Subsequent lines contain commands:
- `put key value` — insert or update key with value
- `get key` — output the value for key or -1

## Output Specification

For each `get` command, output the result on its own line.

## Constraints

- `1 <= capacity <= 3000`
- `0 <= key <= 10^4`
- `0 <= value <= 10^5`
- At most `2 * 10^5` calls will be made to `get` and `put`.

## Hints

<details>
<summary>Hint 1</summary>

Use a hash map to store key-to-node mappings for O(1) lookups, and a doubly linked list to maintain the order of usage.
</details>

<details>
<summary>Hint 2</summary>

The most recently used item should be at the head of the linked list, and the least recently used at the tail. When you get or put an item, move it to the head.
</details>

<details>
<summary>Hint 3</summary>

Use dummy head and tail sentinel nodes in the linked list to avoid null checks when adding and removing nodes.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/medium/lru-cache)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Design Browser History](./design-browser-history).