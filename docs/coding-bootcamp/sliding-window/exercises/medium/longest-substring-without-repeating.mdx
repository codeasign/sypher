---
title: Longest Substring Without Repeating Characters
sidebar_label: Longest Substring
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'sliding-window-longest-substring-without-repeating', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: 'abcabcbb\n', expectedOutput: '3\n' },
    { stdin: 'bbbbb\n', expectedOutput: '1\n' },
    { stdin: 'pwwkew\n', expectedOutput: '3\n' },
    { stdin: '\n', expectedOutput: '0\n' },
    { stdin: 'a\n', expectedOutput: '1\n' },
    { stdin: 'abcdef\n', expectedOutput: '6\n' },
    { stdin: 'aab\n', expectedOutput: '2\n' },
    { stdin: 'dvdf\n', expectedOutput: '3\n' },
    { stdin: 'abba\n', expectedOutput: '2\n' },
    { stdin: 'tmmzuxt\n', expectedOutput: '5\n' },
    { stdin: 'abcdefghijklmnopqrstuvwxyz\n', expectedOutput: '26\n' },
    { stdin: '  \n', expectedOutput: '1\n' },
    { stdin: '!@#!@#\n', expectedOutput: '3\n' },
    { stdin: 'abcaabcd\n', expectedOutput: '4\n' },
    { stdin: 'abcddcba\n', expectedOutput: '4\n' },
    { stdin: 'a b c\n', expectedOutput: '3\n' },
    { stdin: 'au\n', expectedOutput: '2\n' },
    { stdin: 'aavabcd\n', expectedOutput: '5\n' },
    { stdin: 'abac\n', expectedOutput: '3\n' },
    { stdin: 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\n', expectedOutput: '52\n' },
  ]}
  starterCode={{
    python: `def length_of_longest_substring(s: str) -> int:
    # your code here
    pass
`,
    python27: `def length_of_longest_substring(s):
    # your code here
    pass
`,
    python35: `def length_of_longest_substring(s: str) -> int:
    # your code here
    pass
`,
    python36: `def length_of_longest_substring(s: str) -> int:
    # your code here
    pass
`,
    javascript: `function lengthOfLongestSubstring(s) {
    // your code here
}
`,
    java: `class Solution {
    public int lengthOfLongestSubstring(String s) {
        // your code here
        return 0;
    }
}
`,
    java8: `class Solution {
    public int lengthOfLongestSubstring(String s) {
        // your code here
        return 0;
    }
}
`,
    cpp: `class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        // your code here
        return 0;
    }
};
`,
    cpp14: `class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        // your code here
        return 0;
    }
};
`,
    cpp83: `class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        // your code here
        return 0;
    }
};
`,
    c: `int lengthOfLongestSubstring(char* s) {
    // your code here
    return 0;
}
`,
    c_gcc7: `int lengthOfLongestSubstring(char* s) {
    // your code here
    return 0;
}
`,
    c_gcc8: `int lengthOfLongestSubstring(char* s) {
    // your code here
    return 0;
}
`,
    csharp: `class Solution {
    public int LengthOfLongestSubstring(string s) {
        // your code here
        return 0;
    }
}
`,
    csharp_mono52: `class Solution {
    public int LengthOfLongestSubstring(string s) {
        // your code here
        return 0;
    }
}
`,
    csharp_mono54: `class Solution {
    public int LengthOfLongestSubstring(string s) {
        // your code here
        return 0;
    }
}
`,
    rust: `fn length_of_longest_substring(s: &str) -> i32 {
    // your code here
    0
}
`,
    rust120: `fn length_of_longest_substring(s: &str) -> i32 {
    // your code here
    0
}
`,
    typescript: `function lengthOfLongestSubstring(s: string): number {
    // your code here
    return 0;
}
`,
    go: `func lengthOfLongestSubstring(s string) int {
    // your code here
    return 0
}
`,
    kotlin: `class Solution {
    fun lengthOfLongestSubstring(s: String): Int {
        // your code here
        return 0
    }
}
`,
  }}
  harness={{
    python: `import sys
s = sys.stdin.read().rstrip('\n')
print(length_of_longest_substring(s))
`,
    python27: `import sys
s = sys.stdin.read().rstrip('\n')
print length_of_longest_substring(s)
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    console.log(lengthOfLongestSubstring(line));
    rl.close();
});
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String s = sc.nextLine();
        System.out.println(new Solution().lengthOfLongestSubstring(s));
    }
}
`,
    cpp: `int main() {
    string s;
    getline(cin, s);
    Solution sol;
    cout << sol.lengthOfLongestSubstring(s) << endl;
    return 0;
}
`,
    c: `int main() {
    char s[100005];
    fgets(s, sizeof(s), stdin);
    s[strcspn(s, "\n")] = 0;
    printf("%d\n", lengthOfLongestSubstring(s));
    return 0;
}
`,
    csharp: `class Program {
    static void Main() {
        string s = Console.ReadLine();
        Console.WriteLine(new Solution().LengthOfLongestSubstring(s));
    }
}
`,
    rust: `fn main() {
    let mut s = String::new();
    std::io::stdin().read_line(&mut s).unwrap();
    println!("{}", length_of_longest_substring(s.trim()));
}
`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    console.log(lengthOfLongestSubstring(line));
    rl.close();
});
`,
    go: `package main
import (
    "bufio"
    "fmt"
    "os"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Buffer(make([]byte, 1024*1024), 1024*1024)
    scanner.Scan()
    s := scanner.Text()
    fmt.Println(lengthOfLongestSubstring(s))
}
`,
    kotlin: `fun main() {
    val s = readLine()!!
    val sol = Solution()
    println(sol.lengthOfLongestSubstring(s))
}
`,
  }}
  defaultLanguage="python"
>

# Longest Substring Without Repeating Characters

**Difficulty:** Medium

Given a string `s`, find the length of the **longest substring** without repeating characters.

## Example 1

**Input:** `s = "abcabcbb"`  
**Output:** `3`  
**Explanation:** The answer is `"abc"`, with the length of 3.

## Example 2

**Input:** `s = "bbbbb"`  
**Output:** `1`  
**Explanation:** The answer is `"b"`, with the length of 1.

## Example 3

**Input:** `s = "pwwkew"`  
**Output:** `3`  
**Explanation:** The answer is `"wke"`, with the length of 3. Notice that the answer must be a substring (contiguous), not a subsequence. `"pwke"` is a subsequence, not a substring.

## Input Specification

A single line containing the string `s`. The string may contain any printable ASCII characters including spaces.

## Output Specification

Print a single integer representing the length of the longest substring without repeating characters.

## Constraints

- `0 <= s.length <= 5 * 10⁴`
- `s` consists of English letters, digits, symbols, and spaces

## Hints

<details>
<summary>Hint 1</summary>

Use a sliding window with two pointers (left and right). Expand the right pointer to grow the window, and move the left pointer forward when a duplicate is found.
</details>

<details>
<summary>Hint 2</summary>

Use a hash set or hash map to track characters currently in the window. When a duplicate character is encountered, shrink the window from the left until the duplicate is removed.
</details>

<details>
<summary>Hint 3</summary>

For an O(n) solution, use a hash map that stores the last seen index of each character. When a duplicate is found, jump the left pointer directly to `lastSeen[char] + 1` instead of incrementing one step at a time.
</details>

</ProblemEditor>

---

**View the [detailed solution](../solutions/medium/longest-substring-without-repeating)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Try [Minimum Window Substring](../hard/minimum-window-substring).