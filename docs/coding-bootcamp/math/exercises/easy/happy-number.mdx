---
title: Happy Number
sidebar_label: Happy Number
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'math-happy-number', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '19\n', expectedOutput: 'true\n' },
    { stdin: '2\n', expectedOutput: 'false\n' },
    { stdin: '1\n', expectedOutput: 'true\n' },
    { stdin: '7\n', expectedOutput: 'true\n' },
    { stdin: '4\n', expectedOutput: 'false\n' },
    { stdin: '10\n', expectedOutput: 'true\n' },
    { stdin: '13\n', expectedOutput: 'true\n' },
    { stdin: '0\n', expectedOutput: 'false\n' },
    { stdin: '20\n', expectedOutput: 'false\n' },
    { stdin: '23\n', expectedOutput: 'true\n' },
    { stdin: '100\n', expectedOutput: 'true\n' },
    { stdin: '111\n', expectedOutput: 'false\n' },
    { stdin: '68\n', expectedOutput: 'true\n' },
    { stdin: '99\n', expectedOutput: 'false\n' },
    { stdin: '139\n', expectedOutput: 'true\n' },
    { stdin: '166\n', expectedOutput: 'false\n' },
    { stdin: '789\n', expectedOutput: 'false\n' },
    { stdin: '986\n', expectedOutput: 'false\n' },
    { stdin: '1000\n', expectedOutput: 'true\n' },
    { stdin: '123456789\n', expectedOutput: 'false\n' },
  ]}
  starterCode={{
    python: `def is_happy(n: int) -> bool:
    # your code here
    pass
`,
    java: `class Solution {
    public boolean isHappy(int n) {
        // your code here
        return false;
    }
}
`,
    cpp: `class Solution {
public:
    bool isHappy(int n) {
        // your code here
        return false;
    }
};
`,
    javascript: `function isHappy(n) {
    // your code here
}
`,
    typescript: `function isHappy(n: number): boolean {
    // your code here
    return false;
}
`,
    rust: `fn is_happy(n: i32) -> bool {
    // your code here
    false
}
`,
    c: `bool isHappy(int n) {
    // your code here
    return false;
}
`,
    csharp: `class Solution {
    public bool IsHappy(int n) {
        // your code here
        return false;
    }
}
`,
    go: `func isHappy(n int) bool {
    // your code here
    return false
}
`,
  }}
  harness={{
    python: `import sys
n = int(sys.stdin.read().strip())
print(str(is_happy(n)).lower())
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        int n = sc.nextInt();
        System.out.println(new Solution().isHappy(n));
    }
}
`,
    cpp: `int main() {
    int n;
    cin >> n;
    Solution sol;
    cout << (sol.isHappy(n) ? "true" : "false") << endl;
    return 0;
}
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line) => {
    console.log(isHappy(parseInt(line.trim())));
});
`,
    typescript: `import * as readline from 'readline';
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    console.log(isHappy(parseInt(line.trim())));
});
`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let n: i32 = line.trim().parse().unwrap();
    println!("{}", is_happy(n));
}
`,
    c: `int main() {
    int n;
    scanf("%d", &n);
    printf("%s\\n", isHappy(n) ? "true" : "false");
    return 0;
}
`,
    csharp: `class Program {
    static void Main() {
        int n = int.Parse(Console.ReadLine());
        Console.WriteLine(new Solution().IsHappy(n).ToString().ToLower());
    }
}
`,
    go: `package main
\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    n, _ := strconv.Atoi(strings.TrimSpace(scanner.Text()))
    fmt.Println(strconv.FormatBool(isHappy(n)))
}
`,
  }}
  defaultLanguage="python"
>

# Happy Number

A **happy number** is a number defined by the following process:

- Starting with any positive integer, replace the number by the sum of the squares of its digits.
- Repeat the process until the number equals 1 (where it will stay), or it loops endlessly in a cycle that does not include 1.
- Those numbers for which this process ends in 1 are **happy**.

Return `true` if `n` is a happy number, and `false` otherwise.

## Examples

**Example 1:**
```
Input: n = 19
Output: true
Explanation:
1² + 9² = 82
8² + 2² = 68
6² + 8² = 100
1² + 0² + 0² = 1
```

**Example 2:**
```
Input: n = 2
Output: false
Explanation:
2² = 4
4² = 16
1² + 6² = 37
3² + 7² = 58
5² + 8² = 89
8² + 9² = 145
1² + 4² + 5² = 42
4² + 2² = 20
2² + 0² = 4
... enters a cycle [4, 16, 37, 58, 89, 145, 42, 20, 4]
```

## Constraints

- `1 <= n <= 2³¹ - 1`

## Hints

<details>
<summary>Hint 1</summary>

If a number is not happy, the sequence will eventually enter a cycle. Use Floyd's cycle detection (tortoise and hare) to detect the cycle without extra space.

</details>

<details>
<summary>Hint 2</summary>

You can also use a HashSet to track seen numbers. If you encounter a number you've seen before, you're in a cycle.

</details>

<details>
<summary>Hint 3</summary>

The sum of squares of digits of a number never exceeds 243 for 3-digit numbers (9² + 9² + 9² = 243). This limits the possible states significantly.

</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../../solutions/easy/happy-number)** to see implementations in all 9 languages with explanations.

## Next

- [Plus One](./plus-one) — add one to a digit array
- [Pow(x, n)](../medium/pow-x-n) — exponentiation by squaring
- [Back to Theory](../../index)