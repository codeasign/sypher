---
title: Two Sum II — Input Array Is Sorted
sidebar_label: Two Sum II
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'two-sum-ii', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '4 9\n2 7 11 15\n', expectedOutput: '1 2\n' },
    { stdin: '3 6\n2 3 4\n', expectedOutput: '1 3\n' },
    { stdin: '2 -1\n-1 0\n', expectedOutput: '1 2\n' },
    { stdin: '5 8\n1 2 3 4 5\n', expectedOutput: '3 5\n' },
    { stdin: '2 3\n1 2\n', expectedOutput: '1 2\n' },
    { stdin: '2 0\n-5 5\n', expectedOutput: '1 2\n' },
    { stdin: '3 10\n1 4 9\n', expectedOutput: '1 3\n' },
    { stdin: '6 5\n1 2 2 3 3 5\n', expectedOutput: '2 5\n' },
    { stdin: '7 20\n2 5 8 10 12 15 18\n', expectedOutput: '1 7\n' },
    { stdin: '5 100\n10 20 30 40 60\n', expectedOutput: '4 5\n' },
    { stdin: '3 -2\n-5 -1 3\n', expectedOutput: '1 3\n' },
    { stdin: '6 0\n-10 -5 0 5 10 15\n', expectedOutput: '1 5\n' },
    { stdin: '4 50\n5 15 25 35\n', expectedOutput: '2 4\n' },
    { stdin: '3 15\n3 7 12\n', expectedOutput: '1 3\n' },
    { stdin: '5 6\n1 1 2 4 5\n', expectedOutput: '1 5\n' },
    { stdin: '4 1\n-3 -1 2 4\n', expectedOutput: '1 4\n' },
    { stdin: '6 22\n1 3 5 7 9 15\n', expectedOutput: '4 6\n' },
    { stdin: '3 100\n30 40 70\n', expectedOutput: '1 3\n' },
  ]}
  starterCode={{
    python: `def two_sum(numbers: List[int], target: int) -> List[int]:`,
    python35: `def two_sum(numbers: List[int], target: int) -> List[int]:`,
    python36: `def two_sum(numbers: List[int], target: int) -> List[int]:`,
    python27: `def two_sum(numbers, target):`,
    javascript: `function twoSum(numbers, target) { }`,
    java: `class Solution { public int[] twoSum(int[] numbers, int target) { } }`,
    java8: `class Solution {
    public int[] twoSum(int[] numbers, int target) {
        // your code here
        return new int[]{};
    }
}`,
    cpp: `class Solution { public: vector<int> twoSum(vector<int>& numbers, int target) { } };`,
    cpp14: `class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        // your code here
        return {};
    }
};`,
    cpp83: `class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        // your code here
        return {};
    }
};`,
    c: `int* twoSum(int* numbers, int numbersSize, int target, int* returnSize) { }`,
    c_gcc7: `int* twoSum(int* numbers, int numbersSize, int target, int* returnSize) {
    // your code here
    return NULL;
}`,
    c_gcc8: `int* twoSum(int* numbers, int numbersSize, int target, int* returnSize) {
    // your code here
    return NULL;
}`,
    csharp: `class Solution { public int[] TwoSum(int[] numbers, int target) { } }`,
    csharp_mono52: `class Solution {
    public int[] TwoSum(int[] numbers, int target) {
        // your code here
        return new int[0];
    }
}`,
    csharp_mono54: `class Solution {
    public int[] TwoSum(int[] numbers, int target) {
        // your code here
        return new int[0];
    }
}`,
    rust: `fn two_sum(numbers: &Vec<i32>, target: i32) -> Vec<i32> { }`,
    rust120: `fn two_sum(numbers: &Vec<i32>, target: i32) -> Vec<i32> {
    // your code here
    vec![]
}`,
    typescript: `function twoSum(numbers: number[], target: number): number[] { }`,
  }}
  harness={{
    python: `import sys
data = sys.stdin.read().strip().splitlines()
if len(data) >= 2:
    _, target = data[0].split()
    numbers = list(map(int, data[1].split()))
    result = two_sum(numbers, int(target))
    print(' '.join(map(str, result)))`,
    python27: `import sys
data = sys.stdin.read().strip().splitlines()
if len(data) >= 2:
    _, target = data[0].split()
    numbers = map(int, data[1].split())
    result = two_sum(numbers, int(target))
    print ' '.join(map(str, result))`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l));
rl.on('close', () => {
    const target = parseInt(lines[0].split(' ')[1]);
    const numbers = lines[1].split(' ').map(Number);
    console.log(twoSum(numbers, target).join(' '));
});`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        int n = sc.nextInt();
        int target = sc.nextInt();
        int[] numbers = new int[n];
        for (int i = 0; i < n; i++) numbers[i] = sc.nextInt();
        Solution sol = new Solution();
        int[] result = sol.twoSum(numbers, target);
        System.out.println(result[0] + " " + result[1]);
    }
}`,
    cpp: `int main() {
    int n, target;
    cin >> n >> target;
    vector<int> numbers(n);
    for (int i = 0; i < n; i++) cin >> numbers[i];
    Solution sol;
    auto res = sol.twoSum(numbers, target);
    cout << res[0] << " " << res[1] << endl;
    return 0;
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
int main() {
    int n, target;
    scanf("%d %d", &n, &target);
    int numbers[30005];
    for (int i = 0; i < n; i++) scanf("%d", &numbers[i]);
    int returnSize;
    int* res = twoSum(numbers, n, target, &returnSize);
    printf("%d %d\n", res[0], res[1]);
    free(res);
    return 0;
}`,
    csharp: `class Program {
    static void Main() {
        string[] input = System.Console.ReadLine().Split();
        int n = int.Parse(input[0]);
        int target = int.Parse(input[1]);
        int[] numbers = new int[n];
        string[] nums = System.Console.ReadLine().Split();
        for (int i = 0; i < n; i++) numbers[i] = int.Parse(nums[i]);
        Solution sol = new Solution();
        int[] res = sol.TwoSum(numbers, target);
        System.Console.WriteLine(res[0] + " " + res[1]);
    }
}`,
    rust: `fn main() {
    let mut input = String::new();
    std::io::stdin().read_line(&mut input).unwrap();
    let nums: Vec<i32> = input.trim().split_whitespace().map(|s| s.parse().unwrap()).collect();
    let n = nums[0] as usize;
    let target = nums[1];
    let mut line2 = String::new();
    std::io::stdin().read_line(&mut line2).unwrap();
    let numbers: Vec<i32> = line2.trim().split_whitespace().map(|s| s.parse().unwrap()).collect();
    let res = two_sum(&numbers, target);
    println!("{} {}", res[0], res[1]);
}`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    const target = parseInt(lines[0].split(' ')[1]);
    const numbers = lines[1].split(' ').map(Number);
    console.log(twoSum(numbers, target).join(' '));
});`,
  }}
  defaultLanguage="python"
>

# Two Sum II — Input Array Is Sorted

**Difficulty:** Easy

Given a **1-indexed** array of integers `numbers` that is already
**sorted in non-decreasing order**, find two numbers that add up to a
specific `target` number. Return the indices of the two numbers
(1-indexed) as an integer array `[index1, index2]` of length 2.

The tests are generated such that there is **exactly one solution**.
You may **not** use the same element twice.

Your solution must use only **constant** extra space.

## Example 1

**Input:** `numbers = [2,7,11,15]`, `target = 9`  
**Output:** `[1,2]`  
**Explanation:** The sum of 2 and 7 is 9. Therefore index1 = 1,
index2 = 2.

## Example 2

**Input:** `numbers = [2,3,4]`, `target = 6`  
**Output:** `[1,3]`  
**Explanation:** The sum of 2 and 4 is 6. Therefore index1 = 1,
index2 = 3.

## Input Specification

The first line contains two space-separated integers: `n` (the length
of the array) and `target` (the target sum).
The second line contains `n` space-separated integers representing the
sorted array `numbers`.

## Output Specification

Print two space-separated integers representing the 1-indexed positions
of the two numbers that sum to the target.

## Constraints

- `2 <= numbers.length <= 3 * 10⁴`
- `-1000 <= numbers[i] <= 1000`
- `numbers` is sorted in **non-decreasing order**
- `-1000 <= target <= 1000`
- The tests are generated such that **exactly one solution exists**

## Hints

<details>
<summary>Hint 1</summary>

The array is sorted — that's the key to using two pointers efficiently.
</details>

<details>
<summary>Hint 2</summary>

If the current sum is too large, move the right pointer left. If too
small, move the left pointer right.
</details>

<details>
<summary>Hint 3</summary>

Return 1-indexed positions (add 1 to each index).
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/easy/two-sum-ii)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Three Sum](../medium/three-sum).