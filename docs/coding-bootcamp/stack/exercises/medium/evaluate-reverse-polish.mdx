---
title: Evaluate Reverse Polish Notation
sidebar_label: Eval RPN
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/stack/evaluate-reverse-polish', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '2 1 + 3 *\n', expectedOutput: '9\n' },
    { stdin: '4 13 5 / +\n', expectedOutput: '6\n' },
    { stdin: '10 6 9 3 + -11 * / * 17 + 5 +\n', expectedOutput: '12\n' },
    { stdin: '3 4 +\n', expectedOutput: '7\n' },
    { stdin: '5 1 2 + 4 * + 3 -\n', expectedOutput: '14\n' },
    { stdin: '2 3 * 4 +\n', expectedOutput: '10\n' },
    { stdin: '8 2 /\n', expectedOutput: '4\n' },
    { stdin: '7 3 -\n', expectedOutput: '4\n' },
    { stdin: '6 2 / 3 +\n', expectedOutput: '6\n' },
    { stdin: '-3 4 +\n', expectedOutput: '1\n' },
    { stdin: '6 -2 /\n', expectedOutput: '-3\n' },
    { stdin: '3 -4 *\n', expectedOutput: '-12\n' },
    { stdin: '10 2 /\n', expectedOutput: '5\n' },
    { stdin: '100 50 - 10 *\n', expectedOutput: '500\n' },
    { stdin: '2 2 2 2 + + +\n', expectedOutput: '8\n' },
    { stdin: '1 2 + 3 * 4 - 5 /\n', expectedOutput: '1\n' },
    { stdin: '9 3 / 2 * 1 -\n', expectedOutput: '5\n' },
    { stdin: '-5 -3 *\n', expectedOutput: '15\n' },
    { stdin: '7 2 3 * -\n', expectedOutput: '1\n' },
    { stdin: '4 2 5 * + 1 3 2 * + /\n', expectedOutput: '2\n' },
  ]}
  starterCode={{
    python: `def evaluate_rpn(tokens: list) -> int:
    # tokens is a list of strings (numbers and operators)
    pass`,
    java: `class Solution {
    public int evalRPN(String[] tokens) {
        // your code here
        return 0;
    }
}`,
    cpp: `class Solution {
public:
    int evalRPN(vector<string>& tokens) {
        // your code here
        return 0;
    }
};`,
    javascript: `function evalRPN(tokens) {
    // tokens: string[]
    // your code here
}`,
    typescript: `function evalRPN(tokens: string[]): number {
    // your code here
    return 0;
}`,
    rust: `fn eval_rpn(tokens: &[&str]) -> i64 {
    // your code here
    0
}`,
    c: `int evalRPN(char** tokens, int tokensSize) {
    // your code here
    return 0;
}`,
    csharp: `class Solution {
    public int EvalRPN(string[] tokens) {
        // your code here
        return 0;
    }
}`,
    go: `func evalRPN(tokens []string) int {
    // your code here
    return 0
}`,
  }}
  harness={{
    python: `import sys
line = sys.stdin.readline().strip()
tokens = line.split()
print(evaluate_rpn(tokens))`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String line = sc.nextLine().trim();
        String[] tokens = line.split("\\\\s+");
        Solution sol = new Solution();
        System.out.println(sol.evalRPN(tokens));
    }
}`,
    cpp: `#include <bits/stdc++.h>
using namespace std;
int main() {
    string line;
    getline(cin, line);
    vector<string> tokens;
    istringstream ss(line);
    string tok;
    while (ss >> tok) tokens.push_back(tok);
    Solution sol;
    cout << sol.evalRPN(tokens) << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l.trim()));
rl.on('close', () => {
    const tokens = lines[0].split(' ');
    console.log(evalRPN(tokens));
});`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l.trim()));
rl.on('close', () => {
    const tokens = lines[0].split(' ');
    console.log(evalRPN(tokens));
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let tokens: Vec<&str> = line.split_whitespace().collect();
    println!("{}", eval_rpn(&tokens));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    char line[100000];
    fgets(line, sizeof(line), stdin);
    char *tokens[10000];
    int n = 0;
    char *p = strtok(line, " \\t\\n");
    while (p) {
        tokens[n] = p;
        n++;
        p = strtok(NULL, " \\t\\n");
    }
    printf("%d\\n", evalRPN(tokens, n));
    return 0;
}`,
    csharp: `class Program {
    static void Main() {
        string line = System.Console.ReadLine().Trim();
        string[] tokens = line.Split(' ');
        Solution sol = new Solution();
        System.Console.WriteLine(sol.EvalRPN(tokens));
    }
}`,
    go: `package main
import (
    "bufio"
    "fmt"
    "os"
    "strings"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    tokens := strings.Fields(scanner.Text())
    fmt.Println(evalRPN(tokens))
}`,
  }}
  defaultLanguage="python"
>

# Evaluate Reverse Polish Notation

**Difficulty:** Medium

You are given an array of strings `tokens` that represents an arithmetic expression in **Reverse Polish Notation (RPN)**.

Evaluate the expression and return the result as an integer.

Valid operators are `+`, `-`, `*`, and `/`. Each operand may be an integer or another expression. Division between two integers **truncates toward zero**.

It is guaranteed that the expression is valid and there is no division by zero.

## Example 1

**Input:** `tokens = ["2","1","+","3","*"]`  
**Output:** `9`  
**Explanation:** `((2 + 1) * 3) = 9`

## Example 2

**Input:** `tokens = ["4","13","5","/","+"]`  
**Output:** `6`  
**Explanation:** `(4 + (13 / 5)) = (4 + 2) = 6`

## Example 3

**Input:** `tokens = ["10","6","9","3","+","-11","*","/","*","17","+","5","+"]`  
**Output:** `12`  
**Explanation:** `((10 * (6 / ((9 + 3) * -11))) + 17) + 5 = 12`

## Input Specification

A single line of space-separated tokens representing the RPN expression. Each token is either an integer (possibly negative) or one of the operators `+`, `-`, `*`, `/`.

## Output Specification

Print a single integer — the result of evaluating the RPN expression.

## Constraints

- `1 <= tokens.length <= 10^4`
- `tokens[i]` is either an operator `+`, `-`, `*`, `/` or an integer in the range `[-200, 200]`
- The expression is always valid (no division by zero, no malformed input)
- The answer fits in a 32-bit signed integer

## Hints

<details>
<summary>Hint 1</summary>

Use a stack. Iterate through each token. If it is a number, push it onto the stack. If it is an operator, pop the top two values, apply the operator, and push the result back.

</details>

<details>
<summary>Hint 2</summary>

Be careful about operand order when you pop from the stack: the first value popped is the **right** operand and the second popped is the **left** operand. This matters for subtraction and division.

</details>

<details>
<summary>Hint 3</summary>

For integer division that truncates toward zero (not floor division), use `int(a / b)` in Python rather than `a // b`, since `//` floors toward negative infinity. For example, `-7 // 2 = -4` but `int(-7 / 2) = -3`.

</details>

</ProblemEditor>

---

**View the [detailed solution](../../solutions/medium/evaluate-reverse-polish)** for a step-by-step walkthrough, multiple approaches, and code in all languages.
