---
title: Valid Parentheses
sidebar_label: Valid Parentheses
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'valid-parentheses', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '()\n', expectedOutput: 'true\n' },
    { stdin: '()[]{}\n', expectedOutput: 'true\n' },
    { stdin: '(]\n', expectedOutput: 'false\n' },
    { stdin: '([)]\n', expectedOutput: 'false\n' },
    { stdin: '{[]}\n', expectedOutput: 'true\n' },
    { stdin: '\n', expectedOutput: 'true\n' },
    { stdin: '{\n', expectedOutput: 'false\n' },
    { stdin: ']\n', expectedOutput: 'false\n' },
    { stdin: '((()))\n', expectedOutput: 'true\n' },
    { stdin: '((())\n', expectedOutput: 'false\n' },
    { stdin: '[]{}()\n', expectedOutput: 'true\n' },
    { stdin: '[({})]\n', expectedOutput: 'true\n' },
    { stdin: '[(])]\n', expectedOutput: 'false\n' },
    { stdin: '(((((((((())))))))))\n', expectedOutput: 'true\n' },
    { stdin: '(((((((((()))))))))\n', expectedOutput: 'false\n' },
    { stdin: '()[]{}()[]{}\n', expectedOutput: 'true\n' },
    { stdin: '(([]){})\n', expectedOutput: 'true\n' },
    { stdin: '([{}])\n', expectedOutput: 'true\n' },
    { stdin: '((()))[{}]\n', expectedOutput: 'true\n' },
    { stdin: '([[{}]]())\n', expectedOutput: 'true\n' },
  ]}
  starterCode={{
    python: `def is_valid(s: str) -> bool:
    # your code here
    pass
`,
    python27: `def is_valid(s):
    # your code here
    pass
`,
    python35: `def is_valid(s: str) -> bool:
    # your code here
    pass
`,
    python36: `def is_valid(s: str) -> bool:
    # your code here
    pass
`,
    javascript: `function isValid(s) {
    // your code here
}
`,
    java: `class Solution {
    public boolean isValid(String s) {
        // your code here
        return false;
    }
}
`,
    java8: `class Solution {
    public boolean isValid(String s) {
        // your code here
        return false;
    }
}
`,
    cpp: `class Solution {
public:
    bool isValid(string s) {
        // your code here
        return false;
    }
};
`,
    cpp14: `class Solution {
public:
    bool isValid(string s) {
        // your code here
        return false;
    }
};
`,
    cpp83: `class Solution {
public:
    bool isValid(string s) {
        // your code here
        return false;
    }
};
`,
    c: `bool isValid(char* s) {
    // your code here
    return false;
}
`,
    c_gcc7: `bool isValid(char* s) {
    // your code here
    return false;
}
`,
    c_gcc8: `bool isValid(char* s) {
    // your code here
    return false;
}
`,
    csharp: `class Solution {
    public bool IsValid(string s) {
        // your code here
        return false;
    }
}
`,
    csharp_mono52: `class Solution {
    public bool IsValid(string s) {
        // your code here
        return false;
    }
}
`,
    csharp_mono54: `class Solution {
    public bool IsValid(string s) {
        // your code here
        return false;
    }
}
`,
    rust: `fn is_valid(s: &str) -> bool {
    // your code here
    false
}
`,
    rust120: `fn is_valid(s: &str) -> bool {
    // your code here
    false
}
`,
    typescript: `function isValid(s: string): boolean {
    // your code here
    return false;
}
`,
  }}
  harness={{
    python: `import sys
s = sys.stdin.read().strip()
print(str(is_valid(s)).lower())
`,
    python27: `import sys
s = sys.stdin.read().strip()
print str(is_valid(s)).lower()
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    console.log(isValid(line));
    rl.close();
});
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String s = sc.nextLine();
        System.out.println(new Solution().isValid(s));
    }
}
`,
    cpp: `int main() {
    string s;
    getline(cin, s);
    Solution sol;
    cout << (sol.isValid(s) ? "true" : "false") << endl;
    return 0;
}
`,
    c: `int main() {
    char s[100005];
    fgets(s, sizeof(s), stdin);
    s[strcspn(s, "\n")] = 0;
    printf("%s\n", isValid(s) ? "true" : "false");
    return 0;
}
`,
    csharp: `class Program {
    static void Main() {
        string s = Console.ReadLine();
        Console.WriteLine(new Solution().IsValid(s) ? "true" : "false");
    }
}
`,
    rust: `fn main() {
    let mut s = String::new();
    std::io::stdin().read_line(&mut s).unwrap();
    println!("{}", is_valid(s.trim()));
}
`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    console.log(isValid(line));
    rl.close();
});
`,
  }}
  defaultLanguage="python"
>

# Valid Parentheses

**Difficulty:** Easy

Given a string `s` containing just the characters `'('`, `')'`, `'{'`, `'}'`, `'['` and `']'`, determine if the input string is valid.

An input string is valid if:

1. Open brackets must be closed by the same type of brackets.
2. Open brackets must be closed in the correct order.
3. Every close bracket has a corresponding open bracket of the same type.

## Example 1

**Input:** `s = "()"`  
**Output:** `true`  

## Example 2

**Input:** `s = "()[]{}"`  
**Output:** `true`  

## Example 3

**Input:** `s = "(]"`  
**Output:** `false`  

## Example 4

**Input:** `s = "([)]"`  
**Output:** `false`  

## Input Specification

A single line containing the string `s`. The string contains only bracket characters.

## Output Specification

Print `true` if the string has valid bracket ordering, otherwise `false`.

## Constraints

- `1 <= s.length <= 10^4`
- `s` consists of parentheses `'()[]{}'` only

## Hints

<details>
<summary>Hint 1</summary>

Use a stack data structure. Push opening brackets onto the stack and pop when you encounter a closing bracket.
</details>

<details>
<summary>Hint 2</summary>

Use a hash map or dictionary to store the mapping of closing brackets to their corresponding opening brackets.
</details>

<details>
<summary>Hint 3</summary>

At the end, check if the stack is empty. If it is, all brackets were properly closed.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/easy/valid-parentheses)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Daily Temperatures](../medium/daily-temperatures).