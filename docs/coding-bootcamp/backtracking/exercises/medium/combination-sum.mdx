---
title: Combination Sum
sidebar_label: Combination Sum
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/backtracking/combination-sum', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '2 3 6 7 7\n', expectedOutput: '2 2 3,7\n' },
    { stdin: '2 3 5 8\n', expectedOutput: '2 2 2 2,2 3 3,3 5\n' },
    { stdin: '2 1\n', expectedOutput: '\n' },
    { stdin: '1 2\n', expectedOutput: '1 1\n' },
    { stdin: '3 5 8\n', expectedOutput: '3 5\n' },
    { stdin: '2 4 6 8\n', expectedOutput: '2 2 2 2,2 2 4,2 6,4 4\n' },
    { stdin: '1 2 3 4\n', expectedOutput: '1 1 1 1,1 1 2,1 3,2 2\n' },
    { stdin: '2 6 6\n', expectedOutput: '2 2 2,6\n' },
    { stdin: '2 3 7 9 11\n', expectedOutput: '2 2 2 2 3,2 2 7,2 3 3 3,2 9\n' },
    { stdin: '1 5 10 25 2\n', expectedOutput: '1 1\n' },
  ]}
  starterCode={{
    python: `def combination_sum(candidates: list[int], target: int) -> list[list[int]]:
    # Your code here
    pass`,
    java: `class Solution {
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        // Your code here
        return new ArrayList<>();
    }
}`,
    cpp: `class Solution {
public:
    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
        // Your code here
        return {};
    }
};`,
    javascript: `function combinationSum(candidates, target) {
    // Your code here
}`,
    typescript: `function combinationSum(candidates: number[], target: number): number[][] {
    // Your code here
    return [];
}`,
    rust: `pub fn combination_sum(candidates: Vec<i32>, target: i32) -> Vec<Vec<i32>> {
    // Your code here
    vec![]
}`,
    c: `int** combinationSum(int* candidates, int candidatesSize, int target, int* returnSize, int** returnColumnSizes) {
    // Your code here
    *returnSize = 0;
    return NULL;
}`,
    csharp: `public class Solution {
    public IList<IList<int>> CombinationSum(int[] candidates, int target) {
        // Your code here
        return new List<IList<int>>();
    }
}`,
    go: `func combinationSum(candidates []int, target int) [][]int {
    // Your code here
    return nil
}`,
  }}
  harness={{
    python: `import sys
data = sys.stdin.read().strip().split()
candidates = [int(x) for x in data[:-1]]
target = int(data[-1])
result = combination_sum(candidates, target)
result.sort()
print(','.join(' '.join(str(n) for n in combo) for combo in result))`,
    java: `import java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String[] parts = sc.nextLine().trim().split(" ");
        int[] all = new int[parts.length];
        for (int i = 0; i < parts.length; i++) all[i] = Integer.parseInt(parts[i]);
        int[] candidates = Arrays.copyOf(all, all.length - 1);
        int target = all[all.length - 1];
        Solution sol = new Solution();
        List<List<Integer>> result = sol.combinationSum(candidates, target);
        result.sort((a, b) -> {
            for (int i = 0; i < Math.min(a.size(), b.size()); i++) {
                if (!a.get(i).equals(b.get(i))) return a.get(i) - b.get(i);
            }
            return a.size() - b.size();
        });
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < result.size(); i++) {
            if (i > 0) sb.append(',');
            List<Integer> combo = result.get(i);
            for (int j = 0; j < combo.size(); j++) {
                if (j > 0) sb.append(' ');
                sb.append(combo.get(j));
            }
        }
        System.out.println(sb.toString());
    }
}`,
    cpp: `#include <iostream>
#include <sstream>
#include <vector>
#include <algorithm>
using namespace std;
int main() {
    string line; getline(cin, line);
    istringstream iss(line); vector<int> all; int v;
    while (iss >> v) all.push_back(v);
    int target = all.back(); all.pop_back();
    Solution sol;
    vector<vector<int>> result = sol.combinationSum(all, target);
    sort(result.begin(), result.end());
    for (int i = 0; i < (int)result.size(); i++) {
        if (i > 0) cout << ',';
        for (int j = 0; j < (int)result[i].size(); j++) {
            if (j > 0) cout << ' ';
            cout << result[i][j];
        }
    }
    cout << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const parts = line.trim().split(' ').map(Number);
    const candidates = parts.slice(0, -1);
    const target = parts[parts.length - 1];
    const result = combinationSum(candidates, target);
    result.sort((a, b) => {
        for (let i = 0; i < Math.min(a.length, b.length); i++) {
            if (a[i] !== b[i]) return a[i] - b[i];
        }
        return a.length - b.length;
    });
    console.log(result.map(c => c.join(' ')).join(','));
    rl.close();
});`,
    typescript: `import * as readline from 'readline';
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const parts = line.trim().split(' ').map(Number);
    const candidates: number[] = parts.slice(0, -1);
    const target: number = parts[parts.length - 1];
    const result: number[][] = combinationSum(candidates, target);
    result.sort((a, b) => {
        for (let i = 0; i < Math.min(a.length, b.length); i++) {
            if (a[i] !== b[i]) return a[i] - b[i];
        }
        return a.length - b.length;
    });
    console.log(result.map((c: number[]) => c.join(' ')).join(','));
    rl.close();
});`,
    rust: `use std::io::{self, BufRead};
fn main() {
    let stdin = io::stdin();
    let line = stdin.lock().lines().next().unwrap().unwrap();
    let nums: Vec<i32> = line.split_whitespace().map(|s| s.parse().unwrap()).collect();
    let candidates = nums[..nums.len()-1].to_vec();
    let target = *nums.last().unwrap();
    let mut result = combination_sum(candidates, target);
    result.sort();
    let out: Vec<String> = result.iter().map(|c| c.iter().map(|n| n.to_string()).collect::<Vec<_>>().join(" ")).collect();
    println!("{}", out.join(","));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX_RESULTS 10000
#define MAX_DEPTH 50
static int results[MAX_RESULTS][MAX_DEPTH];
static int result_sizes[MAX_RESULTS];
static int result_count;
static int cands[100], cands_size, tgt;
static int path[MAX_DEPTH];
static void backtrack(int remaining, int start, int depth) {
    if (remaining == 0) {
        memcpy(results[result_count], path, depth * sizeof(int));
        result_sizes[result_count++] = depth;
        return;
    }
    for (int i = start; i < cands_size; i++) {
        if (cands[i] > remaining) break;
        path[depth] = cands[i];
        backtrack(remaining - cands[i], i, depth + 1);
    }
}
static int cmp(const void* a, const void* b) { return *(int*)a - *(int*)b; }
int main() {
    char line[10000]; fgets(line, 10000, stdin);
    char* t = strtok(line, " \\n");
    int all[100], n = 0;
    while (t) { all[n++] = atoi(t); t = strtok(NULL, " \\n"); }
    tgt = all[n-1]; cands_size = n - 1;
    for (int i = 0; i < cands_size; i++) cands[i] = all[i];
    qsort(cands, cands_size, sizeof(int), cmp);
    result_count = 0;
    backtrack(tgt, 0, 0);
    for (int i = 0; i < result_count; i++) {
        if (i > 0) printf(",");
        for (int j = 0; j < result_sizes[i]; j++) {
            if (j > 0) printf(" ");
            printf("%d", results[i][j]);
        }
    }
    printf("\\n");
    return 0;
}`,
    csharp: `using System;
using System.Collections.Generic;
using System.Linq;
class Program {
    static void Main() {
        int[] all = Console.ReadLine().Trim().Split(' ').Select(int.Parse).ToArray();
        int[] candidates = all.Take(all.Length - 1).ToArray();
        int target = all[all.Length - 1];
        Solution sol = new Solution();
        IList<IList<int>> result = sol.CombinationSum(candidates, target);
        var sorted = result.OrderBy(c => string.Join(",", c)).ToList();
        Console.WriteLine(string.Join(",", sorted.Select(c => string.Join(" ", c))));
    }
}`,
    go: `package main
import (
    "bufio"
    "fmt"
    "os"
    "sort"
    "strconv"
    "strings"
)
func main() {
    reader := bufio.NewReader(os.Stdin)
    line, _ := reader.ReadString('\\n')
    parts := strings.Fields(strings.TrimSpace(line))
    all := make([]int, len(parts))
    for i, p := range parts { all[i], _ = strconv.Atoi(p) }
    candidates := all[:len(all)-1]
    target := all[len(all)-1]
    result := combinationSum(candidates, target)
    sort.Slice(result, func(i, j int) bool {
        a, b := result[i], result[j]
        for k := 0; k < len(a) && k < len(b); k++ {
            if a[k] != b[k] { return a[k] < b[k] }
        }
        return len(a) < len(b)
    })
    parts2 := make([]string, len(result))
    for i, combo := range result {
        strs := make([]string, len(combo))
        for j, n := range combo { strs[j] = strconv.Itoa(n) }
        parts2[i] = strings.Join(strs, " ")
    }
    fmt.Println(strings.Join(parts2, ","))
}`,
  }}
  defaultLanguage="python"
>

# Combination Sum

**Difficulty:** Medium

Given an array of **distinct** integers `candidates` and a target integer `target`, return a list of all **unique combinations** of `candidates` where the chosen numbers sum to `target`. You may return the combinations in any order.

The **same** number may be chosen from `candidates` an **unlimited number of times**. Two combinations are unique if the frequency of at least one of the chosen numbers is different.

## Example 1

**Input:** `candidates = [2, 3, 6, 7]`, `target = 7`  
**Output:** `[[2, 2, 3], [7]]`  

## Example 2

**Input:** `candidates = [2, 3, 5]`, `target = 8`  
**Output:** `[[2, 2, 2, 2], [2, 3, 3], [3, 5]]`  

## Example 3

**Input:** `candidates = [2]`, `target = 1`  
**Output:** `[]`  

## Input Specification

- A single line with space-separated integers: candidates followed by the target.
- `1 <= candidates.length <= 30`

## Output Specification

Print the combinations sorted lexicographically, one combination per entry, each element space-separated, combinations comma-separated. Print an empty line if no combinations exist.

## Constraints

- `1 <= candidates.length <= 30`
- `2 <= candidates[i] <= 40`
- All elements of `candidates` are distinct.
- `1 <= target <= 40`

## Hints

<details>
<summary>Hint 1</summary>

Sort candidates to enable pruning. Use backtracking: at each step, add a candidate and recursively try to reach the remaining target.
</details>

<details>
<summary>Hint 2</summary>

Pass an index parameter to avoid revisiting earlier candidates (which would create duplicates).
</details>

<details>
<summary>Hint 3</summary>

When the remaining target becomes 0, add the current combination to results. Prune when remaining target becomes negative.
</details>

</ProblemEditor>

---
**📖 View the [detailed solution](../solutions/medium/combination-sum)** for a step-by-step walkthrough.
