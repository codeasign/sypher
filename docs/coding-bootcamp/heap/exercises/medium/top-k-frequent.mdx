---
title: Top K Frequent Elements
sidebar_label: Top K Frequent Elements
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'top-k-frequent', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '6 2\n1 1 1 2 2 3\n', expectedOutput: '1 2\n' },
    { stdin: '1 1\n1\n', expectedOutput: '1\n' },
    { stdin: '4 2\n4 4 5 5\n', expectedOutput: '4 5\n' },
    { stdin: '5 3\n1 1 2 2 3\n', expectedOutput: '1 2 3\n' },
    { stdin: '5 2\n-1 -1 0 0 1\n', expectedOutput: '-1 0\n' },
    { stdin: '7 2\n3 0 1 0 1 1 3\n', expectedOutput: '0 1\n' },
    { stdin: '3 1\n1 2 3\n', expectedOutput: '1\n' },
    { stdin: '6 4\n5 5 4 4 3 3\n', expectedOutput: '5 4 3\n' },
    { stdin: '8 3\n8 8 8 7 7 6 6 6\n', expectedOutput: '8 6 7\n' },
    { stdin: '5 5\n9 9 8 8 7\n', expectedOutput: '9 8 7\n' },
    { stdin: '10 2\n2 2 2 2 2 3 3 3 4 5\n', expectedOutput: '2 3\n' },
    { stdin: '6 2\n1 1 1 1 1 1\n', expectedOutput: '1\n' },
    { stdin: '4 2\n100 200 100 200\n', expectedOutput: '100 200\n' },
    { stdin: '6 3\n-5 -5 -3 -3 -1 -1\n', expectedOutput: '-5 -3 -1\n' },
    { stdin: '3 2\n0 0 1\n', expectedOutput: '0 1\n' },
    { stdin: '7 2\n1 2 3 4 5 6 7\n', expectedOutput: '1 2\n' },
    { stdin: '8 3\n5 5 5 6 6 7 7 7\n', expectedOutput: '5 7 6\n' },
    { stdin: '4 1\n2 2 1 1\n', expectedOutput: '2\n' },
    { stdin: '6 3\n10 20 10 20 10 20\n', expectedOutput: '10 20\n' },
    { stdin: '9 4\n1 1 1 2 2 3 3 4 4\n', expectedOutput: '1 2 3 4\n' },
  ]}
  starterCode={{
    python: `def top_k_frequent(nums: list[int], k: int) -> list[int]:
    # your code here
    pass
`,
    javascript: `function topKFrequent(nums, k) {
    // your code here
}
`,
    java: `class Solution {
    public int[] topKFrequent(int[] nums, int k) {
        // your code here
        return new int[0];
    }
}
`,
    cpp: `class Solution {
public:
    vector<int> topKFrequent(vector<int>& nums, int k) {
        // your code here
        return {};
    }
};
`,
    typescript: `function topKFrequent(nums: number[], k: number): number[] {
    // your code here
    return [];
}
`,
    rust: `fn top_k_frequent(nums: Vec<i32>, k: i32) -> Vec<i32> {
    // your code here
    vec![]
}
`,
    c: `int* topKFrequent(int* nums, int numsSize, int k, int* returnSize) {
    // your code here
    *returnSize = 0;
    return NULL;
}
`,
    csharp: `class Solution {
    public int[] TopKFrequent(int[] nums, int k) {
        // your code here
        return new int[0];
    }
}
`,
  }}
  harness={{
    python: `import sys
data = sys.stdin.read().strip().split()
if not data: exit()
n, k = int(data[0]), int(data[1])
nums = list(map(int, data[2:2 + n]))
result = top_k_frequent(nums, k)
print(' '.join(map(str, result)))
`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lineNum = 0, n, k, nums;
rl.on('line', line => {
    if (lineNum === 0) {
        const parts = line.trim().split(/\\s+/).map(Number);
        n = parts[0]; k = parts[1];
    } else {
        nums = line.trim().split(/\\s+/).map(Number).slice(0, n);
        const result = topKFrequent(nums, k);
        console.log(result.join(' '));
        rl.close();
    }
    lineNum++;
});
`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        int n = sc.nextInt();
        int k = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) nums[i] = sc.nextInt();
        int[] result = new Solution().topKFrequent(nums, k);
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < result.length; i++) {
            if (i > 0) sb.append(' ');
            sb.append(result[i]);
        }
        System.out.println(sb);
    }
}
`,
    cpp: `int main() {
    int n, k;
    cin >> n >> k;
    vector<int> nums(n);
    for (int i = 0; i < n; i++) cin >> nums[i];
    Solution sol;
    vector<int> result = sol.topKFrequent(nums, k);
    for (int i = 0; i < result.size(); i++) {
        if (i) cout << ' ';
        cout << result[i];
    }
    cout << endl;
    return 0;
}
`,
    c: `int main() {
    int n, k;
    scanf("%d %d", &n, &k);
    int* nums = (int*)malloc(n * sizeof(int));
    for (int i = 0; i < n; i++) scanf("%d", &nums[i]);
    int returnSize;
    int* result = topKFrequent(nums, n, k, &returnSize);
    for (int i = 0; i < returnSize; i++) {
        if (i) printf(" ");
        printf("%d", result[i]);
    }
    printf("\\n");
    free(nums);
    free(result);
    return 0;
}
`,
    csharp: `class Program {
    static void Main() {
        string[] parts = Console.ReadLine().Split();
        int n = int.Parse(parts[0]);
        int k = int.Parse(parts[1]);
        parts = Console.ReadLine().Split();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) nums[i] = int.Parse(parts[i]);
        int[] result = new Solution().TopKFrequent(nums, k);
        Console.WriteLine(string.Join(" ", result));
    }
}
`,
    rust: `fn main() {
    let mut input = String::new();
    std::io::stdin().read_line(&mut input).unwrap();
    let first: Vec<i32> = input.trim().split_whitespace().map(|s| s.parse().unwrap()).collect();
    let n = first[0] as usize;
    let k = first[1];
    let mut input2 = String::new();
    std::io::stdin().read_line(&mut input2).unwrap();
    let nums: Vec<i32> = input2.trim().split_whitespace().map(|s| s.parse().unwrap()).collect();
    let result = top_k_frequent(nums, k);
    let result: Vec<String> = result.iter().map(|x| x.to_string()).collect();
    println!("{}", result.join(" "));
}
`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lineNum = 0, n: number, k: number, nums: number[];
rl.on('line', (line: string) => {
    if (lineNum === 0) {
        const parts = line.trim().split(/\\s+/).map(Number);
        n = parts[0]; k = parts[1];
    } else {
        nums = line.trim().split(/\\s+/).map(Number).slice(0, n);
        const result = topKFrequent(nums, k);
        console.log(result.join(' '));
        rl.close();
    }
    lineNum++;
});
`,
  }}
  defaultLanguage="python"
>

# Top K Frequent Elements

**Difficulty:** Medium

Given an integer array `nums` and an integer `k`, return the **k most frequent elements**. You may return the answer in **any order**.

## Example 1

**Input:** `nums = [1, 1, 1, 2, 2, 3], k = 2`  
**Output:** `[1, 2]`  
**Explanation:** The elements 1 appears 3 times, 2 appears 2 times, and 3 appears 1 time. The 2 most frequent are 1 and 2.

## Example 2

**Input:** `nums = [1], k = 1`  
**Output:** `[1]`  
**Explanation:** Only one element, so it is the most frequent.

## Input Specification

The first line contains two integers `n` and `k`. The second line contains `n` integers representing the array.

## Output Specification

Print `k` space-separated integers representing the k most frequent elements (in any order).

## Constraints

- `1 <= n <= 10^5`
- `1 <= k <= n`
- `-10^4 <= nums[i] <= 10^4`
- The answer is unique

## Hints

<details>
<summary>Hint 1</summary>

Start by counting the frequency of each element using a hash map.
</details>

<details>
<summary>Hint 2</summary>

Use a min-heap to track the k most frequent elements. Only keep the top k by frequency in the heap.
</details>

<details>
<summary>Hint 3</summary>

Alternatively, think about how you could use QuickSelect or bucket sort for an O(n) solution.
</details>

</ProblemEditor>

---

**📖 View the [detailed solution](../solutions/medium/top-k-frequent)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Find Median from Data Stream](../hard/find-median-data-stream).