---
title: Task Scheduler — Solution
sidebar_label:   Solution
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Task Scheduler — Solution

## Approach: Greedy with Frequency Count

**Algorithm:** Find max frequency. Minimum intervals = (maxFreq-1)*(n+1) + count(maxFreq). Take max with len(tasks).

**Complexity:** O(n) time, O(1) space.

## Code

<Tabs>
  <TabItem value="python" label="Python">

```python
from collections import Counter

def least_interval(tasks: list[str], n: int) -> int:
    freq = Counter(tasks)
    max_freq = max(freq.values())
    max_count = sum(1 for v in freq.values() if v == max_freq)
    return max(len(tasks), (max_freq - 1) * (n + 1) + max_count)
```

  </TabItem>
</Tabs>

## Back to Problem

[← Back to Problem](../../exercises/medium/task-scheduler)