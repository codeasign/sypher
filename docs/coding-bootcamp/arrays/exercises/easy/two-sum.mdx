---
title: Two Sum
sidebar_label: Two Sum
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/arrays/two-sum', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '2 7 11 15\n9\n', expectedOutput: '0 1\n' },
    { stdin: '3 2 4\n6\n', expectedOutput: '1 2\n' },
    { stdin: '3 3\n6\n', expectedOutput: '0 1\n' },
    { stdin: '1 5 9 12\n10\n', expectedOutput: '0 2\n' },
    { stdin: '-3 4 3 90\n0\n', expectedOutput: '0 2\n' },
    { stdin: '0 4 3 0\n0\n', expectedOutput: '0 3\n' },
    { stdin: '-1 -2 -3 -4 -5\n-8\n', expectedOutput: '2 4\n' },
    { stdin: '1 2 3 4 5\n9\n', expectedOutput: '3 4\n' },
    { stdin: '10 20 30 40 50 60\n90\n', expectedOutput: '3 4\n' },
    { stdin: '-10 -5 0 5 10\n0\n', expectedOutput: '1 3\n' },
    { stdin: '100 200 300 400\n500\n', expectedOutput: '1 2\n' },
    { stdin: '7 1 5 3 6 4\n8\n', expectedOutput: '0 1\n' },
    { stdin: '2 5 5 11\n10\n', expectedOutput: '1 2\n' },
    { stdin: '1 6 3 2 5\n11\n', expectedOutput: '1 4\n' },
    { stdin: '0 0 0 0\n0\n', expectedOutput: '0 1\n' },
    { stdin: '-5 0 5 10\n-5\n', expectedOutput: '0 1\n' },
    { stdin: '3 2 4 1\n5\n', expectedOutput: '0 1\n' },
    { stdin: '8 7 2 5 3 1\n10\n', expectedOutput: '0 2\n' },
    { stdin: '1 15 2 7 11\n9\n', expectedOutput: '2 3\n' },
    { stdin: '4 5 6 7 8\n13\n', expectedOutput: '2 3\n' },
  ]}
  starterCode={{
    python: `def two_sum(nums: list, target: int) -> list:
    pass`,
    java: `class Solution {
    public int[] twoSum(int[] nums, int target) {
        // your code here
        return new int[]{};
    }
}`,
    cpp: `class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        // your code here
        return {};
    }
};`,
    javascript: `function twoSum(nums, target) {
    // your code here
}`,
    typescript: `function twoSum(nums: number[], target: number): number[] {
    // your code here
    return [];
}`,
    rust: `fn two_sum(nums: &Vec<i32>, target: i32) -> Vec<i32> {
    // your code here
    vec![]
}`,
    c: `int* twoSum(int* nums, int numsSize, int target, int* returnSize) {
    // your code here
    *returnSize = 2;
    int* result = (int*)malloc(2 * sizeof(int));
    return result;
}`,
    csharp: `class Solution {
    public int[] TwoSum(int[] nums, int target) {
        // your code here
        return new int[0];
    }
}`,
    go: `func twoSum(nums []int, target int) []int {
    // your code here
    return nil
}`,
  }}
  harness={{
    python: `import sys
data = sys.stdin.read().strip().splitlines()
nums = list(map(int, data[0].split()))
target = int(data[1].strip())
result = two_sum(nums, target)
print(f"{result[0]} {result[1]}")`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String line = sc.nextLine();
        String[] parts = line.split(" ");
        int[] nums = new int[parts.length];
        for (int i = 0; i < parts.length; i++) nums[i] = Integer.parseInt(parts[i]);
        int target = sc.nextInt();
        Solution sol = new Solution();
        int[] result = sol.twoSum(nums, target);
        System.out.println(result[0] + " " + result[1]);
    }
}`,
    cpp: `int main() {
    vector<int> nums;
    int val, target;
    string line;
    getline(cin, line);
    stringstream ss(line);
    while (ss >> val) nums.push_back(val);
    cin >> target;
    Solution sol;
    vector<int> res = sol.twoSum(nums, target);
    cout << res[0] << " " << res[1] << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines = [];
rl.on('line', l => lines.push(l));
rl.on('close', () => {
    const nums = lines[0].split(' ').map(Number);
    const target = parseInt(lines[1]);
    const result = twoSum(nums, target);
    console.log(result[0] + ' ' + result[1]);
});`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
let lines: string[] = [];
rl.on('line', (l: string) => lines.push(l));
rl.on('close', () => {
    const nums = lines[0].split(' ').map(Number);
    const target = parseInt(lines[1]);
    const result = twoSum(nums, target);
    console.log(result[0] + ' ' + result[1]);
});`,
    rust: `fn main() {
    let mut line1 = String::new();
    std::io::stdin().read_line(&mut line1).unwrap();
    let nums: Vec<i32> = line1.trim().split_whitespace()
        .map(|s| s.parse().unwrap()).collect();
    let mut line2 = String::new();
    std::io::stdin().read_line(&mut line2).unwrap();
    let target: i32 = line2.trim().parse().unwrap();
    let result = two_sum(&nums, target);
    println!("{} {}", result[0], result[1]);
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
int main() {
    int nums[10000];
    int n = 0, target;
    char line[100000];
    fgets(line, sizeof(line), stdin);
    char *p = line;
    while (sscanf(p, "%d", &nums[n]) == 1) {
        n++;
        while (*p && *p != ' ') p++;
        if (*p == ' ') p++;
    }
    scanf("%d", &target);
    int returnSize;
    int* res = twoSum(nums, n, target, &returnSize);
    printf("%d %d\n", res[0], res[1]);
    free(res);
    return 0;
}`,
    csharp: `class Program {
    static void Main() {
        string[] parts = System.Console.ReadLine().Split();
        int[] nums = new int[parts.Length];
        for (int i = 0; i < parts.Length; i++) nums[i] = int.Parse(parts[i]);
        int target = int.Parse(System.Console.ReadLine());
        Solution sol = new Solution();
        int[] res = sol.TwoSum(nums, target);
        System.Console.WriteLine(res[0] + " " + res[1]);
    }
}`,
    go: `package main
\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    parts := strings.Fields(scanner.Text())
    nums := make([]int, len(parts))
    for i, s := range parts {
        nums[i], _ = strconv.Atoi(s)
    }
    scanner.Scan()
    target, _ := strconv.Atoi(scanner.Text())
    result := twoSum(nums, target)
    fmt.Printf("%d %d\n", result[0], result[1])
}`,
  }}
  defaultLanguage="python"
>

# Two Sum

**Difficulty:** Easy

Given an array of integers `nums` and an integer `target`, return the **indices** of the two numbers that add up to `target`.

You may assume that each input has **exactly one solution**, and you may not use the same element twice.

You can return the answer in any order.

## Example 1

**Input:** `nums = [2, 7, 11, 15]`, `target = 9`  
**Output:** `[0, 1]`  
**Explanation:** Because `nums[0] + nums[1] == 9`, we return `[0, 1]`.

## Example 2

**Input:** `nums = [3, 2, 4]`, `target = 6`  
**Output:** `[1, 2]`  
**Explanation:** `nums[1] + nums[2] = 2 + 4 = 6`.

## Example 3

**Input:** `nums = [3, 3]`, `target = 6`  
**Output:** `[0, 1]`  
**Explanation:** `nums[0] + nums[1] = 3 + 3 = 6`.

## Input Specification

The first line contains space-separated integers representing the array `nums`.
The second line contains a single integer `target`.

## Output Specification

Print two space-separated integers representing the 0-based indices of the two numbers that sum to the target.

## Constraints

- `2 <= nums.length <= 10^4`
- `-10^9 <= nums[i] <= 10^9`
- `-10^9 <= target <= 10^9`
- There is **exactly one valid solution**.

## Hints

<details>
<summary>Hint 1</summary>

A brute-force approach checks every pair — O(n^2) time. Can you do better?

</details>

<details>
<summary>Hint 2</summary>

A hash map gives O(1) lookup. Store each number with its index as you iterate.

</details>

<details>
<summary>Hint 3</summary>

For each element `nums[i]`, check if `target - nums[i]` already exists in the hash map. If it does, you have found the pair.

</details>

</ProblemEditor>

---

**View the [detailed solution](../../solutions/easy/two-sum)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Best Time to Buy and Sell Stock](../easy/best-time-to-buy-and-sell-stock).