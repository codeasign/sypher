---
title: Product of Array Except Self
sidebar_label: Product of Array Except Self
hide_table_of_contents: true
---

import ProblemEditor from '@site/src/components/ProblemEditor/Index';

<ProblemEditor
  meta={{ id: 'coding-bootcamp/arrays/product-of-array-except-self', timeLimitSeconds: 2, memoryLimitKb: 262144 }}
  testCases={[
    { stdin: '1 2 3 4\n', expectedOutput: '24 12 8 6\n' },
    { stdin: '-1 1 0 -3 3\n', expectedOutput: '0 0 9 0 0\n' },
    { stdin: '2 3 4 5\n', expectedOutput: '60 40 30 24\n' },
    { stdin: '1 1 1 1\n', expectedOutput: '1 1 1 1\n' },
    { stdin: '10 20 30 40\n', expectedOutput: '24000 12000 8000 6000\n' },
    { stdin: '1 2\n', expectedOutput: '2 1\n' },
    { stdin: '5 6 7 8\n', expectedOutput: '336 280 240 210\n' },
    { stdin: '2 4 6 8 10\n', expectedOutput: '1920 960 640 480 384\n' },
    { stdin: '3 1 2\n', expectedOutput: '2 6 3\n' },
    { stdin: '6 2 3 4\n', expectedOutput: '24 72 48 36\n' },
    { stdin: '1 2 3 4 5 6\n', expectedOutput: '720 360 240 180 144 120\n' },
    { stdin: '-2 -3 4 -5\n', expectedOutput: '60 40 -30 24\n' },
    { stdin: '7 8 9 10\n', expectedOutput: '720 630 560 504\n' },
    { stdin: '2 2 2 2\n', expectedOutput: '8 8 8 8\n' },
    { stdin: '10 1 10 1\n', expectedOutput: '10 100 10 100\n' },
    { stdin: '4 5 6\n', expectedOutput: '30 24 20\n' },
    { stdin: '3 5 2 4\n', expectedOutput: '40 24 60 30\n' },
    { stdin: '100 200 300\n', expectedOutput: '60000 30000 20000\n' },
    { stdin: '1 2 3 0 5\n', expectedOutput: '0 0 0 30 0\n' },
    { stdin: '0 0 1 2\n', expectedOutput: '0 0 0 0\n' },
  ]}
  starterCode={{
    python: `def product_except_self(nums: list) -> list:
    pass`,
    java: `class Solution {
    public int[] productExceptSelf(int[] nums) {
        // your code here
        return new int[]{};
    }
}`,
    cpp: `class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {
        // your code here
        return {};
    }
};`,
    javascript: `function productExceptSelf(nums) {
    // your code here
}`,
    typescript: `function productExceptSelf(nums: number[]): number[] {
    // your code here
    return [];
}`,
    rust: `fn product_except_self(nums: &Vec<i32>) -> Vec<i32> {
    // your code here
    vec![]
}`,
    c: `int* productExceptSelf(int* nums, int numsSize, int* returnSize) {
    // your code here
    *returnSize = numsSize;
    return NULL;
}`,
    csharp: `class Solution {
    public int[] ProductExceptSelf(int[] nums) {
        // your code here
        return new int[0];
    }
}`,
    go: `func productExceptSelf(nums []int) []int {
    // your code here
    return nil
}`,
  }}
  harness={{
    python: `import sys
nums = list(map(int, sys.stdin.read().strip().split()))
result = product_except_self(nums)
print(' '.join(map(str, result)))`,
    java: `public class Main {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        String line = sc.nextLine();
        String[] parts = line.split(" ");
        int[] nums = new int[parts.length];
        for (int i = 0; i < parts.length; i++) nums[i] = Integer.parseInt(parts[i]);
        Solution sol = new Solution();
        int[] result = sol.productExceptSelf(nums);
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < result.length; i++) {
            if (i > 0) sb.append(" ");
            sb.append(result[i]);
        }
        System.out.println(sb.toString());
    }
}`,
    cpp: `int main() {
    vector<int> nums;
    int val;
    while (cin >> val) nums.push_back(val);
    Solution sol;
    vector<int> res = sol.productExceptSelf(nums);
    for (int i = 0; i < res.size(); i++) {
        if (i > 0) cout << " ";
        cout << res[i];
    }
    cout << endl;
    return 0;
}`,
    javascript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', line => {
    const nums = line.split(' ').map(Number);
    const result = productExceptSelf(nums);
    console.log(result.join(' '));
    rl.close();
});`,
    typescript: `const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
rl.on('line', (line: string) => {
    const nums = line.split(' ').map(Number);
    const result = productExceptSelf(nums);
    console.log(result.join(' '));
    rl.close();
});`,
    rust: `fn main() {
    let mut input = String::new();
    std::io::stdin().read_line(&mut input).unwrap();
    let nums: Vec<i32> = input.trim().split_whitespace()
        .map(|s| s.parse().unwrap()).collect();
    let result = product_except_self(&nums);
    let output: Vec<String> = result.iter().map(|x| x.to_string()).collect();
    println!("{}", output.join(" "));
}`,
    c: `#include <stdio.h>
#include <stdlib.h>
int main() {
    int nums[10000];
    int n = 0;
    char line[100000];
    fgets(line, sizeof(line), stdin);
    char *p = line;
    while (sscanf(p, "%d", &nums[n]) == 1) {
        n++;
        while (*p && *p != ' ') p++;
        if (*p == ' ') p++;
    }
    int returnSize;
    int* res = productExceptSelf(nums, n, &returnSize);
    for (int i = 0; i < returnSize; i++) {
        if (i > 0) printf(" ");
        printf("%d", res[i]);
    }
    printf("\n");
    free(res);
    return 0;
}`,
    csharp: `class Program {
    static void Main() {
        string[] parts = System.Console.ReadLine().Split();
        int[] nums = new int[parts.Length];
        for (int i = 0; i < parts.Length; i++) nums[i] = int.Parse(parts[i]);
        Solution sol = new Solution();
        int[] res = sol.ProductExceptSelf(nums);
        System.Console.WriteLine(string.Join(" ", res));
    }
}`,
    go: `package main
\u0069mport (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    parts := strings.Fields(scanner.Text())
    nums := make([]int, len(parts))
    for i, s := range parts {
        nums[i], _ = strconv.Atoi(s)
    }
    result := productExceptSelf(nums)
    strResults := make([]string, len(result))
    for i, v := range result {
        strResults[i] = strconv.Itoa(v)
    }
    fmt.Println(strings.Join(strResults, " "))
}`,
  }}
  defaultLanguage="python"
>

# Product of Array Except Self

**Difficulty:** Medium

Given an integer array `nums`, return an array `answer` such that `answer[i]` is equal to the product of all the elements of `nums` except `nums[i]`.

The product of any prefix or suffix of `nums` is guaranteed to fit in a 32-bit integer.

You must solve it **without using division** and in O(n) time.

## Example 1

**Input:** `nums = [1, 2, 3, 4]`  
**Output:** `[24, 12, 8, 6]`  
**Explanation:**  
- `answer[0] = 2 * 3 * 4 = 24`  
- `answer[1] = 1 * 3 * 4 = 12`  
- `answer[2] = 1 * 2 * 4 = 8`  
- `answer[3] = 1 * 2 * 3 = 6`

## Example 2

**Input:** `nums = [-1, 1, 0, -3, 3]`  
**Output:** `[0, 0, 9, 0, 0]`  
**Explanation:**  
- `answer[2] = (-1) * 1 * (-3) * 3 = 9`  
- All other positions contain a zero in the product.

## Input Specification

The input line contains space-separated integers representing the array `nums`.

## Output Specification

Print space-separated integers representing the product array.

## Constraints

- `2 <= nums.length <= 10^5`
- `-30 <= nums[i] <= 30`
- The product of any prefix or suffix fits in a 32-bit integer.

## Hints

<details>
<summary>Hint 1</summary>

Think about using prefix and suffix products. Can you compute the product of all elements to the left and right of each index?

</details>

<details>
<summary>Hint 2</summary>

Build a left-to-right pass storing cumulative products, then a right-to-left pass multiplying by the suffix products.

</details>

<details>
<summary>Hint 3</summary>

You can do it in O(1) extra space (excluding the output array) by storing the result in the output array itself during the two passes.

</details>

</ProblemEditor>

---

**View the [detailed solution](../../solutions/medium/product-of-array-except-self)** for a step-by-step walkthrough, multiple approaches, and code in all languages.

## Next

Now try [Rotate Array](../medium/rotate-array).